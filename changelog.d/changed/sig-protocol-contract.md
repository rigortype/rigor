- **[sig]** `Plugin::ProtocolContract` (ADR-28) is declared under `sig/` — the validated record's readers (`path_glob`, `method_name`, `singleton`, `param_types`, `return_type_name`, `severity`), the `ParamType` Data member, `initialize` coercion surface, and `with_path_glob`/`to_h`/`hash`/`==`/`eql?` — unblocking `Manifest#protocol_contracts`, the manifest kwarg, `Base#protocol_contracts`, and newly-declared `Registry#protocol_contracts`/`#contracts_for_path`, all `Array[ProtocolContract]`. Third slice of the #1181 Class B backlog. [#1186](https://github.com/rigortype/rigor/pull/1186)
