- **[sig]** `Analysis::Baseline::Bucket` and `::DriftRow` — the first `Struct.new` classes declared under `sig/` — carry the member types their construction sites prove (`message_regex` is `Regexp?`, `status` is the four-way drift verdict), which tightens `Baseline#audit` to `Array[DriftRow]`, `buckets` to `Array[Bucket]`, and `initialize`/`without`/`filter` to their real shapes. The member rows are hand-written under [#1183](https://github.com/rigortype/rigor/issues/1183) (sig-gen emits Struct members `untyped`, the `Struct` analogue of #1150). First slice of the #1181 Class B backlog. [#1184](https://github.com/rigortype/rigor/pull/1184)
