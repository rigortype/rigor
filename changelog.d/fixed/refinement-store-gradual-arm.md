- **[inference]** A captured `non-empty-array` / `non-empty-hash` that a block rewrites in place with `map!`, `transform_values!` or a similar mutator no longer reads its entry element type at every position of a `map` / `select` / `find` fold, so a call the rewritten element defines is no longer reported as `call.undefined-method`. ([#1269](https://github.com/rigortype/rigor/pull/1269))
