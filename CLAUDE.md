# CLAUDE.md

Project-level briefing for Claude (and any other agent that reads top-level docs by convention). The authoritative agent contract for this repository is [`AGENTS.md`](AGENTS.md) — read it first; this file is a navigation index that points at the documents an agent typically needs and the per-task skills bundled with the project.

## Read these first

| Document | Purpose |
| --- | --- |
| [`AGENTS.md`](AGENTS.md) | Development environment (Flake mandate, target Ruby), common commands, directory layout, references/ submodule rules, implementation guidelines, commit-message style, release cadence, and verification protocol. **Required reading.** |
| [`README.md`](README.md) | User-facing project overview (CLI, what `rigor check` does today). |
| [`docs/handbook/`](docs/handbook/README.md) | Twelve-chapter (plus six appendices) end-user walkthrough of the type model. Reach for this when you need to explain Rigor concepts to a Ruby programmer (or to yourself) without diving into the spec corpus. Informational; the spec binds. |
| [`docs/types.md`](docs/types.md) | One-page quick guide to the Rigor type system. Faster mental-model warm-up than the handbook when you only need the carrier zoo. |
| [`docs/manual/`](docs/manual/README.md) | **User Manual** — the operational reference companion to the handbook. Installation, CLI command reference, configuration, diagnostics, baselines, plugins, editor / LSP integration, CI, caching, troubleshooting. Reach for this when a user is asking *how to operate* Rigor rather than *what its types mean*. |
| [`docs/ROADMAP.md`](docs/ROADMAP.md) | Forward-looking commitment envelope. Holds the active in-flight cycle in full detail + future-cycle plans + open questions. Released versions are reduced to one-line pointers to `CHANGELOG.md` (which is the authoritative "what shipped" record). Update entries in the same commit when scope changes. |
| [`docs/CURRENT_WORK.md`](docs/CURRENT_WORK.md) | Resume bookmark for the next implementer. Names the current ship-readiness state, the next-session entry slice, parallel tracks, and open engineering items. Transient — refresh when you take a substantial change set across the finish line. |
| [`docs/design/20260508-rails-plugins-roadmap.md`](docs/design/20260508-rails-plugins-roadmap.md) | Roadmap for the `rigor-*` Rails ecosystem plugins. Tier table, dependency graph, per-plugin sketches, subtree-split readiness checklist. Use when planning new Rails-side plugin work. |
| [`plugins/README.md`](plugins/README.md) | **Production plugin catalogue** for real gems / frameworks (Rails, RSpec, dry-rb, Sorbet, Devise, Sidekiq, etc.). Where to send users who want to install plugins. The count drifts as new plugins land — consult here rather than hard-coding in upstream docs. |
| [`examples/README.md`](examples/README.md) | **Plugin-contract walkthroughs** — tutorials over deliberately simplified virtual use cases (`rigor-deprecations`, `rigor-lisp-eval`, `rigor-pattern`, `rigor-routes`, `rigor-units`). Each spotlights a single architectural surface. Reach for this when authoring a NEW plugin or answering "how do I use surface X of the plugin contract?". |

## Authoritative specifications

When a change touches type-language behaviour or analyzer-internal contracts, the spec binds. ADRs record design rationale and rejected/deferred alternatives.

### Type specification (normative)

| Document | Scope |
| --- | --- |
| [`docs/type-specification/README.md`](docs/type-specification/README.md) | Reading order + RFC 2119 conventions for the spec corpus. |
| [`docs/type-specification/overview.md`](docs/type-specification/overview.md) | Core principle (RBS superset), design priorities. |
| [`docs/type-specification/robustness-principle.md`](docs/type-specification/robustness-principle.md) | Postel's law for types — strict on returns, lenient on parameters. The asymmetric authorship rule every Rigor-authored type observes. |
| [`docs/type-specification/relations-and-certainty.md`](docs/type-specification/relations-and-certainty.md) | Subtyping (`<:`) and gradual consistency, trinary certainty. |
| [`docs/type-specification/value-lattice.md`](docs/type-specification/value-lattice.md) | Lattice identities and `Dynamic[T]` algebra. |
| [`docs/type-specification/special-types.md`](docs/type-specification/special-types.md) | `top`, `bot`, `untyped`/`Dynamic[T]`, `void`, `nil`, `bool`/`boolish`. |
| [`docs/type-specification/rbs-compatible-types.md`](docs/type-specification/rbs-compatible-types.md) | RBS forms accepted by Rigor. |
| [`docs/type-specification/rigor-extensions.md`](docs/type-specification/rigor-extensions.md) | Refinements and other internal-only forms beyond RBS. |
| [`docs/type-specification/imported-built-in-types.md`](docs/type-specification/imported-built-in-types.md) | Reserved built-in refinement names (`non-empty-string`, `positive-int`, …). |
| [`docs/type-specification/type-operators.md`](docs/type-specification/type-operators.md) | `~T`, `T - U`, indexed access, display contract. |
| [`docs/type-specification/structural-interfaces-and-object-shapes.md`](docs/type-specification/structural-interfaces-and-object-shapes.md) | RBS interfaces, inferred object shapes, capability roles. |
| [`docs/type-specification/control-flow-analysis.md`](docs/type-specification/control-flow-analysis.md) | Edge-aware narrowing, equality semantics, fact stability, mutation effects. |
| [`docs/type-specification/rbs-extended.md`](docs/type-specification/rbs-extended.md) | `%a{rigor:v1:…}` annotations, predicate / assertion / return-override grammar. |
| [`docs/type-specification/normalization.md`](docs/type-specification/normalization.md) | Deterministic normalization rules. |
| [`docs/type-specification/rbs-erasure.md`](docs/type-specification/rbs-erasure.md) | Conservative erasure to RBS. |
| [`docs/type-specification/inference-budgets.md`](docs/type-specification/inference-budgets.md) | Budget table and boundary contracts. |
| [`docs/type-specification/diagnostic-policy.md`](docs/type-specification/diagnostic-policy.md) | Diagnostic identifier taxonomy and suppression markers. |

### Analyzer-internal contracts (normative)

| Document | Scope |
| --- | --- |
| [`docs/internal-spec/README.md`](docs/internal-spec/README.md) | Index of analyzer-side surfaces. |
| [`docs/internal-spec/internal-type-api.md`](docs/internal-spec/internal-type-api.md) | Public type-object surface (the contract every `Rigor::Type::*` carrier satisfies). |
| [`docs/internal-spec/inference-engine.md`](docs/internal-spec/inference-engine.md) | Engine surface (`Scope`, fact store, effect model, capability-role inference, normalization, RBS erasure routing). |
| [`docs/internal-spec/implementation-expectations.md`](docs/internal-spec/implementation-expectations.md) | Engine-surface stability and public-API contract. |

### Architecture decision records (rationale)

ADRs record design rationale and rejected / deferred alternatives. **The canonical index — title + current implementation status for every ADR — is [`docs/adr/README.md`](docs/adr/README.md)**; open the individual ADR for its full working-decision detail. When an ADR and the spec disagree on analyzer behaviour, the spec binds.

- [ADR-0](docs/adr/0-concept.md) — Project concept and design boundaries
- [ADR-1](docs/adr/1-types.md) — Type model and RBS-superset strategy
- [ADR-2](docs/adr/2-extension-api.md) — Plugin extension API
- [ADR-3](docs/adr/3-type-representation.md) — Internal type-object representation
- [ADR-4](docs/adr/4-type-inference-engine.md) — Type inference engine
- [ADR-5](docs/adr/5-robustness-principle.md) — Robustness principle (Postel's law for types)
- [ADR-6](docs/adr/6-cache-persistence-backend.md) — Cache persistence backend
- [ADR-7](docs/adr/7-v0.1.0-slice-decisions.md) — v0.1.0 slice decisions
- [ADR-8](docs/adr/8-steep-inspired-improvements.md) — Steep-inspired diagnostic improvements
- [ADR-9](docs/adr/9-cross-plugin-api.md) — Cross-plugin API (`FactStore` + `prepare(services)` + `produces:` / `consumes:`)
- [ADR-10](docs/adr/10-dependency-source-inference.md) — Opt-in dependency-source inference
- [ADR-11](docs/adr/11-sorbet-input-adapter.md) — Sorbet input adapter (`rigor-sorbet`)
- [ADR-12](docs/adr/12-dry-rb-packaging.md) — dry-rb adapter packaging
- [ADR-13](docs/adr/13-typenode-resolver-plugin.md) — `TypeNodeResolver` + shape-projection type functions
- [ADR-14](docs/adr/14-rbs-sig-generation.md) — `rigor sig-gen` RBS generation
- [ADR-15](docs/adr/15-ractor-concurrency.md) — Ractor concurrency model (fork backend active, Ractor pool deferred)
- [ADR-16](docs/adr/16-macro-expansion.md) — Macro / DSL expansion substrate
- [ADR-17](docs/adr/17-monkey-patch-pre-evaluation.md) — Project-side monkey-patch pre-evaluation (`pre_eval:`)
- [ADR-18](docs/adr/18-substrate-per-call-site-return-type.md) — Substrate per-call-site return type (`returns_from_arg:`)
- [ADR-19](docs/adr/19-language-server-packaging.md) — Language Server packaging (`rigor lsp`)
- [ADR-20](docs/adr/20-lightweight-hkt.md) — Lightweight HKT (`App[F, A]`)
- [ADR-21](docs/adr/21-rubydex-evaluation.md) — Rubydex evaluation (proposed; reject / defer / conditional-accept)
- [ADR-22](docs/adr/22-baseline-and-project-onboarding.md) — Baseline + project onboarding (`.rigor-baseline.yml`)
- [ADR-23](docs/adr/23-diagnostic-triage-command.md) — `rigor triage` diagnostic triage (WD6, v0.2.3: the distribution/selectors/hotspots volume views exclude `:info` by default — a Rails project's info is dominated by plugin recognition trace that buried the genuine signal and inverted the hotspot ranking toward the most *working* code; summary keeps the full count, hints still see the full stream so `gem-without-rbs` survives, the count-based H5/H6 guard against info, `--include-info` restores; BC-bearing default-output change shipped in v0.2.3)
- [ADR-24](docs/adr/24-self-method-call-resolution.md) — Implicit-self method-call resolution (WD3 in-body adoption gate opened by ADR-57, 2026-06-12)
- [ADR-25](docs/adr/25-plugin-contributed-rbs.md) — Plugin-contributed RBS (`signature_paths:`)
- [ADR-26](docs/adr/26-activerecord-relation-typing.md) — ActiveRecord relation typing (`open_receivers:`)
- [ADR-27](docs/adr/27-tool-distribution-model.md) — Tool distribution + installation model
- [ADR-28](docs/adr/28-path-scoped-protocol-contracts.md) — Path-scoped protocol contracts (`protocol_contracts:`)
- [ADR-29](docs/adr/29-browser-playground.md) — Browser playground
- [ADR-30](docs/adr/30-rigor-ffi-plugin-shape.md) — `rigor-ffi` plugin family shape (proposed; not implemented)
- [ADR-31](docs/adr/31-contribution-and-supply-chain-policy.md) — Contribution + supply-chain policy
- [ADR-32](docs/adr/32-rbs-inline-comment-ingestion.md) — Inline-RBS comment ingestion (`rigor-rbs-inline`)
- [ADR-33](docs/adr/33-mcp-server.md) — MCP server packaging (`rigor mcp`)
- [ADR-34](docs/adr/34-toplevel-unresolved-self-call-default.md) — Toplevel unresolved-self-call diagnostic
- [ADR-35](docs/adr/35-override-signature-compatibility.md) — Override signature compatibility (Liskov signature rule)
- [ADR-36](docs/adr/36-mangrove-enum-nested-class-emission.md) — Macro-substrate nested-class emission tier (Mangrove `Enum`; Slice A implemented)
- [ADR-37](docs/adr/37-plugin-interface-segregation.md) — Plugin interface segregation (narrow extension protocols; accepted, Slices 1–3)
- [ADR-38](docs/adr/38-additional-initializers.md) — Plugin-declared additional initializers (`additional_initializers:`; accepted, def-form)
- [ADR-39](docs/adr/39-plugin-target-library-invocation.md) — Plugins may invoke their target library's safe methods directly (PHPStan-style; allow-list + rescue harness; accepted — `Plugin::Inflector` over real `ActiveSupport::Inflector` + 3 consumers migrated)
- [ADR-40](docs/adr/40-config-schema-defaults.md) — `config_schema` declared defaults (`{kind:, default:}`; accepted — `Base#config` merges manifest defaults; 13 plugins migrated off the `DEFAULT_*` idiom)
- [ADR-41](docs/adr/41-inference-budget-design.md) — Inference budget design (proposed — spec `budgets:` table unwired; widen-and-diagnose on-hit policy; measurement-gated defaults; Layer 1 doc hygiene + Layer 2 wiring queued)
- [ADR-42](docs/adr/42-plugin-binary-operator-return-types.md) — Plugin-contributed binary-operator return types (proposed, low priority — self/left-operand operators already work via `dynamic_return` (spec-confirmed); coerce-direction is a narrow false positive, cheapest fix is the WD-D engine mitigation, precision via the ADR-20 HKT route; demand-gated)
- [ADR-43](docs/adr/43-rbs-complete-ancestor-resolution.md) — RBS-complete ancestor resolution (accepted — fully landed, WD1–WD6; allow-list inherited-method dispatch so `rigor check` resolves a Ruby subclass's inherited calls against an RBS-complete ancestor (seed `Plugin::Base`) and warns on contract misuse standalone, without Steep's own-helper FP wall; zero net FP on the plugin lib tree; blanket resolution rejected on Rails-controller FP grounds; `make check-plugins` gate wired into `verify` + CI)
- [ADR-44](docs/adr/44-dispatch-allocation-churn.md) — Per-dispatch / per-narrow allocation churn (accepted — landed: collapsed the body-scope `with_*` chains into a single `Scope.new` (GC runs −29%) + per-dispatch allocation hygiene; mutable pooled Scope/CallContext rejected on re-entrancy/FP grounds; `ProjectScope` regrouping downgraded — object-shape benchmark shows it cuts size not allocation count)
- [ADR-45](docs/adr/45-unchanged-project-fast-path.md) — Unchanged-project fast path / run-result cache (accepted — landed: a pre-analysis fingerprint rejected as unsound (plugins read project files during analysis (Pundit), per the cross-process `pundit_plugin_spec` regression); the sound record-and-validate cache (`Cache::Store#fetch_or_validate` + `Descriptor#fresh?`) ships — unchanged Mastodon `app/models` 11.6s → 1.8s (~6×), gate runs `--no-cache`)
- [ADR-46](docs/adr/46-incremental-dependency-graph.md) — Incremental analysis via a cross-file dependency graph (proposed — design; per-file incremental successor to ADR-45: record per-file what it reads from other files via the `Scope` accessor choke point, invert into a `dependents` index, on edit re-analyze only `ΔF ∪ dependents` (leaf controller → 1 file; model → model + callers); declaration-fingerprint vs structural tiers; soundness gated by a mandatory `--verify-incremental` byte-identical cross-check)
- [ADR-47](docs/adr/47-narrowing-driven-clause-reachability.md) — Narrowing-driven clause reachability (accepted — WD1 + WD2 + WD3a implemented v0.1.17; Elixir-v1.20-inspired `flow.unreachable-clause`: extends the existing `if`/`unless` reachability rules to `case`/`when`, reusing the `falsey_scope` the flow engine already threads through `eval_case_when_branches` — a clause is dead when its `body_scope` narrows the subject to `bot` (per-clause disjointness) or the entry `falsey_scope` already carries a `bot` subject (prior-exhaustion); inherits `always-truthy-condition`'s FP envelope, never fires on `Dynamic[T]`; WD3a extends it to `case`/`in` bare class patterns (`in C` / `in C => x`, pure `is_a?`, narrowed like `when C`); WD4 swept 16 OSS corpora (zero firings → balanced stays `:info`, strict `:warning`); WD3b deconstructing/value/variable pattern exhaustiveness remains, demand-gated)
- [ADR-48](docs/adr/48-data-struct-value-folding.md) — Struct / Data value folding (accepted — `Data.define` slices 1–4 implemented v0.1.17; two new member-shape carriers `Type::DataClass` + `Type::DataInstance` (HashShape-shaped but nominal/class-tagged) + a `DataFolding` tier + a cross-file `Scope#data_member_layouts` side-table so `Point = Data.define(:x, :y); Point.new(1, 2).x → Constant[1]` for all three forms (constant / subclass / local), positional+keyword construction, and the `[]`/`to_h`/`deconstruct`/`members`/`with` projections; slice 4 guards a body-redefined reader's read via `Scope#user_def_for`; precision-additive only — no diagnostic, no FP surface; reuses the existing `ScopeIndexer` `data_define_call?`/`meta_member_names` recognition; `Struct` deferred behind its mutation-soundness story; remaining demand-gated: bare-local block-form parity + Struct follow-up)
- [ADR-49](docs/adr/49-adr-authoring-guidelines.md) — ADR authoring guidelines (accepted — in force, living rubric; meta-ADR for writing/scoring ADRs at *necessary-and-sufficient* information density: tag each ADR's archetype (deliberative / mechanical-policy / evaluation-proposal) + stakes (reversibility × blast radius × FP-envelope), then score eight axes (intent, decision criterion, actionability/code-anchoring, constraints/guardrails, verifiability/evidence, status fidelity, connectedness, economy/proportionality); economy is bidirectional (penalizes thin *and* bloated); archetype-exempt axes leave the denominator so semi-automatic ADRs aren't over-penalized — thin intent is a defect only at high stakes; calibration set ADR-43 / ADR-46 deliberative-high + ADR-40 mechanical-concise)
- [ADR-50](docs/adr/50-release-engineering-and-stability-strategy.md) — Release engineering + stability strategy v0.2.0→v1.0.0 (proposed; PHPStan-modelled): v0.2.0 = release-engineering trial (machinery + minor-non-break pledge as rehearsal), v1.0.0 = hard contract freeze (resolves the ADR-25/32-vs-37 freeze-timing split in favour of 1.0). Compatibility = enumerated public surface frozen / engine internals free; diagnostic *output* non-contract (strengthenings flow in a minor, baseline absorbs) but a *new required discipline* is BC, gated off-by-default behind a PHPStan-style inspectable bleeding-edge overlay (gem-versioned default-override set, `show-bleedingedge` diff command, granular per-feature `bleeding_edge:` opt-in) until graduation (default-on at the next major after a ~4-week soak, BC-readiness-driven; pre-1.0 rehearses at minor) (rule IDs / suppression markers / baseline+`severity_overrides:` keys / bleeding-edge feature ids stay contract vocabulary); committed `make bench-perf` + CI perf gate (Mastodon/Redmine/GitLab, wall/allocs/peak-RSS vs stored baseline within a tolerance band); support = latest + previous minor, then PHPStan `1.x`-default-branch post-1.0, latest-Ruby-only reconciled (backports keep their line's Ruby pin). WD6 = release acceptance gate. Several WDs propose concrete shapes open to adjustment before ratification)
- [ADR-51](docs/adr/51-ci-diagnostic-output-formats.md) — CI-native diagnostic output formats (accepted — partially implemented v0.1.18; `rigor check --format sarif|github|gitlab|checkstyle|junit|teamcity` + **runtime CI auto-detection** WD7 (`Rigor::CLI::CiDetector`, PHPStan `CiDetectedErrorFormatter`-modelled: default `text` output auto-emits the native form for first-class CIs — GitHub Actions→`github`, TeamCity→`teamcity` on top of the log — GitLab hints `--format gitlab`, second-class CIs hint reviewdog/`junit`; augments text only, opt-out `--no-ci-detect`/`RIGOR_CI_DETECT=0`): SARIF 2.1.0 cross-platform anchor (+ reviewdog `-f=sarif`) + GitHub Actions workflow commands (inline PR annotations, no upload step) + GitLab Code Quality JSON (MR widget) + Checkstyle XML (reviewdog `-f=checkstyle` → *any* reviewdog reporter — GitHub PR review/GitLab MR/Gerrit/Bitbucket/Gitea — + Jenkins) + JUnit XML (test-report CIs) + TeamCity inspection messages, each a pure presentation layer over the same `Analysis::Diagnostic` fields `--format json` exposes — no new analysis; criterion = *a CI platform renders the stream inline from it*; comparator PHPStan ships github/gitlab/checkstyle/junit/teamcity but **no SARIF**; WD2 fixes the severity/identifier contract table (error/warning/info → SARIF error/warning/note · GitHub error/warning/notice · GitLab major/minor/info · Checkstyle error/warning/info · JUnit failure-type; rule → `ruleId`/`title=`/`check_name`/Checkstyle `source`/JUnit `classname`), WD3 the stable GitLab fingerprint (SHA256 of `(path, rule, line, column, message)`, not index-based), WD4 leaves the exit code unchanged, WD5 maps every diagnostic to a JUnit `failure` (surfacing not gating, severity in `type`); new output format is public contract under ADR-50 WD1; WD6 ships ADR-27 § WD3's queued copy-paste CI setup templates (+ a reviewdog `action-setup` variant) and the bundled `rigor-ci-setup` skill; the GitHub *default* surface is `github` annotations not SARIF (SARIF upload needs code scanning = GHAS on private repos); `--output FILE` / reviewdog `rdjson` / rich SARIF rule metadata deferred)
- [ADR-52](docs/adr/52-compiled-plugin-contribution-dispatch.md) — Compiled plugin contribution dispatch (accepted — slices 1+2+3-partial+4-partial implemented 2026-06-10/11, corpus-/spec-gated byte-identical; slice 4 (run-time method-name callable) landed its engine + the lisp-eval/pattern examples + rigor-sorbet (2026-06-11: `dynamic_return methods:` callable over the assertion vocabulary ∪ `:absurd` ∪ `Catalog#method_names`, `T.bind`'s fact split onto `type_specifier`, strap + dependabot-core corpora byte-identical, dependabot-core wall 1262.6s→33.4s ~38×); slice 5a (per-file `file_methods:` gate — callable receiving the path, memoised per (rule, path), exclusive with `methods:`, nil-path fail-closed) landed 2026-06-11 + migrated rigor-rspec's let-binding rule (demo + GitLab corpus byte-identical), and rigor-activerecord migrated 2026-06-11 via the same run-time `methods:` gate (the receivers-gate blocker does not apply to a name gate — block keeps the AST/self_type resolution; no new gate form needed; GitLab corpus byte-identical), so ALL FIVE legacy users are migrated; slice 5b landed 2026-06-11 (hook DELETED — load-time ArgumentError for definers + CHANGELOG migration note) and slice 6 landed 2026-06-11 (engine-owned `Plugin::NodeRuleWalk`, one walk per file, plugin-major order preserved byte-identical) — the ADR is complete; slice 2 = receiver-less `dynamic_return` (`methods:`-only static gate) + rigor-units; slice 3 = run-time `receivers:` callable (lazy `instance_exec` memo, resolution in the instance `dynamic_return_type` path not the ContributionIndex, set is a safe over-approximation of the block filter) + rigor-activestorage (GitLab-gated); rigor-activerecord BLOCKED — its class-side dispatch is AST/`self_type`-keyed but the `dynamic_return` gate keys on `receiver_type`, and project model constants type as `Dynamic` (a migration attempt was reverted; needs an AST-constant/implicit-self gate form or engine Singleton-typing of discovered classes); AUDIT CORRECTION made while implementing slice 2 — activesupport-core-ext ships no `flow_contribution_for` (pure RBS bundle) and sorbet's catalog matches arbitrary def names (run-time method-name set, slice 4), so no production plugin fit the static gate; grounded in the 2026-06-10 plugin-architecture structural audit note; criterion: every per-call/per-def/per-file/per-node plugin consultation is gated by a key the engine already holds (method-name Symbol / receiver class name / file path / node class) via a structure compiled once per run at registry build — plugin code runs only on candidate hits, and a capability that cannot declare its key is a DSL vocabulary gap, not a license for an ungated hook; WD1 compiled contribution table (method-name inverted indexes, verb-keyed `block_as_methods`, `open_receivers` Set, ancestry-memoised `owns_receivers`/`additional_initializers`, per-path contract memo) + WD2 three new DSL forms (run-time receiver sets after `#prepare`, method-name-only gating, per-file name sets) + WD3 migrate the five legacy `flow_contribution_for` users (activerecord/activestorage/activesupport-core-ext/sorbet/rspec) then **delete** the hook (deliberate pre-1.0 BC break) + WD4 single engine-owned node-rule walk per file; WD5 rejects a per-call-node result cache (scope-sensitive → FP risk); WD6 gate = byte-identical diagnostics on Mastodon/GitLab corpora + stackprof deltas + `make bench-perf`; frozen table is Ractor-shareable (ADR-15 Phase 4); completes the ADR-37 segregation arc)
- [ADR-53](docs/adr/53-scope-discovery-index-separation.md) — Scope discovery-index separation + check-rule walk consolidation (accepted — Track A complete: A1+A2 landed 2026-06-10/11, the 14 per-table writers deleted and seeding collapsed onto `with_discovery`, corpus byte-identical; Track B (shadow-harness-gated walk consolidation onto the landed ADR-52 `Plugin::NodeRuleWalk`) remaining; grounded in the 2026-06-10 lib/rigor architecture re-review note, Phase 4; Track A extracts the 14 seed-time discovery tables (`discovered_*` / `declared_types` / `class_ivars`+`class_cvars`+`program_globals` / `data_member_layouts`) out of `Scope` into one immutable `Scope::DiscoveryIndex` — membership criterion: no flow transition ever varies the field (litmus: `Scope#==` ignores it, `#join` copies it unexamined); readers stay on `Scope` as delegates (plugins unaffected, ADR-46 `DependencyRecorder` choke point preserved), `with_discovered_*` writers collapse into `with_discovery`; re-adopts the regrouping ADR-44 downgraded as an allocation lever, now on the boundary/boilerplate criterion (new side-table: 7 edit sites → 1, `rebuild` 24 kwargs → 11), perf neutral `make bench-perf`-gated; Track B consolidates `CheckRules`' five per-file AST walks (main pass + 4 collectors) into one engine-owned rule walk (ADR-52 WD4 model for built-ins, → 2 walks/file with the plugin walk) behind a **mandatory shadow-run equivalence harness** — collectors transplant verbatim, only traversal merges; every slice gates on byte-identical diagnostics)
- [ADR-54](docs/adr/54-cache-slimming.md) — Cache slimming: definitions-blob retirement, payload compression, default eviction (accepted — WD1–WD4 implemented 2026-06-10; grounded in the 2026-06-10 cache disk + warm-load audit note; criterion: a cache tier earns its bytes only if it beats recomputation from the next-cheapest tier on the warm path; WD1 retired the `rbs.instance_definitions`/`rbs.singleton_definitions` disk blobs (net-negative given the cached env: 366 ms `Marshal.load` vs 137 ms build-all; 23.4 MB/project) — dispatch path goes lazy per-class on the existing per-process memos, ADR-15 prewarm/Reflection keep eager full tables built from the cached env; WD2 zlib-deflates entry payloads behind a format-byte bump (env blob lands at 1.76 MB = 16 % of raw, old entries read as silent misses — no migration); WD3 defaults `cache.max_bytes` to 256 MB so `evict!` stops being a permanent no-op (the rigor repo's own cache held ~180 MB of orphans vs a ~2 MB active set; explicit `null` restores unbounded); WD4 memoises `RbsDescriptor.build` per loader; envelope ~33.7 MB → ~2 MB/project (−94 %), warm runs touching definitions save up to ~550 ms / 1.6 M allocs; partially supersedes ADR-7 § Slice 6-D; cross-project shared root deferred, ADR-45 `fresh?` mtime fast-path rejected on soundness; gated per slice on diagnostics-identical self-check + Mastodon-corpus runs)
- [ADR-55](docs/adr/55-recursive-return-precision.md) — Recursive-method return-type precision (accepted — slice 1 fueled constant-arg unroll implemented 2026-06-11 (guard key → `(receiver, method, arg values)` when all args value-pinned, fuel 32 + 64-node value-size cap, exhaustion = today's widening via `BudgetTrace::RECURSION_UNROLL_FUEL`; value-pinned self-call results adopted only while an unroll is in progress so `factorial(5) → Constant[120]` folds without leaking adoption into the main check walk); governing clamp (WD1, corpus-FAIL fix 2026-06-11): the unroll may only ever surface a fully value-pinned result — a guarded re-entry whose body folds non-pinned clamps back to `untyped`, and value-pinned self-call adoption is confined to in-progress unrolls (blanket adoption leaked the body evaluator's block-internal-`return` blind spot → 3 corpus FPs on mastodon/haml); slice 2 fixpoint return summaries implemented 2026-06-11 (Kleene from `bot`, `RECURSION_FIXPOINT_CAP = 3`, final-round `Constant→Nominal` widening, non-convergence → `untyped` + `BudgetTrace::RECURSION_FIXPOINT_CAP`; summary table keyed by plain `(receiver, method)` signature shared with extended unroll frames, per-signature *consulted* flag gates the loop, entry dropped at stack-empty alongside the fuel reset; in-cycle re-entry returns the assumed summary instead of `Dynamic[top]` so a `String`-builder → `String` and `factorial(x: Integer) → 1 | Integer` with no `Dynamic` contribution; WD4 narrowed — the slice-1 clamp stays bare `untyped` (soundness backstop, the in-progress summary is a Kleene lower bound), only guard/fuel paths consult the summary; mutual recursion across distinct signatures degrades soundly to `Dynamic[top]`, termination load-bearing); instantiates ADR-41 WD5's deferred precision-unroll, preserves ADR-24 WD5's guard-key decision; precision-additive only — no new diagnostic, both Mastodon `app/models` + haml `lib` byte-identical to the pre-slice-2 baseline, `make bench-perf` neutral)
- [ADR-56](docs/adr/56-block-captured-local-mutation.md) — Block-captured local write-back + loop-body fixpoint (accepted — slice A non-escaping-block captured-local write-back **implemented 2026-06-11** (`write_back_block_captures` + shared `Inference::BodyFixpoint` cap-3 helper for slice B + `BudgetTrace::BLOCK_WRITEBACK_CAP`; corpus one-removal/zero-new, perf neutral), slice B loop-body fixpoint **implemented 2026-06-11** (`eval_loop` overlays a `BodyFixpoint.converge` result for body-rebound locals over the historical single-pass base; pre-existing vs body-first seed partition, per-iteration predicate-edge re-narrowing, no-write loops byte-identical; gate green — Mastodon/haml byte-identical, kramdown two FP removals + message-rewordings at stable sites; one in-slice blind-spot fix — body-first nil seeds kept only as a join constituent, not overlaid into the body re-eval); **soundness fixes**: block exit scopes are discarded so `result = 1; 1.upto(6) { result *= i }` keeps `Constant[1]` (runtime 720) and `while` joins one body pass (`1 | 2`), violating the spec captured-local MUST; criterion: a captured local a block/loop body can rebind never keeps its pre-call binding unmodified — pre-state joined with capped-fixpoint post-state (cap 3, final `Constant→Nominal` widen, per-local `Dynamic[top]` floor on non-convergence; one shared helper); gate is adjudicated-not-zero-delta (corrected constants may fire genuine diagnostics) + mandatory hand-probed discriminating shapes; slice C receiver-content element-type join (WD2.5) **implemented 2026-06-12** — the A/B write-back covered local *rebinding* but not receiver *content* mutation (`out = [0]; [1,2,3].each { |x| out << x }` → `Array[0]` for a runtime `[0,1,2,3]`, unsound with derived wrong folds `out.first.zero? → true`); `content_writeback_block_captures` (block), `loop_content_writeback` (loop), `each_with_object_return` (B3) JOIN appended/stored element/key/value types into the continuation collection reusing the A/B carrier-widening; covers Array/Hash/String mutators + index-write forms; gate green — haml one FP removal, kramdown one message-rewording, Mastodon byte-identical, zero new genuine firings)
- [ADR-57](docs/adr/57-self-call-return-adoption.md) — Opening the implicit-self call return-adoption gate (accepted — gate opened 2026-06-12, slices 1–3; ADR-24 WD3 revisit closing the largest remaining Dynamic source for procedural Ruby: a resolved in-body user-method call now adopts the callee's inferred return, `adoptable_self_call_result?` removed. WD1 measured +25 self-check firings on unconditional opening; criterion = open per adjudicated firing class (genuine = win, artifact = engine bug fixed at root). Slice 1 fixed the tail-only body evaluator dropping explicit / block-internal / multi-value `return` (15 of 25); slices 2–3 fixed the escaping block-captured content floor + receiver-chain / cross-method-boundary variants, FP-safe optional-tuple-slot destructure softening (−2 gate-closed haml FPs), the live String fold-guard, and 2 over-strict self-authored RBS. Residual all genuine-or-win (self-check + plugin self-check firing-free; haml/kramdown identical; Mastodon one pre-existing error reworded more precisely). Cost ~+12 % cold `--no-cache lib` wall (intrinsic callee re-typing); sound return-result memo = queued perf follow-up (ADR-52/24 WD5 forbid the naive caches). **WD3 (2026-07-10) closes module-singleton resolution** — the typing tier had landed but the project-wide discovery seed registered only classes, so a module constant typed `Dynamic[top]` cross-file (`Feature.enabled?` = 695 unprotected GitLab sites); modules now seed on the same terms as classes, the old `Kernel.select → Array[String]` hazard measured as non-reproducing (of the 28 Object/Kernel/Module/Class methods that resolve on a bare module, 26 are correct; `M.new`/`M.superclass` mistype only code that raises). Adjudicated per WD2: one self-check latent-strictness firing fixed at root (`DiagnosticFormats.render` made total by raising on an unknown format); haml/kramdown/liquid/rgl/Mastodon byte-identical; one Redmine `possible nil receiver` traced to an INDEPENDENT artifact class — `resolve_rbs_extended_method` hands a union receiver no `rigor:v1:predicate-if-true` facts, so an ActiveSupport `present?` guard never narrows nil — queued as its own FP-removal slice (on a zero-arg predicate's truthy edge, drop every union arm whose RBS return is literally `false`))
- [ADR-58](docs/adr/58-ivar-field-typing.md) — Instance-variable field typing (accepted — not yet implemented; 94 % of algorithm-corpora possible-nil errors are node-field ivar reads typed `Dynamic[top] | nil` whose ctor-seed nil fires on invariant-guarded reads; criterion: declaration-sourced nil is not diagnostic fuel — only flow-live nil fires; slices: WD1 provenance-gated firing → WD2 homogeneous-write field reads (`current.next → Node | nil`) → WD3 ctor definite assignment via same-class calls (ipaddr cluster); precision deliberately sequenced AFTER the firing-policy change so it cannot manufacture FPs)
- [ADR-59](docs/adr/59-spec-assertions-are-not-signatures.md) — Spec assertions are not implementation signatures (accepted — strong form rejected as policy; retires the founding-era `expect(foo(1)).to be_a(String)` ⇒ `foo: (Integer) -> String` idea: assertions are per-input *witnesses* bounding signatures from the wrong side on both ends — observed returns / example args are lower bounds where ADR-5 needs upper bounds — plus pending/tag-filter/suite-greenness unknowability; criterion: spec-derived claims may land only where being wrong cannot fire a diagnostic — the `Dynamic` interior, the sig-gen review channel, or a check *against* inference; the blocker is policy not architecture (the factorybot `producer` fact pattern would carry it); three demand-gated weak forms recorded: `spec.impossible-assertion` contradiction diagnostic (matcher narrowing → `bot`), `Dynamic[top]`→`Dynamic[T]` interior hints via a `spec_return_claims` fact, runtime TracePoint → sig-gen in a separate gem; README commitment #2 reworded in the same change)
- [ADR-60](docs/adr/60-pre-freeze-plugin-contract-consolidation.md) — Pre-freeze plugin contract consolidation (accepted — last-window BC breaks before the ADR-50 v1.0 surface freeze, from the 2026-06-13 plugin-interface review; criterion: a surface enters the freeze only if wired, consistently named, and incapable of silently-wrong results when used as documented; WD1 removes the never-wired `external_files:` field, WD2 renames `BlockAsMethod` `verbs:`→`method_names:` + `NestedClassTemplate` `name_arg_position:`→`symbol_arg_position:`, WD3 moves `cache_for` onto record-and-validate (`fetch_or_validate` + post-compute IoBoundary capture) with `producer watch:` glob coverage via `Cache::Descriptor::GlobEntry` — retiring the "prime before cache_for" stale-cache hazard, WD4 ships the `read_fact` / `producer_value` / `diagnostics_for` authoring helpers + bundled-corpus migration, WD5 aligns both plugin-author SKILLs; keep-verdicts recorded: `dynamic_return`/`type_specifier` split, `diagnostics_for_file`, `config_schema` dual grammar; gate = ADR-52 slice-5b precedent + cross-process watched-file cache regression + corpus byte-identical)
- [ADR-61](docs/adr/61-agent-friendly-diagnostic-statistics.md) — Agent-friendly diagnostic statistics (accepted — implemented 2026-06-13, precision-additive/byte-identical; ADR-23 follow-up letting an AI agent / `jq` compute class-method statistics over a run without parsing message text; Criterion A: message text is presentation not contract — any datum an agent aggregates/branches on must be a structured JSON field, never recoverable only from the `message` (ADR-23 WD3's structured-not-string rule promoted to the public surface); Criterion B: lossy folding lives at the aggregation layer not the primitive stream — `check --format json` carries faithful per-site `receiver_type`/`method_name` (a `Constant<"x">` renders `"x"`), `triage` owns the normalisation; WD1 un-hides the two fields on `Diagnostic#to_h` (omit-when-nil), WD2 adds a `triage` `selectors` axis (`{receiver, method, count, files, rules}` keyed on `(receiver, method)`, uncapped JSON / text-capped 15, `--selectors-only`), WD3 places the literal-fold in `Triage.normalize_receiver` shared with the heuristic `Catalogue` (guards `"x".nope` vs `name.nope` fragmenting), WD4 populates the fields on the 11 call/def rules with a dispatch subject, excluding `flow.*`/ivar (no subject); guardrail: the axis routes attention (`pre_eval:`/RBS overlay), never feeds severity; frozen public vocabulary under ADR-50 WD1, landed in the ADR-60 pre-freeze window; the two onboarding skills teach the `jq` workflow)
- [ADR-62](docs/adr/62-mutation-testing-teeth-measurement.md) — Mutation-testing the analyzer (false-negative / teeth measurement) (accepted — harness + first fixes landed 2026-06-13, methodology + adjudication discipline in force, remaining backlog demand-gated; a dev-only mutation harness `tool/mutation/` breaks analyzed code in type-visible ways and reads *surviving* mutants as false-negative candidates — the dual of Rigor's anti-FP discipline, making the anecdotal "teeth verified" (ADR-43/24) measurable; Criterion A = a survivor is signal only where Rigor holds a concrete type (most mutations are type-invariant equivalent mutants whose survival is correct → the type-aware anchor filter, not raw kill-rate, is load-bearing, and the harness is in-process to consult the engine's own `type_of`); Criterion B = adjudicate-don't-assume (a survivor is a candidate not a bug; some are deliberate silence; a fix lands only where soundness is total and `make verify` stays clean, ADR-57 protocol); WD1 build-our-own Prism-native, `mbj/mutant` rejected; WD2 warm loop (`ProjectContext` env+scan once + `prebuilt:`-bypassed run cache → ~6–12 ms/mutant); WD3 sweep-as-backlog (cluster survivors by `(operator, receiver type)`, `--json` for an agent → self-improving loop); WD4 dev-only, off the ADR-50 frozen surface; WD5 landed = FP-safe union-receiver undefined-method teeth (fires when every non-nil arm certainly lacks the method) + RBS class-alias resolution (`Mutex = Thread::Mutex`) + broad-fuzz mode (`mutate.rb fuzz` crash/hang/non-determinism, 2,706 lib/rigor mutants clean); studied+REJECTED = nilable-union teeth (corpus study 13 projects → ~0 firings + an FP, N3 silence kept; harvested 2 FP guards onto shipped slice-1 — generic-metaclass + distinct-class, fixing a mail external FP); deferred = `argument-type-mismatch` adjudication, `Type::*` self-dogfood RBS, user-facing type-protection coverage; grounding `docs/notes/20260613-mutation-teeth-harness.md`)
- [ADR-63](docs/adr/63-type-protection-coverage.md) — User-facing type-protection coverage (accepted — Tier 1 static proxy = v1, Tier 2 mutation effectiveness designed + phased; extends `rigor coverage` with a *protection* dimension = "if I introduce a bug, would Rigor catch it", productizing ADR-62's teeth work; framing criterion = always effectiveness/where-to-add-a-type, never raw mutation survival; Tier 1 = one-pass dispatch-site scan, a call is protected when `concrete_class_name(receiver)` is non-nil (Rigor *can* bite) vs `Dynamic`, sound upper bound, reuses `PrecisionScanner`, lists unprotected "add a type here" sites; Tier 2 = per-site mutation kill rate, opt-in `--protection --mutation`, changed-files-scoped, ships only a narrow subset of the harness — the dev sweep/fuzz/clustering stay dev-only per ADR-62 WD4; WD1 extend `coverage` not a new command reusing `--threshold` gate + `--format json`; rejected = raw survival headline / a `rigor teeth` command / mutation-as-default / shipping the full harness)
- [ADR-64](docs/adr/64-non-nil-argument-type-mismatch.md) — Non-nil argument-type-mismatch and the coerce barrier (accepted — the non-nil channel is built + gated for **multi-overload** methods; the cycle first shipped the FP-safe `nil` channel of `call.argument-type-mismatch` — a pure-`nil` arg rejected by every overload, nil-admittance decided on the RBS param type so it sees through multi-overload + interface-alias params via `param_admits_nil?`, commits `a2775c7b`/`0afc65ea`; the ADR-62 sweep left the non-nil `type_swap` wrong-typed-arg cluster ~900 mutants as the largest remaining false-negative, blocked by Ruby's **`coerce` protocol** — `5 + Money.new` is valid via `Money#coerce` though no RBS `Integer#+` overload lists `Money`, so a naive "no overload accepts the arg" rule false-positives on coercible user types; criterion = fire only where runtime acceptance is decidable from types alone: WD1 **exclude the coerce-dispatch operators** (`+ - * / < > …`, fixed allow-list à la `UNIVERSAL_EQUALITY_METHODS`), WD2 generalize `param_admits_nil?` → `param_accepts_arg_class?` for non-operator methods (does the arg's class implement the param's `_ToStr`/`_ToInt` interface, reusing `expand_type_alias` + `interface_method_names`), WD3 single concrete RBS-known arg class only; gate **MET** — zero new firings across 31 `rigor-survey` projects (incl. Mastodon `app`+`lib`), `type_swap` mutation kills up (kramdown 5→9, liquid 0→7); rejected = naive every-overload-reject / modeling `coerce`; carry-over = namespaced type-alias resolution `::MatchData::capture`, the single-overload comparison-operator path unchanged)
- [ADR-65](docs/adr/65-diagnostic-evidence-tier-and-doc-url.md) — Diagnostic evidence tier + documentation URL (accepted — implemented 2026-06-15, precision-additive/byte-identical; from the 2026-06-15 type-tool-comparison developer-feedback report §4+§5.1; two additive `--format json` / rule-catalogue fields: `evidence_tier` (`high`/`medium`/`low`, none for informational) = Rigor's own confidence a firing is a true positive, + a stable `documentation_url`; ADR-61 follow-up adding a *confidence* axis + a *reference* axis; WD1 tier is a **per-rule** `RuleCatalog` property not a per-firing computation (FP-discipline filters uncertainty *before* a rule fires → confidence is dominated by the rule's gates; the feedback's own §3 reasons per-rule-kind); WD2 semantics — high=concrete static type/no metaprog escape, medium=flow/inference proof with a documented FP envelope, low=resolution/coverage-gap routing to review (`call.unresolved-toplevel`→`pre_eval:`) — **orthogonal to severity, never gates**; WD3 `documentation_url` = per-rule anchor in the published catalogue (`04-diagnostics.md#rule-<id>`), no `rigor.dev/rules` site invented (honesty ethos), configurable base deferred; WD4 emitted per diagnostic on `check --format json` (built-in rules only, enriched in the CLI JSON path so no construction site changes, omit-when-nil) + per rule on `rigor explain`/`--format json` (= the machine-readable taxonomy §5.1 wanted, free); **flat field** not the feedback's nested `evidence` object; also closes a catalogue gap (`call.unresolved-toplevel` had no entry) + adds a completeness spec; rejected per-firing dynamic tiers / the nested object / a docs site to non-existent pages / feeding the tier into severity)
- [ADR-66](docs/adr/66-discriminated-union-member-typing.md) — Discriminated-union member typing / tag-keyed narrowing (proposed — not implemented, demand-gated; narrow a polymorphic payload field `kramdown::Element#value` by its sibling discriminant tag `Element#type`, for the AST/visitor/converter genre `case node.type; when …; node.<payload>` where the payload type is a function of a tag — today `untyped` = safe-but-unprotected; Rigor already owns both halves (ADR-47 tag narrowing + ADR-48 member layouts), the missing connective is a `(tag ⇒ payload)` map; criterion (FP envelope): narrow only on a sound over-approximation else no-op — always *adds* protection, never fires on guarded code (ADR-58 kinship); staged WD1 additive-only floor / WD2 Tier 1 declared `%a{rigor:v1:variant}` map + tag-indexed carrier, both-sides-authored FP-safe / WD3 Tier 2 inferred map budget-gated (unsound under dynamic construction) / WD4 Tier 3 visitor-dispatch tag binding via ADR-16 — hardest but where most consumers live, the asymmetry ranking this below ADR-58 / WD5 mutation invalidates; rejected flat-union-that-fires / unguarded whole-program inference / general GADT; grounded in the 2026-06-16 protection-uplift pilot)
- [ADR-67](docs/adr/67-parameter-type-inference.md) — Parameter type inference / the M3 frontier (proposed — demand-gated; re-opens the algorithm-survey's "M3 untyped-param → whole-method Dynamic: EXCUSED, do not pursue" verdict on protection-coverage evidence — `def` params default `untyped` (no call-site inference; block params ARE inferred), so `@line = line` (param) keeps `@line` Dynamic and `@line.<method>` unprotected, distinct from ADR-58 whose ivar typing is *realized* for concrete-write fields but can't touch a param-sourced ivar; survey excused M3 on leaf scripts with no call-site seed, the 2026-06-16 pilot shows it's the dominant remaining protection hole on real apps (faraday/haml) where call sites exist → re-opens it; criterion (robustness reconciliation, ADR-5 keeps params lenient): inferred param type is precision-additive ONLY — sharpens downstream ivar/receiver/protection/folding, NEVER rejects a caller's arg (param boundary stays lenient, RBS-declared wins); staged WD1 precision-additive-only contract (inferred-not-declared provenance, ADR-58 WD1 pattern) / WD2 in-body structural lower bound (no call sites, helps leaf scripts) / WD3 call-site union TypeProf-style (app-scale lever, whole-program-ish → budget-gated, ADR-46 tension, dynamic-dispatch → untyped fallback) / WD4 soundness fallbacks / WD5 budget+termination (ADR-41 cap, ADR-57 memo reuse); rejected inferred-as-declared (ADR-5 breach) / survey's do-not-pursue (superseded for protection) / full TypeProf interpreter as default; grounding algorithm-corpora survey + 2026-06-16 pilot + TypeProf-internals note)
- [ADR-68](docs/adr/68-class-builder-folding.md) — Plugin-declarable class-builder folding (proposed — demand-gated; generalize the ADR-48 member-shape carrier substrate (`StructClass`/`DataClass` + `data_member_layouts`) from hard-coded `Struct.new`/`Data.define` to a *declared* family of Struct-like builders, so faraday's `ConnectionOptions = Options.new(:a, :b, …)` types as a named member-shape class instead of Dynamic — the 2026-06-16 pilot's faraday ceiling ~31% is gated by `Options.new`-built constants (`Env`/`*Options`) typing as dynamic class objects; same shape ADR-48 solved, blocked only on recognising a non-core builder; criterion: a constant from a *declared* Struct-like builder folds to an ADR-48 carrier (members = symbol args), builder must be declared (plugin/allow-list) or followed to a Struct ancestor, NEVER guessed from arbitrary `X = Y.new` (a `.new` not returning a class must not misfold), precision-additive only (unrecognised → today's Dynamic); WD1 reuse carriers add only recognition / WD2 plugin-declared via ADR-16 substrate (rigor-faraday Options.new first consumer) / WD3 inferred builder-following deferred (`.new` may be overridden) / WD4 compose with ADR-26 open_receivers for the dynamic surface (known-but-open) / WD5 mis-recognition guard; distinct from ADR-26's AR open-receiver case (unbounded surface ≠ fixed members); rejected new carrier / guessing any X=Y.new / folding AR here; grounded in the 2026-06-16 protection-uplift pilot)
- [ADR-69](docs/adr/69-pluggable-mutation-substrate.md) — Pluggable mutation substrate (kill-oracle + operator seam) (accepted — **both seams implemented 2026-06-17** co-landed with ADR-70; Seam 1 kill-oracle extracted `DiagnosticOracle`, Seam 2 site-selector surfaced as `--include-dynamic` (`Mutator#dispatch_site_mutations` mutates every dispatch site incl. Dynamic, gated to `--with-tests`) pulled forward from ADR-71 the same day when validation showed the fused overlay was blind to Dynamic sites; generalize the ADR-62/63 mutation machinery so the **kill oracle** and **site selector** are injected collaborators of `Protection::MutationScanner`, not assumptions baked in — today's only kill is "a new Rigor diagnostic" (`mutation_scanner.rb` classify) and only selector is "keep sites Rigor can bite" (`mutator.rb` filter_by_type), both wrong for a test-suite consumer that kills by *running tests* and wants the inverted `Dynamic`-site selection; criterion: the `Mutator` knows only how to splice source + where contracts live (the anchor), *what counts as a kill* + *which sites to mutate* belong to the consumer; Seam 1 = `DiagnosticOracle` (byte-identical) + the `TestSuiteOracle` interface ADR-70 implements, Seam 2 = the type filter becomes a swappable `BiteableSites`/`AllSites` strategy; no new CLI/JSON surface (ADR-50 freezes vocabulary not `Protection::*` shapes); co-lands with ADR-70's first consumer; grounding the 2026-06-17 strategy note)
- [ADR-70](docs/adr/70-fused-protection-coverage.md) — Fused static∪dynamic protection coverage (accepted — implemented 2026-06-17 `coverage --protection --mutation --with-tests`, co-landed with ADR-69, **validated on faraday/liquid/mail**; opt-in **dynamic overlay** — for each mutant the type checker fails to kill, ask whether a *test* kills it (ADR-69 `TestSuiteOracle`, runner hook `--test-command`, run with a stripped bundler env), classify each site type-protected / test-protected / **unprotected** (conceptual doubly-protected collapses into type-protected via the gradual short-circuit — a type-killed mutant never reaches the suite); `--include-dynamic` (ADR-69 Seam 2) extends to Dynamic sites, completing the map (liquid lexer 76→115 sites, 38 test-protected + 2 real gaps); the artifact is the fusion (a line is unprotected only when NEITHER a type nor a test guards it) + the **cheapest-missing-axis** attribution ("add a type" vs "add a test"); no tool fuses static type-protection and dynamic test-protection; criterion extends ADR-63 framing — always effectiveness, payload is attribution not a single %, unprotected only when both miss; **gradual short-circuit** = type-killed mutants never reach the suite (cost ∝ the hole); WD2 the test oracle is an ADJUDICATED candidate not a verdict (ADR-59 — pending/tag/flaky unknowable); **selection is the suite's job NEVER the dependency graph** (type-read graph ≠ runtime call-graph → skips the killing test → false survivor = FP); rejected run-suite-for-every-mutant / single fused % / graph test-selection / a `rigor teeth` command; grounding the 2026-06-17 strategy note)
- [ADR-71](docs/adr/71-type-guided-external-mutation-testing.md) — Type-guided external incremental mutation testing (proposed — deferred/demand-gated, nothing implemented; evaluates growing the substrate into an EXTERNAL library driving RSpec/minitest pruned by Rigor's types + dependency graph; verdict DEFER — the pitch "types prune the mutation space → smarter than ordinary mutation testing" is OVERSTATED, the surviving novel wedge is **incremental/CI-scoped** mutation testing on ADR-46 not smart pruning; a distinct *third* product (mutation testing of the user's TEST SUITE, survivor=test gap) sharing only the Prism splicer ~20% with ADR-62's analyzer-teeth tool, the test-runner 80% is mutant's graveyard; criterion = each lever only where sound: runtime COVERAGE selects tests (a static graph over-approximates + misses dynamic/metaprog calls → skipped killing test → false survivor = FP), the dependency GRAPH scopes only which files to mutate on a diff, TYPE info does generation + stratified reporting never test selection; re-eval triggers (both) = (a) ADR-46 incremental landed + (b) demand demonstrated; rejected static-graph test-selection / "type pruning makes it worthwhile" (bites low-value typed sites) / build-it-now pre-freeze / mutant-as-engine; grounding the 2026-06-17 strategy note's full expansive-vs-critical analysis)
- [ADR-72](docs/adr/72-gemfile-lock-gated-rbs-overlays.md) — Gemfile.lock-gated bundled RBS overlays (accepted — implemented 2026-06-17; from the 2026-06-17 `evidence_tier` calibration feedback, where a consumer trusting `evidence_tier == high` (ADR-65) auto-promoted 171/512 high-tier `call.undefined-method` on Mastodon (~33%, all ActiveSupport core-ext like `3.minutes`) to "live bug"; when a gem is locked in Gemfile.lock but ships no RBS (`RbsCoverageReport` `:missing`), Rigor auto-loads a bundled per-gem RBS overlay `data/gem_overlay/<gem>/` for its core-class extensions so a Rails project stops seeing the systematic false undefined-method while a project WITHOUT the gem still sees the genuine diagnostic; activesupport is the first/only v1 overlay — the auto-applied lock-gated twin of the opt-in rigor-activesupport-core-ext plugin; criterion = fix the FP at the source not relabel it (down-tiering does not serve FP-discipline — tier never feeds severity per ADR-65 WD2, the red error stays on working code); WD1 gate = locked ∧ `:missing` ∧ no conflicting opt-in plugin loaded (`GEM_OVERLAY_PLUGIN_IDS`), on by default via `bundler.auto_detect`; gating on actual gem presence is what makes it sound / WD2 FP-safe direction only — can only remove a diagnostic where the method is real at runtime, a genuine typo (`5.minuets`) still fires `:high` / WD3 overlay dirs ride in `for_project`'s `loader_signature_paths` so the env cache descriptor digests + invalidates them for free / WD4 PRESERVES ADR-65 WD1 (tier stays per-rule, no per-firing dynamic tier) and does NOT reverse the ADR-27/31 plugin auto-load deferral (that defers plugin code; this loads RBS data only, gated on presence — the `core_overlay` mechanism made lockfile-conditional) / WD5 generalizes to other core-class monkeypatchers (i18n/Sequel) by adding a directory, demand-gated; rejected per-firing tier demotion (the feedback's literal ask — reverses ADR-65's rejected per-firing-dynamic-tiers, leaves the red FP) / severity suppression (false-negative on real core-type typos) / full-plugin auto-load (runs code) / unconditional overlay (unsound for non-Rails))
- [ADR-73](docs/adr/73-skill-driven-user-experience.md) — SKILL-driven Rigor user experience (accepted — WD1–WD5 implemented 2026-06-20 (`describe` catalogue designed to grow; first addition `rigor-rbs-setup`, same day); a single SKILL-driven "what next" entry point `rigor-next-steps` backed by a live, version-current `rigor skill --describe` so distributed onboarding guidance never goes stale (陳腐化); criterion = guidance is frozen into the distributed SKILL ONLY if it must run before `rigor` exists (command resolution, install), everything version-coupled — the downstream-skill catalog, descriptions, project-state→next-step recommendation — is emitted LIVE by `rigor skill --describe`; WD1 `rigor-next-steps` body = bootstrap chain only (resolve `rigor` → install via docs/install.md → project-init if unconfigured → else delegate to `--describe`); WD2 `--describe` = a presence-only project-state probe (config/baseline/`sig/`/CI — NEVER runs `rigor check`) + a recommended next skill + a frontmatter-description catalog + an agent action-prompt, read-only/side-effect-free on `SkillCommand` (lib/rigor/cli/skill_command.rb); WD3 the install branch is the one un-`--describe`-able step (no binary yet) so it points at docs/install.md's raw URL which defers back to the same routing; WD4 promotes the parked pilot-validated `rigor-protection-uplift` (ADR-63 WD5) into `skills/`; WD5 two-channel distribution (vercel-labs/skills `npx skills add` pre-install + gem-bundled `rigor skill` post-install, one `skills/` source per ADR-31) + `.claude/skills/` marked `metadata.internal: true` + a new `skills/README.md`; new `--describe` output + skill ids freeze as public vocabulary at v1.0 under ADR-50 WD1; rejected embedding routing in the distributed SKILL (the staleness this prevents) / `--describe` running a full analysis / a `rigor` subcommand entry point (cannot bootstrap its own absence) / a separate `rigor-skills` gem / leaving `.claude/skills/` discoverable; catalogue extends via cheap presence-only `describe` branches (DX/integration skills are catalogue-first when their config is user-local) — first wave of 7 landed same day — `rigor-rbs-setup` (headline branch) + catalogue-first DX `rigor-editor-setup`/`rigor-mcp-setup` + catalogue-only maintenance `rigor-monkeypatch-resolve`/`rigor-plugin-tune`/`rigor-upgrade`/`rigor-doctor`; a skill earns a headline recommendation only with a reliable presence signal, else catalogue-only; **2026-07-05 amendment generalizes WD1's thin-shell/live-core split from the entry point to EVERY skill body** — each now carries a "First: load the version-current copy" directive that re-fetches the current body via the new `rigor skill --full <name>` (inlines SKILL.md body + all `references/*.md` in one call), because a `npx skills`-vendored copy is frozen at install time while `rigor skill` always reads the gem, so the directive resolves upgrade-time divergence toward the installed version; hybrid calibration keeps version-coupled step detail in `references/` and the stable scaffold in SKILL.md (`rigor-doctor` split as the demo, most skills directive-only), `rigor-next-steps` stays a pure pre-install bootstrap, `rigor skill --full` freezes as public vocabulary at v1.0 under ADR-50 WD1)
- [ADR-74](docs/adr/74-offline-doc-access-and-llms-txt.md) — Offline doc access (`rigor docs`) + `llms.txt` linkage (accepted — design ratified 2026-06-20, implementation queued WD1–WD4; completes ADR-73 on the docs axis: an installed Rigor serves the docs an agent needs OFFLINE via a new `rigor docs` command over a gem-bundled `docs/manual`, and the `llms.txt` agent doc-discovery index reflects the new skill surface + records the offline path; criterion = a doc an agent consults to DRIVE Rigor belongs in the gem served by `rigor docs` (the doc twin of `rigor skill print`), so once Rigor is installed the SKILL-driven UX never needs the network — the web `llms.txt` + GitHub raw URLs stay the PRE-INSTALL fallback (installing Rigor necessarily precedes a local `rigor`); WD1 bundle `docs/manual/**/*.md` + `docs/install.md` in the gemspec (drive-Rigor chapters only; the contributor-facing ADR/spec/notes corpus stays web-only); WD2 `rigor docs` (index / `<name>` chapter / `list` / `path`, `lib/rigor/cli/docs_command.rb`, mirrors `rigor skill`'s shape, reads the bundled `docs/`, no network) + a gem-side `llms.txt` mirror; WD3 the editor/mcp/next-steps skills prefer `rigor docs <chapter>` local with the GitHub URL as the pre-install fallback (only next-steps' install step is irreducibly network — `docs/install.md` precedes `rigor`); WD4 refresh the site `llms.md`/`llms-ja.md` (one-prompt → `rigor-next-steps`, skill list → catalogue + `rigor skill describe`, index ch.17, add the offline note) — the manual is the single source both `llms.txt` copies index; rejected web-only docs / a separate docs gem / runtime-fetching the site `llms.txt` / bundling the whole `docs/`; `rigor docs` + `llms.txt` vocabulary freeze at v1.0 under ADR-50 WD1)
- [ADR-75](docs/adr/75-dynamic-provenance.md) — `Dynamic[T]` provenance and explanation (accepted — implemented 2026-06-24 `01e291cb`; from the 2026-06-22 compatibility-safe strengthening survey §3 / P2 + the ADR-73 coverage-tractability-labels follow-up that named this its blocker: the engine carries `Dynamic[T]` for an unchecked value but never records WHY it became dynamic, so `coverage --protection`'s "add a type here" list can't tell a tractable hole (install RBS / enable a plugin) from an intractable one (a DSL boundary); criterion = provenance is metadata ABOUT a dynamic value, never part of WHAT it is — a fixed cause set (external-gem-without-rbs / framework-dsl-boundary / analyzer-budget-cutoff / explicit-untyped / unsupported-syntax) carried as a side fact and surfaced additively, precision-additive only, no diagnostic, never feeds severity, `untyped = Dynamic[top]` unchanged; WD1 a side-channel fact keyed at the introduction-site AST node — implemented as the identity-keyed `Scope#dynamic_origins` side-table (`record_dynamic_origin`, excluded from `Scope#==`/`#hash` per the ADR-53 litmus), NOT the trace-only `FallbackTracer` (nil outside `rigor trace`) and NOT a field on `Type::Dynamic` — adding one would break the `value_fields :static_facet` equality that dedups `Dynamic[T]` in unions/cache and fork the lattice by origin; WD2 surfaced structured-not-string (ADR-61) as a `dynamic_origin` JSON field per site (omit-when-nil) + `coverage --protection` tractability labels; WD3 provenance never participates in subtyping / consistency / normalization / erasure; WD4 a future strict-dynamic discipline consults it as evidence base but ships behind `bleeding_edge:` (ADR-50), out of scope here; rejected a `provenance:` field on the carrier (value-equality break + lattice fork) / a default-on provenance diagnostic (FP discipline) / re-deriving at report time (wrong layer))
- [ADR-76](docs/adr/76-effect-modeling-freeze-dup-shape-preservation.md) — Effect modeling for `freeze` / `dup` / `clone` and shape-carrier preservation (accepted — WD1 implemented 2026-06-24 `2751bc78`, WD2 fully landed 2026-06-26 (HashShape + Tuple, after the ADR-78 WD1 block-form over-fold guard); from the survey §4 + the 2026-06-14 precision-foldgap-recon note; a SPLIT decision on effect/mutation modeling; WD1 the conservative-invalidation half (non-mutating calls preserve facts, aliased-mutation calls invalidate them) is compatibility-safe internal precision that REDUCES spurious `always-truthy` / nilability / shape firings — LANDED: `MutationWidening` treats `freeze`/`dup`/`clone`/`itself` as pure self-returners (`PURE_SELF_RETURNERS` + early-returns in `widen_after_call`/`widen_for_outer_receiver`), self-check clean (no reflexive `always-truthy` reappeared — change is in the invalidation path, not the dispatch return-type tier) — bucket 1; WD2 the high-value shape-carrier preservation through the pure self-returners (`freeze` / `dup` / `clone` / `itself` — today they degrade `HashShape` → `Hash`, `Tuple` → `Array` via nominal `() -> self` RBS resolution → a later `#[]` types `Dynamic`, the `MESSAGES = {…}.freeze; MESSAGES[reason]` gap) is DEFERRED to a separate branch (bucket 3) — the recon note's corpus-FP-safe tier was backed out because it surfaced 12 reflexive `flow.always-truthy-condition` on rigor's OWN constant-folder (self-analysis folds `receiver.public_send(method_name)` to a constant → provably-truthy over-fold) and the discipline forbids `# rigor:disable`; ordering load-bearing — the reflexive over-fold must be fixed at root FIRST then the shape tier re-applies; WD3 the four pure self-returners on a shape carrier return the receiver type unchanged, the mutating self-returners (`<<`, `merge!`) keep today's nominal degradation; rejected ship-the-tier-and-disable (fix the cause) / unconditional preservation incl. the reflexive case (manufactures FPs) / optimistic preservation as default (over-preservation fires on working code))
- [ADR-77](docs/adr/77-doctor-and-upgrade-commands.md) — `rigor doctor` and `rigor upgrade` evidence-routing commands (accepted — implemented 2026-06-24 `8048991c` (`upgrade` skeleton); from the survey §7 / P5 + ADR-50 WD7 + ADR-73; two additive CLI commands that ROUTE existing evidence rather than change default `check`; `rigor doctor` classifies already-produced findings (config-resolution warnings, `RBS classes available: 0`, Rails-locked-but-no-Rails-plugins, baseline drift) into setup-problem vs clean-run with a routed next action — an aggregation/presentation layer over data `check` already produces, no new analysis pass, no new rule; `rigor upgrade` = the ADR-50 WD7 migration command (deferred until a concrete BC target); both promote the ADR-73 catalogue-only `rigor-doctor` / `rigor-upgrade` SKILLs (hand-composed today) into first-class commands; compatibility-safe (additive, current behaviour preserved); criterion = reuse the existing check/baseline/plugin surfaces, any JSON is a stable structured contract not a message mirror; WD1 doctor classifies, doesn't re-analyze loudly; WD2 upgrade = ADR-50 WD7; WD3 both consume a shared "run check → `Analysis::Result`" helper extracted from `CheckCommand#build_check_runner` (the same extraction ADR-73's `describe --deep` needs — three callers share it); WD4 the deep probe is opt-in, never wired into the presence-only `rigor skill describe` headline (preserves ADR-73 WD2); rejected deep analysis as the default `describe` headline / duplicating the check-runner setup / a separate diagnostics gem (ADR-31) / a new "setup problem" diagnostic rule; command vocabulary freezes at v1.0 under ADR-50 WD1)
- [ADR-78](docs/adr/78-reflexive-overfold-always-truthy.md) — Reflexive over-fold and the `flow.always-truthy-condition` envelope (accepted — fully implemented 2026-06-26: WD2 `REFLECTIVE_SEND_METHODS` guard + WD1 carry-over block-form over-fold guard (`ShapeDispatch.try_dispatch` declines on a block; the residual class was `CONST=[...].freeze; CONST.any?{…}` folding the no-block result) + WD3 HashShape & Tuple preservation; the ADR-scoped fix the 2026-06-14 precision-foldgap-recon note flagged; unblocks ADR-76 WD2; `flow.always-truthy-condition` + the `&&`/`||` `ExpressionTyper#constant_value_polarity` gate conclude provable truthiness from a `Type::Constant` predicate, which is unsound when the constant is an OVER-FOLD — a fold sound only for a narrower form than the actual runtime expression; concrete case: `receiver.public_send(method_name)` with a runtime-variable method name folds to a single `Type::Constant` once the receiver keeps a shape carrier → the 12 reflexive firings on rigor's own constant-folder that forced ADR-76 WD2 to be backed out; criterion WD1 = provable truthiness rests only on a GENUINE compile-time constant, never a fold conditional on a narrower form — the over-fold is the typing bug, the rule reading it is the symptom, fix the fold not the rule's skip envelope; WD2 fix the cause = the dispatcher declines to constant-fold a reflective send (`public_send`/`send`/`__send__`) unless the method-name arg is a value-pinned literal Constant — precision-neutral (`x.public_send(:foo)` still folds; `x.public_send(name)` degrades to RBS `untyped`) and strictly FP-reducing (declining an unsound fold only removes a spurious constant, never adds/loses a genuine firing); WD3 re-apply ADR-76 WD2 once the over-fold no longer yields a constant — the payoff closing the `MESSAGES={…}.freeze; MESSAGES[reason]` gap; rejected suppress-the-rule-for-reflective-predicates (symptom patch, leaves the unsound constant for other consumers) / provenance-tag the constant (ADR-75 kinship — more machinery, bad constant still typed) / `# rigor:disable` the 12 firings (forbidden, the reason WD2 was backed out))
- [ADR-79](docs/adr/79-rbs-version-range-over-pinned-determinism.md) — RBS version-range fidelity over checker-pinned determinism (accepted 2026-06-26; prompted by the rigor-rs port's reproducibility critique (it vendors+embeds core RBS per its ADR-0007) + [rigortype/rigor#22](https://github.com/rigortype/rigor/pull/22) (RBS 3.x compat fix + a `Gemfile.rbs-compat` CI matrix `~> 3.10` ∧ `~> 4.0`); Rigor keeps reading core/stdlib RBS from the project's OWN resolved `rbs` gem across the gemspec range `rbs >= 3.0, < 5.0` rather than vendoring a Rigor-pinned core RBS; criterion = **fidelity to the installed toolchain outranks cross-environment bit-determinism** — diagnostic variation across `rbs` versions is checking-what-you-run, not a defect; determinism is bought TACTICALLY only where a version gap yields a WRONG result (an FP on a method real at runtime), never STRUCTURALLY by freezing the RBS source away from the project's actual `rbs`; mitigations are existing+proportionate (PR #22 compat CI keeps the range honest; the unconditional `data/core_overlay/` reopen patches version-lag FPs e.g. `StringScanner#peek_byte` omitted by `rbs 4.0.2` per the 2026-06-16 liquid sweep; the ADR-72 Gemfile.lock-gated `data/gem_overlay/` covers `:missing`-RBS gems — so Rigor already holds a small surgical core-RBS slice, used to fix wrongness not replace the source); DEFERRED (not adopted): a full Rigor-held bundled core RBS as source of truth — rejected as default (decouples diagnostics from the project's actual `rbs`/Ruby = reintroduces the fidelity gap; upstream-release maintenance burden; duplicates `core_overlay`), held as an OPT-IN pinned-RBS mode behind a re-eval trigger (demonstrated CI-vs-dev drift not fixable by an overlay), rigor-rs's vendor+embed as its reference design; rigor-rs (sibling Rust port) adopts the OPPOSITE default for its single-binary Ruby-free goal — this ADR records the deliberate divergence)
- [ADR-80](docs/adr/80-narrowing-facts-rename.md) — Rename the `type_specifier` plugin hook to `narrowing_facts` (accepted 2026-06-26; from the 2026-06-26 rigor-rs port feedback item 5; renames the ADR-37 slice-2 author DSL verb `type_specifier` → `narrowing_facts` — the old name reads as a parallel to `dynamic_return` (which returns a *type*) when it returns post-return narrowing *facts* (internal storage already `post_return_facts`); `type_specifier` survives as a one-time-warning deprecating alias through 0.2.x, REMOVED in 0.3.0; bundled minitest/sorbet/rspec migrated; criterion = a public DSL verb names what the author declares (facts), not by false parallel to a type-returning sibling — clarity renames are paid pre-1.0/pre-adoption (third-party usage zero) where BC cost is lowest and before the ADR-50 v1.0 vocabulary freeze makes the misname permanent; ADR-60's keep-verdict defended the dynamic_return/type_specifier SPLIT not the NAME (its rejected alt was merging), so the name was open; SCOPE = author-facing verb ONLY — engine reader `type_specifiers`, consumer `#type_specifier_facts`, and `rigor plugins --capabilities` JSON `type_specifier_methods` left unchanged (internal API / separately-frozen CLI surface), revisited at 0.3.0 removal; rejected keep-the-name (permanent misname at freeze) / rename-internals-and-JSON-now (scope creep, second frozen surface) / merge-into-dynamic_return (ADR-60 already rejected); sibling rigor-rs ADR-0027 mirrors the contract and should track the rename)
- [ADR-81](docs/adr/81-skill-set-optimization.md) — Skill-set optimization: per-skill freshness + the `waza` evaluation stance (accepted — implemented 2026-07-05; two standing decisions on maintaining the user-facing `skills/` set. **Criterion 1 (freshness)** generalizes ADR-73 WD1's freeze-only-pre-install rule from the entry point to EVERY skill body — a distributed skill freezes only version-stable scaffold, version-coupled step detail is fetched live from the installed gem; load-bearing point = both channels (gem `rigor skill` + vercel-labs `npx skills add`) read one `skills/<name>/SKILL.md`, so freshness cannot come from thinning the file (thins both) but from a "First: load the version-current copy" directive that re-fetches via a new `rigor skill --full <name>` (inlines body + every `references/*.md` in one call, `SkillCommand#run_full`) — a vendored copy is frozen at install time while `rigor skill` always reads the gem, so the directive resolves upgrade-time divergence toward the installed version; hybrid calibration = split volatile detail into `references/` only where the body carries drifting exact flags/config/rule-ids (`rigor-doctor` = demo split) else directive-only, `rigor-next-steps` stays a pure pre-install bootstrap. **Criterion 2 (evaluation)** = a `waza` advisory is adopted only when it flags a defect independent of the agentskills.io publication profile — ADOPT link health (`waza check` caught 4 × `blob/main` 404s in `rigor-ci-setup`; default branch is `master`) + over-specificity of hardcoded doc URLs (route to offline `rigor docs`, converging with Criterion 1); REJECT the 500-token budget / "complexity: comprehensive" (skills are intentionally comprehensive) + the `**WORKFLOW SKILL**`/`USE FOR:`/`INVOKES:` labeled description format (prose `Triggers:`/`NOT for … (use X)` conveys the same and reads better; `waza` penalizing its absence is the publication bias); description length (≤60-word cross-model-density) is a per-skill judgment applied only to an over-broad catch-all (`rigor-ask`, 169 words), never a blanket rule trading trigger recall; `waza dev --auto` stays banned; `rigor skill --full` freezes as public vocabulary at v1.0 (ADR-50 WD1); deferred = dropping vercel-labs distribution of non-entry skills (reverses ADR-73 WD5) / delegating `rigor-ci-setup`'s inline templates to `rigor docs ci`; mechanism detail lives in the ADR-73 2026-07-05 amendment)
- [ADR-82](docs/adr/82-dynamic-provenance-wiring.md) — `Dynamic[T]` provenance wiring: breaking the catch-all on real apps (WD1+WD2+WD3+WD6+WD7+WD8 implemented 2026-07-06, actionability arc largely spent; ADR-75 follow-up from the 2026-07-06 Mastodon coverage measurement, where the `coverage --protection` tractability labels proved uninformative on a real Rails app — **84% of unprotected sites land in the catch-all `unsupported_syntax`→`engine_gap`, 14% carry no cause, and the two user-actionable causes `framework_dsl_boundary` / `external_gem_without_rbs` fire on ZERO sites**, so ordinary idioms (`Tag.find_normalized(...).id`, `current_user&.account`, `params[:limit].present?`) all mislabel as intractable; two engine gaps cause it — **G1** the lookup is receiver-node-local (`ProtectionScanner` reads provenance at the dispatch's immediate receiver node but `MethodDispatcher` records causes at the call node that *produced* the Dynamic, so a local/ivar-read receiver never coincides → nil or the generic `ExpressionTyper#fallback_for` `unsupported_syntax`) and **G2** the specific causes are recorded only under rarely-holding conditions (`framework_dsl_boundary` only when a plugin returns Dynamic — but plugins return concrete = protected; `external_gem_without_rbs` needs opt-in ADR-10/`pre_eval:`; `try_discovered_method`/`try_user_class_fallback` record nothing); criterion = honesty of attribution (record/propagate a cause only where sound; when in doubt keep the generic — a wrong "enable a plugin" hint wastes the user's time, the failure ADR-75 exists to avoid); WD1 propagate provenance across the receiver-node lookup (follow local/ivar-read receivers to their binding's origin — a flow-varying `Scope` side-association, the hard/deferred/perf-gated slice, ADR-53 litmus in reverse + ADR-44 alloc budget) / WD2 record a cause at the two unlabeled user-method tiers (one-line `record_dynamic_origin`) / WD3 a new `inferred_return_untyped` cause (`engine_gap`, routes to ADR-67 param inference / ADR-58 ivar typing) so "engine can't infer this return" stops mislabeling as unsupported-syntax (extends ADR-75's not-yet-frozen cause set) / WD4 generic framework-boundary recording deferred to per-plugin follow-up (guessing violates honesty) / WD5 land WD2+WD3 first, measure re-bucketing, then decide WD1's cost — **DONE 2026-07-06: WD2+WD3 landed (`INFERRED_RETURN_UNTYPED` recorded at the discovered-method + user-class-fallback tiers), re-bucket 211 Mastodon sites (~1%) all correctly-attributed chained-receiver holes (`parsed_uri.path` w/ `def parsed_uri`), ratio unchanged — confirming the residual 84% has local/ivar receivers ⇒ WD1 required. WD1 THEN LANDED 2026-07-06 (`Scope#local_origins`/`#ivar_origins` name→cause side-tables, ==/hash-excluded, reset per body, set at `eval_local_write`/`eval_ivar_write` when rhs is origin-bearing Dynamic, followed by `ProtectionScanner#propagated_origin`): perf-neutral (+0.1% allocs vs master; bench-perf baseline is stale = master fails it too) but MODEST — only ~322 sites out of the null bucket (2872→2550), ratio unchanged. Adjudicating the residual OVERTURNS the "primary lever" framing: the dominant catch-all receivers are NOT bare vars but intermediate call/chain expressions (`signed_request_account.uri[…]`, `account_id_param.present?`) that lose provenance when `.foo` dispatches on a Dynamic receiver → the evidenced next lever is **WD6 chain-origin inheritance**. WD6 THEN LANDED 2026-07-06 (`ExpressionTyper#inherit_receiver_origin` records a Dynamic receiver's effective origin — shared `Inference::OriginLookup.origin_for` = `dynamic_origins[node] || WD1 local/ivar propagation`, now used by `ProtectionScanner` too — onto the call it produces; the `call_type_for` comment "the result inherits the dynamic origin" was aspirational/unwired): perf-neutral (+0.03% allocs A/B vs master) and it more than HALVES the causeless bucket (Mastodon null 2872→**1356**, WD1+WD6 from baseline 2921). BUT labels are unsupported_syntax-dominated (null→unsupported +1551) because chain ROOTS record unsupported (implicit-self memoized reader, `params[:x]` index, metaprog) → WD6 buys provenance COMPLETENESS not tractability ACTIONABILITY (both unsupported+null route engine_gap). Next lever = ENRICH THE ROOTS. WD7 THEN LANDED 2026-07-06 = (a) exact per-site `cause_site_counts` metric (tractability_summary derives from it) which **CORRECTED WD1/WD6's numbers as a lossy group-dominant artifact**: true causeless after WD1+2+3+6 is **10,390/21,119 (49%)** NOT 1,356 (group-dominant hid mixed groups' null sites; completeness ~51% not ~94%) + (b) param enrichment — `build_method_entry_scope` seeds untyped param `local_origins` = inferred_return_untyped (untyped param = archetypal ADR-67 gap), WD1/WD6 propagate through chains → **~3,100 Mastodon holes causeless→inferred_return_untyped (none 10390→7305, inferred 351→3460)**, genuine ACTIONABILITY gain (ADR-67 routing), seed-time/hot-path-clean, perf-neutral (+0.15% A/B). Remaining 7305 causeless = mostly unbound ivar (ADR-58) + dynamic_top nodes (yield/super/block). WD8 THEN LANDED 2026-07-06 = unbound ivar enrichment (`type_of_instance_variable_read` records inferred_return_untyped when `scope.ivar` nil — untyped field = ADR-58 gap; read-time not seed since unbound is known at read, but already-dynamic_top branch only, perf-neutral +0.03% A/B), WD6 propagates `@x.foo.bar` → **none 7305→5405, inferred 3460→5399**. Cumulative WD7+WD8: **causeless 10390(49%)→5405(26%), actionable inferred 351→5399 (15×)**, ratio unchanged. Residual causeless = dynamic_top nodes (yield/super/block) + cvar/gvar (genuinely unmodeled) → actionability lever largely spent; unsupported 10063(48%) = unresolved-call-rooted chains = honest engine-gap floor. LESSON: group-dominant `add_a_type_here` aggregation is LOSSY — use per-site `cause_site_counts`, not group sums (my WD1/WD6 measurement narratives were distorted by it)**; precision-additive throughout (no type/diagnostic/severity change per ADR-75 side-channel contract) so the gate is re-bucketing measurement + attribution adjudication NOT a zero-delta corpus; rejected = provenance on the `Dynamic` carrier (value-equality break, ADR-75 WD1) / report-time re-derivation (wrong layer) / collapsing to one "unknown" bucket (concedes the signal) / a provenance diagnostic (FP-discipline); grounding the 2026-07-06 Mastodon coverage/provenance note + 2026-07-04 onboarding note §O5)

## Skills available in this repository

Skills follow the [Anthropic Agent Skills](https://agentskills.io/) shape — a directory with a `SKILL.md` carrying YAML `name:` + `description:` frontmatter. All SKILLs live under [`.claude/skills/`](.claude/skills/) and are auto-discovered by Claude Code inside this repo. They are **contributor workflows** that assume the monorepo's `Makefile`, `spec/integration/`, `.rubocop.yml`, and `plugins/` / `examples/` layout. A separate **external-author** SKILL (for writing a parallel `rigor-foo` gem in your own repo per [ADR-31 WD4](docs/adr/31-contribution-and-supply-chain-policy.md)) is queued for v0.2.0; until then `rigor-plugin-author`'s Phase 0.5 routes non-maintainer plugin requests to ADR-31's third-party path.

| Skill | Use when |
| --- | --- |
| [`rigor-release-prep`](.claude/skills/rigor-release-prep/SKILL.md) | Preparing a RubyGems release: bump `Rigor::VERSION`, update `CHANGELOG.md`, regenerate `Gemfile.lock`, build, and `bundle exec rake release`. |
| [`rigor-builtin-import`](.claude/skills/rigor-builtin-import/SKILL.md) | Importing a Ruby core / stdlib class into the catalog-driven inference pipeline (the nine-stage flow + its non-mechanical decision points). |
| [`rigor-add-reference`](.claude/skills/rigor-add-reference/SKILL.md) | Adding a new upstream repo as a `references/` submodule (the three-file atomic change + full vs. sparse checkout choice). |
| [`rigor-ruby-version-bump`](.claude/skills/rigor-ruby-version-bump/SKILL.md) | Bumping the development Ruby version across every project marker (`flake.nix`, `.ruby-version`, `Gemfile` / `Gemfile.lock`, `AGENTS.md`), with the files that stay untouched. |
| [`rigor-dependency-update`](.claude/skills/rigor-dependency-update/SKILL.md) | Refreshing both dependency layers in one pass — bundled gems (`bundle update`, `Gemfile.lock`) and the Nix Flake dev env (`nix flake update`, `flake.lock`'s nixpkgs pin) — one commit per layer, plus the native-extension rebuild a nixpkgs bump forces. Not for a Ruby-version bump or a `references/` submodule. |
| [`rigor-plugin-author`](.claude/skills/rigor-plugin-author/SKILL.md) | "Create a Rigor plugin for X." Phase 0.5 routes non-maintainers to [ADR-31](docs/adr/31-contribution-and-supply-chain-policy.md); maintainers follow requirements → template → scaffold → walker → integration spec → CHANGELOG. |
| [`rigor-ffi-plugin-author`](.claude/skills/rigor-ffi-plugin-author/SKILL.md) | FFI sibling of `rigor-plugin-author`. Starts by *talking you out of* a plugin when core `rigor-ffi` suffices; otherwise routes through ADR-31. |
| [`rigor-regression-sweep`](.claude/skills/rigor-regression-sweep/SKILL.md) | Multi-version baseline-drift sweep against a real OSS Ruby project; tabulates the surfaced-diagnostic curve and grows the `docs/notes/` corpus. |
| [`rigor-adr-author`](.claude/skills/rigor-adr-author/SKILL.md) | "Write an ADR for X." The procedure for adding a `docs/adr/` record: tag archetype + stakes, pick the matching skeleton, write sized-to-stakes (economy is the corpus's one drift), then the mechanical wiring (next number, the ascending-order `docs/adr/README.md` index row, the `CLAUDE.md` bullet, verify). Defers to [ADR-49](docs/adr/49-adr-authoring-guidelines.md) for the binding quality rubric. |
| [`rigor-docs-review`](.claude/skills/rigor-docs-review/SKILL.md) | "Review the docs / manual / handbook" / "査読して". The multi-lens review battery over `docs/manual/` + `docs/handbook/`: L0 mechanical gate (`make docs-check`) → L1 fidelity → L2 reader lenses → L3 bloat (inverted) → L4 copyedit, run as independent-context subagents, parallel-within / sequential-across, findings to `docs/notes/`. Methodology freeze; design in [`docs/notes/20260610-user-docs-review-battery-design.md`](docs/notes/20260610-user-docs-review-battery-design.md). |

### Evaluating skills with `waza`

`waza` is an Agent-Skill evaluation CLI in the Flake. Run `waza check <skill-path>` **once after authoring a new SKILL.md** as a spec-compliance sanity check (frontmatter / required fields / name-matches-directory). Treat everything else it reports as **informational** — its token-budget, module-count, complexity, and `USE FOR:` / `**UTILITY SKILL**` markers are calibrated for agentskills.io publication and do not apply to these comprehensive single-file contributor skills. Do **not** run `waza dev --auto`: it injects frequently-false `USE FOR:` / `INVOKES:` boilerplate. The hand-written `name:` + `description:` pair is the binding surface.

## Commit, release, and verification

These mirror [`AGENTS.md`](AGENTS.md) — it is authoritative; the essentials:

- **Commits**: plain imperative subject in sentence case, no `type:` / `area:` prefixes; why-not-diff body wrapped at ~72 cols. Version bumps use `Bump up version to x.y.z`. See AGENTS.md §§ "Commit Messages", "Release Cadence", "CHANGELOG Style".
- **Verification**: after non-trivial changes run, inside the Flake, `make verify` then `git diff --check`. `make verify` chains `test` / `lint` / `check` / `check-plugins`; the `check` target (`bundle exec exe/rigor check lib`) is Rigor's own self-check and `check-plugins` (`rigor check plugins/*/lib examples/*/lib`, ADR-43) is the plugin-contract self-check — both MUST stay clean. Fix the cause (engine regression, a missing per-class blocklist entry, or a genuine plugin contract misuse), never disable the rule. Mention any skipped verification when the Flake shell is unavailable.

## Notes for delegated agents

- Do NOT bypass the Flake. `bundle`, `rake`, `rspec`, `rubocop`, and `exe/rigor` MUST run inside `nix … develop` per AGENTS.md.
- Do NOT modify `references/` submodules unless the task is "bump references/<name>". The vendored sources are read-only; engine changes happen against Rigor's own code.
- Do NOT run `bundle exec rake release` without explicit user authorisation. The release task tags `vx.y.z`, pushes to origin, and publishes to RubyGems.
- Prefer `rigor sig-gen` (ADR-14) over AI-authored RBS in this repo — gaps in sig-gen are the more valuable signal than freehand RBS. Full policy + the `tighter-return` no-overwrite rule live in [`AGENTS.md`](AGENTS.md) § "RBS Authorship".
- The reading order for a returning implementer is in [`docs/CURRENT_WORK.md`](docs/CURRENT_WORK.md); treat it as the resume bookmark, not a normative spec.
