- **[inference]** `Module#prepend` now places the prepended module ahead of the class it is prepended into — for the `Base.prepend(Loud)` call form and the in-body `prepend Loud` form alike — so a call the module overrides types the value the program actually runs and `super` inside that method reaches the class own definition instead of recursing. ([#1165](https://github.com/rigortype/rigor/pull/1165))
