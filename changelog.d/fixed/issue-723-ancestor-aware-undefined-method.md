- **[check]** A class declared in `sig/` without its project superclass no longer draws a false `call.undefined-method` for a method that superclass defines in source — the rule now asks the project's own ancestry, the same walk the typer already resolves such a call through. ([#723](https://github.com/rigortype/rigor/issues/723), [#733](https://github.com/rigortype/rigor/pull/733))
