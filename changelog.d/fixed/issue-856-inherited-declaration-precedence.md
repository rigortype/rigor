- **[inference]** A method a class defines in its own source is no longer typed from a signature that only an ancestor declares: a `sig/` describing a base class honestly (`def target_class: () -> nil`) made every call on a subclass that overrides it report `undefined method … for nil` on working code, and `rigor type-of` agreed with the declaration rather than with the method that actually runs ([#860](https://github.com/rigortype/rigor/pull/860)).
