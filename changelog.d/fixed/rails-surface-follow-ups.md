- **[plugins]** rigor-activesupport-core-ext no longer types `30.minutes + Time.now` and the other Duration-on-the-left-of-a-Time/Date expressions, which raise at runtime, as `ActiveSupport::Duration`; and rigor-railties now leaves `Rails.logger` / `.cache` / `.configuration` / `.application` alone at sites where the project defines that very reader on its own `Rails`, instead of retyping the project's answer away. ([#588](https://github.com/rigortype/rigor/issues/588))
