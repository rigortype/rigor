- **[engine]** A warm `rigor check --incremental` that changed nothing now reports the same project-signature rows the cold run did — `rbs.coverage.quarantined-signature`, `rbs.coverage.synthesized-namespace`, and `rbs.coverage.definition-build-failed` for a `rigor:v1:conforms-to` class — where they previously vanished on the second run, which under the `reject-unparseable-signatures` feature turned a failing project into a passing one. ([#788](https://github.com/rigortype/rigor/pull/788))
