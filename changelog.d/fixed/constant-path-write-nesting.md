- **[engine]** A constant assigned through a path inside a namespace — `Holder::DEFAULT = Post` written in `module Admin` — is now recorded under the constant Ruby actually names and typed with that namespace in scope, so reads spelled either `Holder::DEFAULT` or `Admin::Holder::DEFAULT` get the right class instead of an unrelated top-level one or no answer at all; the same assignment written at the top level, through `::Holder`, or on a runtime receiver is unchanged. ([#690](https://github.com/rigortype/rigor/issues/690))
