- **[inference]** A local copied from an instance variable that is set to `nil` only in `initialize` no longer reports a false `call.possible-nil-receiver` error after the code mutates it in place — `r = @name; r << "!"; r.size`, the same mutation inside a block, a `while` loop or a retried `begin` — while a local reassigned to a new value still reports. ([#1396](https://github.com/rigortype/rigor/pull/1396))
