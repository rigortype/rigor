- **[release-gate]** `make bench-perf` now measures each target twice (`--reps N`, default 2), each rep in a fresh process, and gates on the **lower** `wall_s` / `peak_rss_kb` of the reps while `allocations` and the diagnostic count stay single-sample — so a ±7% host spread on the noisy axes no longer decides a release gate whose RSS band is +10%. ([#1041](https://github.com/rigortype/rigor/pull/1041))
