- **[inference]** A block passed to an Enumerable method on an `IO`, `File` or `StringIO` (`each_with_index`, `detect`, `map`, …) now gets the same loop-body re-narrowing as `each_line`, so a flag set in one `case` arm and tested in another no longer makes the method's caller report `flow.always-truthy-condition` on correct code. ([#1308](https://github.com/rigortype/rigor/pull/1308))
