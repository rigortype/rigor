- **[dev]** A new `spec/docs` gate checks every `raises`-omitting row in `data/builtins/ruby_core/*.yml` against the C function its own `c_body_at` cites, and fails when that function actually raises — it caught ten more rows across `date.yml`, `hash.yml`, `io.yml`, `random.yml`, `re.yml` and `struct.yml` beyond the 36 already fixed, and skips cleanly rather than failing when the optional `references/ruby` submodule isn't checked out. ([#756](https://github.com/rigortype/rigor/issues/756))
