- **[cache]** A warm `rigor check` now re-analyzes when a file a plugin had looked for and not found — a `db/schema.rb` added to a project that had none — appears later, instead of replaying the diagnostics of the run that found it missing. ([#577](https://github.com/rigortype/rigor/issues/577))
