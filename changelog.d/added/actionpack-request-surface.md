- **[plugins]** rigor-actionpack now types the rest of the controller request context: the fifteen `request` predicates (`post?`, `xhr?`, `ssl?`, …) as `bool`, `request.format` as `Mime::Type | Mime::NullType`, and `flash.now` / `flash.keep` / `flash.discard` as their Action Pack carriers, so a chain written on any of them keeps a concrete receiver. ([#534](https://github.com/rigortype/rigor/issues/534))
