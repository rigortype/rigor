- **[sig-gen]** With the `effects:` opt-in on, `rigor sig-gen` now writes `%a{pure}` — and, under the new `--effect-envelopes`, `%a{rigor:v1:effect …}` — above every method whose effect closure it fully read and found empty, withholding it from a method that is clean only because `effects.tolerated:` says so, one that calls something nothing describes, one carrying a surviving `≤` label, and leaving a declaration that already carries annotations byte-untouched. [#1036](https://github.com/rigortype/rigor/pull/1036)
