- **[engine]** A narrowed run — a `--verify-incremental` partition or an `--incremental` recheck — now builds its RBS environment over the whole project, so it carries the same plugin-synthesized signatures a full run does, and its rechecks no longer report a run-level row twice (`effect.annotations-unchecked`, `source-rbs-annotation-not-honoured`) or drop the rbs-inline `effect.annotations-unchecked` on a warm run that changed nothing. ([#793](https://github.com/rigortype/rigor/issues/793), [#788](https://github.com/rigortype/rigor/pull/788))
