- **[plugins]** A controller action's effect summary now includes what its template does — `render :show`, `render partial: "card"`, `render template:` / `action:` and the implicit render of `<controller>/<action>` all reach the view's own unit, a template reaches the partials it renders, and the `template-not-analysed` taint clears on a render that reached one and survives on a computed target or a layout; a first-party plugin's statement about a framework method is now proven rather than declared, so a `views: strict` envelope reports the lazy `user.posts.count` in a view that `views: lenient` allows. [#1057](https://github.com/rigortype/rigor/pull/1057)
