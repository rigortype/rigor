- **[plugins]** rigor-actionpack now keeps the `ActionController::Parameters` chain typed through `except`, `without`, `extract!`, `slice!`, `merge`, `merge!`, `reverse_merge`, `reverse_merge!`, `with_defaults`, `with_defaults!`, `compact`, `compact_blank` and `deep_dup`, so a builder written around them stays a concrete receiver end to end. ([#534](https://github.com/rigortype/rigor/issues/534))
