- **[inference]** Destructuring into instance variables (`@first, *@rest = ints`, `@a, @b = 1`, nested and union right-hand sides) now follows the same rules as locals instead of recording `Dynamic[top]`, both in the method that assigns them and in the type other methods of the class read; a slot filled from a non-tuple `Array[T]` reads as `T` in those other methods, as `@first = ints.first` does. ([#1114](https://github.com/rigortype/rigor/pull/1114))
