- **[plugins]** `rigor-railties` now resolves the root-qualified `::Rails` receiver for its four singleton readers (`logger`, `cache`, `configuration`, `application`) through the same constant-resolution path as the bare `Rails` spelling, dropping the plugin's own workaround for the shadow-module bug the engine itself now fixes directly. ([#626](https://github.com/rigortype/rigor/issues/626))
