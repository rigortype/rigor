- **[inference]** `Enumerable#sum(init) { … }`, and user signatures of the same shape whose block does not receive the variable, now type the result as the value class (`Integer`, `Float`, `Rational`, `Complex` or `String`) that the argument and the block share instead of leaving it untyped, so `rigor sig-gen` emits `-> Integer` for `h.sum(0) { |_k, v| v }` again; a seed and a block of different classes, `inject`, `Enumerator.produce` and `transform_keys` stay untyped. ([#1293](https://github.com/rigortype/rigor/pull/1293))
