- **[engine]** Rigor's own self-check (`rigor check lib`) is warning-free again: the recursive-method return fixpoint (ADR-55) now owns its iteration cap inside the loop instead of a continuation sentinel and a loop fall-through, which Rigor itself had flagged as a `def.return-type-mismatch` on the incremental session's return re-evaluation entry point. ([#810](https://github.com/rigortype/rigor/pull/810))
