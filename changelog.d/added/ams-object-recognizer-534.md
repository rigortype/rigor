- **[plugins]** The new `rigor-active-model-serializers` plugin types the implicit-self `object` reader inside an `ActiveModel::Serializer` subclass as the model the serializer serializes, so `object.username` reads that column's type, and leaves `object` alone unless the model answers every name the serializer reads off its resource. [#1044](https://github.com/rigortype/rigor/pull/1044)
