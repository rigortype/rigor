- **[effects]** A `--verify-incremental` partition, an `--incremental` recheck, or an editor-buffer run now walks effect envelopes over the whole project, so an envelope declared in a file outside the analysed subset is no longer missed and `effect.unknown-label` matches the full run. ([#795](https://github.com/rigortype/rigor/issues/795), [#800](https://github.com/rigortype/rigor/pull/800))
