- **[check]** A Ruby class that subclasses a core or stdlib class now resolves its inherited method calls against that class's signatures, so `class SubHash < Hash` reads `has_key?` as `bool` and `class MyError < StandardError` reads `message` as `String` instead of leaving both untyped; a subclass of a gem class, and any name the subclass or a nearer ancestor of your own defines, are unchanged. [#1131](https://github.com/rigortype/rigor/pull/1131)
