- **[cli]** `rigor unused` no longer lists a gem's class as an unused candidate when your project reopens it: the report's ownership test now reads the same dependency sources `rigor check` does — the bundle's per-gem `sig/` and an installed `rbs collection` — instead of core and stdlib alone ([#882](https://github.com/rigortype/rigor/issues/882), [#888](https://github.com/rigortype/rigor/pull/888))
