- **[engine]** A local assignment inside a `->() { }` body now binds, so the arrow spelling of a lambda types exactly as the `lambda { }` spelling of the same object does — a later read in the body sees the written value instead of keeping its pre-write type, and an outer local the body rebinds no longer keeps a narrowing the closure can invalidate. ([#879](https://github.com/rigortype/rigor/pull/885))
