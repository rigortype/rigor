- **[inference]** A value destructured from an optimistically nil-free read (`k, v = pairs.first`, `x, y = pairs.first, 1`) or reached through safe navigation (`pairs.first&.last`) no longer has its nil-freeness read as proof, so `k ? 1 : "none"` keeps both arms instead of reporting `undefined method` on the arm a miss really takes. [#1295](https://github.com/rigortype/rigor/pull/1295)
