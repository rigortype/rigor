- **[sig-gen]** A proposal whose type is a union matching the expansion of a type alias your own `sig/` declares now renders the alias name — `-> ::Rigor::Type::t` instead of its 22 members — in returns, `--params=observed` parameters, `attr_*` accessors and `Data` / `Struct` members alike, across `--print`, `--diff`, `--write` and the JSON payload's `rbs` field. [#1032](https://github.com/rigortype/rigor/pull/1032)
