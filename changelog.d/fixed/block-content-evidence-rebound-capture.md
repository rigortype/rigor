- **[inference]** A block that stores a local it also reassigns — `total += x; out << total`, a block parameter reassigned before it is stored, or the same into an `each_with_object` memo — now stores the value that local holds where the store runs, so `out` reads `Array[Integer]` instead of `Array[0]` and `out.last == 3` no longer reports an always-falsey condition; a block using `next`, `break` or `retry`, or a store nested in an inner block or loop, keeps the earlier reading. ([#1221](https://github.com/rigortype/rigor/pull/1221))
