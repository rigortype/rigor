- **[sig-gen]** A return `rigor sig-gen` proves as a literal is no longer proposed as a `tighter-return` against a wider existing declaration: `def describe: () -> String` whose body returns `"top"` keeps the declared `String`, because a declaration wider than its body is the abstraction you chose over it — a method no `.rbs` declares still gets the literal. ([#850](https://github.com/rigortype/rigor/pull/850))
