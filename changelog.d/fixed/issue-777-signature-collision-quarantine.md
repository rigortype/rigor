- **[engine]** A `signature_paths:` `.rbs` that kind-collides with bundled RBS (e.g. project `class Base64` vs bundled `module Base64`) is now QUARANTINED instead of collapsing the whole RBS environment to nil, so the rest of the type universe and dependent diagnostics keep firing. ([#777](https://github.com/rigortype/rigor/issues/777), [#780](https://github.com/rigortype/rigor/pull/780))
