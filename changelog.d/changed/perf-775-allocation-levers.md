- **[engine]** `rigor check` allocates about 40% fewer objects for the same analysis (36.5M → 21.9M on Rigor's own `lib`, diagnostics identical), recovering most of the v0.3.7 cost: the translation of a closed RBS type alias, a class's type-parameter names and the split of a class name are computed once per run, and dispatch no longer builds throwaway pairs, lambdas and empty answers on every call. ([#819](https://github.com/rigortype/rigor/pull/819))
