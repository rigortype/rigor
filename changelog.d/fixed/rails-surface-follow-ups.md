- **[plugins]** rigor-activesupport-core-ext no longer types `30.minutes + Time.now` and the other Duration-on-the-left-of-a-Time/Date expressions, which raise at runtime, as `ActiveSupport::Duration`; and rigor-railties leaves `Rails.logger` / `.cache` / `.configuration` / `.application` to a project's own `module Rails` definition instead of retyping it. ([#588](https://github.com/rigortype/rigor/issues/588))
