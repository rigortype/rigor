# Architecture Decision Records

This directory contains the Architecture Decision Records (ADRs) for Rigor. Each document captures a significant design decision, its context, the options considered, and the consequences.

## How to Read

- **ADR-0** is the foundation document — start here for the project's core principles and architecture.
- **ADR-1** through **ADR-3** define the type model, extension API, and type representation — the analyzer's conceptual core.
- Higher-numbered ADRs build on the foundation and can be read as needed.
- Each ADR has a **Status** field: `Accepted`, `Proposed`, or `Superseded`. Accepted ADRs whose implementation is still in flight carry a parenthetical note (e.g. *partially implemented*, *slice N deferred*).

## Index

| # | Title | Status |
| --- | --- | --- |
| ADR-0 | [Foundation and Core Architecture of Rigor](0-concept.md) | Accepted |
| ADR-1 | [Type Model and RBS Superset Strategy](1-types.md) | Accepted |
| ADR-2 | [Extension API Strategy](2-extension-api.md) | Accepted |
| ADR-3 | [Type Representation](3-type-representation.md) | Accepted |
| ADR-4 | [Type Inference Engine](4-type-inference-engine.md) | Accepted |
| ADR-5 | [Robustness Principle](5-robustness-principle.md) | Accepted |
| ADR-6 | [Cache Persistence Backend](6-cache-persistence-backend.md) | Accepted |
| ADR-7 | [v0.1.0 Slice Decisions](7-v0.1.0-slice-decisions.md) | Accepted |
| ADR-8 | [Steep-Inspired Improvements](8-steep-inspired-improvements.md) | Accepted |
| ADR-9 | [Cross-Plugin API](9-cross-plugin-api.md) | Accepted (implemented in v0.1.1) |
| ADR-10 | [Dependency Source Inference](10-dependency-source-inference.md) | Accepted |
| ADR-11 | [Sorbet Input Adapter](11-sorbet-input-adapter.md) | Accepted |
| ADR-12 | [dry-rb Packaging](12-dry-rb-packaging.md) | Accepted |
| ADR-13 | [TypeNode Resolver Plugin](13-typenode-resolver-plugin.md) | Accepted |
| ADR-14 | [RBS Sig Generation](14-rbs-sig-generation.md) | Accepted |
| ADR-15 | [Ractor Concurrency](15-ractor-concurrency.md) | Accepted (fork backend active; Ractor pool deferred) |
| ADR-16 | [Macro Expansion](16-macro-expansion.md) | Accepted |
| ADR-17 | [Monkey Patch Pre-Evaluation](17-monkey-patch-pre-evaluation.md) | Accepted (implemented in v0.1.13) |
| ADR-18 | [Substrate Per-Call-Site Return Type](18-substrate-per-call-site-return-type.md) | Accepted (implemented in v0.1.6) |
| ADR-19 | [Language Server Packaging](19-language-server-packaging.md) | Accepted (LSP v1 implemented in v0.1.6; v2 + follow-ups across v0.1.x) |
| ADR-20 | [Lightweight HKT](20-lightweight-hkt.md) | Accepted (partial implementation) |
| ADR-21 | [Rubydex Evaluation](21-rubydex-evaluation.md) | Proposed |
| ADR-22 | [Baseline and Project Onboarding](22-baseline-and-project-onboarding.md) | Accepted |
| ADR-23 | [Diagnostic Triage Command](23-diagnostic-triage-command.md) | Accepted (slices 1+2+3+4 implemented) |
| ADR-24 | [Self Method Call Resolution](24-self-method-call-resolution.md) | Accepted (slice 4 gated; WD3 in-body adoption gate opened by ADR-57, 2026-06-12) |
| ADR-25 | [Plugin Contributed RBS](25-plugin-contributed-rbs.md) | Accepted |
| ADR-26 | [ActiveRecord Relation Typing](26-activerecord-relation-typing.md) | Accepted |
| ADR-27 | [Tool Distribution and Installation Model](27-tool-distribution-model.md) | Accepted (partially implemented; Nix flake + container image + machine-readable install guide shipped, and the copy-paste CI workflow templates landed via [ADR-51](51-ci-diagnostic-output-formats.md) WD6 — `docs/manual/ci-templates/` + the bundled `rigor-ci-setup` skill; the self-contained single binary remains deferred) |
| ADR-28 | [Path-scoped Method-Protocol Contracts](28-path-scoped-protocol-contracts.md) | Accepted |
| ADR-29 | [Browser Playground](29-browser-playground.md) | Accepted (server-side playground in v0.1.10–0.1.11; the in-browser `ruby.wasm` build (Option A / WD8–WD10) went green and shipped 2026-06-14/15 — `apps/rigor-playground/wasm/`, published to Cloudflare R2 on release tags and served as a static asset by the docs site) |
| ADR-30 | [`rigor-ffi` Plugin Shape](30-rigor-ffi-plugin-shape.md) | Proposed (not implemented) |
| ADR-31 | [Contribution and Supply-chain Policy](31-contribution-and-supply-chain-policy.md) | Accepted (in force) |
| ADR-32 | [Inline-RBS Comment Ingestion](32-rbs-inline-comment-ingestion.md) | Accepted (implemented in v0.1.10) |
| ADR-33 | [MCP Server Packaging](33-mcp-server.md) | Accepted (implemented in v0.1.10) |
| ADR-34 | [Toplevel Unresolved Implicit-self Calls Warn by Default](34-toplevel-unresolved-self-call-default.md) | Accepted (implemented in v0.1.13; the ADR-29 Playground default-severity wiring shipped too — its sandbox config sets `severity_profile: strict`) |
| ADR-35 | [Override Signature Compatibility (Liskov signature rule)](35-override-signature-compatibility.md) | Accepted (slices 1–4 done; slice 5 deferred) |
| ADR-36 | [Macro-substrate Nested-class Emission Tier (Mangrove `Enum`)](36-mangrove-enum-nested-class-emission.md) | Accepted (Slice A implemented; `is_a?` exhaustiveness deferred) |
| ADR-37 | [Plugin Interface Segregation (narrow extension protocols)](37-plugin-interface-segregation.md) | Accepted (Slices 1–3 implemented; all bundled walker plugins migrated; `flow_contribution_for` deleted 2026-06-11 per ADR-52 WD3) |
| ADR-38 | [Plugin-declared Additional Initializers](38-additional-initializers.md) | Accepted (def-form implemented; block-form deferred) |
| ADR-39 | [Plugins may invoke their target library's safe methods directly](39-plugin-target-library-invocation.md) | Accepted (Plugin::Inflector + 3 consumers migrated; slice 3 deferred) |
| ADR-40 | [`config_schema` declared defaults (`{kind:, default:}`)](40-config-schema-defaults.md) | Accepted (mechanism + 13 plugins migrated off the `DEFAULT_*` idiom) |
| ADR-41 | [Inference budget design (wiring, on-hit policy, measurement-gated defaults)](41-inference-budget-design.md) | Proposed (spec table unwired; Layer 1 doc hygiene + Layer 2 measurement-gated wiring queued) |
| ADR-42 | [Plugin-contributed binary-operator return types (coerce-direction)](42-plugin-binary-operator-return-types.md) | Proposed, low priority (self/left-operand case already works via `dynamic_return`, spec-confirmed; coerce direction is a narrow false positive — cheapest fix is the WD-D engine mitigation, precision via the ADR-20 HKT route; demand-gated) |
| ADR-43 | [RBS-complete ancestor resolution (allow-list inherited-method dispatch)](43-rbs-complete-ancestor-resolution.md) | Accepted — fully landed (WD1–WD6; `rigor check` resolves a Ruby subclass's inherited calls against an allow-listed RBS-complete ancestor (seed `Plugin::Base`) so contract misuse warns standalone, without Steep's own-helper FP wall; zero net FP on the plugin lib tree; blanket resolution rejected on Rails-controller FP grounds; `make check-plugins` gate in `verify` + CI, teeth verified) |
| ADR-44 | [Per-dispatch / per-narrow allocation churn (Scope, CallContext)](44-dispatch-allocation-churn.md) | Accepted — landed: collapsed the ~12-deep body-scope `with_*` chains into a single `Scope.new` (GC runs −29%, diagnostics byte-identical) + `owners_for` shared-empty + `CallContext` positional `new`; mutable pooled Scope/CallContext **rejected** (re-entrant dispatch → silent narrowing corruption → false positives); `ProjectScope` field-regrouping downgraded (object-shape benchmark: 3–24 ivars all allocate one object, so it cuts size not count) |
| ADR-45 | [Unchanged-project fast path (run-result cache)](45-unchanged-project-fast-path.md) | Accepted — landed. A pre-analysis whole-run fingerprint was **rejected as unsound** (plugins like Pundit read project files *during* analysis → stale hit, caught by the cross-process `pundit_plugin_spec` regression). The sound **record-and-validate** cache (`Cache::Store#fetch_or_validate` + `Descriptor#fresh?`): key on the inputs known up front, store the result with the dependency set the run actually read, re-digest it on the next run. Unchanged Mastodon `app/models` (248 files) 11.6 s → 1.8 s (~6×), diagnostics byte-identical; gate runs `--no-cache` |
| ADR-46 | [Incremental analysis via a cross-file dependency graph](46-incremental-dependency-graph.md) | Accepted — implemented (`rigor check --incremental`, v0.1.17). The per-file incremental successor to ADR-45's coarse whole-run cache: record, per file, what it read from other files (declarations, inferred-return bodies, plugin reads) through the `Scope` accessor choke point; invert into a `dependents` index; on an edit re-analyze only `ΔF ∪ dependents[ΔF]`, serving the rest from a cross-process disk snapshot. A leaf `PostsController` edit → 1 file; a `Post` model edit → model + its callers. Slices 1+2 (body tier) ship the core; slices 3 (structural-tier negative-dependency tracking, including incremental file addition/removal) and 4 (symbol granularity) are landed. Measured on Rigor's own `lib` (262 files): warm no-change 0.75 s vs 7.2 s full (~9.6×), one-file leaf edit 1.15 s (~6.3×), diagnostics identical. Soundness is enforced by the mandatory `--verify-incremental` byte-identical cross-check, wired into CI as `make check-incremental` |
| ADR-47 | [Narrowing-driven clause reachability (`flow.unreachable-clause`)](47-narrowing-driven-clause-reachability.md) | Accepted — WD1 + WD2 + WD3a implemented + WD4 sweep run (v0.1.17; 16-corpus FP sweep zero firings → balanced stays `:info`). Extends Rigor's two existing `if`/`unless` reachability rules (`flow.unreachable-branch` literal-only + `flow.always-truthy-condition` inferred-constant) to `case`/`when` clauses, inspired by Elixir v1.20's redundant-clause reporting. The flow engine already narrows `case` (`eval_case_when_branches` threads a `falsey_scope` across `when` branches via `Narrowing.case_when_scopes`); a clause is unreachable exactly when its computed `body_scope` narrows the subject to `bot` (per-clause disjointness) or its entry `falsey_scope` already carries a `bot` subject (prior-exhaustion). Reuses `always-truthy-condition`'s FP envelope (subject must narrow, skip loops/blocks, skip defensive shapes, never fire on `Dynamic[T]`); `in`/pattern clauses gated behind `InNode` exhaustiveness (ADR-36 neighbour); corpus FP gate before default-on |
| ADR-48 | [Struct / Data value folding (member-shape carriers)](48-data-struct-value-folding.md) | Accepted — `Data.define` slices 1–4 implemented (v0.1.17). Two new carriers (a member-class `Type::DataClass` + a member-instance `Type::DataInstance`, HashShape-shaped but nominal/class-tagged) + a `DataFolding` dispatch tier + a cross-file `Scope#data_member_layouts` side-table, so `Point = Data.define(:x, :y); Point.new(1, 2).x` folds to `Constant[1]` for all three forms (constant-assigned / `class X < Data.define(...)` subclass / bare local), positional + keyword construction, and the `[]`/`to_h`/`deconstruct`/`deconstruct_keys`/`members`/`with` projections. Reuses the existing `ScopeIndexer` `data_define_call?` / `meta_member_names` recognition (which only registered member *existence*). Slice 4 (block-body hardening): a member whose reader the class body redefines (`def x`) no longer folds on read (gated on a real def node via `Scope#user_def_for`); value accessors bypass the reader and still fold. Precision-additive only — no diagnostic, no FP surface, degrades to the class nominal on any uncertainty. `Struct` deferred behind its mutation-soundness story (setters/`[]=` invalidate the instance member map; `Data`'s frozen instances are why it is first). Remaining (demand-gated): bare-local block-form parity + the Struct follow-up. Grounded in the Phase-5 `struct-encoding-coverage` audit |
| ADR-49 | [ADR authoring guidelines (a rubric for necessary-and-sufficient ADRs)](49-adr-authoring-guidelines.md) | Accepted — in force (living rubric). A meta-ADR: tag each ADR's **archetype** (deliberative / mechanical-policy / evaluation-proposal) and **stakes** (reversibility × blast radius × FP-envelope), then score **eight axes** (intent · decision criterion · actionability/code-anchoring · constraints/guardrails · verifiability/evidence · status fidelity · connectedness · economy/proportionality), dropping archetype-exempt axes from the denominator rather than scoring them zero. Goal is *necessary-and-sufficient* information — economy is bidirectional (punishes both thin and bloated). The don't-over-penalize rule: mechanical/semi-automatic ADRs are exempted on intent/criterion (never high-stakes ones, where thin intent is the headline finding). Calibration set: ADR-43 / ADR-46 (deliberative, high) + ADR-40 (mechanical, appropriately concise) |
| ADR-50 | [Release engineering and stability strategy (v0.2.0 → v1.0.0)](50-release-engineering-and-stability-strategy.md) | Proposed — the strategy for taking Rigor from the v0.1.x feature line to an enterprise-safe stable product, PHPStan-modelled. **v0.2.0 = release-engineering trial** (machinery + minor-non-break pledge as rehearsal), **v1.0.0 = hard contract freeze** (resolves the ADR-25/32-vs-ADR-37 freeze-timing inconsistency in favour of 1.0). Five pillars: (1) trial-then-freeze staging; (2) compatibility = enumerated public surface frozen, engine internals free; (3) diagnostic *output* is non-contract (genuine strengthenings flow in a minor, absorbed by the baseline) but a *new required discipline* is BC and ships off-by-default behind a PHPStan-style **inspectable bleeding-edge overlay** (a gem-versioned default-override set, surfaced by a `show-bleedingedge` diff command, adopted granularly per-feature via `bleeding_edge:`) until graduation (default-on at the next major after a ~4-week soak, BC-readiness-driven; pre-1.0 rehearses at minor) — the stable vocabulary (rule IDs, suppression markers, baseline/`severity_overrides:` keys, bleeding-edge feature ids) stays contract; (4) a committed `make bench-perf` + CI perf gate over the Mastodon/Redmine/GitLab corpora (wall/allocations/peak-RSS vs a stored baseline within a tolerance band); (5) support = latest + previous minor, switching to PHPStan's `1.x`-default-branch model post-1.0, with latest-Ruby-only reconciled (a backport keeps its line's Ruby pin). WD6 defines the release acceptance gate. Several WDs propose a concrete shape open to adjustment before ratification |
| ADR-51 | [CI-native diagnostic output formats](51-ci-diagnostic-output-formats.md) | Accepted — partially implemented (v0.1.18). Six CI-native renderings of the existing diagnostic stream behind `rigor check --format`: **`sarif`** (SARIF 2.1.0, the cross-platform anchor — GitHub code-scanning + any SARIF tool + reviewdog `-f=sarif`), **`github`** (GitHub Actions `::error file=…,line=…::` workflow commands → inline PR annotations, no upload step), **`gitlab`** (GitLab Code Quality / CodeClimate-subset JSON → the MR widget), **`checkstyle`** (Checkstyle XML → reviewdog `-f=checkstyle` posts to *any* reviewdog reporter — GitHub PR review / GitLab MR / Gerrit / Bitbucket / Gitea — and Jenkins), **`junit`** (JUnit XML → GitHub/GitLab/CircleCI/Jenkins test reports), **`teamcity`** (TeamCity inspection service messages). Each is a pure presentation layer over the same `Analysis::Diagnostic` fields `--format json` exposes — no new analysis. Criterion for a `--format` value: *a CI platform renders the stream inline from it* (so CSV / generic shapes stay out — `json` serves them). WD1 adds the broad-reach formats on a *reach-per-cost* basis; comparator = PHPStan (ships github/gitlab/checkstyle/junit/teamcity, **no SARIF** — Rigor leads with SARIF, PHPStan's gap). The GitHub *default* surface is `github` annotations, not SARIF (SARIF upload needs code scanning = GHAS on private repos). WD2 fixes the severity + identifier **contract table** across all six formats; WD3 the GitLab fingerprint (SHA256 of `(path, rule, line, column, message)` — stable, not index-based); WD4 leaves the exit code unchanged (file output via `>` redirect + `if: always()` upload); WD5 maps every diagnostic to a JUnit `failure` (surfacing, not gating — severity in `type`); **WD7 = runtime CI auto-detection** (`Rigor::CLI::CiDetector`, PHPStan `CiDetectedErrorFormatter`-modelled): on the default `text` output, a detected **first-class** CI auto-emits its native form (GitHub Actions → `github`, TeamCity → `teamcity`, on top of the human log), GitLab hints `--format gitlab`, and **second-class** CIs (CircleCI/Jenkins/Travis/Azure/Bitbucket/Buildkite/Drone/…) hint reviewdog/`junit`; augments text only (explicit `--format` untouched), hints only when diagnostics exist, opt-out via `--no-ci-detect` / `RIGOR_CI_DETECT=0`. A new output format is public contract under [ADR-50](50-release-engineering-and-stability-strategy.md) WD1. WD6 also ships [ADR-27](27-tool-distribution-model.md) § WD3's queued copy-paste CI setup templates + the bundled `rigor-ci-setup` skill. Deferred: `--output FILE`, reviewdog native `rdjson`, richer SARIF rule metadata |
| ADR-52 | [Compiled plugin contribution dispatch](52-compiled-plugin-contribution-dispatch.md) | Accepted — slices 1+2+3-partial+4-partial implemented 2026-06-10/11, corpus-/spec-gated byte-identical; slice 3's rigor-activerecord is **blocked** (its class-side dispatch is AST/`self_type`-keyed, not receiver-type-keyed — a migration attempt was reverted); slice 4 (run-time method-name callable) landed its engine + the lisp-eval/pattern examples + rigor-sorbet (2026-06-11: `dynamic_return methods:` callable over the assertion vocabulary ∪ `:absurd` ∪ `Catalog#method_names`, `T.bind`'s fact split onto `type_specifier`, strap + dependabot-core corpora byte-identical, dependabot-core wall 1262.6s→33.4s ~38×); slice 5a (per-file `file_methods:` gate — callable receiving the path, memoised per (rule, path), exclusive with `methods:`, nil-path fail-closed) landed 2026-06-11 + migrated rigor-rspec's let-binding rule (demo + GitLab corpus byte-identical), and rigor-activerecord migrated 2026-06-11 via the same run-time `methods:` gate (the receivers-gate blocker does not apply to a name gate — block keeps the AST/self_type resolution; no new gate form needed; GitLab corpus byte-identical), so ALL FIVE legacy users are migrated; slice 5b landed 2026-06-11 (hook DELETED — load-time ArgumentError for definers + CHANGELOG migration note) and slice 6 landed 2026-06-11 (engine-owned `Plugin::NodeRuleWalk`, one walk per file, plugin-major order preserved byte-identical) — the ADR is complete. Grounded in the [2026-06-10 plugin-architecture structural audit](../notes/20260610-plugin-architecture-perf-audit.md). Criterion: every per-call / per-def / per-file / per-node plugin consultation is gated by a key the engine already holds (method-name Symbol, receiver class name, file path, node class) via a structure **compiled once per run at registry build** — plugin code runs only on candidate hits; a capability that cannot declare its key is a DSL vocabulary gap to fix, not a license for an ungated hook. **Slice 1 (WD1)** extends `ContributionIndex` into a compiled contribution table (method-name inverted indexes for `type_specifiers` + `methods:`-gated `dynamic_returns`, verb-keyed `block_as_methods`, `open_receivers` Set, ancestry-memoised `owns_receivers` / `additional_initializers`, per-path contract memo, `diagnostics_for_file`-override bit) + the six engine call-site rewirings (the GitLab gate caught + fixed a nameless-`&:symbol`-node regression). **Slice 2 (WD2 static gate)** makes `dynamic_return receivers:` optional — a rule may gate on `methods:` alone, for a plugin whose receiver carrier is a refinement not a nominal class; migrated the `rigor-units` example. **Audit correction (made while implementing slice 2):** the audit's slice-2 targets were both wrong — activesupport-core-ext ships no `flow_contribution_for` (pure RBS bundle), and sorbet's catalog path matches arbitrary `def` names (a run-time, not static, set); no production plugin fit the static gate, units was its first consumer. WD3 (now slices 3–4) adds the run-time receiver/method-set + per-file callable forms and migrates the remaining users (activerecord/activestorage receiver sets, sorbet catalog name set, rspec per-file, the config-gated examples), then **deletes** the legacy hook (deliberate pre-1.0 BC break — cheap before the ADR-50 freeze); WD4 folds per-plugin node-rule AST walks into one engine-owned walk per file; WD5 rejects a per-call-node result cache (scope-sensitive → FP risk); WD6 gates every slice on byte-identical diagnostics (Mastodon 6-plugin / GitLab 11-plugin corpora) + stackprof deltas + `make bench-perf`. The frozen table is Ractor-shareable (feeds ADR-15 Phase 4); completes the ADR-37 segregation arc for the per-call surface |
| ADR-53 | [Scope discovery-index separation + check-rule walk consolidation](53-scope-discovery-index-separation.md) | Accepted — **Track A complete** (A1 `031f161e` + A2 `063823e4`, 2026-06-10/11; landing it surfaced + fixed the predicted bug class twice — both ADR-44 hand-copy body-scope constructors had silently dropped later-added tables); **Track B complete** (B1+B2 `6858872c` 2026-06-11, B3+B4 2026-06-13: the shadow-harness-gated walk consolidation landed — the five built-in per-file AST walks (main pass + 4 collectors) plus the plugin walk now ride ONE engine-owned per-file traversal via `CheckRules::RuleWalk` driven inside ADR-52 WD4's `Plugin::NodeRuleWalk`). Grounded in the [2026-06-10 lib/rigor architecture re-review](../notes/20260610-lib-rigor-architecture-rereview.md) (Phase 4, the two design-judgment items). **Track A** extracts the 14 seed-time discovery tables (`discovered_*`, `declared_types`, `class_ivars`/`class_cvars`/`program_globals`, `data_member_layouts`) out of `Scope` into one immutable `Scope::DiscoveryIndex` — membership criterion: *no flow transition ever varies the field* (litmus already in the code: `Scope#==` ignores it, `#join` copies it unexamined); readers stay on `Scope` as delegates (plugins unaffected, the ADR-46 `DependencyRecorder` choke point preserved), the per-table `with_discovered_*` writers collapse into `with_discovery` (pre-1.0 cleanup, ADR-52 WD3 posture). Re-adopts the regrouping ADR-44 downgraded as an *allocation* lever, on the *boundary/boilerplate* criterion (a new side-table: 7 edit sites → 1; `rebuild` 24 kwargs → 11); perf claimed neutral, `make bench-perf`-gated. **Track B** consolidates `CheckRules`' five per-file AST walks (main pass + 4 collectors) into one engine-owned rule walk (node class → collector hooks — the ADR-52 WD4 model applied to built-ins; converging with the plugin walk → 2 walks/file total), **hard precondition: a shadow-run equivalence harness** (legacy vs merged, identical results over self-check + Mastodon/GitLab corpora) — collectors transplant verbatim, only traversal merges. Every slice gates on byte-identical diagnostics + `make bench-perf` |
| ADR-54 | [Cache slimming: definitions-blob retirement, payload compression, default eviction](54-cache-slimming.md) | Accepted — WD1–WD4 implemented 2026-06-10; grounded in the [2026-06-10 cache disk + warm-load audit](../notes/20260610-cache-disk-runtime-audit.md). Criterion: *a cache tier earns its bytes only if it beats recomputation from the next-cheapest tier on the warm path* (measured against its own upstream cache, not a cold rebuild). WD1 retired the `rbs.instance_definitions` / `rbs.singleton_definitions` disk blobs — measured net-negative given the cached env (366 ms `Marshal.load` vs 137 ms build-all from env; 23.4 MB/project): the dispatch path goes lazy per-class on the existing per-process memos, while the ADR-15 prewarm/Reflection consumers keep eager full tables built from the cached env. WD2 zlib-deflates every entry's value payload behind a format-byte bump (the env blob lands at 1.76 MB = 16 % of its 11.0 MB raw; inflate is an order of magnitude below the `Marshal.load` it precedes; old-format entries read as silent misses — no migration code). WD3 defaults `cache.max_bytes` to 256 MB so `evict!` stops being a permanent no-op (the rigor repo's own cache had accumulated ~180 MB of orphans against a ~2 MB active set — a capped trial run reaped exactly the stale entries; explicit `null` restores unbounded). WD4 memoises `RbsDescriptor.build` per loader. Envelope: ~33.7 MB → ~2 MB per project (−94 %); warm runs touching definitions save up to ~550 ms / 1.6 M allocs. Partially supersedes ADR-7 § Slice 6-D (the definitions-blob half; the per-class-disk-entry rejection stands). Cross-project shared cache root deferred (post-WD1/WD2 duplication is ~1.7 MB × N); ADR-45 `fresh?` mtime fast-path rejected on soundness. Gated on diagnostics-identical runs (self-check `--no-cache`/cold/warm + the Mastodon corpus) per slice |
| ADR-55 | [Recursive-method return-type precision](55-recursive-return-precision.md) | Accepted — slice 1 (fueled constant-arg unroll) and slice 2 (fixpoint return summaries) both implemented 2026-06-11. Instantiates ADR-41 WD5's deferred precision-unroll and adds the mechanism WD5 did not envision: a Kleene fixpoint from `bot` over the recursive return summary, replacing the depth-1 guard's in-cycle `untyped` so a recursive `String`-builder returns `String`, not `String \| Dynamic[top]` (`factorial(x: Integer) → 1 \| Integer`, no `Dynamic` contribution). Slice 2 keys the summary table by the plain `(receiver, method)` signature (shared with extended unroll frames), gates the iteration on a per-signature *consulted* flag, and adds `BudgetTrace::RECURSION_FIXPOINT_CAP`; the WD4 clamp-vs-summary composition was narrowed (the slice-1 clamp stays bare `untyped` — a soundness backstop, not a summary consumer — since the in-progress summary is a Kleene lower bound; only the guard/fuel paths consult it); mutual recursion across distinct signatures degrades soundly to `Dynamic[top]` (termination is the load-bearing property). Both Mastodon `app/models` + haml `lib` byte-identical to the pre-slice-2 baseline. Criterion: *a recursive-return precision mechanism is admissible only if its exhaustion path is byte-identical to today's widening* — fuel out / iteration cap (3, final round widens `Constant→Nominal` to force convergence) / value blow-up all degrade to the current `untyped`, inside hard non-configurable termination caps (ADR-41 WD4). Slice 1 extends the guard key to `(receiver, method, argument values)` only when every bound arg is value-pinned, fuel 32 + value-size cap (preserves ADR-24 WD5's type-key rejection — values not types, fuel-bounded); the `Constant[120]` outcome is explicitly low-value, the slice earns its place as guard-key generalization + `BudgetTrace` wiring. Governing clamp (WD1, corpus-FAIL fix 2026-06-11): the unroll may only ever surface a *fully value-pinned* result — a guarded re-entry whose body folds non-pinned clamps back to `untyped`, and value-pinned self-call adoption is confined to in-progress unrolls; the first landing's blanket adoption leaked the body evaluator's block-internal-`return` blind spot as 3 new corpus diagnostics (mastodon `account.rb`/`base_action.rb`, haml `children_compiler.rb`), now byte-identical to the pre-slice-1 baseline. Precision-additive only: no new diagnostic; gate = `make verify` + `make bench-perf` neutral + corpus diagnostic delta zero-or-removals (haml's 421 guard hits are the cost stress case). Cross-call summary memoization + configurability + the ADR-41 WD2 `static.*` exhaustion diagnostic deferred |
| ADR-56 | [Block-captured local write-back and loop-body fixpoint](56-block-captured-local-mutation.md) | Accepted — slice A (non-escaping block captured-local write-back) **implemented 2026-06-11** (`StatementEvaluator#write_back_block_captures` after `record_closure_escape_if_any`, shared `Inference::BodyFixpoint` cap-3 helper reused by slice B, `captured_local_writes` extended to all five write forms, `Combinator.widen_value_pinned` promoted + `Refined`/`IntegerRange` widening, `BudgetTrace::BLOCK_WRITEBACK_CAP`; gate green — corpus one removal/zero new, perf neutral); slice B (loop-body fixpoint) **implemented 2026-06-11** (`eval_loop` keeps the historical single-pass join as the base and overlays a `BodyFixpoint.converge` result for body-rebound locals; `loop_body_local_writes` partitions pre-existing (post-predicate seed) vs body-first (nil seed for the 0-iteration path); `loop_body_exit_bindings` re-applies the predicate loop-entry edge per iteration (`while`→truthy/`until`→falsey); no-write loops stay byte-identical (fast path); gate green — Mastodon `app/models` 5/5 + haml `lib` 13/13 byte-identical, kramdown `lib` two removals (`html.rb:455` `stack.pop`-in-`until` no longer wrong-nil-folds) + five message-rewordings at stable sites, no new genuine firings; one in-slice blind-spot fix: body-first nil seeds are NOT overlaid into the body re-evaluation, only kept as a join constituent, else the nil leaks past a condition-form assignment the engine drops). **Soundness fixes, not precision additions**: the engine evaluates every block body but discards its exit scope, so `result = 1; 1.upto(6) { result *= i }; result` types `Constant[1]` (runtime 720) and every captured-write form (`=`/`+=`/multi-assign) is dropped for `:non_escaping` blocks; `while` joins a single body pass (`1 \| 2`, missing `4, 8, …`). This violates the spec's captured-local MUST (`control-flow-analysis.md` § Fact stability). Criterion: *a captured outer local a block/loop body can rebind must never keep its pre-call binding unmodified* — continuation = pre-state joined with a capped-fixpoint post-state (cap 3, final-round `Constant→Nominal` widening, per-local `Dynamic[top]` on non-convergence = the existing escaping-block floor; one shared helper for both slices, ADR-55 pattern). Escaping/unknown paths and unwritten locals unchanged. Gate (WD4) is *adjudicated, not zero-delta*: corrected constants may fire genuine diagnostics — each new firing judged genuine-keep vs artifact-fix; removals are expected wins; hand-probed discriminating shapes mandatory alongside corpora (the ADR-55 lesson). slice C (receiver-content element-type join, WD2.5) **implemented 2026-06-12** (the slice-A/B write-back covered local *rebinding* but not receiver *content* mutation: `out = [0]; [1,2,3].each { \|x\| out << x }` typed `Array[0]` for a runtime `[0,1,2,3]` — unsound, with derived wrong folds `out.first.zero? → true`; `content_writeback_block_captures` (block), `loop_content_writeback` (loop, fast-path + fixpoint), and `each_with_object_return` (B3) JOIN the appended/stored element/key/value types into the continuation collection via `MutationWidening.join_array_content`/`join_hash_content`, reusing the slice-A/B carrier-widening; `CONTENT_ADDERS` covers Array/Hash/String mutators + index-write forms (`h[k] \|\|= v` no longer leaves `h` an empty `{}`); String accumulators widen to `String`; gate green — Mastodon `app/models` byte-identical, haml `lib` one removal (`parser.rb:746` `dynamic_attributes == "{}"` no longer wrong-always-falsey), kramdown one message-rewording, zero new genuine firings; the `loop_body_fixpoint` `acc.push(m)` fixture tightened `Array[Dynamic[top]] \| []` → `Array[Integer]`). Blanket yield-invalidates-everything and 1-shot exit-scope adoption rejected; per-method iteration-count summaries deferred |
| ADR-57 | [Opening the implicit-self call return-adoption gate](57-self-call-return-adoption.md) | **Accepted — gate opened 2026-06-12 (slices 1–3); overridable-method adoption gate added 2026-06-13 (see Addendum).** ADR-24 WD3 revisit closing the single largest remaining Dynamic source for plain procedural Ruby (`def outer = helper(3)` → was `Dynamic[top]`, now the callee's inferred return; also unblocks ADR-55's fixpoint summaries at in-body call sites). WD1 measured unconditional opening on the post-ADR-55/56 engine as +25 self-check firings; criterion = *the gate opens per adjudicated firing class, not wholesale* — each classified genuine (lands as a win) vs artifact (adopted type wrong = evaluator blind spot, fixed at root). Slice 1 fixed the dominant artifact (the tail-only body evaluator dropping explicit / block-internal / multi-value `return`, 15 of 25). Slices 2–3 fixed the rest: the escaping block-captured content floor + its receiver-chain and cross-method-boundary (`build_option_parser(opts).parse!`) variants, FP-safe optional-tuple-slot destructure softening (removed 2 gate-closed haml FPs), a live String fold-guard, and two over-strict self-authored RBS corrections. Residual all genuine-or-win (self-check + plugin self-check firing-free; haml/kramdown identical; Mastodon one pre-existing error with a more precise message), so `adoptable_self_call_result?` was removed and adoption made unconditional. Cost ~+12 % cold `--no-cache lib` wall (intrinsic callee-body re-typing); a sound per-call return-result memo is a queued perf follow-up (ADR-52 WD5 / ADR-24 WD5 forbid the obvious caches). **WD3 (2026-07-10) completed module-singleton resolution**: tier 4 had landed the typing half (a module-constant call re-types the callee's `def self.x` body), but the project-wide discovery seed still registered only classes, so a module constant typed `Dynamic[top]` from every *other* file — the facade shape real apps consume (`Feature.enabled?` = 695 unprotected GitLab sites). Modules now seed on the same terms as classes; the exclusion's stated hazard (`Kernel.select → Array[String]`) does not reproduce — of the 28 Object/Kernel/Module/Class methods that resolve on a bare module, 26 are correct, and the two that are not (`M.new` / `M.superclass`) mistype only code that raises at runtime. Adjudicated per WD2: one latent-strictness self-check firing fixed at root (`DiagnosticFormats.render` made total); haml/kramdown/liquid/rgl/Mastodon byte-identical; the one Redmine `possible nil receiver` traced to an independent artifact class — a union receiver never receives `rigor:v1:predicate-if-true` facts, so an ActiveSupport `present?` guard does not narrow nil — fixed at its root in the immediately-following union-arm predicate-polarity slice (which removes the WD3 firing plus 3 more Redmine and 16 GitLab FPs, zero new). WD3 payoff: GitLab `lib` protection 0.2457 → 0.2757 (+2,812 protected sites, identical denominator), `make bench-perf` green |
| ADR-58 | [Instance-variable field typing](58-ivar-field-typing.md) | Accepted — WD1 (binding-provenance subset) + WD2 (resolved as already-realized — the flow-insensitive write-union already yields `join(writes) \| nil`) + WD3 (ctor definite assignment through unconditional same-class calls, closing the ipaddr `@mask_addr` cluster) implemented 2026-06-12; the WD1b method-return-transit residual is queued. The algorithm-corpora survey measured **94 % of possible-nil errors (109/116) as node-field ivar reads** (`current.next`, rotation reads under tree invariants) — `attr_accessor` + ctor `@x = nil` seed types reads `Dynamic[top] \| nil` and the nil fires on invariant-guarded reads flow cannot prove. Criterion: *ivar-sourced optionality fires `possible-nil-receiver` only when the nil is flow-live (assigned/observed on a path reaching the read within the method); declaration-sourced nil (ctor seed via the class-ivar index) stays in the displayed type but is not diagnostic fuel* — the working program's invariant is assumed (ADR-5; ADR-57 slice-3 destructure softening is the precedent). Slice plan (as realized): WD1 provenance-gated firing (the 109-FP fix) → WD2 homogeneous-write field reads (`current.next → Node \| nil`; on landing, found already-realized by the flow-insensitive write-union, corpus yield ~zero) → WD3 ctor definite assignment through same-class calls (ipaddr/uri/ldap stdlib cluster). Cross-method definite assignment alone rejected as headline (node fields are genuinely nil-able); Dynamic-bearing-union suppression rejected as criterion (stops holding after WD2; provenance is the stable rule). Gate per ADR-56 WD4 + mandatory discriminating probes (a genuine local `@x = nil` read MUST keep firing) |
| ADR-59 | [Spec assertions are not implementation signatures](59-spec-assertions-are-not-signatures.md) | Accepted (strong form rejected) — retires the founding-era idea that `expect(foo(1)).to be_a(String)` could yield `foo: (Integer) -> String`. Criterion: *an assertion is a per-input witness, not a universal claim — witnesses bound a signature from the wrong side on both ends (observed returns / example args are lower bounds where ADR-5 needs upper bounds), so spec-derived claims may land only where being wrong cannot fire a diagnostic*: the `Dynamic` interior, the sig-gen review channel, or a check *against* inference. The blocker is policy, not architecture (the factorybot `producer` pattern would carry it). Three demand-gated weak forms recorded: `spec.impossible-assertion` contradiction diagnostic (matcher narrowing → `bot`), `Dynamic[top]`→`Dynamic[T]` interior hints from a `spec_return_claims` fact, runtime TracePoint → sig-gen in a separate gem. README commitment #2 reworded in the same change |
| ADR-60 | [Pre-freeze plugin contract consolidation](60-pre-freeze-plugin-contract-consolidation.md) | Accepted — last-window BC breaks before the ADR-50 v1.0 surface freeze, from the 2026-06-13 plugin-interface review. Criterion: *a surface enters the freeze only if it is wired, consistently named, and incapable of silently-wrong results when used as documented*. WD1 removes the never-wired `external_files:` manifest field (+ `Macro::ExternalFile`; ADR-16 Tier D returns demand-gated with its scanner). WD2 renames `BlockAsMethod` `verbs:`→`method_names:` and `NestedClassTemplate` `name_arg_position:`→`symbol_arg_position:` (no shim). WD3 fixes the structural stale-cache hazard: `cache_for` moves onto ADR-45's `fetch_or_validate` (dependency descriptor recorded **after** the producer block runs, so in-block `IoBoundary` reads are always captured) + new `producer watch:` glob coverage via `Cache::Descriptor::GlobEntry` (one entry covers content/addition/removal); the "prime before cache_for" idiom and hand-composed `glob_descriptor` sites are deleted. WD4 additive helpers (`read_fact` / `producer_value`+`producer_error` / `diagnostics_for`) define the final authoring idiom and migrate the 12 `*_or_nil` memo + ~23 violation-mapping sites. Keep-verdicts recorded against relitigation: `dynamic_return`/`type_specifier` split, `diagnostics_for_file`, `config_schema` dual grammar. Gate = ADR-52 slice-5b precedent + cross-process watched-file cache regression + corpus byte-identical |
| ADR-61 | [Agent-friendly diagnostic statistics (structured selector axis)](61-agent-friendly-diagnostic-statistics.md) | Accepted — implemented 2026-06-13; precision-additive (no new diagnostic, byte-identical). Lets an AI agent / `jq` compute class-method statistics over a run without parsing message text. Criterion A: *message text is presentation, not contract — any datum an agent aggregates or branches on must be a structured JSON field, never recoverable only from the `message`* (ADR-23 WD3's structured-not-string rule promoted to the public surface). Criterion B: *lossy folding lives at the aggregation layer, never on the primitive stream* — `check --format json` carries the faithful per-site `receiver_type` / `method_name` (a `Constant<"x">` renders `"x"`), while `triage` owns the normalisation that makes statistics meaningful. WD1 un-hides the two structured fields on `Diagnostic#to_h` (omit-when-nil). WD2 adds a `triage` `selectors` axis — `{receiver, method, count, files, rules}` keyed on `(receiver, method)`, uncapped JSON / text-capped, `--selectors-only`. WD3 places the literal-fold in `Triage.normalize_receiver` (shared with the heuristic `Catalogue`; guards `"x".nope` vs `name.nope` fragmenting). WD4 populates the fields on the 11 call/def rules with a dispatch subject, excluding `flow.*` / ivar (no subject). Guardrail: the axis routes attention (`pre_eval:` / RBS overlay), never feeds severity (`feedback_false_positive_discipline`). Frozen public vocabulary under ADR-50 WD1, landed in the ADR-60 pre-freeze window. The two onboarding skills teach the `jq` workflow |
| ADR-62 | [Mutation-testing the analyzer (false-negative / teeth measurement)](62-mutation-testing-teeth-measurement.md) | Accepted — harness + first fixes landed 2026-06-13; methodology + adjudication discipline in force; remaining backlog demand-gated. A dev-only mutation harness (`tool/mutation/`) breaks analyzed code in type-visible ways and reads *surviving* mutants as false-negative candidates — the dual of Rigor's anti-FP discipline; "teeth verified" (ADR-43/24) becomes measurable. Criterion A: *a survivor is signal only where Rigor holds a concrete type* — most mutations are type-invariant equivalent mutants whose survival is correct, so the type-aware anchor filter (not the raw kill-rate) is load-bearing, and the harness is in-process to consult the engine's own `type_of`. Criterion B: *adjudicate, don't assume* — a survivor is a candidate, not a bug; some are deliberate silence; a fix lands only where soundness is total and `make verify` stays clean (ADR-57 protocol). WD1 build-our-own Prism-native, `mbj/mutant` rejected (needs in-process `type_of`; parser/process-model/operator mismatch). WD2 warm loop (`ProjectContext` env+scan once + `prebuilt:`-bypassed run cache → ~6–12 ms/mutant). WD3 sweep-as-backlog (cluster survivors by `(operator, receiver type)`, `--json` for an agent → self-improving loop). WD4 dev-only, off the ADR-50 frozen surface. WD5 landed fixes — FP-safe union-receiver undefined-method teeth (fires when every non-nil arm certainly lacks the method) + RBS class-alias resolution (`Mutex = Thread::Mutex` now has a method surface) + broad-fuzz mode (`mutate.rb fuzz` — crash/hang/non-determinism; 2,706 lib/rigor mutants clean). Studied + REJECTED: nilable-union teeth (corpus study across 13 projects → ~0 firings + an FP, N3 silence kept; the study still harvested two FP guards onto shipped slice-1 — generic-metaclass + distinct-class, fixing a mail external FP). Deferred: `argument-type-mismatch` adjudication, `Type::*` self-dogfood RBS, user-facing type-protection coverage. Grounding: `docs/notes/20260613-mutation-teeth-harness.md` |
| ADR-63 | [User-facing type-protection coverage](63-type-protection-coverage.md) | Accepted — Tier 1 (static proxy, `rigor coverage --protection`) and Tier 2 (mutation effectiveness, `rigor coverage --protection --mutation`) both implemented 2026-06-14. Extends `rigor coverage` with a *protection* dimension — not "how precise are my types" but "if I introduce a bug, would Rigor catch it" (the user-facing surfacing of ADR-62's teeth work). Framing criterion (ADR-62 Criterion A): always *effectiveness / where-to-add-a-type*, never raw mutation survival (which would frighten working code). Tier 1 = a one-pass dispatch-site scan classifying each call by whether its **receiver** types concrete (`concrete_class_name` non-nil = Rigor *can* bite) vs `Dynamic` — a sound upper bound on protection, cheap enough to run interactively/CI, listing the unprotected "add a type here" sites; reuses `PrecisionScanner`. Tier 2 = the truth tier, per-site mutation kill rate (ADR-62), opt-in `--protection --mutation`, changed-files-scoped, productizing only a **narrow** subset of the harness (per-file effectiveness — not the dev sweep/fuzz/clustering, which stay dev-only per ADR-62 WD4; a scoped refinement, not a reversal). WD1 extend `coverage` not a new command (reuse `--threshold` gate + `--format json` = report + gate). WD2 protected = `concrete_class_name(receiver)` non-nil, implicit-self excluded. WD3 ranked "add a type here" list + ADR-61 JSON. WD4 Tier 2 phased + changed-files default. Rejected: raw survival headline, a `rigor teeth` command, mutation-as-default-tier, shipping the full harness. Grounding: ADR-62 + `docs/notes/20260613-mutation-teeth-harness.md` |
| ADR-64 | [Non-nil argument-type-mismatch and the coerce barrier](64-non-nil-argument-type-mismatch.md) | Accepted — the non-nil channel is built + gated for **multi-overload** methods. The cycle first shipped the FP-safe `nil` channel of `call.argument-type-mismatch` (a pure-`nil` argument rejected by every overload, deciding nil-admittance on the RBS param type so it sees through multi-overload + interface-alias params; `param_admits_nil?`, commits `a2775c7b`/`0afc65ea`). The non-nil (`type_swap`) wrong-typed-argument cluster (~900 mutants, the ADR-62 sweep's largest remaining false-negative) is blocked by Ruby's **`coerce` protocol**: `5 + Money.new` is valid via `Money#coerce` though no RBS `Integer#+` overload lists `Money`, so a naive "no overload accepts the arg" rule false-positives on every coercible user type. Criterion = fire only where runtime acceptance is **decidable from types alone**: (WD1) **exclude the coerce-dispatch operators** (`+ - * / < > …`, a fixed allow-list like `UNIVERSAL_EQUALITY_METHODS`) where `coerce` can always rescue, and (WD2) for the remaining non-operator methods generalize `param_admits_nil?` to `param_accepts_arg_class?` (does the arg's class implement the param's `_ToStr`/`_ToInt` interface, reusing `expand_type_alias` + `interface_method_names`), (WD3) single concrete RBS-known arg class only. Gate (build only when a corpus shows real teeth with zero new corpus FPs) **MET**: zero new firings across 31 `rigor-survey` projects (including Mastodon `app`+`lib`); `type_swap` mutation kills rose (kramdown 5→9, liquid 0→7). Rejected: naive every-overload-reject (coerce FP); modeling `coerce` (unbounded). Carry-over: namespaced type-alias resolution (`::MatchData::capture`); the single-overload comparison-operator path is unchanged. Grounding: ADR-62 + the teeth note |
| ADR-65 | [Diagnostic evidence tier and documentation URL](65-diagnostic-evidence-tier-and-doc-url.md) | Accepted — implemented 2026-06-15; precision-additive (no new diagnostic, byte-identical). Two additive fields on the public diagnostic surface, from the 2026-06-15 type-tool-comparison developer-feedback report (§4 + §5.1): `evidence_tier` (`high` / `medium` / `low`, none for an informational helper) = Rigor's own confidence a firing is a true positive, and a stable `documentation_url`. Follow-up to [ADR-61](61-agent-friendly-diagnostic-statistics.md) (structured stream fields); adds a *confidence* axis and a *reference* axis. WD1: the tier is a **per-rule** property of `RuleCatalog`, not a per-firing computation — Rigor's FP-discipline filters the uncertain cases *before* a rule fires, so the residual confidence is dominated by the rule's gates (the feedback's own §3 reasons per-rule-kind, not per-site). WD2: tier semantics — `high` = concrete static type, no metaprogramming escape; `medium` = a flow/inference proof with a documented FP envelope; `low` = a resolution/coverage-gap signal that routes to review (`call.unresolved-toplevel` → `pre_eval:`) — **orthogonal to severity, never feeds gating** (the FP-discipline guardrail). WD3: `documentation_url` is a per-rule anchor in the published catalogue (`04-diagnostics.md#rule-<id>`), mirroring the gemspec `documentation_uri` scheme; no `rigor.dev/rules` site invented (a URL to a non-existent page is worse than none — honesty ethos); a configurable base deferred. WD4: emitted per diagnostic on `check --format json` (built-in rules only, enriched in the CLI JSON path from the rule id so no construction site changes; `evidence_tier` omit-when-nil) and per rule on `rigor explain` / `--format json` (which makes that the machine-readable taxonomy with doc URLs §5.1 also wanted, for free); **flat field**, not the feedback's nested `evidence` object (rationale lives in `rigor explain`; `fp_suppression_considered` is universally true). Also closes a catalogue gap (`call.unresolved-toplevel` had no `RuleCatalog` entry) + adds a completeness spec. Rejected: per-firing dynamic tiers; the nested object; a docs site to non-existent pages; feeding the tier into severity / exit code. Frozen public vocabulary under [ADR-50](50-release-engineering-and-stability-strategy.md) WD1 |
| ADR-66 | [Discriminated-union member typing (tag-keyed narrowing)](66-discriminated-union-member-typing.md) | **Proposed — not implemented; demand-gated.** Narrow a polymorphic member field (e.g. `kramdown::Element#value`) by its sibling **discriminant tag** field (`Element#type`) so AST/visitor/converter code — the parser/template/linter genre where the payload type is a function of a tag (`case node.type; when …; node.<payload>`) — types its payload reads instead of falling to `Dynamic`. Rigor already owns both halves and hasn't connected them: the consumer side narrows the tag on `==` / `case`/`when` ([ADR-47](47-narrowing-driven-clause-reachability.md)), the producer side carries per-instance member layouts ([ADR-48](48-data-struct-value-folding.md) `DataInstance` + `data_member_layouts`); the missing connective is a `(tag ⇒ payload)` map + a rule narrowing the payload when the discriminant narrows. Criterion (FP envelope, non-negotiable): narrow only when the map is a sound over-approximation, else keep the flat / `untyped` type and no-op — the feature only ever *adds* protection, never converts working code's silence into a diagnostic ([ADR-58](58-ivar-field-typing.md) kinship). Staged: WD1 additive-only floor; WD2 Tier 1 declared `%a{rigor:v1:variant(…)}` map → tag-indexed member carrier (both-sides-authored = FP-safe, helps the explicit-`case` idiom); WD3 Tier 2 inferred map (budget-gated; unsound under dynamic construction → adopt only a clean over-approximation); WD4 Tier 3 visitor-dispatch tag binding (`send("convert_#{el.type}", el)` via the [ADR-16](16-macro-expansion.md) substrate) — hardest but where most real consumers live, the asymmetry that ranks this below ADR-58. WD5 mutation invalidates the narrowing. Re-eval when ADR-58 lands / a corpus shows explicit-`case` is common / ADR-63 surfaces this hole often. Rejected: a flat union that fires; unguarded whole-program inference; a general GADT system. Grounding: the 2026-06-16 protection-uplift pilot |
| ADR-67 | [Parameter type inference (the M3 frontier)](67-parameter-type-inference.md) | **Accepted — WD1+WD3+WD5 (call-site parameter inference + capped fixpoint) implemented; wired into `coverage --protection` only (v0.2.8, not the `check` walk — byte-identical diagnostics). WD2 (in-body structural inference) spiked and demand-gated.** Re-opens the algorithm-survey's "M3 — untyped-param → whole-method Dynamic: **EXCUSED, do not pursue**" verdict on **protection-coverage** evidence. Method/ctor params default to `untyped` (the gradual entry point; block params are inferred but `def` params are not inferred from call sites or usage), so `def initialize(line); @line = line` makes `@line` `Dynamic` and every `@line.<method>` unprotected — distinct from [ADR-58](58-ivar-field-typing.md), whose ivar typing is *realized* for concrete-write fields (`@line = Line.new` reads `Line`) but cannot touch a **param-sourced** ivar. The survey excused M3 because leaf scripts have no call-site seed; the 2026-06-16 pilot shows it is the **dominant remaining protection hole on real apps** (faraday `Connection`/`Options`, haml `compile(node)` / `@template = template`), where call sites exist — so protection re-opens it. Criterion (reconciliation with [ADR-5](5-robustness-principle.md), which keeps params lenient): an inferred param type is **precision-additive only** — it sharpens the ivar/receiver/protection/folding it feeds but is **never** consulted to reject a caller's argument (the param boundary stays lenient; an RBS-declared param always wins). Staged: WD1 precision-additive-only contract (inferred-not-declared provenance reusing ADR-58 WD1's bit, can never fire a boundary diagnostic); WD2 in-body usage **structural lower bound** (`arr.delete_at` ⇒ responds-to bound; no call sites → helps even the excused leaf scripts); WD3 **call-site union** (TypeProf-style, the app-scale lever; whole-program-ish → budget-gated, in tension with [ADR-46](46-incremental-dependency-graph.md); unseen-call-site / dynamic dispatch → falls back to `untyped`); WD4 soundness fallbacks; WD5 budget+termination (worklist fixpoint capped under [ADR-41](41-inference-budget-design.md), reuse [ADR-57](57-self-call-return-adoption.md) memo). Rejected: treat inferred-as-declared (ADR-5 breach); the survey's "do not pursue" (superseded for the protection lens); a full TypeProf whole-program interpreter as default. Grounding: algorithm-corpora survey + the 2026-06-16 pilot + the TypeProf-internals prior-art note |
| ADR-68 | [Plugin-declarable class-builder folding](68-class-builder-folding.md) | **Proposed — demand-gated.** Generalize the [ADR-48](48-data-struct-value-folding.md) member-shape carrier substrate (`StructClass`/`DataClass` + `data_member_layouts`) from the two hard-coded builders (`Struct.new` / `Data.define`) to a **declared** family of Struct-like class builders, so `ConnectionOptions = Options.new(:a, :b, …)` (faraday's custom builder) types as a named member-shape class tied to the constant instead of `Dynamic`. The 2026-06-16 pilot's faraday ceiling (~31%) is gated by `Options.new`-built constants (`Env` / `*Options`) typing as dynamic class objects Rigor cannot connect to the RBS namespace — the same member-shape shape ADR-48 already solved, blocked only on *recognising* a non-core builder. Criterion: a constant (or subclass / local) assigned from a **declared** Struct-like builder folds to an ADR-48 carrier with members = the builder call's symbol args; the builder must be declared (plugin contribution / built-in allow-list) or followed to a Struct ancestor — never guessed from an arbitrary `X = Y.new` (a `.new` not returning a class must not misfold); precision-additive only (unrecognised builder → today's `Dynamic`, ADR-48's no-FP contract). WD1 reuse the carriers + tiers, add only recognition; WD2 plugin-declared builders via the [ADR-16](16-macro-expansion.md) substrate (`rigor-faraday` `Options.new` the first consumer); WD3 inferred recognition by following the builder definition (deferred — `.new` may be overridden to return a non-member value); WD4 compose with [ADR-26](26-activerecord-relation-typing.md) `open_receivers` for the builder's dynamic method surface (folded class is known-but-open), don't re-derive; WD5 precision-additive / mis-recognition guard. Distinct from ADR-26's AR case (an *unbounded* method surface — open receivers — not a fixed member layout). Rejected: a new carrier; guessing any `X = Y.new`; folding the AR case here. Grounding: the 2026-06-16 protection-uplift pilot |
| ADR-69 | [Pluggable mutation substrate (kill-oracle + operator seam)](69-pluggable-mutation-substrate.md) | **Accepted — both seams implemented 2026-06-17, co-landed with ADR-70.** Seam 1 (kill oracle) extracted the `DiagnosticOracle` so a `TestSuiteOracle` can sit beside it; Seam 2 (site selector, surfaced as `--include-dynamic`) was pulled forward from ADR-71 the same day when validation showed the fused overlay was blind to `Dynamic` sites — `Mutator#dispatch_site_mutations` mutates every dispatch site (Dynamic receiver included), gated to `--with-tests` (a Dynamic site's only protection is a test). Generalize the [ADR-62](62-mutation-testing-teeth-measurement.md) / [ADR-63](63-type-protection-coverage.md) mutation machinery so the **kill oracle** and the **site selector** are injected collaborators of `Protection::MutationScanner`, not assumptions baked into it. Today the only kill is "a new Rigor diagnostic appeared" (`mutation_scanner.rb` `classify`) and the only selector is "keep sites Rigor can bite" (`mutator.rb` `filter_by_type`); both are correct for the analyzer-teeth use and **wrong** for a test-suite consumer, which kills by *running tests* and wants the **inverted** selection (mutate `Dynamic` sites, where tests are the only net). Criterion (reusable rule): the `Mutator` knows only how to *splice source* + *where contracts live* (the anchor); *what counts as a kill* and *which sites are worth mutating* belong to the consumer — a capability the substrate can't express by swapping an oracle/selector is a seam gap, not a reason to copy the mutator. Seam 1 = extract a `DiagnosticOracle` (byte-identical to today) + define the `TestSuiteOracle` interface [ADR-70](70-fused-protection-coverage.md) implements; Seam 2 = the type-aware filter becomes one `BiteableSites` strategy, separable so a consumer can pass `AllSites`. No new user surface (the ADR-50 freeze is the CLI/JSON vocabulary, not `Protection::*` class shapes). Rejected: copy the mutator per consumer (maintenance trap); a single both-axes oracle (defeats ADR-70's gradual short-circuit); a public plugin API now (deferred to ADR-71). Co-land with ADR-70's first dynamic consumer so the interface is exercised, not speculative. Grounding: the 2026-06-17 strategy note |
| ADR-70 | [Fused static∪dynamic protection coverage](70-fused-protection-coverage.md) | **Accepted — implemented 2026-06-17 (`coverage --protection --mutation --with-tests`), co-landed with ADR-69, validated on faraday / liquid / mail.** Adds an optional **dynamic overlay** to `coverage --protection --mutation`: for each mutant the *type* checker fails to kill, ask whether a *test* kills it (the ADR-69 `TestSuiteOracle`, runner hook `--test-command`, run with a stripped bundler env), and classify each site by **both** axes — type-protected / test-protected / **unprotected** (the conceptual *doubly-protected* bucket collapses into type-protected: a type-killed mutant never reaches the suite — the gradual short-circuit). `--include-dynamic` (ADR-69 Seam 2) extends it to `Dynamic`-receiver sites, completing the map (liquid `lexer.rb` 76 → 115 sites, 38 test-protected + 2 real gaps). The artifact is the fusion: a line is truly unprotected only when **neither** a type nor a test guards it, and the report names the **cheaper missing axis** ("add a type" vs "add a test"). No tool fuses static type-protection and dynamic test-protection into one map. Criterion (extends ADR-63's framing): always effectiveness / where-to-add-protection, never raw survival — and the payload is the **attribution** (cheapest missing axis), not a single number; a site is unprotected only when both axes miss. Gradual short-circuit: a type-killed mutant never reaches the suite (type = first line, tests = second), so cost is proportional to the protection hole. WD1 survivor-scoped, opt-in, changed-files default; WD2 the test oracle is an **adjudicated candidate, not a verdict** ([ADR-59](59-spec-assertions-are-not-signatures.md): pending/tag/flaky tests are unknowable — a survivor prioritizes attention, it does not certify a gap); WD3 consume ADR-69's seam, `DiagnosticOracle` path byte-identical; WD4 new flag + JSON keys frozen under ADR-50. **Selection is the suite's job, NEVER the dependency graph** (a type-read graph ≠ the runtime call-graph → skips the killing test → a **false survivor** = an FP, the line `feedback_false_positive_discipline` won't cross). Rejected: run the suite for every mutant (no short-circuit); a single fused %; graph-based test selection; a new `rigor teeth` command; treating a test-survivor as a proven gap. Grounding: the 2026-06-17 strategy note (judgement step 2) |
| ADR-71 | [Type-guided external incremental mutation testing](71-type-guided-external-mutation-testing.md) | **Proposed — deferred / demand-gated; nothing implemented.** Evaluates growing Rigor's mutation substrate into an **external** library driving RSpec / minitest, pruning the mutation space with Rigor's types + dependency graph. Verdict: **defer** — the headline pitch ("types prune the mutation space → smarter/faster than ordinary mutation testing") is **overstated**, and the surviving novel wedge is **incremental / CI-scoped** mutation testing built on [ADR-46](46-incremental-dependency-graph.md), not smart pruning. This is a distinct *third* product — mutation testing of the **user's test suite** (a survivor = a test gap), sharing only the Prism splicer (~20 %) with ADR-62's analyzer-teeth tool; the test-runner 80 % is `mutant`'s graveyard. Criterion (each lever only where sound): **runtime coverage** selects which tests to run (a static graph over-approximates and *misses* dynamic/metaprogrammed calls → a skipped killing test → a **false survivor** = FP); the **dependency graph** scopes only *which files to mutate on a diff*; **type info** does generation + stratified reporting, **never** test selection. Re-evaluation triggers (both): (a) ADR-46 incremental has landed (the diff-closure engine the CI wedge needs); (b) demand is demonstrated (ADR-70's dynamic axis shows value on real projects, or users ask). Rejected: static-graph test selection (loses to coverage, false survivors); "type pruning makes it worthwhile" (bites at low-value typed sites, prunes the cheap axis); build-it-now (pre-freeze, the 80 % graveyard); adopting `mutant`/`mutest` as the engine (revisit at build time); standalone type-guided generation (folded into ADR-69/70). Grounding: the 2026-06-17 strategy note (the full expansive-vs-critical analysis) |
| ADR-72 | [Gemfile.lock-gated bundled RBS overlays](72-gemfile-lock-gated-rbs-overlays.md) | **Accepted — implemented 2026-06-17.** When a gem is locked in the project's `Gemfile.lock` but ships no RBS through any resolution path (the `RbsCoverageReport` `:missing` class), Rigor auto-loads a bundled per-gem RBS overlay (`data/gem_overlay/<gem>/`) for its core-class extensions, so a Rails project stops seeing a systematic false `call.undefined-method` on `3.minutes` / `"x".underscore` / `hash.symbolize_keys` while a project WITHOUT the gem still sees the genuine diagnostic. ActiveSupport is the first (v1: only) overlay; the auto-applied, lock-gated twin of the opt-in [`rigor-activesupport-core-ext`](../../plugins/rigor-activesupport-core-ext/) plugin. Grounding: the 2026-06-17 `evidence_tier` calibration feedback — a downstream consumer trusting `evidence_tier == "high"` ([ADR-65](65-diagnostic-evidence-tier-and-doc-url.md)) auto-promoted 171/512 high-tier `call.undefined-method` on Mastodon (~33 %, all ActiveSupport core-ext) to "live bug." Criterion: fix the FP at the source, never relabel it — the project's first value is false-positive discipline, and down-tiering does not serve it (the tier never feeds severity per ADR-65 WD2, so the red error stays on working code). WD1 gate = locked ∧ `:missing` ∧ no conflicting opt-in plugin loaded (`GEM_OVERLAY_PLUGIN_IDS`), piggybacking on `bundler.auto_detect` (default true); gating on actual gem presence is what makes it sound (a plain-Ruby project keeps the genuine `3.minutes` miss). WD2 FP-safe direction only — the overlay can only *remove* a diagnostic, and only where the method is real at runtime; a genuine typo (`5.minuets`) still fires at `:high` (the discrimination the down-tier alternative could not make). WD3 overlay dirs ride in `for_project`'s `loader_signature_paths`, so the env cache descriptor digests them and invalidates on lockfile change — no new cache plumbing. WD4 **preserves ADR-65 WD1** (tier stays per-rule; no per-firing dynamic tier) and **does not reverse the ADR-27/31 plugin auto-load deferral** (that defers plugin *code*; this loads RBS *data* only, gated on presence — the `core_overlay` mechanism made lockfile-conditional). WD5 `GEM_OVERLAY_PLUGIN_IDS` + the `data/gem_overlay/` tree generalize to other systematic core-class monkeypatchers (i18n, Sequel) by adding a directory, demand-gated. Rejected: per-firing tier demotion (the feedback's literal ask — reverses ADR-65's rejected per-firing-dynamic-tiers, and leaves the red FP on working code); severity downgrade/suppression under monkeypatch risk (trades a false-negative on real core-type typos); auto-loading the full plugin (runs code, ADR-27/31); an unconditional overlay (unsound for non-Rails projects). Grounding: the 2026-06-17 `evidence_tier` calibration feedback |
| ADR-73 | [SKILL-driven Rigor user experience](73-skill-driven-user-experience.md) | **Accepted — WD1–WD5 implemented 2026-06-20; the `describe` catalogue is designed to grow (first addition `rigor-rbs-setup`, landed same day).** Establishes a single SKILL-driven "what next" entry point (`rigor-next-steps`) backed by a live, version-current `rigor skill --describe`, so distributed onboarding guidance never goes stale (陳腐化). Criterion: a piece of guidance is frozen into the distributed SKILL **only if it must run before `rigor` exists** (command resolution, install); everything that changes as Rigor evolves — the downstream-skill catalog, descriptions, and the project-state → next-step recommendation — is emitted **live** by `rigor skill --describe` and never copied into the SKILL. WD1 `rigor-next-steps`'s body = the bootstrap chain only (resolve `rigor` → install via `docs/install.md` → `project-init` if unconfigured → else delegate to `--describe`). WD2 the `--describe` contract = a cheap **presence-only** project-state probe (config / baseline / `sig/` / CI — **never** runs `rigor check`) + a recommended next skill + the catalog with frontmatter descriptions + an agent action-prompt, read-only and side-effect-free on `SkillCommand` ([`lib/rigor/cli/skill_command.rb`](../../lib/rigor/cli/skill_command.rb)). WD3 the install branch is the one un-`--describe`-able step (no binary yet) so it points at `docs/install.md`'s raw URL, and `install.md` Step 4 defers back to the same routing so they cannot diverge. WD4 promotes the parked, pilot-validated `rigor-protection-uplift` ([ADR-63](63-type-protection-coverage.md) WD5, +4.7pp / zero-regression on 5 OSS repos) into `skills/`, settling its packaging as the same dual channel. WD5 distribution hygiene = two channels, one source (vercel-labs/skills `npx skills add` pre-install + gem-bundled `rigor skill` post-install, both over the same `skills/` tree, no second artifact per [ADR-31](31-contribution-and-supply-chain-policy.md)) + `.claude/skills/` marked `metadata.internal: true` so a bulk install does not ship contributor workflows + a new `skills/README.md`. Rejected: embedding routing in the distributed SKILL (the staleness this prevents); `--describe` running a full analysis (slow / side-effectful for a presence-only hint); a `rigor` subcommand entry point (cannot bootstrap its own absence); a separate `rigor-skills` gem / repo (duplicates the source); leaving `.claude/skills/` discoverable (ships release tooling to users). The new `--describe` output + skill ids become public vocabulary frozen at v1.0 under [ADR-50](50-release-engineering-and-stability-strategy.md) WD1. The catalogue is open: each new mechanical, describe-routed skill is one cheap presence-only branch in the gem-side decision tree — A first wave of seven landed the same day: `rigor-rbs-setup` (`rbs collection install`, headline branch on the gem-RBS gap), the catalogue-first DX/integration pair `rigor-editor-setup` (`rigor lsp`) + `rigor-mcp-setup` (`rigor mcp`) routing to the manual's editor/MCP chapters, and the catalogue-only maintenance/validation skills `rigor-monkeypatch-resolve` (`pre_eval:`), `rigor-plugin-tune` (`rigor plugins --strict`), `rigor-upgrade` (`rigor diff`/`baseline regenerate`) and `rigor-doctor` (`config_warnings` + `plugins --strict` + `baseline drift`, no new command). The journey-branch vs catalogue-first distinction is the rule: a skill earns a headline recommendation only if it exposes a reliable presence signal. Field-trial follow-ups (2026-06-20 conference-app + rigor-survey dogfood, `docs/notes/20260620-skill-driven-onboarding-dogfood.md`): the probe was accurate 7/7; landed UX fixes (target_ruby-floor message, actionable meta-gem error, warn-and-skip missing path, `rigor describe` alias, **WD2-preserving check-aware agent-prompt routing**); open decisions deferred for ratification — headline check-awareness (`.rigor/` cache-read vs a `describe --deep` opt-in, never the default running `check`) + `rbs-setup` priority softening. Grounding: the parked act-on-coverage skeleton + `docs/install.md` + the existing `rigor skill` command |
| ADR-74 | [Offline doc access (`rigor docs`) + `llms.txt` linkage](74-offline-doc-access-and-llms-txt.md) | **Accepted — WD1–WD4 implemented (`rigor docs`, v0.2.2).** Completes [ADR-73](73-skill-driven-user-experience.md) on the docs axis: an installed Rigor serves the docs an agent needs **offline** (a new `rigor docs` command over a gem-bundled `docs/manual`), and the project's `llms.txt` agent doc-discovery index both reflects the new skill surface and records the offline path. Criterion: a doc an agent consults to *drive Rigor* belongs in the gem and is served by `rigor docs` (mirroring `rigor skill print` for skills) — so once Rigor is installed the SKILL-driven UX never needs the network for guidance the gem already carries; the web `llms.txt` + GitHub raw URLs stay the **pre-install** fallback (installing Rigor necessarily precedes a local `rigor`). WD1 bundle `docs/manual/**/*.md` + `docs/install.md` in the gemspec (the drive-Rigor chapters; the contributor-facing ADR/spec/notes corpus stays web-only). WD2 `rigor docs` (index / `<name>` chapter / `list` / `path`, `lib/rigor/cli/docs_command.rb`, mirroring `rigor skill`'s shape; reads the bundled `docs/`, no network) + a gem-side `llms.txt` mirror. WD3 the editor/mcp/next-steps skills prefer `rigor docs <chapter>` (local) with the GitHub URL as the pre-install fallback (only `next-steps`' install step is irreducibly network — `docs/install.md` precedes `rigor`). WD4 refresh the site `llms.md` / `llms-ja.md` (route the "one prompt" through `rigor-next-steps`, skill list → catalogue + `rigor skill describe`, index chapter 17, add the offline-access note) — the manual is the single source both `llms.txt` copies index. Rejected: keep docs web-only (the network dependency); a separate docs gem (duplicates ADR-31's single gem); fetch the site `llms.txt` at runtime (re-couples to the network); bundle the whole `docs/` (contributor-facing bloat). `rigor docs` + `llms.txt` vocabulary freeze at v1.0 under [ADR-50](50-release-engineering-and-stability-strategy.md) WD1. Grounding: ADR-73 + the deployed `llms.txt` + the field-trial note |
| ADR-75 | [`Dynamic[T]` provenance and explanation](75-dynamic-provenance.md) | **Accepted — implemented 2026-06-24 (`01e291cb`).** `Dynamic[T]` is the right carrier for an unchecked value, but the engine did not record *why* a value became dynamic; this adds a precision-additive **provenance** side-channel (a fixed cause set: external-gem-without-rbs / framework-dsl-boundary / analyzer-budget-cutoff / explicit-untyped / unsupported-syntax) surfaced additively through `coverage --protection` labels + `--format json`, so a tractable hole (install RBS, enable a plugin) is told apart from an intractable one (a DSL boundary) — without changing `untyped = Dynamic[top]` semantics, firing a diagnostic, or feeding severity. Criterion: provenance is metadata *about* a dynamic value, never part of *what it is*. WD1 it is a side-channel fact keyed at the introduction-site AST node — implemented as the identity-keyed `Scope#dynamic_origins` side-table (`record_dynamic_origin`; excluded from `Scope#==` / `#hash` per the ADR-53 litmus), NOT the trace-only `FallbackTracer` (nil outside `rigor trace`) and NOT a field on `Type::Dynamic` (which would break the `value_fields :static_facet` equality that dedups `Dynamic[T]` in unions/cache and fork the lattice by origin). WD2 surfaced additively, structured-not-string ([ADR-61](61-agent-friendly-diagnostic-statistics.md)) — a `dynamic_origin` JSON field per site, omit-when-nil. WD3 preserves the relation semantics (provenance never participates in subtyping / consistency / normalization / erasure). WD4 a future strict-dynamic discipline consults provenance as its evidence base but ships behind `bleeding_edge:` ([ADR-50](50-release-engineering-and-stability-strategy.md)) — out of scope here. Rejected: a `provenance:` field on the carrier (breaks value equality + forks the lattice); a default-on provenance diagnostic (FP discipline); re-deriving provenance at report time (wrong layer, can disagree with the real fallback path). Grounding: the 2026-06-22 compatibility-safe strengthening survey §3 / P2 + the ADR-73 coverage-tractability-labels follow-up |
| ADR-76 | [Effect modeling for `freeze` / `dup` / `clone` and shape-carrier preservation](76-effect-modeling-freeze-dup-shape-preservation.md) | **Accepted — WD1 implemented 2026-06-24 (`2751bc78`); WD2 fully landed 2026-06-26 (HashShape + Tuple preservation, once the ADR-78 WD1 block-form over-fold guard removed the residual reflexive firings).** A split decision on effect / mutation modeling. WD1 the *conservative-invalidation* half (non-mutating calls preserve facts, aliased-mutation calls invalidate them) is compatibility-safe internal precision that *reduces* spurious `always-truthy` / nilability / shape firings — **landed**: `MutationWidening` treats `freeze` / `dup` / `clone` / `itself` as pure self-returners (`PURE_SELF_RETURNERS` + early-returns in `widen_after_call` / `widen_for_outer_receiver`) so they preserve facts, self-check clean (no reflexive `always-truthy` reappeared — the change is in the invalidation path, not the dispatch return-type tier). WD2 the high-value *shape-carrier preservation through the pure self-returners* (`freeze` / `dup` / `clone` / `itself` — which today degrade `HashShape` → `Hash`, `Tuple` → `Array` via the nominal `() -> self` RBS resolution, making a subsequent `#[]` `Dynamic`: the `MESSAGES = {…}.freeze; MESSAGES[reason]` gap) is **deferred to a separate branch** (bucket 3) — the recon note's already-written, corpus-FP-safe tier was backed out because it surfaced 12 reflexive `flow.always-truthy-condition` on Rigor's OWN constant-folder (self-analysis folds `receiver.public_send(method_name)` to a constant → provably-truthy over-fold), and the discipline forbids `# rigor:disable`-ing them. Ordering is load-bearing: the reflexive over-fold must be fixed at the root FIRST, then the shape tier re-applies. WD3 the four pure self-returners on a shape carrier return the receiver type unchanged (carrier preserved); the mutating self-returners (`<<`, `merge!`) keep today's nominal degradation. Rejected: ship-the-tier-and-disable (fix the cause, never disable); unconditional shape preservation incl. the reflexive case (manufactures the FPs); optimistic preservation as the default stance (over-preservation fires on working code). Grounding: the 2026-06-22 survey §4 + the [2026-06-14 precision-foldgap recon note](../notes/20260614-precision-foldgap-recon.md) |
| ADR-77 | [`rigor doctor` and `rigor upgrade` evidence-routing commands](77-doctor-and-upgrade-commands.md) | **Accepted — implemented 2026-06-24 (`8048991c`; `upgrade` skeleton).** Two additive CLI commands that *route existing evidence* rather than change default `check`. `rigor doctor` classifies already-produced findings (config-resolution warnings, `RBS classes available: 0`, Rails-locked-but-no-Rails-plugins, baseline drift) into setup-problem vs clean-run with a routed next action — an aggregation/presentation layer over data `check` already produces, no new analysis pass, no new rule. `rigor upgrade` is the [ADR-50](50-release-engineering-and-stability-strategy.md) WD7 migration command (deferred until a concrete BC gives it a target). Both promote the [ADR-73](73-skill-driven-user-experience.md) catalogue-only `rigor-doctor` / `rigor-upgrade` SKILLs (which compose the flow by hand today) into first-class commands. Compatibility-safe (additive, current behaviour preserved). Criterion: reuse the existing check/baseline/plugin surfaces; any JSON is a stable structured contract, not a message mirror. WD1 `doctor` classifies, doesn't re-analyze loudly. WD2 `upgrade` = ADR-50 WD7. WD3 both consume a shared "run check → `Analysis::Result`" helper extracted from `CheckCommand#build_check_runner` (the same extraction ADR-73's `describe --deep` needs — three callers share it). WD4 the deep probe is opt-in, never wired into the presence-only `rigor skill describe` headline (preserves ADR-73 WD2). Rejected: deep analysis as the default `describe` headline (breaks presence-only); duplicating the check-runner setup; a separate diagnostics gem (ADR-31 single-gem); a new "setup problem" diagnostic rule (it is a routing view, not analysis). New command vocabulary freezes at v1.0 under ADR-50 WD1. Grounding: the 2026-06-22 survey §7 / P5 + ADR-50 WD7 + ADR-73 |
| ADR-78 | [Reflexive over-fold and the `flow.always-truthy-condition` envelope](78-reflexive-overfold-always-truthy.md) | **Accepted — fully implemented 2026-06-26.** WD2 (`REFLECTIVE_SEND_METHODS` guard) + the WD1 carry-over (block-form over-fold guard in `ShapeDispatch.try_dispatch` — the residual class was not `public_send` but `CONST = [...].freeze; CONST.any? { … }` folding the no-block result, ignoring the block) + WD3 (shape-carrier preservation for HashShape **and** Tuple, diagnostic-identical to master on lib + mail/kramdown corpora). `flow.always-truthy-condition` (and the `&&`/`||` `ExpressionTyper#constant_value_polarity` gate it shares) concludes a predicate is provably truthy when it folds to a `Type::Constant` — unsound when that constant came from an **over-fold**, a fold whose soundness holds only for a narrower form than the actual runtime expression. The concrete case (the [2026-06-14 precision-foldgap recon note](../notes/20260614-precision-foldgap-recon.md)): a reflective dispatch `receiver.public_send(method_name)` with a *runtime-variable* method name folds to a single `Type::Constant` once the receiver keeps a foldable shape carrier, so the rule reads it provably truthy — the **12 reflexive firings on Rigor's own constant-folder** that forced [ADR-76](76-effect-modeling-freeze-dup-shape-preservation.md) WD2 (shape-carrier preservation through `freeze`/`dup`) to be backed out. Criterion (WD1): provable truthiness may rest only on a *genuine* compile-time constant, never on a fold conditional on a narrower form; the over-fold is the typing bug, the rule reading it is the symptom — fix the fold, not the rule's skip envelope. WD2 fix the cause: the dispatcher declines to constant-fold a reflective send (`public_send`/`send`/`__send__`) unless the method-name arg is a value-pinned literal `Constant` — precision-neutral (`x.public_send(:foo)` still folds; `x.public_send(name)` degrades to the RBS `untyped` it always should have been) and **strictly FP-reducing** (declining an unsound fold can only remove a spurious constant, never add a firing, loses no genuine firing). WD3 re-apply ADR-76 WD2 (the already-written, corpus-FP-safe tier) once the over-fold no longer yields a constant — the payoff that closes the `MESSAGES = {…}.freeze; MESSAGES[reason]` fold gap. Rejected: suppress the rule for reflective-derived predicates (patches one rule's symptom, leaves the unsound constant for other consumers); tag the constant with over-fold provenance ([ADR-75](75-dynamic-provenance.md) kinship — more machinery, the bad constant still exists as a typed value); `# rigor:disable` the 12 firings (forbidden, the reason WD2 was backed out). Grounding: the 2026-06-14 precision-foldgap recon note |
| ADR-79 | [RBS version-range fidelity over checker-pinned determinism](79-rbs-version-range-over-pinned-determinism.md) | **Accepted 2026-06-26.** Records the decision to keep reading core/stdlib RBS from the project's own resolved `rbs` gem across the gemspec range `rbs >= 3.0, < 5.0`, rather than vendoring a Rigor-pinned core RBS for bit-determinism. Prompted by the rigor-rs port's reproducibility critique (it vendors+embeds core RBS, its ADR-0007) and by [rigortype/rigor#22](https://github.com/rigortype/rigor/pull/22) which fixed RBS 3.x compat and added a `Gemfile.rbs-compat` CI matrix (`~> 3.10` ∧ `~> 4.0`). Criterion: **fidelity to the installed toolchain outranks cross-environment bit-determinism** — variation in diagnostics across `rbs` versions is checking-what-you-run, not a defect; determinism is bought *tactically* only where a version gap yields a *wrong* result (a false positive on a method real at runtime), never *structurally* by freezing the RBS source away from the project's actual `rbs`. Mitigations are existing and proportionate: PR #22's compat CI matrix keeps the range honest, the unconditional `data/core_overlay/` reopen patches version-lag FPs (e.g. the `StringScanner#peek_byte` gap `rbs 4.0.2` omits, from the 2026-06-16 liquid sweep), and the ADR-72 Gemfile.lock-gated `data/gem_overlay/` covers `:missing`-RBS gems — so Rigor already holds a small surgical slice of its own core RBS, used to fix wrongness, not to replace the source. Deferred (not adopted): a full Rigor-held bundled core RBS as source of truth — rejected as default because it decouples diagnostics from the project's actual `rbs`/Ruby (reintroducing the fidelity gap), adds an upstream-release maintenance burden, and duplicates `core_overlay`; held as an **opt-in pinned-RBS mode** behind a re-evaluation trigger (demonstrated CI-vs-dev drift not fixable by an overlay), with rigor-rs's vendor+embed as its reference design. Grounding: PR #22 / issue #21, the 2026-06-16 liquid v5.x sweep note, the 2026-06-26 rigor-rs design audit note (R2) |
| ADR-80 | [Rename the `type_specifier` plugin hook to `narrowing_facts`](80-narrowing-facts-rename.md) | **Accepted 2026-06-26; COMPLETED in 0.3.0.** Renames the ADR-37 slice-2 plugin-author DSL verb `type_specifier` → `narrowing_facts`; `type_specifier` survives as a warning-emitting deprecating alias through 0.2.x and is **removed in 0.3.0**; the bundled minitest/sorbet/rspec plugins are migrated. Prompted by the 2026-06-26 rigor-rs port feedback (item 5): `type_specifier` reads as a parallel to `dynamic_return` (which returns a *type*) when it actually returns **post-return narrowing facts** — the internal storage is already truer (`post_return_facts`). [ADR-60](60-pre-freeze-plugin-contract-consolidation.md)'s keep-verdict defended the `dynamic_return`/`type_specifier` *split* (its rejected alternative was *merging* them), **not** the name — so the name was open; and [ADR-50](50-release-engineering-and-stability-strategy.md) WD1 freezes the plugin vocabulary at v1.0, making pre-1.0/pre-adoption (third-party usage zero) the lowest-cost and last moment to fix it. Criterion: **a public DSL verb names what the author declares** (facts), not by false parallel to a sibling that returns a type; clarity renames are paid pre-1.0 where BC cost is lowest. Scope is the author-facing verb ONLY — the engine-facing reader `type_specifiers`, the consumer `#type_specifier_facts`, and the `rigor plugins --capabilities` JSON field `type_specifier_methods` are left unchanged (internal API / a separately-frozen CLI-output surface), revisited at the 0.3.0 alias removal — and **decided there in favour of full consistency** (0.3.0 addendum): `type_specifiers`→`narrowing_facts_rules`, `#type_specifier_facts`→`#narrowing_facts_for`, JSON `type_specifier_methods`→`narrowing_facts_methods`, because the JSON key freezes as public vocabulary at v1.0 (ADR-50 WD1) so a breaking minor was the last free window to correct the misname. Rejected: keep the name (permanent misname at freeze); rename internals + capability JSON in this slice (scope creep across a second frozen surface); merge into `dynamic_return` (ADR-60 already rejected — the split is principled). Sibling rigor-rs ADR-0027 mirrors the contract and should track the rename. Grounding: the 2026-06-26 rigor-rs port feedback (item 5) |
| ADR-81 | [Skill-set optimization: per-skill freshness + the `waza` evaluation stance](81-skill-set-optimization.md) | **Accepted — implemented 2026-07-05.** Two standing decisions about maintaining the user-facing `skills/` set. **Criterion 1 (freshness):** generalize [ADR-73](73-skill-driven-user-experience.md) WD1's freeze-only-pre-install rule from the entry point to *every* skill body — a distributed skill freezes only version-stable scaffold, version-coupled step detail is fetched live from the installed gem. The load-bearing point: both channels (gem `rigor skill` + vercel-labs `npx skills add`) read one `skills/<name>/SKILL.md`, so freshness cannot come from thinning the file (that thins both) — it comes from a **"First: load the version-current copy" directive** that re-fetches the current body via a new `rigor skill --full <name>` (inlines the body + every `references/*.md` in one call, `SkillCommand#run_full`); a frozen vendored copy is stuck at install time while `rigor skill` always reads the gem, so the directive resolves upgrade-time divergence toward the installed version. Hybrid calibration: split volatile detail into `references/` only where the body carries drifting exact flags/config/rule-ids (`rigor-doctor` = demo split), else directive-only; `rigor-next-steps` stays a pure pre-install bootstrap. **Criterion 2 (evaluation):** a `waza` advisory is adopted only when it flags a defect independent of the agentskills.io publication profile — **adopt** link health (`waza check` caught 4 × `blob/main` 404s in `rigor-ci-setup`, default branch is `master`) and over-specificity of hardcoded doc URLs (route to offline `rigor docs`, converging with Criterion 1); **reject** the 500-token budget / "complexity: comprehensive" (skills are intentionally comprehensive) and the `**WORKFLOW SKILL**`/`USE FOR:`/`INVOKES:` labeled description format (prose `Triggers:`/`NOT for … (use X)` conveys the same and reads better; `waza` penalizing its absence is the publication bias). Description length (≤60-word cross-model-density) is a per-skill judgment applied only to an over-broad catch-all (`rigor-ask`, 169 words), never a blanket rule trading trigger recall; `waza dev --auto` stays banned. `rigor skill --full` freezes as public vocabulary at v1.0 ([ADR-50](50-release-engineering-and-stability-strategy.md) WD1). Deferred: dropping vercel-labs distribution of non-entry skills (reverses ADR-73 WD5); delegating `rigor-ci-setup`'s inline templates to `rigor docs ci` (parity-checked refactor). Mechanism detail: the ADR-73 2026-07-05 amendment. Grounding: this session; `CLAUDE.md` § "Evaluating skills with `waza`"; [ADR-74](74-offline-doc-access-and-llms-txt.md) |
| ADR-82 | [`Dynamic[T]` provenance wiring: breaking the catch-all on real apps](82-dynamic-provenance-wiring.md) | **WD1+WD2+WD3+WD6+WD7+WD8 implemented 2026-07-06; WD9 (external-gem-constant provenance → `external_gem_without_rbs`/`add_rbs`) implemented 2026-07-11; the actionability arc is largely spent.** [ADR-75](75-dynamic-provenance.md) added the `Dynamic[T]` provenance side-channel and its `coverage --protection` tractability labels, but a Mastodon field measurement (app+lib, 21,119 unprotected sites) shows the labels are uninformative on a real Rails app: **84% land in the catch-all `unsupported_syntax` (→ `engine_gap`) and 14% carry no cause**, while the two user-actionable causes — `framework_dsl_boundary` and `external_gem_without_rbs` — fire on **zero** sites, so ordinary idioms (`Tag.find_normalized(...).id`, `current_user&.account`, `params[:limit].present?`) all mislabel as intractable. Two engine gaps cause it: **G1** the lookup is receiver-node-local (`ProtectionScanner` reads provenance at the dispatch's immediate receiver node, but `MethodDispatcher` records causes at the call node that *produced* the Dynamic — a local/ivar-read receiver never coincides), and **G2** the specific causes are recorded only under rarely-holding conditions (`framework_dsl_boundary` only when a plugin returns Dynamic, but plugins return concrete types = protected; `external_gem_without_rbs` needs opt-in ADR-10/`pre_eval:`; and `try_discovered_method`/`try_user_class_fallback` record nothing). Criterion = honesty of attribution (record/propagate a cause only where sound; when in doubt keep the generic cause — a wrong "enable a plugin" hint wastes the user's time). WD1 propagate provenance across the receiver-node lookup (follow local/ivar-read receivers to their binding's origin; a flow-varying `Scope` side-association — the hard, deferred, perf-gated slice) · WD2 record a cause at the two unlabeled user-method tiers · WD3 a new `inferred_return_untyped` cause (`engine_gap`, routes to [ADR-67](67-parameter-type-inference.md)/[ADR-58](58-ivar-field-typing.md)) so "engine can't infer this return" stops mislabeling as unsupported syntax · WD4 generic framework-boundary recording deferred to per-plugin follow-up (guessing violates honesty) · WD5 land WD2+WD3 first, measure the re-bucketing, then decide WD1's cost. **WD2+WD3 landed + measured (2026-07-06): re-bucket 211 sites (~1%) into `inferred_return_untyped`, all correctly-attributed chained-receiver holes, ratio unchanged. WD1 then landed (`Scope#local_origins`/`#ivar_origins` name→cause side-tables set at assignment, followed by `ProtectionScanner`): perf-neutral (+0.1% allocs) but modest (~322 sites out of the null bucket) — adjudicating the residual shows the dominant catch-all is intermediate call/chain receivers, not bare variables, redirecting the next lever to WD6 chain-origin inheritance. WD6 then landed too (a `Dynamic`-receiver dispatch records its receiver's effective origin — shared `Inference::OriginLookup` — onto the call it produces, so a cause survives a chain): perf-neutral (+0.03% allocs), and it more than halves the causeless bucket (Mastodon null 2,921→1,356 across WD1+WD6). But most propagated causes are `unsupported_syntax` because the chain roots record it, so WD6 buys provenance *completeness* over tractability *actionability* — the next lever is enriching the roots. WD7 then landed two coupled changes: (a) an exact per-site `cause_site_counts` metric (`tractability_summary` now derives from it) — which **corrected the WD1/WD6 numbers above as a lossy group-dominant artifact**: the true causeless count after WD1+WD2+WD3+WD6 is 10,390 of 21,119 (49%), not 1,356, so provenance completeness was ~51% not ~94%; and (b) the first root-enrichment — seeding an undeclared parameter's provenance to `inferred_return_untyped` at method entry (an untyped param is the archetypal ADR-67 gap), which WD1/WD6 propagate through chains, moving ~3,100 Mastodon holes from causeless to parameter-inference-routed (a genuine actionability gain; causeless 10,390→7,305). WD8 then did the same for unbound ivar reads (`@x.foo` where the field is untracked → ADR-58), −1,900 more. Cumulative WD7+WD8: causeless 10,390 (49%)→5,405 (26%), actionable `inferred_return_untyped` 351→5,399 (15×), ratio unchanged; the residual causeless is `dynamic_top` node kinds (yield/super/block) and class-/global-variable reads (mostly genuinely unmodeled), so the actionability lever is largely spent.** Precision-additive throughout (no type/diagnostic/severity change, ADR-75 side-channel contract); the gate is re-bucketing measurement + attribution adjudication, not a zero-delta corpus. Rejected: provenance on the `Dynamic` carrier (value-equality break, ADR-75 WD1), report-time re-derivation (wrong layer), collapsing to one "unknown" bucket (concedes the signal), a provenance diagnostic (FP-discipline). Grounding: the [2026-07-06 Mastodon coverage/provenance note](../notes/20260706-mastodon-coverage-provenance-and-siggen-rbs-validity.md) + [2026-07-04 onboarding note](../notes/20260704-rails-coverage-onboarding-carrier-trap.md) §O5 |
| ADR-83 | [Dynamic-origin algebra: keep union arms over absorbing into `Dynamic`](83-dynamic-origin-algebra.md) | **Accepted 2026-07-11.** The founding-era dynamic-origin **join** algebra in [`value-lattice.md`](../type-specification/value-lattice.md) (`T \| Dynamic[U] = Dynamic[T \| U]`, absorbing a concrete union arm into `Dynamic`) is **superseded** — `Type::Combinator` never implemented it, and a spike (implement all three identities, measure) found **zero user-visible value**: self-check `lib` diagnostics identical, coverage `precise_ratio` on `lib` identical (0.5632; dynamic_opaque actually *rose*), Mastodon `app/models` diagnostics identical, while adding a `Dynamic`-scan + recursion to the hottest paths (`union`/`intersection`). Criterion: *a value-combination rule earns its place only if it improves precision or protection measurably; a rule that only re-canonicalizes representation, or trades precision for provenance no consumer reads, is not adopted.* The join absorbs concrete arms into `Dynamic` (precision-negative, against the [ADR-82](82-dynamic-provenance-wiring.md)/[ADR-67](67-parameter-type-inference.md) protection arc) — keeping distinct union arms is strictly more precise and carries per-arm provenance. The **meet** rule (`Dynamic[T] & U = Dynamic[T & U]`) is the only precision-positive one, but flow narrowing already does *more* (`narrowing.rb:2317` concretizes a guarded `Dynamic[top]` to `Nominal[String]` = protected); the provenance-preserving form matters only for the demand-gated strict-dynamic discipline ([ADR-75](75-dynamic-provenance.md) WD4) and would regress protection today. Decision: keep the engine's union-preserving + narrowing-concretizing behaviour as normative and revise `value-lattice.md` / `normalization.md` to match. Rejected: implement the algebra (zero value, precision-negative, hot-path cost); mark it "planned" (it is deliberately not-wanted, not pending). Grounding: the [2026-07-11 dynamic-facet spike note](../notes/20260711-dynamic-facet-algebra-spike.md) |
| ADR-84 | [Cross-file return-memo scoping and the taint-precise store gate](84-cross-file-return-memo-scoping.md) | **Accepted — WD1 (profile counters + recording-soundness pin) landed in [PR #79](https://github.com/rigortype/rigor/pull/79); WD2 (run-scoped bucket + dependency cache-and-replay) and WD3 (event-taint store gate) implemented.** Repeated callee-body evaluation is the third-time-surfaced structural cold cost ([ADR-57](57-self-call-return-adoption.md) ~+12% cold; the PR #62 bisect step); PR #79's counters corrected the ADR-57 follow-up memo's believed shape on two points: the bucket keys on the per-file merged `discovered_def_nodes` identity so hits never cross a file boundary (mail's `Mail::Part#has_content_type?` evaluated **564×**), and the blanket unroll-in-flight candidacy exclusion refuses 82% of mail's body evaluations regardless of actual transient influence. Criterion 1 **finality** — an entry may hold only a deterministic pure function of `(def_node, receiver, arg_types)` plus the frozen run-global index; transience is detected *post-hoc by events bracketing the compute*, never by blanket pre-gates over machinery that merely might have interfered. Criterion 2 **observational equivalence** — a hit must be indistinguishable from a fresh evaluation for every load-bearing consumer: the returned type AND the [ADR-46](46-incremental-dependency-graph.md) recorded dependency edges, so cross-file hits REQUIRE entry-attached read-set replay (a naive bypass-the-memo-while-recording measured **>200×** wall on the analyzer-shaped subtree and is the recorded rejected form). WD2 = a run-generation token minted per `Runner#run_analysis` and seeded through `scope.discovery` (per-file `def_nodes`-identity fallback for runner-less probes; a single retained bucket slot so LSP / ADR-62 warm-loop re-runs roll over instead of accumulating; entries key on def-node object identity, held strongly against id reuse — at most 2 node identities per def per run, project-index parse + own-file parse) paired with `DependencyRecorder.capture`/`replay`: an observe-and-forward sub-capture around the first compute stores the callee's UNfiltered read-set on the entry (the per-consumer self-read filter re-applies at replay), nested captures stay transitive, and a hit replays the set into the current consumer. WD3 = candidacy reduces to "plain signature not on the recursion guard stack"; the store gate brackets the compute over a **load-bearing transient-event depth log** (guard hit / unroll-fuel exhaustion / ADR-55 WD1 clamp / fixpoint-cap collapse, each logged at the stack position of the frame whose state it referenced, all routed through the `note_transient_fallback` choke point with a spec-pinned audit table) and refuses only when an event referenced a frame BELOW the bracket's entry depth — ancestor context influenced the result; a compute's own machinery (a recursive method's converged fixpoint at top of stack) stores fine, so recursion-heavy code keeps the memo. Measured: mail body evals 3,355→557 (`has_content_type?` 564→2), rigor lib 9,977→7,339, unroll-in-flight refusals →0 on both; corpus byte-identical (mail / kramdown / mastodon app-models / redmine app-models / rigor lib). WD4 pins two measured-dead designs against re-chase (finalization-aware summary-taint gate — `consult-tainted = 0`; param-read-mask key normalization — distinct keys ≤ 9); WD5 keeps per-worker fork scoping. Rejected: the recording bypass (>200×); key coarsening / eval-cap-then-widen (changes folds → not byte-identical; precision-for-speed belongs to [ADR-41](41-inference-budget-design.md)); per-call-node result caching (re-affirms [ADR-52](52-compiled-plugin-contribution-dispatch.md) WD5 — this memo keys inputs, never nodes). Grounding: the [2026-07-13 corpus perf campaign note](../notes/20260713-corpus-perf-campaign.md) + the PR #79 counter tables |
| ADR-85 | [Per-file seed bundles and lazy def-node handles (pre-pass incrementalization)](85-seed-bundles-and-lazy-def-node-handles.md) | **Accepted — WD1 (incremental plugin-prepare cache threading) landed in [PR #81](https://github.com/rigortype/rigor/pull/81); WD2/WD3 (seed bundles + lazy handles) in [PR #82](https://github.com/rigortype/rigor/pull/82).** Completes the [PR #77](https://github.com/rigortype/rigor/pull/77) warm-arc: a cache HIT already skips the discovery pre-pass, but every `--incremental` recheck is a MISS-path run that parses + walks every project file to rebuild the cross-file index before re-analyzing its 1-file subset — 80–92% of a plugin-light warm incremental (rigor lib / mail). **WD1** first (sequencing-critical): `IncrementalSession#build_runner` built every internal analyzer with `cache_store: nil`, so plugin `cache_for` bypassed the disk cache and each `#prepare` producer recomputed per cross-process recheck — 86% (14.2M allocs) of gitlab's warm incremental; threading a `Cache::Store` through the session (honoring `--no-cache`) lets those producers serve, and the whole-run ADR-45 result cache is excluded from these runs (`run_result_cacheable?` drops `record_dependencies` / `analyze_only` — a cache-served baseline records no dependency graph, a subset run's partial diagnostics share the full key). Measured gitlab `app/models` **16.7M → 2.8M allocs (~6×)**, `#prepare` **14.3M → 0.34M**. **WD2** caches each file's pre-finalize contribution as a Marshal-clean bundle keyed by its digest, hosted on `IncrementalSnapshot` (SCHEMA 5→6; a pre-6 blob loads nil = clean cold rebuild); `ScopeIndexer.discovered_project_index_incremental` folds bundles in canonical file order (re-walking only changed files) then `finalize_def_index` once — the sound reconstruction, since the merged tables are Set-union / later-wins / whole-project-finalize (delta-patch is unsound). **WD3** stores def-node tables as `DefHandle(path, node_id, name, fingerprint)` for *unchanged* files only (cold baseline + re-walked changed files keep LIVE nodes — no on-demand re-parse for a file just walked, a mixed table); the three accessor choke points (`Scope#user_def_for` / `#singleton_def_for` / `#top_level_def_for`) resolve a handle via `DefNodeResolver`, a per-run parse memo yielding **one stable node per `(path, node_id)` per run** (the ADR-84 return memo keys on def-node identity), `node_id` primary + `name` cross-check → fresh-walk-by-name fallback → nil (conservative, never silent). The handle carries `fingerprint` for `Runner#symbol_fingerprints` — a fourth node-value consumer the recon's Q6 audit missed — so ADR-46 change-detection reads it without re-parsing. Criterion (ADR-54, applied to ADD a tier): load-and-fold must measurably beat recompute (36×–570× wall) and def-node materialization follows demand (0–6 files/recheck) — eager AST Marshal rejected (forfeits the parse-alloc win), per-file `Cache::Store` producers rejected (N fs-ops per rebuild vs one snapshot read). Measured plugin-light warm incremental: rigor lib **540k → 149k** allocs (pre-pass 436k → 25k), mail **529k → 54k** (485k → 5k); gitlab combined-stack **16.7M → 2.06M (~8×)**; cold diagnostics byte-identical on mail / kramdown / mastodon app-models / redmine app-models / rigor lib, `--verify-incremental` green. Divergences from the recon recorded (the `classes` key-list assumption is false for `Class.new(Super)`; the missed `symbol_fingerprints` consumer). Grounding: the [2026-07-14 P6 seed-cache recon](../notes/20260714-p6-seed-cache-recon.md) |

| ADR-86 | [Partial native extensions for residual hot paths (rejected; rigor-rs owns native speed)](86-partial-native-extensions.md) | **Accepted, 2026-07-14 — a standing rejection.** Evaluated against the v0.3.0 campaign's closing profile: the classically native-able hot paths are ALREADY native (rbs C parser, prism, psych, digest/Marshal/zlib — their residual cost is Ruby-side materialization), and the two pure-Ruby residuals both fail the three-part criterion (a candidate must be a representation-complete kernel with no per-element Ruby-object FFI traffic, not already winnable via YJIT/caching/parallelism, and worth the ADR-27/29/31/15 platform taxes): the flat inference engine has no extractable kernel (porting it = rigor-rs by a worse route), and the equality/union kernel (2–5% of residual allocations, kramdown CPU 15.8% self) is representation-bound — a native win requires the interning redesign already triple-rejected on measured evidence. Division of labor made explicit: native-speed demand routes to the rigor-rs sibling port (ADR-79's recorded divergence); the Ruby rigor keeps its ecosystem/interop/wasm advantages. One demand-gated non-native candidate staged cheapest-first (WD4): the monorepo glob+digest warm floor — Ruby thread pool → FS-event/daemon → only then a native batch hasher. Go rejected specifically (cgo in-process fit). Re-eval triggers: a ≥15% corpus-wide pure-Ruby frame satisfying criterion 1, the equality bucket growing past ~15%, rigor-rs abandonment, or the WD4 ladder exhausting its non-native rungs. |

| ADR-87 | [The null-build floor: stat-then-digest validation, zero-change snapshot skip, hit-path boot slimming](87-null-build-floor.md) | **Accepted — WD1–WD5 implemented ([PR #85](https://github.com/rigortype/rigor/pull/85)).** Frames the warm paths in build-system terms (the NULL build = zero files changed, and the single-edit build) and removes the three measured floor components that are not intrinsic. **WD1** — a `:stat` `FileEntry` comparator packing `digest + (size, mtime_ns, ctime_ns, inode) + recording-instant`: validation stats first and rehashes only when the tuple moved or the racy guard fires, so a true null hashes **0 content bytes** while the SHA-256 digest stays the sole change authority (git-index model; `ctime` + inode + racy-window; escape hatch `cache.validation: digest` / `RIGOR_STRICT_VALIDATION=1`). `:stat` rides validation-only descriptors; KEY descriptors keep `:digest`. `SCHEMA_VERSION` 4→5. **WD2** — `GlobEntry`'s aggregate signature becomes a SHA-256 over per-file STAT tuples instead of content digests (0 content bytes on an unchanged watched tree); the draft's per-file table is **rejected** (a 20k-file table Marshal-regressed gitlab incremental 1.85s→5.3s — a watched dependency is all-or-nothing, so partial re-hash bought nothing but a heavy table). **WD3** — `run_incremental` skips the unconditional `snapshot.save` on a zero-change recheck. **WD4** — a lightweight `RunCacheProbe` serves an ordinary `check` HIT before the engine loads: it builds the ADR-45 key through the shared `RunCacheKey` (config-derived libraries + `RBS::VERSION`, no env build) and `Store#peek_validated`s it, so a HIT's `$LOADED_FEATURES` has **no `rigor/inference`** (pinned by a subprocess spec + MISS control); required decoupling the rule-id constants to a light `check_rules/rule_ids.rb` and lazy-requiring `coverage_scan`/`check_runner_factory`. **WD5** — the staleness battery + touch-stays-a-HIT end-to-end guard. Measured (host): gitlab null 1.41s→**0.34s** (115 MB→0 hashed), gitlab null-incremental 1.85s→**1.44s**, small-project null ~0.36s→**~0.22s**; gitlab 1-edit is honestly **neutral** (closure-bound, not glob-bound — the recon's glob-hash attribution was corrected). Supersedes [ADR-54](54-cache-slimming.md)'s rejected mtime fast-path (keeps digest authority). Grounding: the [2026-07-14 null-build recon](../notes/20260714-nullbuild-recon.md) + the [2026-07-13 corpus perf campaign note](../notes/20260713-corpus-perf-campaign.md) |

| ADR-88 | [Incremental plugin-fact soundness](88-incremental-plugin-fact-soundness.md) | **Accepted — WD1–WD4 implemented ([PR #89](https://github.com/rigortype/rigor/pull/89)); WD5 deferred.** The `--incremental` snapshot's global fingerprint (config / gems / RBS env / `signature_paths:`) does not capture a plugin's value computed from files those inputs miss — a Sorbet catalog from an `.rbi` tree under `rbi_paths:`, an AR `model_index` from `db/schema.rb`. Editing such a file moves the types unchanged call sites resolve WITHOUT touching any analyzed file, so the recheck serves a stale cached diagnostic (the plugin read is internal, not a `Scope` choke point, so no ADR-46 edge). **WD1** — `Analysis::PluginFactFingerprint` digests the plugin **fact surface** ((a) ADR-9 `fact_store` publications, (b) ADR-60 producer VALUES, (c) an optional `incremental_state_fingerprint` hook), rides the snapshot (`SCHEMA` 8→9, pre-9 loads nil), and invalidates it on a mismatch (surfaced in the banner + `--cache-stats`). Digests the producer **value** not its cache blob (the blob carries the input descriptor → over-invalidates on a value-preserving edit: a measured gitlab controller-edit → 23s full vs ~9.5s incremental), and is read POST-HOC from the analysis runner's prepared registry (reusing the recheck's prepare + memoised producers → ~0.24s ≈ 2.5%, vs a ~1.0s separate probe kept only as the pooled-mode fallback; both paths agree, so the decision is pool-independent). A contributing plugin with NO surface is **opaque** — named + snapshot un-reusable every run (forcing function); the bundled contributing set is all non-opaque (producers, or a per-file/static hook sentinel backstopped by `--verify-incremental`). **WD2** — the Sorbet catalog becomes an ADR-60 `producer :catalog` (`watch:` the sig trees, Marshal-clean `{catalog, sigil_by_path, parse_errors}` bundle; `Dir.glob` sorts by default so the last-sig-wins fold is deterministic), disk-cached + WD1-digested for free. **WD3** — `Scope#user_def_site_for` records the cross-file method edge its sibling `#user_def_for` records, so a def-site line-shift re-checks the `call.undefined-method` consumer. **WD4a** — a fabricated-edit battery (Sorbet `.rbi` sig edit invalidates + re-analyzes the consumer, red-before/green-after; dry-types alias + AR schema-column digest moves; WD3 line-shift; opacity; pooled/sequential parity). **WD4b** — the pre-existing gitlab `--verify-incremental` red (1,161 incremental-only, all `:info` Action Pack trace) root-caused NOT to a producer but to a **plugin-loader idempotency bug**: the verify harness runs baseline→subset→full in one process, and the `full` oracle is a SECOND in-process `Plugin::Loader.load` whose `require` no-ops → empty newly-registered delta → a bare-string entry hits "did not register any plugin" → the oracle loaded ZERO plugins (101 vs 2,494). Fixed at the loader (`Plugin.record_gem_registration` / `ids_for_gem` recover the gem's ids on a re-load); after the fix gitlab verify is byte-identical (887/1,774, 2,494, **0 mismatch**). **WD5** (per-consumer plugin-read tracking) deferred — whole-snapshot invalidation is sound and, with the value-digest precision + producer determinism, does not over-invalidate on the common value-preserving edit. Precision-additive (no type/diagnostic/severity change; cold diagnostics byte-identical). Grounding: the [2026-07-14 incremental-plugin-fact audit](../notes/20260714-incremental-plugin-fact-audit.md) |
| ADR-89 | [Semantic propagation gates: declaration-shape and observed-key return summaries](89-semantic-propagation-gates.md) | **Accepted — WD1 (declaration-shape gate) + WD2 (observed-key return-summary gate) implemented ([PR #90](https://github.com/rigortype/rigor/pull/90)).** Extends PR #88's B1 comment-only gate to BODY edits: a dependent is re-analyzed only when something it can consume actually changed (sound only on top of ADR-88's plugin-fact value fingerprints). Two over-propagations remain after #87/#88: **(a)** any body edit re-analyzes ALL ancestry/file-level dependents (the recon's S5a — a 13-caller `def self.safe_find_or_create_by` body edit → 341 files, which consume only the class's *declaration shape*, unchanged by a body edit); **(b)** a body edit whose returns are unchanged at every observed call shape still re-analyzes all callers. Criterion: a dependent D of edited F is re-analyzed iff (what D can consume from F) × (what changed) is non-empty, proven by comparing PERSISTED summaries each covering a COMPLETE consumable surface. **WD1** — the {ScopeIndexer.declaration_signature}: the ADR-85 seed bundle with per-def CODE fingerprints replaced by per-def SIGNATURE shape (name, kind, full parameter structure, visibility, ancestry, member layouts, method existence, AND def start LINE), computed from a single-file live index (parameter structure straight from the def node — deliberately syntactic). A declaration-stable changed file drops out of `unstable`, so its ancestry/file-level dependents are skipped; this generalises B1 (code-stable ⟹ declaration-stable) so the gate switched off the comment-stripped code fingerprint (`SCHEMA` 9→10). Def LINES live IN the signature (**divergence from the draft's per-site precision** — the ADR-88 WD3 symbol fingerprint is line-INVARIANT, so only the file-level edge covers the ADR-17 `project_definition_site` consumer): a line-shifting body edit is conservatively declaration-unstable (keeps the full dependent set, = B1's behaviour), only a SAME-LINE body edit collapses. **WD2** — per-def observed-key return summaries (the ADR-84 memo's `(receiver, args) → return` entries, bounded 8 keys/def · 4000 defs, `describe(:short)` descriptors + the ADR-56 content-mutation effect set); `MemoEntry` gained the call descriptor so a summary carries the actual key types, harvested via `ExpressionTyper.harvest_return_memo` + `Runner#return_summaries`. On recheck a declaration-stable changed def with an unchanged signature is re-evaluated at each old key (`Scope#user_method_return` → the memo, final values only) through a session-side probe (`Runner#evaluate_return_types` — builds discovery + env once, no file analysis, run ONLY when a stable pair carries a summary so a comment edit pays zero WD2 cost); all returns + the effects set equal → drop the def's symbol dependents, else keep (conservative). The return-drop is gated by `gate_eligible_def?` (**divergence — soundness completeness**: a caller also consumes a callee's ivar definite-assignment + `yield` values, so the drop fires only for a def that writes no ivar/cvar, does not yield, and makes no implicit-self call — a conservative sufficient condition under which return + content-mutation ARE the complete cross-file body surface; the general all-surfaces gate is deferred). **WD3** — the gates apply only when ADR-88's plugin-fact fingerprints matched, asserted-not-assumed (the gated recheck is trusted only inside `run_incremental`'s `if reuse` branch; a mismatch/opaque plugin discards it), plus the retained comment-ingesting off-switch. **WD4** — a fabricated battery (arity/visibility/added-method/return-visible → propagate; same-line/return-preserving → skip; mutation-effect change/ineligible def → keep; line-shift → site consumer re-checks), each byte-identical-to-full. Measured: gitlab `safe_find_or_create_by` return-preserving edit **341 → 1** (WD1 drops all 340 ancestry deps; 0 in-scope symbol callers), `label.rb reference_prefix` literal change **19 → 1**, both byte-identical; `--verify-incremental` byte-identical (887/1,774, 2,494, 0 mismatch); cold mail 26 / kramdown 68 byte-identical; S1 recheck wall within noise of master. Measurement divergence: the gitlab S5a closure is **1** not the recon-estimated ~13–14 (`safe_find_or_create_by` has 0 recorded in-scope symbol callers), so WD1 is the gitlab headline and WD2's return-drop is proven by the fabricated battery. Grounding: [`20260714-edit-shape-recon.md`](../notes/20260714-edit-shape-recon.md) |
| ADR-90 | [Target-library resolution from the analyzed project's bundle](90-target-library-resolution-from-project-bundle.md) | **Accepted — implemented 2026-07-16.** Closes ADR-39's availability gap under ADR-27's standalone distribution: the bundled Rails plugins inflect through the real `ActiveSupport::Inflector` but rigortype carries activesupport only as a dev dependency (the bundled plugins have no gemspec of their own), so every standalone `gem install rigortype` silently degraded all inflection-dependent Rails checking — masked in maintainer environments by the repo's dev bundle. Criterion: a target library is resolved with fidelity to what is on disk, and absence is a clean, visible decline — never a guess, never a crash. WD1: `Isolation.require_with_target_bundle` retries a failed require after appending the analyzed project's bundle gems' `full_require_paths` (from `specifications/*.gemspec`, newest per name) to `$LOAD_PATH` — append-only so Rigor's activated gems keep precedence, child-confined under the default `process` strategy (the root rides each worker request); `Gem.paths` augmentation rejected (Bundler-locked processes silently ignore it), `Bundler.setup` rejected (executes the app's Gemfile, ADR-2), runtime-dep promotion rejected (forces Rails machinery on non-Rails users + pins rules away from the project's own version — ADR-79 fidelity). WD2: the isolation layer's rescues are `::`-qualified — `Rigor::Plugin::LoadError` lexically shadows the global `LoadError`, so the worker's bare `rescue StandardError, LoadError` let the real `::LoadError` (a ScriptError) kill the forked worker and surface as an opaque `worker failed (EOFError)`; same fix in rigor-rspec-rails's Rack table loader, `Plugin::Loader`'s bare rescues are intended shadowing and stay. WD3: `rigor plugins` probes `Inflector.available?` when a consumer plugin (`Inflector::CONSUMER_PLUGIN_IDS`) is loaded — text warning with the concrete fix, structured JSON `inflection: {required_by:, available:}` (ADR-61). WD4: the bundle root is set in `ProjectPrePasses#run` BEFORE plugin `#prepare` (rails-routes parses routes at prepare time) with the `Environment.for_project` assignment covering pool workers. |
| ADR-91 | [Kernel intrinsic fold ownership gate + spelling-parity invariant](91-kernel-intrinsic-fold-ownership-gate.md) | **Accepted — implemented 2026-07-16 (WD1–WD4; corpus gate byte-identical on mail/kramdown/haml/mastodon-models, parity spec 34 examples).** Closes the bug class PR #110 fixed one instance of (rigor-rs feedback item 1: `Kernel.p` declined while `Kernel.format` folded): the Kernel module-function ownership check is per-fold and opt-in (`Array`/`Integer`/`Float`/`Rational`/`Complex` are still ungated — `conv.Integer("42")` against a user `def Integer` is hijacked today), and the surface has two owners (`KernelDispatch` + `LiteralStringFolding`'s `fold_format`). Criterion = ADR-52/53 gate-by-held-key applied to a built-in tier: a Kernel module-function fold runs only behind a single dispatcher-held gate (method name ∈ a compiled `INTRINSIC_NAMES` table ∧ receiver nil/self/`Singleton("Kernel")` ∧ no user redefinition), the surface is data, and implicit-self vs explicit-`Kernel.` spelling parity is a table-driven spec derived from that same data — so both failure modes (fold forgets the guard / one spelling regresses) become unrepresentable or spec-caught. WD1 hoist the gate into `dispatch_precise_tiers`, delete the per-fold guards; WD2 move `fold_format`/`sprintf` into `KernelDispatch` (one owner; `String#%` stays); WD3 the parity spec iterates `INTRINSIC_NAMES`; WD4 gate = corpus adjudication (expected diffs only foreign-receiver hijack removals, strictly FP-reducing — `Kernel#Integer` is private, an explicit non-Kernel receiver is necessarily a user method). Rejected: spelling normalization pre-dispatch (RBS-resolution blast radius), keep-per-fold-guards (recurrence guaranteed), rely-on-rigor-rs-differential (external, not a CI gate; deferred as complement). |
| ADR-92 | [Normative status fidelity: the founding-era stratum and the declare-or-mark gate](92-normative-status-fidelity.md) | **Accepted, 2026-07-16 (WD2 verdicts + WD3 markers + WD4 gate in this change set).** `CLAUDE.md` promises the spec binds, and rigor-rs (ADR-79/ADR-91) is the reader that cannot fall back on `lib/` — for the port a normative clause *is* the requirement. A 2026-07-16 investigation found three independent places where the corpus states unshipped behaviour in the present tense, none recorded as deferred anywhere: (1) `special-types.md` § `void` — two MUSTs ("keeps `void` distinct internally", value-context diagnostic, generic-slot preservation) against `rbs_type_translator.rb:51`'s `Bases::Void => :translate_untyped` (no carrier, no rule id, zero specs); (2) `diagnostic-policy.md`'s 12-family taxonomy — `static.*` / `compat.*` / `hint.*` / `generated.*` have zero implemented ids; (3) `internal-type-api.md`'s "contract every type object MUST satisfy" — `normalize` / `traverse` / `consistent_with` / `equal_value` / `has_method` / `subtype_of` absent from all 23 carriers (`Type::Nominal` exposes only `initialize` / `describe` / `erase_to_rbs` / `inspect`). All three are founding-era (ADR-1/2/3) design targets presented as binding contract; ADR-1 even foresaw #1 ("`void` and `untyped` are likely to be treated as broad aliases too early") and it happened unobserved. No gate caught them because every `manual_drift_spec.rb` docs axis runs **impl → doc** (the reverse is unchecked) and every analysis gate is FP-oriented, so a never-implemented diagnostic is *silence* — invisible by construction (ADR-62 kinship: mutation testing needs code to break; an absent feature has none). Criterion: **a normative clause states behaviour in the present tense only if it ships; otherwise it carries an explicit status marker** — the force is the corollary that *silence is never the honest state*, since marking is always available and always cheap, so the cost of implementing or narrowing never justifies a clause that lies. This is ADR-49 axis 6 (*Status & progress fidelity*) applied to the spec corpus instead of the ADR corpus; the norm is already demonstrated twice (`inference-budgets.md:75`'s "not yet wired", `diagnostic-policy.md:43`'s JSON-only `sig.*`) — only enforcement was missing. WD1 scopes the gate to *enumerable declaration tables*, never the ~836 prose clauses (id-ing those manufactures `reference`-grade false assurance — the rejected dspec design). WD2 verdicts: `void` → MARK now, implement-vs-narrow carried over and measurement-gated (implementing is BC under ADR-50 WD1 → `bleeding_edge:`; narrowing reverses ADR-1:30); the four empty families → MARK as reserved-not-implemented (`static.*`'s budget half already has ADR-41 + its marker); `internal-type-api.md` → NARROW to the shipped routing (the capabilities exist via `subtype_verdict` / `acceptance_router.rb`, just not as a carrier surface; the doc's own § Scope carve-out excuses *spelling*, not relocating a surface) and MARK `normalize` / `traverse` (absent outright). WD3 marker shape follows the two precedents (inline, no new schema). WD4 the gate = `manual_drift_spec.rb` gains the doc → impl axis: every declared family has ≥1 implemented id or a marker, read against the **full 39-id vocabulary** (`ALL_RULES`'s 26 plus the 13 non-check ids admitted per-family by `known_suppression_token?`) or it reports false gaps. WD5 adjudicate before gating (a gate landing red on five pre-existing divergences invites a blanket skip). Rejected: whole-corpus clause registry / leave-it (the port is the least able to detect the lie) / implement-all-now (status fidelity must not be hostage to a design decision) / delete the clauses (discards still-wanted intent) / a per-clause `status:` schema (disproportionate). |

## Adding a New ADR

When making a significant architectural decision:

1. Find the next available number in this directory.
2. Copy the template from an existing ADR or create a new file following the same structure: Status, Context, Decisions, Consequences.
3. Add an entry to the index table above.
4. Reference the ADR from relevant code comments, other ADRs, or `AGENTS.md` where appropriate.

## Relationship to Other Documents

- **`docs/types.md`** — Type specification quick guide. When ADR-1 and `docs/types.md` discuss the same area, `docs/types.md` is authoritative for *what the analyzer does*; ADR-1 is authoritative for *why*.
- **`docs/type-specification/`** — Normative type specification, split into topical documents.
- **`docs/internal-spec/`** — Analyzer-internal contracts (engine surface, type-object public API).
- **`docs/handbook/`** — End-user handbook, written for Ruby programmers without prior static-typing background.
- **`AGENTS.md`** — Development contract for agents working in this repository.
