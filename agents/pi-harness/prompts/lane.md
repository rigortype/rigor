---
description: Bind lane role — load rigor-lane skill, push, print Head SHA, stop
argument-hint: "[LaneInput / issue / branch]"
---
You are running as **lane** (DeepSeek Flash-class; stay on this band).

1. Load and follow skill `/skill:rigor-lane`.
2. Consume the architect LaneInput: ${@:-see prior architect output / user paste}.
3. Implement under the contract, push, print head SHA, and **stop** (no CI watch).
4. End with exactly `Head SHA <sha> — lane done` or `Blocked — need human`.
