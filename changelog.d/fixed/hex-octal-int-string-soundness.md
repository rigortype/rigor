- **[inference]** `Integer#to_s(8)` / `#to_s(16)` on a bounded non-negative `Integer` range and a bare hex/octal-digit regex capture (`\h+`, `[0-9a-fA-F]+`, `[0-7]+`, and their case variants) no longer type as `octal-int-string` / `hex-int-string` — refinements whose predicate requires the `0o` / `0x` prefix that neither producer's output ever carries (`64.to_s(8)` is `"100"`, `255.to_s(16)` is `"ff"`) — and now type as `non-empty-string` instead. [#1013](https://github.com/rigortype/rigor/pull/1013)
