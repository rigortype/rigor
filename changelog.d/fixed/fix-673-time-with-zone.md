- **[activesupport]** `ActiveSupport::TimeWithZone` is modelled as a subclass of `Time`, so the readers only a zone-aware time has — `time_zone`, `time`, `period`, `comparable_time` — resolve on `Time.current` and on the whole `1.hour.ago` family instead of reporting `undefined method`, while every `Time` method and `Time`-typed parameter downstream keeps working exactly as before ([#916](https://github.com/rigortype/rigor/pull/916))
