- **[sig]** The collection side of `Effects::*` is declared under `sig/` — `Summary`, `EffectTable`, `FileCollection`, `PluginFacts` and their `Data` members — unblocking `Runner#effect_table`, `#effect_collection`, `#effect_plugin_facts` and `#effect_collections_by_path` and declaring `effect_ancestry`, `forced_file_effects`, `adopt_effect_collections`, `adopt_effect_summary` and `effects_served_from_cache?` for the first time; `EnvelopeIndex.build`'s `plugin_facts:` tightens to `PluginFacts?`. Sixth slice of the #1181 Class B backlog. [#1189](https://github.com/rigortype/rigor/pull/1189)
