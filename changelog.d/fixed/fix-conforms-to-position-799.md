- **[rigor check]** An unsatisfied `%a{rigor:v1:conforms-to}` directive is now reported at the line it is written on whether or not the RBS environment came from the cache, so `--verify-incremental` no longer fails and a warm `--incremental` run no longer moves the row on an unchanged tree. ([#799](https://github.com/rigortype/rigor/issues/799), [#804](https://github.com/rigortype/rigor/pull/804))
