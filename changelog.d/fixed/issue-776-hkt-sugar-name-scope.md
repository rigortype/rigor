- **[engine]** `rigor check` no longer fails every file with `internal analyzer error: ArgumentError: unknown keyword: :name_scope` (or `args must be non-empty`) on a project that has an `rbs collection` installed; a recursive `type` alias whose body contains a tuple, record, interface, or unbound variable is now registered, and a malformed self-referential alias is declined, instead of either aborting the shared HKT-registry build. ([#776](https://github.com/rigortype/rigor/issues/776), [#786](https://github.com/rigortype/rigor/issues/786), [#783](https://github.com/rigortype/rigor/pull/783))
