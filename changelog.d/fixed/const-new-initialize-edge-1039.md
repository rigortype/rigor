- **[effects]** A `Const.new` call now resolves to that class's `#initialize`, so a constructor's labels reach its callers' effect closures and no longer withhold `%a{pure}` from them; a project `def self.new` still wins, `Class.new` is untouched, and a class with no constructor anywhere in its project ancestry leaves its callers neither labelled nor unclaimed. [#1042](https://github.com/rigortype/rigor/pull/1042)
