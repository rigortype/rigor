- **[incremental]** A warm `--incremental` recheck that changed nothing now reports the same `rbs.coverage.definition-build-failed` and `rbs.coverage.hkt-scan-failed` rows the cold run did, and a project without `signature_paths:` pays no RBS environment load on such a recheck. ([#978](https://github.com/rigortype/rigor/pull/978))
