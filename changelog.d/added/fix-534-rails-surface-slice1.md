- **[plugins]** The Rails plugins now declare their own framework constants, so `rescue ActiveRecord::RecordNotFound => e` types `e` instead of leaving it opaque — the Active Record and Action Controller exception hierarchies, the `ActiveModel` and `Arel` namespaces, and `ActiveSupport::Concern`, each declared leniently so no member they omit becomes a false positive. ([#974](https://github.com/rigortype/rigor/pull/974))
