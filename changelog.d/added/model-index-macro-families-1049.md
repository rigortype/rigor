- **[plugins]** rigor-activerecord now folds `delegate` (including the `prefix:` spellings), the associations a concern declares in its `included do ... end`, the Paperclip and Active Storage attachment macros and an `enum`'s value predicates into each model's recorded surface and into the published `:model_index` fact, so a consumer such as rigor-active-model-serializers stops declining a model that really does answer `followers_count`, `avatar` or `limited_visibility?`. [#1052](https://github.com/rigortype/rigor/pull/1052)
