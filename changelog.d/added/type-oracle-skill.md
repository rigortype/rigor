- **[skills]** New `rigor-type-oracle` skill — before an AI agent writes a type anywhere (RBS, an inline `#:` / `# @rbs` annotation, a Sorbet `sig`, a YARD tag, a doc sentence, a review comment), it obtains it from `rigor type-of` / `annotate` / `sig-gen` instead of guessing from the source, reports gaps rather than filling them, and ships the paragraph to keep in your `AGENTS.md` so the rule holds every session. ([#826](https://github.com/rigortype/rigor/pull/826))
