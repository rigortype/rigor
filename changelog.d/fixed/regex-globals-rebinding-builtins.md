- **[engine]** A lookup between a `=~` match and a read of `$1` whose arguments are all literals (`val = row[:name]`, `csv.split(",")`, `list.index(3)`), and `s.match?(re)`, no longer drop the match's narrowing, so the read no longer reports a false `call.possible-nil-receiver`, while `u !~ re`, `u.start_with?(/re/)`, `u[/re/] = v`, `u.send(:=~, re)`, an eval of matching code and a matching call inside an argument, receiver chain or array, hash or string literal now drop it as Ruby does. ([#1378](https://github.com/rigortype/rigor/pull/1378))
