- **[engine]** The opt-in Ractor worker pool (`RIGOR_POOL_BACKEND=ractor`) no longer stops a worker with `Ractor::IsolationError` when it reads a frozen but not deeply shareable constant, and no longer switches off `Gemfile.lock` dependency discovery in each worker with a warning that wrongly called the lockfile malformed: the coordinator now reads the lockfile and passes the result to every worker. [#1068](https://github.com/rigortype/rigor/pull/1068)
