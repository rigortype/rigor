- **[plugins/rigor-activerecord]** `user.posts.delete_all(:nullify)` and `delete_all(:delete_all)` on a `has_many` reader no longer report a false `call.wrong-arity`: the reader returns a `CollectionProxy`, whose `delete_all` takes an optional `dependent` argument, and the bundled `ActiveRecord::Relation` signature now accepts it. ([#1312](https://github.com/rigortype/rigor/pull/1312))
