- **[engine]** The opt-in Ractor worker pool (`RIGOR_POOL_BACKEND=ractor`) no longer loses every worker in its constructor to a class-level `@memo ||= …` — a class-ivar write a non-main Ractor may not perform — which degraded every run on every project to in-process re-analysis; what the backend still cannot do is inventoried in [#1064](https://github.com/rigortype/rigor/issues/1064). [#1061](https://github.com/rigortype/rigor/pull/1061)
