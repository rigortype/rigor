- **[inference]** `Integer#to_s` / `#inspect` type as `decimal-int-string` and `Float#to_s` / `#inspect` type as `numeric-string` when the receiver carries a finiteness proof (`finite-float`, a bounded `Float[...]` range, or `f.finite?`'s truthy edge) and `non-empty-string` otherwise, instead of the bare `String` both used to widen to ([#1001](https://github.com/rigortype/rigor/pull/1001)).
