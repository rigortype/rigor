- **[plugin-api]** `Plugin::Base#declared_members(class_name)` lets a plugin enumerate the members it synthesizes on a class — `{name:, kind:, type:}` rows for the `rigor lens` declaration map (ADR-113 WD4) — and rigor-activerecord now answers it with columns, associations, scopes, enums, and macro-defined names off its prepared model index. ([#1088](https://github.com/rigortype/rigor/pull/1088))
