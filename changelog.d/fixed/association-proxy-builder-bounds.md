- **[effects]** rigor-activerecord no longer claims that `blog.posts.build` / `.new` is pure: an association reader is typed as `ActiveRecord::Relation[Model]` but returns a `CollectionProxy`, whose builders, `create` family, `reset`, `reload`, `delete_all`, `destroy_all` and bulk inserts change the association's target, so those Relation bounds now carry `mutate.self`, and the proxy-only writers (`<<`, `concat`, `replace`, `delete`, `clear`, …) are coloured `io.db` + `mutate.self` instead of reading as trivial; `rigor sig-gen` stops writing `%a{pure}` on a method that builds into a held association. [#1309](https://github.com/rigortype/rigor/pull/1309)
