- **[inference]** A constant compound write — `H ||= x`, `H &&= x`, `H += x` and their `Conf::H` path forms — is now typed as the value it stores rather than its right-hand side alone, so `H = { x: 1 }; v = (H ||= 0); v[:x]` no longer reports `Integer#[]` on correct code; a constant nothing binds keeps the memoizing `||=` reading. ([#1243](https://github.com/rigortype/rigor/pull/1243))
