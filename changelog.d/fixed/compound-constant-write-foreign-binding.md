- **[inference]** A constant compound write (`H ||= x`, `H &&= x` and their `Conf::H` path forms) whose constant another project file writes now stores `Dynamic[top] | x` instead of `x` alone, so `w = (H2 ||= 0); w[:x]` beside a sibling file's `H2 = { x: 1 }` no longer reports `Integer#[]`; a constant nothing else writes keeps the memoizing `||=` reading. ([#1276](https://github.com/rigortype/rigor/pull/1276))
