- **[inference]** A call whose earlier RBS overload takes a project module the argument's class includes only in the Ruby source (`class Integer; include Printable; end` with `def show: (Printable) -> String | (Integer) -> Integer` in `sig/`) no longer types with the later overload and reports `call.undefined-method` on its result. ([#1353](https://github.com/rigortype/rigor/pull/1353))
