- **[sig-gen]** A generated declaration now names the superclass Ruby resolves: sig-gen flattens a nested `class Sub < Base` into a compact header, where RBS resolves `Base` at the top level, so a superclass reached through the declaration's nesting used to be emitted as a different class than the one the checker resolves. ([#722](https://github.com/rigortype/rigor/issues/722), [#738](https://github.com/rigortype/rigor/pull/738))
