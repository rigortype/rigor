- **[rigor check]** A `--workers N` run that falls back to sequential analysis (no `fork`, or `RIGOR_POOL_BACKEND=ractor` with `--no-cache`) now reports the same synthesized-namespace, `rigor:v1:conforms-to` conformance, and definition-build-failed rows as a plain sequential run over the same project, instead of silently dropping them. ([#1201](https://github.com/rigortype/rigor/pull/1201))
