- **[check]** A method whose `RBS::Extended` return is `non-empty-array[T]`, `non-empty-hash[K, V]` or `non-zero-int` no longer reports `def.return-type-mismatch` on a body that proves the empty value absent — a non-empty array literal, a hash literal with a key, or an `Integer` range that skips `0` such as `xs.size + 1` — while `[]`, `{}` and a range covering `0` still fire. ([#1255](https://github.com/rigortype/rigor/pull/1255))
