- **[rigor coverage]** `--protection --mutation` no longer reports mutants as survivors when Rigor's own analysis crashed while measuring them: an indeterminate run is counted in the `harness_errors` bucket the command already warns about, instead of inflating the survivor count with breakages that were never actually measured ([#686](https://github.com/rigortype/rigor/issues/686)).
