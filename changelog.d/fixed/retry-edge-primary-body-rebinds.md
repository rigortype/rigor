- **[inference]** A `begin` whose rescue arm retries now widens the locals and instance variables the primary body rebinds before raising, not only those the arm rebinds, so the retry-counter idiom `tries += 1; raise "boom" if tries < 3` inside `begin … rescue; retry; end` no longer reports `flow.always-truthy-condition`, and neither does a guard on the counter inside the retrying arm. ([#1259](https://github.com/rigortype/rigor/pull/1259))
