- **[engine]** `call.argument-type-mismatch` no longer fires against a parameter whose declared class carries no signature anywhere — a `sig/` naming `ActiveSupport::TimeWithZone` in a project that never loads ActiveSupport's RBS reported every call as a mismatch, when nothing about that class was known well enough to refute any argument ([#914](https://github.com/rigortype/rigor/pull/914))
