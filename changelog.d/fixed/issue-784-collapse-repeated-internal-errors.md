- **[engine]** When an internal error breaks analysis on every file (a shared sub-build that raised), `rigor check` now reports one run-level `analyzer.internal-error` summary — the count, the file total, and a sample location — instead of one identical `internal analyzer error` row per file with nothing saying the analyzer never ran. ([#784](https://github.com/rigortype/rigor/issues/784), [#787](https://github.com/rigortype/rigor/pull/787))
