- **[plugins]** rigor-rails-i18n's view-template diagnostics are now reported exactly once per run at their own view file and line under `--workers N`, instead of once per worker, through a new `Plugin::Base#emit_once(key, diagnostics)` channel that lets any plugin emit a positioned project-wide batch once per run; unlike the #1054 and #1056 disclosure moves this is baseline-neutral, because the rows keep their file and qualified rule, so existing baseline entries still match and no `rigor baseline regenerate` is needed. ([#1067](https://github.com/rigortype/rigor/pull/1067))
