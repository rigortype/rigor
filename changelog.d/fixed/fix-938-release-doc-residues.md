- **[skills]** `rigor skill describe` now recommends `rigor-ci-setup` ahead of `rigor-rbs-setup` on a configured project with CI unwired, since CI wiring is free and local while `rbs collection install` is network-bound, and the gem's offline `docs/llms.txt` index now lists every packaged manual chapter (it had silently fallen two chapters behind), gated so it cannot drift again. ([#956](https://github.com/rigortype/rigor/pull/956))
