- **[types]** A class method defined on a project base class now resolves on its subclasses instead of typing `Dynamic[top]` — every inherited factory, registry and `class << self` helper in a project hierarchy gains its return type (+62 precise sites on a redmine run, with diagnostics unchanged). ([#731](https://github.com/rigortype/rigor/issues/731), [#748](https://github.com/rigortype/rigor/pull/748))
