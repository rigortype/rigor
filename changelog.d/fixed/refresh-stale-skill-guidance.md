- **[skills]** The shipped agent skills no longer give commands that fail or claims that are out of date: `rigor-upgrade` no longer runs a bare `rigor diff`, `rigor-doctor` uses `rigor doctor`, `rigor-plugin-author` pins the current `rigortype` minor rather than `< 0.2.0`, and `rigor-ci-setup` takes its templates from `rigor docs ci`. ([#1222](https://github.com/rigortype/rigor/pull/1222))
