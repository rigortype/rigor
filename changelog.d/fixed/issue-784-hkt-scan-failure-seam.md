- **[engine]** When the implicit HKT scan over RBS `type` aliases raises, `rigor check` now reports it once per run as `rbs.coverage.hkt-scan-failed` and keeps analysing over the bundled and plugin HKT registrations, instead of every file failing with an identical `internal analyzer error` and nothing saying the analyzer never ran. ([#784](https://github.com/rigortype/rigor/issues/784), [#788](https://github.com/rigortype/rigor/pull/788))
