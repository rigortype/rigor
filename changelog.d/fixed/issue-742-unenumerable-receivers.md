- **[check]** `call.undefined-method` no longer fires for a method a project base class defines when the subclass is declared in the project's own `sig/`, nor on a receiver typed `Class` or `Module` — a value typed as a metaclass is some class object whose singleton surface cannot be read off the metaclass. ([#742](https://github.com/rigortype/rigor/issues/742), [#743](https://github.com/rigortype/rigor/pull/743))
