- **[inference]** `arr[i, n] = other` and `arr[range] = other` now join `other`'s elements into the array's element type instead of `other` itself, so `a = []; a[0, 2] = [1, 2]; a.last` reads `Integer` rather than `Array[Integer]`; an index the engine cannot classify contributes both readings, thank you @zonuexe! ([#1140](https://github.com/rigortype/rigor/issues/1140), [#1141](https://github.com/rigortype/rigor/pull/1141))
