- **[plugins]** A plugin can now claim non-Ruby project files with `template_globs:` and compile each into a **template unit** — Ruby plus a line map, a declared `self`, locals and ivar seeds — which Rigor parses and types like a file, reports diagnostics for at the template's own line, and lists in `rigor effects` as `view:<name>`. [#1037](https://github.com/rigortype/rigor/pull/1037)
