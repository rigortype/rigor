- **[plugins]** The "failed to discover …" load error from rigor-actioncable, rigor-activejob, rigor-activestorage, rigor-actionmailer, rigor-pundit and rigor-sidekiq is now reported once per run at `.rigor.yml:1:1` instead of being repeated on every analysed file (7506 rows on a mastodon probe, 1218 on redmine, all six plugins failing); **upgrade note:** all six are `:warning` and a baseline entry recorded at the old file position no longer matches, so run `rigor baseline regenerate`. ([#1062](https://github.com/rigortype/rigor/pull/1062))
