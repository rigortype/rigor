- **[inference]** A method call on a union receiver, or on a difference that removes `nil`, now dispatches on each projected member and answers the union of the results, so `params[:key].present?` / `#blank?` / `#==` / `#to_s` and every other call whose receiver is `T?` for an RBS-less `T` resolve instead of reading `Dynamic[top]`; a union with a member nothing can answer still declines. [#1129](https://github.com/rigortype/rigor/pull/1129)
