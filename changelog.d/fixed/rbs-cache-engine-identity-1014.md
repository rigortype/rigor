- **[cache]** A warm `rigor check` run from a source checkout no longer serves the previous build's RBS-derived tables — translated constant types, ancestor chains, the marshalled RBS environment — into a newer build's analysis after an engine edit, so a dropped true positive and a served false positive both disappear and the warm result matches a `--no-cache` run. [#1031](https://github.com/rigortype/rigor/pull/1031)
