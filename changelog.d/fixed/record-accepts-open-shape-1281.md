- **[check]** The `rigor-dry-schema` plugin's `Schema.call(input).to_h` shape is no longer reported by `def.return-type-mismatch` or `call.argument-type-mismatch` against an RBS record with exactly the schema's keys; a schema with a key the record lacks, without a key the record requires, or with a value type the record refuses is still reported. ([#1404](https://github.com/rigortype/rigor/pull/1404))
