- **[rigor-factorybot / rigor-rspec]** `create(:factory)` inside a spec's `let` now types as the factory's model class again — rigor-factorybot was never publishing the `:factory_index` fact rigor-rspec reads, and two probe-command gaps (missing plugin `#prepare`, missing `type-of` source path) that the fix's verification surfaced are fixed too, thank you @zonuexe! ([#950](https://github.com/rigortype/rigor/pull/950))
