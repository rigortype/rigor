- **[inference]** Compact-header re-anchoring no longer lets fold order pick which namespace an ancestor name is resolved in when two declarations of one class both wrote it: the colliding crefs are kept side by side, and a name they resolve to two different project classes now declines silently instead of reporting a spurious `call.wrong-arity`. [#1029](https://github.com/rigortype/rigor/pull/1029)
