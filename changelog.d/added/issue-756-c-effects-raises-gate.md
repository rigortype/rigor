- **[dev]** A new `spec/docs` gate checks every `raises`-omitting row in `data/builtins/ruby_core/*.yml` against the C function its own `c_body_at` cites, and fails when that function actually raises — it caught ten more rows across seven catalogue files beyond the 36 already fixed, and skips cleanly rather than failing when the optional `references/ruby` submodule isn't checked out. ([#756](https://github.com/rigortype/rigor/issues/756), [#759](https://github.com/rigortype/rigor/pull/759))
