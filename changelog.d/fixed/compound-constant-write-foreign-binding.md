- **[inference]** A constant compound write (`H ||= x`, `H &&= x` and their `Conf::H` / `self::H` path forms) now stores `Dynamic[top] | x` instead of `x` alone when something other than a memoizing `||=` writes the constant — another project file, or a multiple or chained assignment in the same file — so `w = (H2 ||= 0); w[:x]` beside a sibling file's `H2 = { x: 1 }` no longer reports `Integer#[]`; a constant only memoizing `||=` writes keeps the memo reading. ([#1276](https://github.com/rigortype/rigor/pull/1276))
