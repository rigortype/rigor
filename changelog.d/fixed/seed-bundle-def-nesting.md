- **[incremental]** `--incremental` rechecks now resolve constants in an unchanged file's methods exactly as a full run does, so a warm run no longer reports an `undefined method` error on correct code that uses compact `class Admin::Thing` declarations, and `--verify-incremental` no longer flips to FAILED on such a project. ([#707](https://github.com/rigortype/rigor/issues/707), [#715](https://github.com/rigortype/rigor/pull/715))
