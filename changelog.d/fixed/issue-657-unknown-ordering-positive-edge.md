- **[check]** A `case`/`is_a?` arm whose class relationship Rigor cannot determine is no longer reported unreachable: reopening a core class to include a project module made every match on that module read as dead code, on arms Ruby takes at runtime. An arm the environment knows is disjoint still reports. ([#657](https://github.com/rigortype/rigor/issues/657), [#751](https://github.com/rigortype/rigor/pull/751))
