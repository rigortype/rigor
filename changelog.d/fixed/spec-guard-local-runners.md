- **[dev]** Every spec that builds its own analyzer now routes the run through the same crash guard the shared harness helpers use, and a new `spec/docs` gate rejects a bare `Runner.new(...).run` that skips it — before this, 1,177 of 1,925 examples across 86 spec files still passed with every check rule crashing, several whole files green while nothing ran. ([#674](https://github.com/rigortype/rigor/issues/674))
