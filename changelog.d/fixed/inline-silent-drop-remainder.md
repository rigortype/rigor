- **[rbs-inline]** An `# @rbs name: T` parameter annotation whose `T` does not parse (e.g. a bounded refinement like `Integer[1..10]`) and an `@rbs`-prefixed tag the gem does not recognise (e.g. `# @rbs-ext …`) are now reported as `source-rbs-annotation-not-honoured` instead of silently leaving the parameter `untyped` or the comment unread. [#1023](https://github.com/rigortype/rigor/pull/1023)
