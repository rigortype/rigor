- **[sig]** Twenty-two `-> untyped` returns under `sig/` now name the type the implementation produces — `Prism::DefNode?` on `Scope#*_def_for`, `Prism::Node?` on `Source::NodeLocator`, `RBS::Definition::Method?` on the `Reflection` / `RbsLoader` method lookups, `RBS::Environment` on `RbsLoader.build_env_for`, `Plugin::Base?` on `Registry#find`, `Type::t?` on `Plugin::Base#dynamic_return_type`, `Array[Manifest::Consumption]` on `Manifest#consumes`, `String` on the `SkipReasonCatalog::Entry` readers, and an `Enumerator[String, void]` overload on `each_known_class_name` — and `sig/` validates under `rbs validate` for the first time (`Rigor::SigGen` was undeclared). [#1174](https://github.com/rigortype/rigor/pull/1174)
