- **[dev]** The spec suite now fails loudly, naming the crashed file and exception, when a check rule or plugin crashes instead of producing diagnostics — previously the crash folded into one untagged diagnostic that a `not_to include(...)` / `to be_empty` / `to all(...)` assertion could pass right through, reading a crashed rule as a rule that correctly declined to fire. ([#665](https://github.com/rigortype/rigor/issues/665), [#676](https://github.com/rigortype/rigor/pull/676))
