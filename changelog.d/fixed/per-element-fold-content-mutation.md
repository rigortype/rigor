- **[inference]** A `map` / `select` / `find` block over an array literal no longer reads a captured hash or array at its pre-loop contents on every element when the block writes into it in place, so `h = { a: 0 }; [:a, :a].map { |k| h[k] = h[k] + 1 }` stops folding to `[1, 1]` and firing a false always-truthy on `r.last == 1`. ([#1203](https://github.com/rigortype/rigor/pull/1203))
