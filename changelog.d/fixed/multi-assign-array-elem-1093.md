- **[inference]** Destructuring a non-tuple `Array[T]` (`x, *y, z = ints`, including a `non-empty-array[T]` after an `empty?` guard and nested or block-parameter `|(g, h)|` targets) now binds each slot to `T` and the rest to `Array[T]` instead of `Dynamic[top]`, and a block auto-splat of a single yielded array (`ints.each_slice(2) { |g, h| }`) binds `T` per parameter instead of the whole array; the slots keep the `nil` branch a short array can take, as `ints.first` does. ([#1103](https://github.com/rigortype/rigor/pull/1103))
