- **[inference]** A parameter declared `Array[untyped]` or `Hash[untyped, untyped]` that a block or loop body appends to keeps its declared gradual arm instead of closing to the appended type, so a later `a.first.upcase` on correct code no longer draws `call.undefined-method`; a `non-empty-array` seed appended to in a loop now carries the appended elements too. ([#586](https://github.com/rigortype/rigor/issues/586))
