- **[inference]** An `attr_writer` / `attr_accessor` declaration now counts as a write to its instance variable, as a hand-written setter does, so an ivar injected through the accessor and set to `nil` or `false` in `initialize` no longer reports a false `flow.always-truthy-condition` on `if @cb` or a false `undefined method for nil` on `@cb.call`, thank you @bash0C7! ([#541](https://github.com/rigortype/rigor/issues/541), [#1409](https://github.com/rigortype/rigor/pull/1409))
