- **[sig-gen]** `sig-gen --write` now anchors a generated superclass reference absolutely (`< ::Help::Show`), so a nested declaration whose superclass shares a name with an enclosing namespace no longer resolves back to itself and collapsing the class — or exhaust the stack — on the next run; and an analysis that dies of a `SystemStackError` or `NoMemoryError` now exits 70 with a readable message instead of leaving an empty report behind a passing exit status, thank you @n-rodriguez! ([#957](https://github.com/rigortype/rigor/pull/957))
