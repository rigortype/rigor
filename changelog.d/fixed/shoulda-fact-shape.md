- **[plugins]** `rigor-shoulda-matchers` now reads the `:model_index` fact published by `rigor-activerecord` as the flat Hash it actually is instead of as a `ModelIndex` object, so `have_db_column` / `validate_presence_of` / `belong_to` / `have_many` and the rest of its matcher checks fire on real projects again instead of raising on the first matcher in every `describe <Model>` block. ([#573](https://github.com/rigortype/rigor/issues/573), [#582](https://github.com/rigortype/rigor/pull/582))
