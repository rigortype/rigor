- **[dev]** `IncrementalSession`'s `#baseline` / `#recheck` / `#run_incremental` / `#run_buffer_recheck` now route through the same crash guard as `Runner` and `WorkerSession`, and the `spec/docs` regrowth gate rejects a new unguarded call the same way — before this, a crashed check rule produced the identical deterministic diagnostic on both sides of an incremental spec's "matches a full run" comparison, so 14 of 60 `incremental_session_spec.rb` examples still passed with every check rule crashing. ([#683](https://github.com/rigortype/rigor/issues/683))
