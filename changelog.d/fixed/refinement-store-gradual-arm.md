- **[inference]** The elements a `map` block returns no longer keep a captured `non-empty-array` / `non-empty-hash`'s entry element type when the block rewrites that collection in place with `map!`, `transform_values!` or a similar mutator, so calling a method the rewritten element defines on the result is no longer reported as `call.undefined-method`. ([#1269](https://github.com/rigortype/rigor/pull/1269))
