- **[plugins]** A plugin's project-global disclosure — rigor-activerecord's missing-schema notice, rigor-rails-i18n's and rigor-rails-routes' load errors — is now reported exactly once per run at `.rigor.yml:1:1`, instead of once per `--workers` worker positioned on whichever file that worker analysed first (which, since ERB template units landed, could be a view). **Upgrade note:** a baseline row that recorded one of these at its old file position no longer matches, so the row surfaces as new — and the rails-i18n / rails-routes ones are `:warning`, which fails `--fail-on=warning`; run `rigor baseline regenerate`. When rigor-activerecord has both a schema error and a model-index failure, the two now print schema-first in a fixed order rather than in discovery order. ([#1054](https://github.com/rigortype/rigor/pull/1054))
