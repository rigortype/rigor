- **[sig]** A second pass over `-> untyped` returns names the ones whose element types are already covered by `sig/` or core: `Environment#hkt_scan_failure` is the documented `[String, String, String?, Symbol]?` tuple, `Inference::Narrowing.analyse` is `[Scope, Scope]?`, `Plugin::Loader.load` / `.load` return `Registry` (and declare the `feature_resolver:` keyword the implementation already takes), the `Plugin::Manifest` readers validated to `Array[String]` / `Array[Symbol]` / `Array[TypeNodeResolver]` now say so, and `Runner`'s core-shaped read surface (`effect_sources`, `return_summaries`, `param_inferred_types`, `collect_param_inference_table`, `evaluate_return_types`) carries its real key/value types. Returns whose element classes still lack `sig/` coverage stay `untyped` by design; the remaining backlog is enumerated in `docs/notes/20260922-untyped-void-top-return-contracts.md`. [#1180](https://github.com/rigortype/rigor/pull/1180)
