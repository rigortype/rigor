# Current Work — Resume Bookmark

A transient bookmark for the next implementer: the immediate next-session entry point plus engine-internal items not fully captured elsewhere. The **normative** contracts live in [`docs/internal-spec/inference-engine.md`](internal-spec/inference-engine.md) and [`docs/adr/4-type-inference-engine.md`](adr/4-type-inference-engine.md); the forward-looking commitment envelope (release strategy + full backlog) lives in [`docs/ROADMAP.md`](ROADMAP.md); the released-version record is [`CHANGELOG.md`](../CHANGELOG.md). If this file disagrees with any of those, the spec / ADR / roadmap binds and this file is out of date.

## Status

**v0.1.19 released (2026-06-13) — effectively the release candidate (RC) for v0.2.0.** This is the final `0.1.x` preview cut; the **next release is v0.2.0**, the first publicly-announced (general / evaluation) version, governed by [ADR-50](adr/50-release-engineering-and-stability-strategy.md). v0.1.19 is a precision-and-trust release for procedural Ruby — method-call results now flow through user-defined helpers ([ADR-57](adr/57-self-call-return-adoption.md)) backed by recursive-return ([ADR-55](adr/55-recursive-return-precision.md)) and block/loop captured-mutation ([ADR-56](adr/56-block-captured-local-mutation.md)) precision plus a large real-world false-positive batch ([ADR-58](adr/58-ivar-field-typing.md) + the CRuby-stdlib and 16-repo sweeps) — and also lands the pre-1.0 plugin-contract consolidation ([ADR-60](adr/60-pre-freeze-plugin-contract-consolidation.md), with BC breaks), agent-friendly structured diagnostic fields ([ADR-61](adr/61-agent-friendly-diagnostic-statistics.md)), the [ADR-50](adr/50-release-engineering-and-stability-strategy.md) WD2 bleeding-edge opt-in *foundation*, and [ADR-54](adr/54-cache-slimming.md) cache slimming (per-project cache ~33.7 MB → ~2 MB). Full record in `CHANGELOG.md` § `[0.1.19]`; do not re-summarise it here. The prior cut was v0.1.18 (CI-environment support, [ADR-51](adr/51-ci-diagnostic-output-formats.md)).

The v0.1.19 engine-resume detail — residuals and demand-gated follow-ups the CHANGELOG omits — follows. **[ADR-55](adr/55-recursive-return-precision.md) recursive-method return-type precision, both slices complete** — slice 1 fueled constant-arg unroll (`factorial(5) → Constant[120]`, fuel 32 + value-size cap, plus the same-day corpus-FAIL clamp: the unroll only ever surfaces a fully value-pinned result) and slice 2 fixpoint return summaries (Kleene from `bot`, cap 3; a bare pass-through recursion now types `:done` instead of `Dynamic[top]`, plus the same-day bot-collapse soundness fix: parameter-widened re-run + explicit-`return` floor so a value-returning recursion never types `bot`). Gate: four corpora (Mastodon/haml/jbuilder/kramdown) byte-identical, perf neutral. Known residuals (demand-gated): top-level recursive defs keep a `Dynamic` constituent (the ADR-24 WD3 adoption gate binds), early-`return` values don't reach the tail-only body evaluator (`pick → Dynamic[top]`, not `nil`), mutual recursion summaries don't cross signatures.

Also landed 2026-06-11: **[ADR-56](adr/56-block-captured-local-mutation.md) block-captured local write-back + loop-body fixpoint, slices A + B complete** — the engine previously discarded block exit scopes, so `result = 1; 1.upto(6) { result *= i }` kept `Constant[1]` (runtime 720; a spec-MUST violation) and `while` joined one body pass; both now converge through the shared `Inference::BodyFixpoint` (cap 3, `Constant→Nominal` final widen, per-local `Dynamic[top]` floor, `BudgetTrace::BLOCK_WRITEBACK_CAP`). Corpus wins: Mastodon 1 wrong-always-falsey removed, kramdown 2 wrong-nil-fold FPs removed + 5 message corrections; zero new firings. Two adjacent precision fixes in the same arc: symbol-form `reduce`/`inject` dispatch (`(1..n).reduce(1, :*) → Integer`, new `ReduceFolding` tier) and `ConstantFolding.numeric_set_of` folding mixed `Constant | IntegerRange` unions to a bounding range (interval accumulators converge to `Integer` instead of leaking `Dynamic`). All three factorial styles (recursive / reduce / accumulator-block) now type precisely.

Landed 2026-06-12 — the **Dynamic-fall campaign** ([survey note](notes/20260612-dynamic-fall-pattern-survey.md), 139 probes / 11 buckets): ADR-56 slice C (receiver-content element-type join — `out << x` accumulators were unsoundly keeping only the seed's element type) + the catalog trio (`flatten` / `Array#to_h { }` / `Hash.new(default)`), then **[ADR-57](adr/57-self-call-return-adoption.md) — the ADR-24 WD3 implicit-self return-adoption gate is OPEN** (accepted, gate opened 2026-06-12): three adjudication slices took the gate-open delta 25 → 10 → 4 → 0 by fixing five artifact mechanisms at the root (explicit-`return` contribution incl. multi-value Tuples, escaping-content floors incl. receiver-chain + cross-method-boundary OptionParser shapes, optional-tuple-slot destructure softening, the String fold-guard stub) plus two self-authored RBS bugs the experiment surfaced. `def outer = helper(3)` now types `6`. Known cost: cold self-check wall +12% (warm ADR-45/46 path unaffected); queued follow-ups in ADR-57 — a sound per-call-site return memo for the perf, module-singleton (`def self.x`) resolution, and the `fib` toplevel `1 | Dynamic` residual (ADR-55 summary × gate interaction).

Also landed 2026-06-12 — the **CRuby-stdlib campaign** ([survey + residual adjudication](notes/20260612-cruby-stdlib-survey.md)): `rigor check` over Ruby's own `lib/` (626 files), errors **313 → 267** in two waves with zero new firings and all prior corpora byte-identical. Wave 1: destructured-param binder crash (`def f((a, b))`, sig-gen now degrades per-def), chained `Struct.new(:a).new` dispatch, regex-match-global narrowing (`$~`/`$1` on proven-match edges — sound but ~0 FP yield; the survey's "~180 regex FPs" was a measurement artifact, unseeded globals read `Dynamic` and fire nothing — *sample-adjudicate radius estimates before sizing work*). Wave 2: terminating `case` branches dropped from the post-case join (−31, the resolv cluster), safe-nav truthy narrowing into `&&` rhs (−4), loop-exit-edge narrowing for `while/until x = expr` with a break guard (−6), and dead-transient ivar-`nil`-write elision (groundwork; the live ipaddr cluster needs a cross-method ivar definite-assignment pass — **queued as an ADR candidate**). Remaining 267 ≈ needs-RBS undefined-method long tail + vendored bundler/rubygems + genuine catches (`untaint` — removed in Ruby 3.2) + the deferred ivar pass.

Landed 2026-06-12 — the **general-code trustworthiness arc** ([algorithm corpora](notes/20260612-algorithm-corpora-survey.md) + [Rails monorepo](notes/20260612-rails-monorepo-survey.md) surveys): **[ADR-58](adr/58-ivar-field-typing.md)** (declaration-sourced ivar nil is not diagnostic fuel; WD1 binding subset landed −29 FP zero-new, WD1b method-return transit + WD2 homogeneous-write reads + WD3 ctor definite-assignment queued), the `Numeric` coercion core-overlay + `Float#round` arity split, `Regexp.last_match` proven-match narrowing (residual: `last_match(N)` group promotion), and — the headline — **the whole-lib scaling wall fixed** (`9a3d6f5c`): a sound run-scoped user-method return memo (keyed def-node identity + receiver + per-arg short descriptors, bypassed while ADR-55 unroll/fixpoint is in progress) kills the ADR-57 cross-file callee re-typing blowup — ActiveStorage whole-`lib` >20 min → ~3 min, cold self-check −26 % (the ADR-57 +12 % recovered). Residual perf item: no per-node type cache within a file (the ~185 s `video_analyzer.rb` floor).

ADR-58 arc closed 2026-06-12 EOD: WD3 ctor definite assignment through same-class calls landed (`4921afdb` — recursive suffix definite-assignment + per-class method-effect summaries, depth cap 3; ruby/lib −6, the ipaddr `@mask_addr ^` cluster, all other corpora byte-identical); WD2 resolved as **already realized** (the ivar write-union is already precise — the container-code Dynamic floor is param-sourced M3 gradual typing, excused); WD1b **demand-gated** after adjudication (`d0dd3163`) — the method-return-transit firings are genuine-conservative (the callee really returns nil; same diagnostic as a foreign `T?`), not artifacts to suppress. Closed 2026-06-12 late: **module-singleton resolution landed** (`4b476918` + follow-ups — `Util.triple(5) → 15`, singleton→singleton helpers resolve, `module_function` covered, own-class only / ancestry deferred; one surfaced mutual-recursion unsoundness fixed at root `67fb8a71`, one surfaced Pathname#expand_path RBS-strictness FP class fixed via the `data/core_overlay/pathname.rbs` overlay `40bf7cdb`, ruby/lib back to 267), and the **per-node cache investigation resolved the real perf culprit instead** — the 9a3d6f5c return memo was bypassed for 99.97 % of calls by its coarse `summaries.empty?` gate (fixpoint seeds an entry for every outermost method); the post-hoc consult-counter fix (swept into `4b476918`) takes `video_analyzer.rb` 179.7 s → **0.79 s (~230×)**, all corpora byte-identical. 2026-06-13 — the **16-repo realistic-usecase sweep** ([template/parsing](notes/20260613-template-parsing-corpora-survey.md) + [app/network](notes/20260613-app-network-corpora-survey.md) surveys): eight NEW mechanisms found, six fixed same-day — cross-file `rescue Const` resolving to core instead of the project's `X = Class.new(Y)` (liquid −4), `Array#compact` element nil-strip, `respond_to?(:m)` truthy non-nil narrowing (nil-method-set guarded), safe-nav `&.` no longer fires undefined-method on the nil constituent (sound), parallel-assignment ivar targets joined into the class-ivar union (`old, @cb = @cb, blk` / popen3 tuples; net-ssh −6, textbringer −1), and — found while clearing a self-check error the killed T1 agent left — **value-position `||`/`&&` now narrows the surviving short-circuit edge** (`b7c155fb`; a method ending in `nullable || fallback` no longer leaks `nil` into its return summary). N2 extended ADR-58's declaration-nil-not-fuel to `argument-type-mismatch` (`9d697b63` — textbringer −5, concurrent-ruby −2, others byte-identical; recorded as an ADR-58 addendum). N5 gates self-call adoption on overridable-method returns (`106b93dd` — rgl 13 always-truthy FPs → 0, corpora byte-identical; recorded as an ADR-57 addendum). WD1b demand keeps accumulating (parser `loc` ×25) — if revisited, as invariant-credit narrowing, not suppression. Open engine queue: ADR-41 budget wiring, G2 over-nilable builtin returns, singleton ancestry chains, and **loop `break`-path binding propagation** (a `flag = true; break` inside a `for`/`while` body is dropped from the post-loop scope → `if flag` false-fires `always-falsey`; common search idiom; designed in [`docs/notes/20260615-loop-break-binding-propagation-design.md`](notes/20260615-loop-break-binding-propagation-design.md), core-flow change pending ratification).

Also landed 2026-06-13 — **[ADR-60](adr/60-pre-freeze-plugin-contract-consolidation.md) pre-freeze plugin contract consolidation, all WDs complete** (from the [plugin-interface review](notes/20260613-plugin-interface-bc-review.md); last-window BC breaks before the ADR-50 v1.0 surface freeze). WD1 removed the never-wired `external_files:` manifest field + `Macro::ExternalFile`; WD2 renamed `BlockAsMethod` `verbs:`→`method_names:` and `NestedClassTemplate` `name_arg_position:`→`symbol_arg_position:` (no alias); WD3 moved `Plugin::Base#cache_for` onto the ADR-45 record-and-validate path (dependency descriptor captured **after** the producer block, so in-block `io_boundary` reads are no longer invisible — the structural stale-cache hazard) + a declarative `producer watch:` backed by a new `Cache::Descriptor::GlobEntry`, migrating all 11 bundled discovery producers off the prime-before-`cache_for` idiom and privatising `glob_descriptor`; WD4 added `read_fact` / `producer_value` + `producer_error` / `diagnostics_for` to `Plugin::Base` and migrated the measured boilerplate (12 `*_or_nil` memos, 7 fact reads, ~23 violation maps); WD5 aligned both `rigor-plugin-author` SKILLs. Gate: `make verify` (self-check + check-plugins) green, the 7 producer-using plugin integration specs unchanged. Keep-verdicts (the `dynamic_return`/`type_specifier` split, `diagnostics_for_file` as the file-rule surface, the `config_schema` dual grammar) recorded as ADR-60 rejected alternatives.

Landed 2026-06-13/14 — **analyzer self-testing ([ADR-62](adr/62-mutation-testing-teeth-measurement.md)) + type-protection coverage ([ADR-63](adr/63-type-protection-coverage.md) Tier 1)**, the false-negative ("teeth") dual of the FP discipline (grounding note [`docs/notes/20260613-mutation-teeth-harness.md`](notes/20260613-mutation-teeth-harness.md)). ADR-62 stood up a **dev-only** mutation harness (`tool/mutation/`, off the ADR-50 frozen surface): inject type-visible mutations → surviving mutants are false-negative candidates; an **in-process** type-aware filter makes the metric meaningful (raw kill-rate is noise — most mutations are correct equivalent mutants); `mutate.rb sweep` clusters survivors into a ranked backlog (`--json`), `mutate.rb fuzz` is the robustness sibling (crash / hang / non-determinism — a 2,706-mutant `lib/rigor` run was clean). **Three engine teeth fixes landed from it** (each `make verify`-clean + regression-spec'd): union-receiver undefined-method (fires when every non-nil arm is certainly absent; + generic-metaclass + distinct-class FP guards), RBS class-alias resolution (`Mutex = Thread::Mutex` and any `X = Y`), and refinement-receiver dispatch (`non-negative-int` / `non-empty-string` resolve to their base class) — a cumulative re-sweep moved `lib/rigor` teeth 61.7 % → 71.4 %. **Nilable-union teeth was studied + REJECTED** — a 13-project `rigor-survey` corpus FP study (ActiveSupport-heavy + plain) found ~zero firings plus a real loss-of-specificity FP, so the deliberate N3 `T | nil` silence is kept; the study still *hardened* the shipped slice-1 (the two FP guards) and fixed a real external `Hash | Hash`#pack FP on mail. ADR-63 shipped **Tier 1** `rigor coverage --protection` (a static dispatch-site receiver-concreteness proxy: "can Rigor catch a wrong call here", with an "add a type here" ranking + the existing `--threshold` gate + `--format json`) **and, 2026-06-14, Tier 2** `rigor coverage --protection --mutation` (the per-file *actual* mutation kill rate). Tier 2 lifts a narrow, curated subset of the dev harness into `lib/rigor/protection/` — the type-visible `Mutator`, the type-aware filter, the warm loop (`MutationScanner` over a once-built `LanguageServer::ProjectContext`), and the kill criterion (a new-diagnostic signature vs a clean baseline) — exposed as `coverage --protection --mutation`, git-changed-files-scoped by default (explicit paths widen / enable the whole-project opt-in); `tool/mutation/mutate.rb` now reuses the lib `Mutator` (one source of truth) and keeps only the dev-only sweep / fuzz / clustering (ADR-62 WD4). Framing is load-bearing: effectiveness / where-to-add-a-type, never raw survival. **ADR-63 is complete; the focus reverts to the road to v0.2.0 — see "Next-session entry point" (A).**

The v0.1.17 cycle (internal-structure review + performance tuning) shipped: incremental analysis (`rigor check --incremental`, [ADR-46](adr/46-incremental-dependency-graph.md)), the unchanged-project fast path ([ADR-45](adr/45-unchanged-project-fast-path.md)), large allocation reductions ([ADR-44](adr/44-dispatch-allocation-churn.md)), Elixir-v1.20-inspired narrowing (`Array` non-empty + `Hash` key-presence) + `flow.unreachable-clause` ([ADR-47](adr/47-narrowing-driven-clause-reachability.md)), the `rigor:v1:conforms-to` directive, the `call.self-undefined-method` rule (shipped `:off`, [ADR-24](adr/24-self-method-call-resolution.md) slice 4), and `Data.define` value folding ([ADR-48](adr/48-data-struct-value-folding.md)). Full record in `CHANGELOG.md` § `[0.1.17]`; do not re-summarise it here.

v0.1.17 also landed the **release-engineering machinery for the road to v0.2.0** (deliberately *not* in the user-facing CHANGELOG — it is process / CI / docs): [ADR-49](adr/49-adr-authoring-guidelines.md) (an ADR-authoring rubric + the `rigor-adr-author` skill + the corpus audit note [`docs/notes/20260605-adr-corpus-rubric-audit.md`](notes/20260605-adr-corpus-rubric-audit.md)); **[ADR-50](adr/50-release-engineering-and-stability-strategy.md)** (the release-engineering + stability strategy, v0.2.0→v1.0.0, PHPStan-modelled); the `release/x.y.z` branch workflow + `release-gate.yml` (advisory) + the `make bench-perf` perf gate; and the `rigor-release-prep` skill's branch + release-summary conventions. **v0.1.17 was the first release cut on this machinery** — which validated it end-to-end (the release gate caught a real config-crash bug, the `source_inference: false` crash, mid-cut; fixed in the same release).

Headline realism numbers (measured at the v0.1.12 OSS-realism cut; still current — later cuts added onboarding / `def.override-*` / the plugin-contract suite / perf rather than new full surveys):

| Project | Scope | Before | After | Delta |
|---|---|---:|---:|---:|
| Mastodon | `app + lib` | 789 | 6 | **−99.2%** |
| Redmine | full plugin set | 163 | 79 | −51% |
| GitLab FOSS | `app/{controllers,mailers,workers,services}` | ~670 | ~140 | ~−79% |

The 6 remaining Mastodon errors are unrelated to engine precision: 5 nil-receiver in test fixtures + 1 upstream `ruby/rbs` `Resolv::DNS#getresources` typeclass-narrowing gap (see [`docs/notes/20260528-rbs-upstream-pr-resolv-typeclass.md`](notes/20260528-rbs-upstream-pr-resolv-typeclass.md)).

## Next-session entry point

> **▶ [ADR-48](adr/48-data-struct-value-folding.md) `Struct` value-folding follow-up — slices 1 + 2 + 3 LANDED 2026-06-15; slice 4 deferred with a full design.** Dedicated `Type::StructClass` / `Type::StructInstance` carriers (the resolved carrier decision), `StructFolding` dispatch tier, the `Scope#struct_member_layout` side-table, fresh-chain member folding, AND (slice 3) **fold-safe bound-local folding** — a member read off a stored local folds when [`Inference::StructFoldSafety`](../../lib/rigor/inference/struct_fold_safety.rb) proves the local is never mutated, aliased, or escaped (`p = Point.new(1, 2); p.x` → `Constant[1]`); a written/aliased/escaped local stays `Dynamic[top]`. The fold-safe scan is a conservative **allow-list** (every use must be a known-pure read — a missed case is over-conservative, never unsound), keyed on a counting identity (`total_reads == pure_receiver_reads`) that catches every mutation/escape/alias without enumerating escape paths; installed once per body on the scope at the top-level + method-body entry points, **measured perf-neutral**. `make verify` green; precision-additive, zero FP surface. **The deferred slice 4 (precise mutated-member re-typing — `s.x = 5; s.x` → the assigned type) is specified in [`docs/notes/20260615-struct-folding-slice3-design.md`](notes/20260615-struct-folding-slice3-design.md)** (route-a write-back narrowed to the slice-3-proven no-alias/no-escape case; ADR-56 slice C prior art). See item (C) 7 below.
>
> **The overarching focus remains the road to v0.2.0, governed by [ADR-50](adr/50-release-engineering-and-stability-strategy.md).** v0.1.19 is released (2026-06-13) and is **effectively the RC**; the next cut is **v0.2.0**, the first publicly-announced (general / evaluation) release. The gate machinery is calibrated and hardened. `make verify` is clean.
>
> **Landed `[Unreleased]` since v0.1.19 (the 2026-06-14 session, all `make verify`-green):** the [ADR-50](adr/50-release-engineering-and-stability-strategy.md) WD2 `--bleeding-edge[=ids]` / `--no-bleeding-edge` CLI flag (`aa9dd945`); the [ADR-64](adr/64-non-nil-argument-type-mismatch.md) non-nil multi-overload `call.argument-type-mismatch` channel (`ab4b0754`, corpus-gated over 31 targets incl. Mastodon `9f307c46`); and the [ADR-24](adr/24-self-method-call-resolution.md) slice-4 `call.self-undefined-method` WD4 corpus FP eval (rule stays `:off`, NOT promotable) + three FP-class narrowings it produced — universal-base exclusion (`1bb5e70a`) and subclass-aware gating + dynamic-superclass guard (`01491c63`). Plus the spun-off `--treat-all-as-inline-rbs` private-method fix (`b07840bc`).
>
> **(A) Release engineering + CI — ADR-50 staged implementation and the next release decision:**
> 1. **ADR-50 staged implementation** (each its own slice; ADR-50 stays Proposed → ratified at v1.0.0): **WD1 — the enumerated public-surface document — is DONE** (drafted 2026-06-13 in [`docs/compatibility.md`](compatibility.md)), the v0.2.0 gate's one substantive deliverable; it binds as a trial at v0.2.0 and freezes at v1.0.0. **WD2 — the bleeding-edge overlay foundation — is DONE** (shipped in v0.1.19): `Rigor::BleedingEdge` is the maintained `Feature` registry (**overlay empty today** — no major-bound discipline queued), `.rigor.yml` `bleeding_edge:` accepts `true` / feature-id list / `{ all:, except: }`, `rigor show-bleedingedge` inspects the overlay + the project's selection (`--format text|json`), and `SeverityProfile.resolve` gained a `bleeding_edge_overrides:` map composed below the user's own `severity_overrides:` and above the profile table (empty → byte-identical). The first queued discipline lands as a single `FEATURES` entry, no engine plumbing. **WD2 CLI mirror — DONE** (landed `[Unreleased]`): `rigor check --bleeding-edge[=ids]` / `--no-bleeding-edge` override the configured `bleeding_edge:` for a single run (CLI-over-config precedence à la `--workers` / `--no-cache`). Rather than thread a parallel override through every `Runner`/worker site, the CLI rebuilds the loaded config via a new `Configuration#with_bleeding_edge` (frozen `dup` re-deriving `bleeding_edge_severity_overrides`), so the two `SeverityProfile.resolve` sites + the worker path see the run's selection unchanged and `Configuration` stays the single source of truth. Uses OptionParser's `=[LIST]` attached-value-only form so a bare `--bleeding-edge lib` checks `lib` (no path-swallow); surfacing the list form also frozen-ized the selector ids in `coerce_bleeding_edge` (restores `Ractor.shareable?` for the config-file list path). **WD2 remaining**: only the dedicated bleeding-edge CHANGELOG *section* (nothing to carry — lands with the first queued feature). Still queued: the **support-line model** (latest + previous minor pre-1.0 → PHPStan `1.x`-default-branch post-1.0, WD5); a **`rigor upgrade`** migration-assist command (WD7, deferred until the first concrete BC gives it a target).
> 2. **Release-cut decision — v0.1.19 SHIPPED (2026-06-13) as the effective RC; the NEXT cut is v0.2.0.** v0.1.19 was the final `0.1.x` preview (the user's call); the ADR-55/56/57/58/60/61 accumulation + the sweeps shipped in it. `[Unreleased]` now accumulates the **v0.2.0-bound** work: the ADR-62/63 analyzer-self-testing teeth fixes (union / class-alias / refinement-receiver) and `rigor coverage --protection` (ADR-63 Tier 1). **v0.2.0 is the first publicly-announced (general / evaluation) release and IS gated by ADR-50** — the cut adopts [`docs/compatibility.md`](compatibility.md) as a binding trial commitment (frozen at v1.0.0), runs through the calibrated/required release gate, and is itself approval-gated (version bump + release task stay user-approved). The substantive v0.2.0 gate (WD1 surface doc) is already met; the ADR-50 WD2 `--bleeding-edge` CLI flag (item 1 above, now landed) was optional, not gating. See [`docs/ROADMAP.md`](ROADMAP.md) § "Release strategy".
>
> **(B) External-corpus-gated — needs `~/repo/ruby/rigor-survey/`, cannot complete in this repo:**
> 5. **ADR-24 slice 4 — corpus FP gate DONE (2026-06-14): rule stays `:off`, NOT promotable.** The WD4 corpus FP eval ran ([`docs/notes/20260614-adr24-slice4-self-undefined-fp-eval.md`](notes/20260614-adr24-slice4-self-undefined-fp-eval.md)): ~454 firings across ~26 `rigor-survey` targets, ~all false positives in two buckets. **Bucket 1 — `Object`/`BasicObject` receiver (287, dominant), FIXED**: a universal-base miss is a self-type-resolution fallback (`class << self` / FFI / class-macro), never a typo; `confidently_closed_self_class?` now excludes `SELF_UNDEFINED_UNIVERSAL_BASES` (protobuf 73→0 whole-project, regression-spec'd, a pure narrowing). **Bucket 2 — non-`Object` (167), partially FIXED**: two sub-classes addressed — **abstract / template-method base classes** (`Mail::CommonField#decoded`→`do_decode`, `Retriever#find`→POP3/IMAP) via **subclass-aware gating** (`method_defined_on_known_subclass?` — invert `discovered_superclasses`, resolve each unqualified parent name in the child's namespace, check `user_def_for`/`discovered_method?` on each subclass) and **dynamic (non-constant) superclass** (`PartsList < DelegateClass(Array)` / `< Struct.new` / `< Data.define`) via a `SelfClosednessScanner` guard (mail 12→0, faraday 5→2; genuine typos still fire). **Remaining (rule stays `:off`)**: C-extension classes (`Numo::NArray` 72), metaprogrammed/platform-conditional surfaces (`Concurrent::…#java` JRuby, `Striped64`, tdiary 31) — the per-class source scan fundamentally cannot enumerate these. **Decision: keep `:off`; do NOT widen the gate to superclass/include chains** (would only grow the C-ext/metaprogramming FP classes).
> 6. **`rigor check --incremental` CI-gate extension** — extend the `--verify-incremental` CI gate to the Mastodon + GitLab survey trees.
>
> **(C) In-repo engine backlog (demand-driven, each ADR-scoped):**
> 6a. **Pre-1.0 architecture re-examination — COMPLETE ([ROADMAP](ROADMAP.md) § "Internal architecture", [ADR-53](adr/53-scope-discovery-index-separation.md)).** All four phases done. Landed 2026-06-11: Phase 1 (sorbet → `Type#accepts`, fallback `CallContext#with`, `CLI::CheckCommand` extraction), ADR-53 Track A (A1 `Scope::DiscoveryIndex` + A2 writer deletion), Track B slices B1+B2 (`CheckRules::RuleWalk` hosting the two flow collectors behind the shadow-run equivalence harness — `rule_walk_equivalence_spec` + `RIGOR_SHADOW_RULE_WALK=1`), the stdlib singleton-folder class-name gate, and the `Analysis::Runner` decomposition (`runner/{pool_coordinator,project_pre_passes,diagnostic_aggregator,run_snapshots}`). Track B B3 landed 2026-06-13 (`b85c51c6` + `4f1745aa` + `963a2947`): all five built-in per-file walks ride one traversal; `rule_walk_equivalence_spec` 174 examples, byte-identical gated. Track B B4 landed 2026-06-13 (`e614ebf3` + `2925d66a`): `CheckRules::RuleWalk` and `Plugin::NodeRuleWalk` converged into **one walk per file total** — shadow-harness + byte-identical gated throughout, equivalence spec 191 examples. Phase 3b landed 2026-06-11: Narrowing owns all certainty judgments; `constant_value_polarity` deliberately Constant-only. The fork-pool cross-file-index bug fixed separately (`b94a8b6f`).
> 6b. **[ADR-52](adr/52-compiled-plugin-contribution-dispatch.md) plugin-dispatch migrations.** COMPLETE 2026-06-11: all five legacy users migrated (AR via the run-time `methods:` gate — no new gate form needed), the hook DELETED with a load-time error + CHANGELOG migration note (slice 5b), and the single engine-owned node-rule walk landed (slice 6). See § "ADR-52" below.
> 7. **[ADR-48](adr/48-data-struct-value-folding.md) `Struct` follow-up — slices 1 + 2 + 3 LANDED 2026-06-15; slice 4 deferred with a design.** **Slices 1 + 2 (fresh-chain):** dedicated `Type::StructClass` / `Type::StructInstance` carriers (the resolved carrier decision — the mutability gate / acceptance projection / `receiver_descriptor` pattern-match on the carrier type, and `keyword_init` diverges the class carrier), the `Inference::MethodDispatcher::StructFolding` tier (after `DataFolding`, superseding the old `meta_new` `struct_new_lift` for the all-symbol case), the `Scope#struct_member_layout` side-table (threaded through the `DiscoveryIndex` / project pre-pass like `data_member_layouts`), and member folding for the anonymous / constant / subclass / local-class forms (positional + `keyword_init:`, `nil`-padded omitted members, `[]`/`to_h`/`deconstruct`/`deconstruct_keys`/`members`/`with` projections). **Slice 3 (fold-safe bound locals):** a member read off a *stored* local now folds when [`Inference::StructFoldSafety`](../../lib/rigor/inference/struct_fold_safety.rb) proves the local is never mutated / aliased / escaped — a conservative whole-body **allow-list** scan keyed on the counting identity `total_reads == pure_receiver_reads` (catches every mutation/escape/alias without enumerating escape paths; a missed case is over-conservative, never unsound), installed once per local-variable scope (`Scope#struct_fold_safe?`, respecting `def`/`class`/`module` boundaries) at the `ScopeIndexer` top-level + `build_method_entry_scope` / `build_user_method_body_scope` entry points; member setters (`s.x = v`) return the RHS type. **Gate:** `make verify` green; the `struct_catalog` fixture + the `struct_class_spec` / `struct_instance_spec` / `struct_folding_spec` / `struct_fold_safety_spec` (17 alias/escape/mutation soundness cases) gate it; precision-additive, zero FP surface, **measured perf-neutral** (the 5.26s vs 6.4s self-check delta was a measurement-moment artifact — a stubbed scan measured the same). **Corpus-validated 2026-06-15:** a before/after diff (pre-Struct `8023c337` vs `master`) over eight Struct-using `rigor-survey` corpora (kramdown / algorithms / Algorithms-and-Data-Structures / faraday / concurrent-ruby / net-ssh / haml / jbuilder) is **byte-identical** — zero FP regression from the precision change. **Deferred (in [`docs/notes/20260615-struct-folding-slice3-design.md`](notes/20260615-struct-folding-slice3-design.md)):** slice 4 = precise re-typing of a mutated member through a setter (`s.x = 5; s.x` → the assigned type, the sibling stays precise; route-a write-back narrowed to the slice-3-proven no-alias/no-escape case, ADR-56 slice C prior art). **Also remaining (smaller):** `Data.define` bare-local block-form parity (`c = Data.define(:x) do … end` — no resolvable class name for the slice-4 reader-redefinition guard, conservative bail). `Data.define` shipped (slices 1–4).
> 8. **[ADR-47](adr/47-narrowing-driven-clause-reachability.md) WD3b** — deconstructing / value / variable-catch-all pattern exhaustiveness (the deferred [ADR-36](adr/36-mangrove-enum-nested-class-emission.md) `is_a?` neighbour; do NOT infer ad hoc; priority lowered by the zero-firing WD4 sweep).
> 9. **`MathFolding` float sign refinements** — **spec-contested, NOT a quick win.** `Math.exp → positive-float` etc. need float sign refinements (`positive-float` / `non-negative-float`), which [`imported-built-in-types.md`](type-specification/imported-built-in-types.md) **deliberately excludes** (refinements are intentionally `Integer`-only; float sign / literal narrowing is "refused by default" on `NaN` / signed-zero / coercion grounds — the spec-blessed door is a future `finite-float` / non-`NaN` proof). And `Math.exp(Float::NAN) → NaN` makes it outright unsound without a non-`NaN` precondition. ADR-worthy + a spec amendment first; demand-gated.
>
> **(D) Type-protection coverage — Tier 2 ([ADR-63](adr/63-type-protection-coverage.md)) — COMPLETE 2026-06-14.**
> 10. **ADR-63 Tier 2 — mutation-based effectiveness — LANDED.** Both tiers shipped: Tier 1 (`rigor coverage --protection`, the static receiver-concreteness proxy) 2026-06-14, and Tier 2 (`rigor coverage --protection --mutation`, the per-file **actual** mutation kill rate) 2026-06-14. Tier 2 productized a narrow subset of `tool/mutation/` into `lib/rigor/protection/` (`Mutator` + `MutationScanner` — the warm loop + type-aware filter + kill criterion from [ADR-62](adr/62-mutation-testing-teeth-measurement.md)); the dev `sweep` / `fuzz` / clustering stay dev-only (ADR-62 WD4) and `tool/mutation/mutate.rb` now reuses the lib `Mutator`. Changed-files-scoped by default (`git status --porcelain`); `--mutation` requires `--protection`; `--threshold` gates on the effectiveness ratio; `--format json` carries `{mode, killed, survived, effectiveness_ratio, files, add_a_type_here}` (ADR-50 public vocabulary). New specs: `protection/mutator_spec`, `protection/mutation_scanner_spec`, `cli/mutation_protection_report_spec`, `cli/coverage_command_spec`. `make verify` green. **Demand-gated leftovers:** an optional per-file mutation cap (`MutationScanner#initialize` already takes a seeded `limit:`, unwired in the CLI — the changed-files default bounds cost), an ADR-46-incremental-backed cheaper whole-project Tier 2, and the remaining ADR-62 backlog (`Type::*` self-dogfood = the ADR-24 `:off` rule; broad-fuzz expansion; an `arity_extra` fixed-arity guard).
>
> **(E) Argument-channel teeth + the chosen-but-deferred non-nil slice (2026-06-14 session).** Landed (all FP-gated, zero new firings across 12 `rigor-survey` projects, `make verify` green): `call.argument-type-mismatch` now fires on a pure-`nil` argument rejected by every overload — **multi-overload** (`5 * nil`, `a2775c7b`) and **interface-alias** params (`"a" + nil`, `0afc65ea`), deciding nil-admittance on the RBS param type (`param_admits_nil?` resolving aliases/interfaces vs `NilClass`'s methods) — plus **difference-type refinement receivers** (`non-empty-array`/`non-empty-string` dispatch as their base, `7188ff09`) and **generic RBS type aliases** (`range[int?]` via `expand_alias2`, `cb758c1f`). CRuby-`lib/` teeth **53.1 % → 58.4 % (+457 kills)**.
> 11. **Non-nil argument-type-mismatch — the `type_swap` channel ([ADR-64](adr/64-non-nil-argument-type-mismatch.md)) — LANDED (multi-overload), 2026-06-14.** `call.argument-type-mismatch` now fires on a non-nil arg every overload of a **multi-overload** method rejects: `[1,2,3].fetch("x")`/`.fetch(:sym)` report (`int` param, String/Symbol have no `to_int`), `.fetch(2.0)` stays clean (`Float#to_int`). WD1 `COERCE_DISPATCH_METHODS` allow-list excludes the coerce operators (`+ - * / < …`); WD2 `param_accepts_arg_class?` decides acceptance on the RBS param type (alias/interface-resolved via `expand_type_alias`/`interface_method_names`, `class_instance_accepts_arg?` via the acceptance engine, `interface_accepts_arg?`/`arg_class_has_method?` for the conversion interfaces); WD3 `single_concrete_arg_class?` restricts to one concrete **RBS-known** class (union / `Singleton` / non-RBS project class skipped). **Gate MET** — a file-swap before/after diff over **31 `rigor-survey` projects** (lib-bearing libraries, the algorithm corpora, and the **Mastodon `app`+`lib`** headline FP benchmark) found zero new firings, while a `type_swap` mutation sweep rose (kramdown 5→9, liquid 0→7). The pre-existing single-overload per-arg acceptance check is **unchanged** (so single-overload comparison operators like `Integer#< : (Numeric)` keep firing on `5 < "y"`, genuine). **Remaining (demand-gated):** the carry-over namespaced type-alias resolution (`::MatchData::capture`); extending the coerce exclusion to the single-overload comparison path (a consistency/FP refinement, not surfaced by the corpus). GOTCHA learned: a baseline worktree's bundler can't materialise gems for a different Gemfile path — **toggle `check_rules.rb` between HEAD and the change in the working tree** (same bundle) for the corpus diff, not a `git worktree`-based baseline.
> 12. **Precision fold gap — `freeze`/identity methods degrade shape carriers (recorded, ADR-scoped).** `{a: 1}.freeze → Hash` drops the `HashShape` → `Hash#[]` Dynamic (a frozen-constant cascade). A fix preserving the receiver type for `freeze`/`itself`/`dup`/`clone` on shape carriers is precision-correct and corpus-FP-safe, **but backed out** — it surfaces 12 reflexive `flow.always-truthy-condition` in rigor's OWN constant-folder (self-analysis over-folds `receiver.public_send(method_name)`), which the discipline forbids papering over with `# rigor:disable`. Clean fix = a separate change entangled with the reflexive over-fold + the `always-truthy` envelope. Full recon + reproducer in [`docs/notes/20260614-precision-foldgap-recon.md`](notes/20260614-precision-foldgap-recon.md). The broader precision finding: the `rigor-survey` corpus is **mature** — most `dynamic_top` (~50 % of expressions) is intrinsic (untyped params / gem returns / `Proc#call`), not fixable fold gaps.
>
> **Do NOT** restart ADR-24 slice 4 via the check-rules route (reverted 2026-06-05, 135 FPs — see below); the evaluation-time recorder is the landed route. **Do NOT** extract the `Runner#initialize` ivar pre-seeds into a helper (hides them from the engine's own flow analysis → self-check FP; see "Gotchas"). **Do NOT** widen a union/nilable-receiver diagnostic on a `make verify`-clean signal alone — gate it against the `rigor-survey` corpus (external code exposes mistyped-receiver FPs; see "Gotchas"). **Do NOT** preserve shape carriers through `freeze`/`dup` without first resolving the reflexive `always-truthy` interaction (item 12).

### Reference reading

1. [`docs/ROADMAP.md`](ROADMAP.md) § "Release strategy — the road to v0.2.0" — what gates v0.2.0 (now governed by ADR-50).
2. [`docs/adr/50-release-engineering-and-stability-strategy.md`](adr/50-release-engineering-and-stability-strategy.md) — the v0.2.0→v1.0.0 release/QA contract (compatibility surface, diagnostic non-contract + bleeding-edge, perf gate, support line, graduation cadence).
3. [`CHANGELOG.md`](../CHANGELOG.md) § `[0.1.17]` — the shipped perf/incremental/diagnostics cycle; § `[0.1.16]` for the plugin-contract suite + ADR-43; § `[0.1.12]` for the OSS-realism cycle.
4. [`docs/internal-spec/public-api.md`](internal-spec/public-api.md) — the public-vs-internal stability boundary (ADR-50 WD1 enumerates it); cross-reference `spec/rigor/public_api_drift_spec.rb` before extending any pinned namespace.

## Gotchas (load-bearing, learned the hard way)

- **ADR-46** — do NOT extract the `Runner#initialize` ivar pre-seeds (`@class_decl_paths_snapshot = {}` etc.) into a helper; moving them out of the constructor hides them from the engine's OWN flow analysis and `make check` self-flags `snapshot.size` as a nil-receiver false positive. Keep them inline (the constructor carries an `AbcSize` disable).
- **ADR-45** — `@collect_stats` is true by default (can't gate the cache on it; a hit returns nil stats); a lazy `@io_boundary ||=` on a frozen plugin → `FrozenError` (use `instance_variable_get`); a cache write / serialize failure is swallowed (never breaks a run).
- **ADR-24** — a check-rules *reimplementation* of self-call resolution diverges from the engine's real one (which already handles `module_function` / `Data.define` accessors / mixins for precision) → 135 FPs (reverted). The landed route is the evaluation-time `SelfCallResolutionRecorder` ("collect, don't recompute").
- **ADR-62 / ADR-63** — `make verify` gates only `lib` + `plugins`; the **external corpus can still expose false positives** from receivers Rigor mistypes. A union/nilable undefined-method rule inherits the scalar rule's "only as good as the inferred type" exposure — the nilable study caught a `Hash | Hash`#pack FP on mail (`compose_codepoints` mistyped an `Array`). Run a `rigor-survey` corpus diff (`cd $proj; BUNDLE_GEMFILE=$rigor/Gemfile nix develop $rigor -c bundle exec $rigor/exe/rigor check --format json $paths`) before trusting any diagnostic-widening on union / nilable receivers. Also: the inference scanners (`PrecisionScanner` / `ProtectionScanner`) are NOT autoloaded by `require "rigor"` (require them explicitly, as their specs do); rubocop enforces annotated `%<name>s` format tokens; and a `Data.define(:method, …)` member shadows `Data#method` (use `:method_name`).
- **bench-perf** — the Make target is `bench-perf`, not `bench` (the bare name collides with the `bench/` data directory; the file keeps its no-`.PHONY` convention). `bench/baseline.json` is now **calibrated** (`ddbcb071`, Linux CI: lib wall 13.75 s / 16.0 M allocs / 206 MB RSS; `calibrated: true`) and the release gate is **required** (not advisory). The OSS sweep is also calibrated (`1d7162a5`, Mastodon v4.5.10 `app lib config`: 445 diagnostics / min precision 0.4284 — exact-count gate, no headroom; a precision change flips it red until recalibrated by design).

## Open engineering items

Engine-internal items the next implementer benefits from seeing directly. The full demand-driven backlog lives in [`docs/ROADMAP.md`](ROADMAP.md) § "Future cycles".

### ADR-52 — compiled plugin contribution dispatch (COMPLETE — slices 1–6 landed 2026-06-10/11; all five legacy users migrated, hook deleted, single node-rule walk landed)

[ADR-52](adr/52-compiled-plugin-contribution-dispatch.md) + the grounding [structural audit note](notes/20260610-plugin-architecture-perf-audit.md) (2026-06-10). **Slice 1 (WD1) LANDED** (`67a552de` + `1deecb2f`): the compiled contribution table + the six engine call-site rewirings. WD6-gated byte-identical on Mastodon `app/models` (6 plugins) + GitLab subset (11 plugins, 2,332 lines) via control-vs-HEAD worktrees. GOTCHA the gate caught: the `&:symbol` block path dispatches with a `Prism::BlockArgumentNode` as `call_node` — a bare `call_node.name` raised, absorbed by `block_return_type_for`'s rescue, flipping `select(&:presence)` onto no-block Enumerator overloads (6 new GitLab errors); always derive gate names via `respond_to?(:name)`, regression specs pin it. **Slice 2 (WD2 static method-name-only gate) LANDED** (`c3550b00` + `cd5d5990`): `dynamic_return receivers:` is now optional — a rule may gate on `methods:` alone (receiver-less), for a plugin whose receiver carrier is a refinement not a nominal class; the engine skips the receiver-ancestry check and the registry gates on the name set. Migrated the `rigor-units` example (byte-identical on its demo, 17 diags).

**AUDIT CORRECTION discovered while implementing slice 2** (recorded in the ADR + audit note): the audit's slice-2 targets were both wrong — **activesupport-core-ext ships no `flow_contribution_for`** (pure RBS bundle, not a target) and **sorbet's catalog path matches arbitrary `def` method names** (a *run-time* name set, not static). No production plugin fits the static gate; units was its first consumer.

**Slice 3 (run-time receiver-set callable) PARTLY LANDED** (`fb5aea04`/`0f1a64b2` engine + `be4c532c` rigor-activestorage). `dynamic_return receivers:` now also accepts a callable, `instance_exec`'d against the plugin and memoised per rule, **resolved lazily on first dispatch** (always after `#prepare`) — the design judgment resolved in favour of lazy-on-first-use, NOT a runner `finalize!`: receiver gating lives in the instance `dynamic_return_type` path, never the ContributionIndex (a receiver-gated rule has no `methods:` gate, so the registry already treats it like a static-receiver rule), so no after-prepare table rebuild is needed. KEY soundness argument: the resolved set is a **safe over-approximation** of the block's own filter (engine admits equal-or-subclass, block re-runs the precise lookup), so every receiver the old hook accepted still reaches the block → byte-identical. The callable must reference a lazy/idempotent index (activestorage's `attachment_index`, AR's `model_index`) and must not raise (return `[]` on nil). Per-rule memo is a Hash created in `initialize` + `||= {}`-guarded so `allocate`-based unit specs and self-freezing plugins both work. activestorage gated on GitLab corpus (Mastodon doesn't load it), 2,322 diags byte-identical.

**rigor-activerecord — blocker RESOLVED, migrated 2026-06-11 via the run-time `methods:` gate (see the MIGRATED paragraph below); the receivers-gate failure record is kept for the historical lesson.** Original record: The `dynamic_return` receiver gate keys on the call's **`receiver_type`**, but AR's two class-side paths read the **AST** (`class_call_return_type` → `constant_receiver_name`) and **`scope.self_type`** (`implicit_self_class_call_return_type`), NOT the receiver type. VERIFIED ROOT CAUSE: a project model not in RBS — i.e. nearly every real model — types its constant as `Dynamic[top]`, so `User.find(1)` dispatches with `receiver_type = Dynamic`, `class_name = nil`, the gate declines, `u` never narrows to `Nominal[User]`, and the instance chain (`u.name → String`) collapses. The old `flow_contribution_for` never saw `receiver_type` (it read the AST), so it worked regardless. AR's caught it via its OWN end-to-end specs (`activerecord_plugin_spec.rb` "types an accessor end-to-end…", "resolves the block element type…") before any corpus run — cheaper signal than the corpus gate. AR's instance/relation paths WOULD gate fine, but a partial migration that leaves the hook for class-side paths defeats slice 5's goal of deleting the hook. **AR needs a new gate form before it can leave the hook** — see ADR-52 "rigor-activerecord blocker": either (A) the engine types a discovered in-source class constant as `Singleton[Class]` (broad, high-risk dispatch change, its own ADR), or (B) a gate keyed on the **AST receiver constant name** and/or **implicit-self in a class body** (the honest successor form for AST-keyed plugins; the slice-4 design question). The exact-membership-Set gate refinement (O(1) for AR's ~100-model set) was prototyped and reverted with the attempt — only land it together with a working AR migration.

**Slice 4 (run-time method-name-set callable) PARTLY LANDED** (`79dc790d` engine + `46b14280` examples). `dynamic_return methods:` now also accepts a callable, symmetric with the slice-3 receivers callable: `instance_exec`'d, lazy-resolved after `#prepare`, memoised as a Symbol Set under a namespaced cache key (`[:methods, rule]`). A callable method set can't compile into the registry name gate (unknown at registry-build time), so `ContributionIndex#build_name_gates` gates only on static-Array `methods:` and treats a callable-methods (or receiver-only) plugin as ungated-by-name — consulted every dispatch, filtered in the instance path. Migrated `rigor-lisp-eval` + `rigor-pattern` (config-derived `@method_name`, AST receiver/arg checks stay in the block → no AR-style Dynamic problem); their end-to-end specs + demos byte-identical, no spec rewrite needed (the example specs are already end-to-end). GOTCHA: the subagent's demo-equivalence runs left `.rigor/cache/` artifacts that got committed and had to be cleaned out — the root `.gitignore` `.rigor/cache/` rule does NOT match `examples/*/.rigor/cache/` (and several `plugins/*/demo/.rigor/cache/` are already tracked — a pre-existing hygiene gap, flagged for a separate fix).

**rigor-sorbet — MIGRATED 2026-06-11.** Landed exactly as the handoff designed it: `dynamic_return methods: -> { recognised_method_names }` (`SORBET_ASSERTIONS` ∪ `:absurd` ∪ the new `Catalog#method_names`), side effects (`@reachable_absurd_nodes` / `record_reveal_type_call` / `record_assert_type_check` / the sigil gate) relocated into the rule block, block returns the bare `Rigor::Type`. The one design point the handoff hadn't resolved: **`T.bind`'s `post_return_facts` cannot ride `dynamic_return`** (the dispatcher consumes only `return_type` from the merge; the statement evaluator consumes only `post_return_facts` from `type_specifier` + the legacy hook) — so the fact moved to a `type_specifier methods: [:bind]` rule with the same sigil gate, and the `Constant[nil]` return stays in the `dynamic_return` rule. `T.absurd`'s `exceptional: :raises` slot needed no replacement — the dispatcher already dropped it (`Merger.merge(...).return_type`); the `bot` return carries the unreachability. The unused `AbsurdRecognizer.contribution` was deleted. **Gate:** the 63-example end-to-end integration spec (no direct hook calls — no rewrite needed) + byte-identical `rigor check --no-cache` on strap and dependabot-core (control = HEAD worktree sharing `vendor/` via symlink + copied `.bundle/config`). **Throughput: dependabot-core 1262.6s → 33.4s (~38×)** — the first WD3 migration where the compiled gate's win is measurable (the ungated hook re-typed the receiver via `scope.type_of` + ran the catalog chain lookup on every named call); this also reopens the survey note's "full `common/lib` did not finish in 4+ min" item.

**rigor-rspec — MIGRATED 2026-06-11 (slice 5a, the per-file gate form).** `dynamic_return file_methods:` is the last WD2 DSL form: a callable receiving the **file path**, `instance_exec`'d and memoised per `(rule, path)` (key `[:file_methods, rule, path]` in the same runtime cache), **mutually exclusive with `methods:`** (one name gate, two scopes), **nil-path fail-closed** (a synthetic call site with no file context declines — the gate has nothing to key on). The registry already treats it as ungated-by-name (rule `[:methods]` is nil → `build_name_gates` yields nil), so no registry change was needed. `dynamic_return_rule_applies?` gained a `path` parameter (from `scope&.source_path`). rspec's let-binding rule is the first consumer: gate set = the new `LetScopeIndex#let_names` (every `let`/`subject` name anywhere in the file — a safe over-approximation of the block's line-scoped `let_block_at`); the block body is the old hook verbatim, returning the bare type. `public_api_drift_spec` pins the new keyword. **Gate:** engine unit specs (fires/declines/per-path memo/fail-closed/validation), rspec integration spec end-to-end, byte-identical demo + GitLab corpus.

**rigor-activerecord — MIGRATED 2026-06-11; the blocker is RESOLVED with NO new gate form.** The 2026-06-11 blocker analysis fixated on `receivers:` gating; AR fits the existing slice-4 run-time **`methods:`** callable, which never reads the receiver type. Gate set = `FINDER_METHOD_NAMES` ∪ scopes ∪ associations ∪ columns (+ `column?` predicates) from the model index — exactly the union of names AR's four paths can type, so the gate is byte-identical to the ungated hook by construction; the block keeps the AST-constant / `self_type` / `type_of` resolution verbatim (the rigor-sorbet shape). Options A (Singleton-typing) and B (AST-keyed gate) are rejected as unnecessary — recorded in ADR-52 § "rigor-activerecord blocker — RESOLVED". The integration spec's 17 direct `flow_contribution_for` calls were rewritten onto `dynamic_return_type` (delegated; gotcha the agent found: `dynamic_return_type` reads `scope.environment`, so spec scope doubles need that method stubbed). **Gate:** 93-example end-to-end spec + GitLab corpus byte-identical.

**Slice 5b — DONE 2026-06-11.** `flow_contribution_for` deleted: Base hook gone, both collector legacy branches removed, ContributionIndex flow set + legacy-disables-global-gate rule removed, and a plugin still defining the hook now raises a load-time `ArgumentError` pointing at the CHANGELOG migration note (silent-never-called would be the worst failure mode for a third-party author). Engine-spec rewrites + the docs/README/skill sweep were delegated; reviewed. `public_api_drift_spec` and the contract-conformance hook list updated.

**Slice 6 — DONE 2026-06-11.** Engine-owned `Plugin::NodeRuleWalk` (one `each_with_ancestors` pass per file, dispatching to every matching `(plugin, rule)`): per-plugin `node_file_context` runs once before its rules; `is_a?` node-type matching preserved via a per-concrete-node-class memo; diagnostics bucketed per plugin and emitted in the old plugin-major order (byte-identical by construction); the per-plugin isolation/error envelope reproduced. `Base#node_rule_diagnostics` kept (public API) but the runner no longer calls it per plugin.

**ADR-52 is COMPLETE.** Demand-gated leftovers only: node-major diagnostic re-sort (deliberately not taken — would break byte-identity), and the exact-membership-Set gate refinement if a profile ever shows the receiver ancestry walk hot.

**rigor-activerecord is BLOCKED** (root cause + the new-gate-form options recorded in the paragraph above and ADR-52 "rigor-activerecord blocker"). **Delegation lessons:** (AR) the Sonnet subagent did the mechanical spec rewrite well AND correctly flagged the regression as engine-not-spec — but the FP-core decision (whether AR is *migratable at all*) was mine; always run the plugin's own END-TO-END specs (not just receiver-stubbing unit/contract specs) before trusting a receiver-gate migration. (examples) a subagent running demos can leave cache artifacts in the tree — review `git status`/the staged file list, not just the reported diff, before committing its work.

### ADR-24 — implicit-self method-call resolution, remaining

- **Slice 4 (recorder + `call.self-undefined-method` rule, shipped `:off`) — LANDED v0.1.17; WD4 corpus FP eval DONE 2026-06-14 → rule stays `:off`, NOT promotable** ([note](notes/20260614-adr24-slice4-self-undefined-fp-eval.md)). The eval landed the universal-base exclusion (Bucket 1, `Object`/`BasicObject`/`Kernel`, 287 corpus FPs) but found the **abstract / template-method base-class pattern** (Bucket 2, 167) is an unaddressable FP under the current per-class gate. **Do NOT widen the standalone-only gate to superclass / include chains** until the abstract-base FP is solved — the required shape is **subclass-aware gating** (record at the recorder whether the missed method is defined on a known subclass; suppress if so). **Arity diagnostics** on resolved closed-class self-calls were NOT part of slice 4 (undefined-method only) — a later extension once the rule proves out.
- **Non-`Bot` general adoption inside class bodies** — a resolved self-call return type is adopted ONLY when it is `Bot`. Unconditional adoption of precise non-`Bot` returns regressed `rigor check lib` by 16 diagnostics (pre-existing callee-return-inference imprecisions surfacing downstream); this follow-up needs callee-return inference precise enough that adopting precise types does not surface those imprecisions.

### AR scope-body lambda `self`

`scope :x, -> { select(...).group(...) }` inside an instance lambda still needs the lambda's `self` rebound to the model class. v0.1.12 closed implicit-self class-side resolution for ordinary method bodies; lambda bodies remain (ADR-26 territory). Empirical case in [`docs/notes/20260523-mastodon-v4.5-regression-sweep-v0.1.9.md`](notes/20260523-mastodon-v4.5-regression-sweep-v0.1.9.md) § "What is increasing" item 2.

### ADR-23 — `rigor triage` slice 4 plugin recognisers

Remaining: a `Plugin` hook letting plugins contribute their own recognisers (deferred). (Structured `receiver_type` / `method_name` fields + the SKILL integration shipped in the v0.1.8 / v0.1.9 cycle.)

### Inference budgets — spec table unwired (Layer 1 doc hygiene DONE)

The spec's configurable `budgets:` table ([`docs/type-specification/inference-budgets.md`](type-specification/inference-budgets.md)) is normative-for-v1 but **not wired** — the only operative cutoffs are three hard-coded silent guards (recursion re-entry ≈ depth 1, ancestor walk 100, HKT fuel 64) plus ADR-10 `budget_per_gem`. **Layer 2 resolved, and it was not a budget:** the large-app cost cliff was a 4.2 M-retained-String leak in `rigor-activerecord` (fixed v0.1.16), and `union_size` was refuted as uncorrelated with memory. Budget wiring is **demand-deferred** — no corpus project demonstrates a budget-shaped cost; if one does, re-run the 2a distribution probe first ([ADR-41 WD3](adr/41-inference-budget-design.md)). The `RIGOR_BUDGET_TRACE` / `RIGOR_HEAP_PROFILE` / `RIGOR_HEAP_TRACE` probes are reusable.

### Stdlib RBS coverage-gap pattern + the staged upstream PR

When an upstream `ruby/rbs` gap is surfaced by a single internal call site, prefer **(a')** an in-source `# rigor:disable` + load the library; across multiple call sites or user-facing code, escalate to **(b)** a focused RBS overlay under Rigor's own `sig/`, or **(c)** an upstream `ruby/rbs` fix. The `references/rbs` branch `widen-strscan-resolv-stdlib-sigs` (widens `StringScanner#[]`, `Resolv#initialize`) is staged — **branch push + `ruby/rbs` PR creation are the user's task.**

### Sig-gen (ADR-14) remaining gaps

`attr_reader` with ivars set from non-`initialize` sources (DB reads, config, side effects) still produce `:untyped_return` → hand-written sig. Deep chains on untyped receivers → `rbs collection install` / ADR-10 `source_inference:`. Dynamic methods (`define_method`, DSL macros) → project plugin. `update_existing` does not collapse sibling parent/child class blocks (workaround: delete the target sig + regenerate). Documented in `skills/rigor-project-init/references/04-sig-uplift.md`.

### ADR-49 corpus economy follow-up (optional)

The 2026-06-05 corpus audit found over-information is the corpus's one systematic drift; ADR-22's SKILL-sketch bloat was trimmed (v0.1.17). ADR-1 / ADR-16 are the remaining length outliers, but their length is "defensible" (high-stakes) per the audit and extraction would split foundational rationale — assessed **not worth it**, recorded for completeness, not queued.
