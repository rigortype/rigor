# Current Work — Inference Engine Checkpoint

This is a transient bookmark used to break a long implementation thread into reviewable chunks. The **normative** contracts and slice roadmap remain in [`docs/internal-spec/inference-engine.md`](internal-spec/inference-engine.md) and [`docs/adr/4-type-inference-engine.md`](adr/4-type-inference-engine.md). The forward-looking commitment envelope (active cycle + queued work) lives in [`docs/ROADMAP.md`](ROADMAP.md); the released-version record is `CHANGELOG.md`. If this file disagrees with any of those, the spec / ADR / roadmap binds and this file is out of date.

## Status

**v0.1.5 released (2026-05-16).** Slice-by-slice recap in `CHANGELOG.md` § `[0.1.5]`. The full v0.1.0 → v0.1.5 release log lives in `CHANGELOG.md`; the planning envelopes that shaped each cut are preserved in git history (`git log -- docs/CURRENT_WORK.md docs/ROADMAP.md` at any commit between adjacent release tags).

v0.1.5 themes (frozen on tag, full detail in CHANGELOG):

1. **ADR-15 Ractor migration end-to-end** (Phases 1, 2a, 2b, 3a, 4a, 4b, 4b.x, 4c) + spec-suite performance wins (162s → 27s on 12-core via `Cache::Store` thread-safe + in-process memo + `parallel_tests`).
2. **Real-world Rails / Ruby survey + production-rigor improvements.** Fourteen projects (31,840 files) drove `examples/rigor-activesupport-core-ext/` (opt-in RBS bundle, −75 % total diagnostics), `data/vendored_gem_sigs/` (six native-extension gems by default), assignment-in-condition narrowing, four deep-shareability Ractor follow-ups, `Hash[K, V] <:= Enumerable[[K, V]]` projection, `CONSTANT_CONSTRUCTORS` Proc-share fix, `RbsLoader#env` failure-memo (~550× speedup).
3. **[ADR-16](adr/16-macro-expansion.md) macro / DSL expansion substrate** — floor + precision promotion landed; four-tier substrate (Tier A block-as-method / Tier B trait-inlining registry / Tier C heredoc-template / Tier D external-file contract-only) with three worked consumer plugins. Closes ROADMAP O2 at the WD13 floor.
4. **O4 Layer 3 (slices 1+2+3) target-project RBS source discovery + DEFAULT_LIBRARIES expansion.** `Rigor::Environment::LockfileResolver`, `RbsCollectionDiscovery`, `RbsCoverageReport`, missing-gem `:info` diagnostic, +31 stdlib libraries auto-loaded (1,273 → 1,427 RBS classes), `is_a?(C)` lexical-nesting constant resolution.

## Where the Work Resumes

The next cycle (`v0.1.6`) is accumulating on `master` (release pending). Slices landed so far (commits `3c99eed` → `8530856`):

- **Engine — `def Foo.method` explicit-receiver form recognised as singleton on the enclosing class.** Driven by the references/ruby/lib survey (open-uri.rb's `def OpenURI.check_options` etc.). `ScopeIndexer` + `StatementEvaluator` get a `def_receiver_targets_lexical_self?` helper that promotes `def C.method` to singleton when the receiver constant resolves to the lexical enclosing class. Cross-class form (`def Bar.x` inside `module Foo`) stays unpromoted. open-uri.rb: 24 → 15 errors. ✓
- **Engine — built-in Prism leaf-node fallback handlers.** Driven by `rigor check --explain references/ruby/lib` survey; eight new `PRISM_DISPATCH` entries cover `__FILE__` / `__LINE__` / backtick / `%x{...}` / `END { … }` / shareable-constant comment / `{ x: }` shorthand / `it` parameter. ~60 `:info` fail-soft fallback events removed across the Ruby core/stdlib reference tree. ✓
- **O4 Layer 3 slice 3** (graceful-degradation coverage diagnostic). ✓
- **`is_a?(C)` lexical-nesting constant resolution**. ✓
- **DEFAULT_LIBRARIES expansion** (+31 stdlib libraries; 1,273 → 1,427 RBS classes). ✓
- **[ADR-10] walker heuristic return-type extraction** (Phase B floor). ✓
- **[ADR-12] dry-rb packaging** accepted + `rigor-dry-types` slices 1+2+3 (Tier A foundation; canonical + nested categories + user-authored compositions; 25th worked plugin). ✓
- **[ADR-17] monkey-patch pre-evaluation** accepted + slices 1+2+3a+4 (`pre_eval:` plumbing + `ProjectPatchedMethods` registry + dispatcher tier + heuristic return-type + duplicate-declaration `:info` + glob support). ✓
- **[ADR-18] substrate per-call-site return-type DSL** proposed + slices 1+2+3+5 (`HeredocTemplate::Emit#returns_from_arg` + scanner extraction + fact-store lookup + `rigor-dry-struct` consumer manifest update — first worked end-to-end precision uplift through cross-plugin facts). ✓
- **Editor mode v1** — `rigor check` / `rigor type-of` gain paired `--tmp-file` / `--instead-of` flags + `BufferBinding` value object threaded through Runner / WorkerSession / pre-passes + `Cache::Store(read_only: true)` for concurrent-safe editor invocations + single-file scope (only the buffer produces per-file diagnostics) + Ractor pool auto-degrade to sequential. Seven-slice cut, design at [`docs/design/20260516-editor-mode.md`](design/20260516-editor-mode.md). Out-of-scope follow-ups (per-file diagnostic cache for "option B", project-context snapshot cache, multi-buffer, `--also`, LSP daemon) queued in ROADMAP § "Editor / IDE integration". ✓
- **Language Server v1** — `rigor lsp` subcommand + in-process Ruby LSP closing ADR-0's deferred commitment. Eight-slice cut, design at [`docs/design/20260517-language-server.md`](design/20260517-language-server.md). Covers `initialize` / `shutdown` / `exit` lifecycle + stdio JSON-RPC + BufferTable + `textDocument/didOpen/Change/Close` + `textDocument/publishDiagnostics` (debounced 200ms) + `textDocument/hover` + `textDocument/documentSymbol` + `workspace/didChangeWatchedFiles` + ProjectContext caching (warm Environment + read-only Cache::Store) + Debouncer (cooperative cancellation) + SynchronizedWriter (Mutex-wrapped STDOUT). New runtime dep `language_server-protocol ~> 3.17`. Packaging shape decided in [ADR-19](adr/19-language-server-packaging.md) (bundled in `rigortype`). ✓
- **Language Server v2** — type-aware hover + completion. Eight-slice cut, design at [`docs/design/20260517-lsp-hover-completion.md`](design/20260517-lsp-hover-completion.md). Hover gets per-Prism-node-class dispatch (CallNode → receiver + signature + return; Constant → FQN + singleton + defined-in; Local/Ivar → name + type + enclosing class; literals → clean Type/Erased + refinement-name surfacing). Completion ships `textDocument/completion` with `[".", ":"]` trigger characters: method completion for `obj.|`, constant-path completion for `Foo::|`, composite-receiver handling (Refined/Tuple/HashShape → underlying nominal; Union → intersection-of-methods; Intersection → union-of-methods), parse-recovery via sentinel-name patching for mid-edit `.` / `::` buffers. ✓
- **LSP follow-up cluster** — `textDocument/signatureHelp` (new `SignatureHelpProvider`; sentinel-patched `obj.foo(` recovery; activeParameter via comma count; advertises `signatureHelpProvider.triggerCharacters: ["(", ","]`) + hash-key completion for `HashShape` carriers (slice D1; `[:` sentinel patch returns `:foo` / `"bar"` keys as KIND_FIELD items) + user-facing [editor-integration guide](lsp-integration.md) (Neovim / VSCode / Helix / Emacs setup, troubleshooting, performance) + end-to-end spec spawning real `exe/rigor lsp` binary via `Open3.popen3`. ✓
- **LSP polish + new capabilities** — six small slices: signatureHelp multi-overload presentation (C2) + RBS comments in hover + signatureHelp (C3) + populated `SignatureInformation.parameters` (C4) + hover `range` field (E1) + new `textDocument/foldingRange` (F1) + new `textDocument/selectionRange` (G1). The LSP now advertises 9 capabilities (textDocumentSync / hoverProvider / completionProvider / signatureHelpProvider / documentSymbolProvider / foldingRangeProvider / selectionRangeProvider + workspace/didChangeWatchedFiles + workspace/didChangeConfiguration). ✓
- **`make verify` parallel-by-default** — commit `086e507` switched the spec phase from sequential `bundle exec rspec` to `rake spec_parallel` (parallel_rspec across `PARALLEL_TEST_PROCESSORS` workers). Wall time 217s → 60s on 12-core (3.6×). Rakefile gains a native `--exclude-pattern` for `runner_pool_spec.rb` (the RSpec config-side exclusion in spec_helper.rb didn't apply to parallel_rspec because it splits files before workers load spec_helper). `make verify-sequential` kept as the slow flake-resistant fallback; `make verify-parallel` retained as a backward-compatible alias. AGENTS.md verification-protocol entry updated. ✓
- **Spec helper — content-keyed sig dir + shared workspace** (CHANGELOG `[Unreleased]` § Performance). `runner_spec.rb` 39.6s → **25.4s (-36%)** isolated; `make verify` parallel 65.6s → **52.6s (-20%)** on 12-core. The persistent `Cache::Store` keys the RBS environment on `(path, sha256)`, so the prior helper's per-call `Dir.mktmpdir` forced every `analyze(sig: ...)` to rebuild env (~1.8 s); the new helper hashes `sig:` content into a stable path so identical sigs share one warm cache entry, and the source-only fast path reuses one per-process empty workspace. Closes "Spec-suite runtime breakdown (a)" through cache-key normalisation in the helper rather than the originally-sketched `before(:context)` Environment sharing — no changes to runner_spec.rb's group structure. Slice contained entirely in `spec/support/runner_helpers.rb`. ✓
- **Spec helper — opt-in shared cache in `plugin_helpers#run_plugin`** (CHANGELOG `[Unreleased]` § Performance). `sorbet_plugin_spec` 13.1s → **4.7s (-64%)** isolated (≈3× faster); integration sequential 90.2s → **78.6s (-13%)**. `PluginHelpers` gains a process-wide `shared_cache_store` + a `default_run_plugin_cache_store` instance method spec files override to opt in (`let(:default_run_plugin_cache_store) { :shared }`). Pattern is opt-in because cache I/O overhead exceeds env-build savings for spec files with 1–11 examples; only heavy spec files (sorbet at 48) recover the cost. Future heavy plugin specs can opt in via the same one-line `let`. Three integration specs (actioncable / actionmailer / rails-i18n) cannot opt in until their plugin cache descriptors include the project files they read from — captured as Open Engineering Item §6 above. ✓
- **LSP / editor mode — project-context pre-pass snapshot cache** (CHANGELOG `[Unreleased]` § Added). New `Rigor::Analysis::ProjectScan` value object + `Runner#prepare_project_scan(paths:)` builder + `Runner.new(prebuilt:)` adoption path. The LSP's `ProjectContext` lazy-builds the snapshot, drops it on `invalidate!` (watched-file / configuration change). `DiagnosticPublisher` passes it to every per-publish `Runner.new` so plugin `#prepare`, `Plugin::Loader.load`, `DependencySourceInference::Builder.build`, and the synthetic-method / project-patched scanners do not re-run per keystroke. The cold pre-pass cost is paid once per session generation; per-buffer publish narrows to buffer parse + check rules. Trivial-project gain is small (~10%); the win scales with plugin / substrate / dep complexity. Bonus correctness: the cached scanner observes the full project rather than buffer-only, so synthetic methods declared in other files become visible during buffer analysis — closing a known gap from the slice-7 LSP design. ✓
- **Plugin cache descriptor correctness fix** (CHANGELOG `[Unreleased]` § Added). New `Plugin::Base#glob_descriptor(roots, *patterns)` helper enumerates every glob-matched file with `:digest` `FileEntry` rows. Three plugin cache producers (actioncable's `:channel_index`, actionmailer's `:mailer_index`, rails-i18n's `:locale_index`) now thread that descriptor through `cache_for(..., descriptor: …)` so the cache key reflects project state BEFORE the producer runs rather than depending on the in-process `IoBoundary` read history. Closes Open Engineering Item §6 from earlier in this carryover. The three integration specs now opt in to the shared cache (proof the fix lands). ✓
- **LSP / editor mode — Environment sharing across publishes** (CHANGELOG `[Unreleased]` § Added). `Runner.new(environment:)` opt-in override + `Environment#attach_reporters!` per-publish reporter swap. The LSP `ProjectContext` now caches the FULL Environment (plugin / scan / bundler / collection axes all baked in) and passes it to every per-publish `Runner.new` so sequential `analyze_files` skips the per-call `Environment.for_project` build (10-50 ms saved on Rails projects with vendored gems). Per-publish reporter isolation handled via mutable `Reporters` slot — frozen env, mutable slot — so diagnostic events stay scoped to one publish. ✓
- **`examples/rigor-dry-schema/` slice 1** (CHANGELOG `[Unreleased]` § Added). Recognises `Foo = Dry::Schema.{Params,JSON,define} { ... }` declarations, publishes `:dry_schema_table` cross-plugin fact, resolves `value(Types::Email)` references through `:dry_type_aliases`. Twenty-sixth worked plugin. ✓
- **`examples/rigor-graphql/` slice 1** (CHANGELOG `[Unreleased]` § Added). Recognises `class T < GraphQL::Schema::Object` subclasses + `field :name, Type, null: ...` declarations, publishes `:graphql_type_table` cross-plugin fact. Closes the last pending Tier 3 ecosystem plugin per the Rails plugins roadmap. Metadata-recorder shape (not ADR-16 substrate). Twenty-seventh worked plugin. ✓
- **CLI editor-mode disk-backed `ProjectScan` snapshot cache — design note only**, [`docs/design/20260518-cli-disk-snapshot-cache.md`](design/20260518-cli-disk-snapshot-cache.md). Implementation pathway documented for the CLI shell-out niche (LSP already optimised). Five phases (Marshalable scan / key derivation / cache producer / Runner integration / FactStore snapshot API). Deferred until a concrete editor extension shells out to `rigor check --tmp-file` and reports the ~1s wall as UX pain. ✓ (design only)
- **rigor-dry-types slice 4 — transitive composition reference resolution.** Two-pass walk inside `AliasScanner#collect_compositions` with cycle detection. Closes the dry-types alias coverage at the WD13 floor — every recognisable user-authored composition (canonical / nested-category / `.constrained` / `.optional` / `.default` chains / multi-level `ManagerEmail = Email` chains) resolves to its underlying class. ✓
- **Design note — `rigor-dry-validation` slicing decision** ([`docs/design/20260517-dry-validation-slicing.md`](design/20260517-dry-validation-slicing.md)). Records the dependency tree (dry-schema before dry-validation), proposes a 5-slice plan, confirms NO ADR-3 amendment needed for validation itself, and frames the `Result[T, E]` / `Maybe[T]` carrier decision for dry-monads. ✓ (design only)
- **`examples/rigor-graphql/` slices 2a + 2b — list wrappers + `Schema::Enum`.** List wrappers (`field :tags, [String]`) recognised as `{type:, nullable:, list: true}`; multi-element / empty list literals drop. `Schema::Enum` subclasses contribute `:graphql_enum_table` (sorted value-name list). Manifest advertises both `:graphql_type_table` + `:graphql_enum_table`. ✓
- **`examples/rigor-dry-schema/` slice 2 — `each(<T>)` list recognition + symmetric `list:` slot.** Per-row value shape now `{type:, list:}` (`each` sets `list: true`; `filled`/`value`/`maybe` set `list: false`). Symmetric with `rigor-graphql`'s field-table shape so future cross-plugin consumers (rigor-dry-validation slice 2) can reason about list-vs-scalar uniformly. ✓
- **`examples/rigor-graphql/` slices 2c + 2d — `Schema::InputObject` + `Schema::Mutation`.** Extends to four `Schema::*` kinds. New helper `collect_arguments` shared between InputObject and Mutation parses `argument :name, Type, required: ...` rows (`required:` default `false` mirrors graphql-ruby's argument default polarity, opposite to `field`'s `null:`). Mutation rows carry both `arguments:` and `fields:` sub-tables. Plugin publishes four facts total (`:graphql_type_table`, `:graphql_enum_table`, `:graphql_input_object_table`, `:graphql_mutation_table`). ✓
- **`examples/rigor-rails/` meta-gem scaffold — Tier 1+2 Rails ecosystem umbrella.** Per ADR-12 WD1. `rigor-rails.gemspec` declares the seven Tier 1+2 sub-plugin dependencies + `lib/rigor-rails.rb` requires them all in one go. Gemfile-convenience only; `.rigor.yml` activation stays per-plugin. Twenty-ninth `examples/` entry. ✓
- **`examples/rigor-dry-validation/` slice 1 — Contract recognition + RBS overlay.** Walks for `class T < Dry::Validation::Contract` subclasses (full-path + lexical-Dry shapes; bare `< Contract` + same-tail different-root rejected). Publishes the sorted+frozen `:dry_validation_contracts` fact. Ships an RBS overlay (`sig/dry_validation.rbs`) typing `Contract#call → Result` + `Result#{success?, failure?, to_h, errors, []}` so `contract.call(input).to_h` chains resolve cleanly. Twenty-eighth worked plugin (now twenty-seven worked + one RBS bundle + one meta-gem = twenty-nine `examples/` entries). ✓

Natural entries for the remaining work (post v0.1.6 in-flight slices):

1. **Release-prep candidate.** With ADR-12 / ADR-17 / ADR-18 all at floor + worked-consumer state, v0.1.6 has shipped substantial user-visible surface. `bundle exec rake release` per [`.codex/skills/rigor-release-prep/SKILL.md`](../.codex/skills/rigor-release-prep/SKILL.md) is the natural next "ship state" decision; awaits explicit user authorisation.
2. **rigor-dry-types slice 4 — transitive composition references — LANDED.** CHANGELOG `[Unreleased]` § Added "rigor-dry-types slice 4". Two-pass walk inside `AliasScanner#collect_compositions` with cycle detection. The dry-types alias coverage is now complete at the WD13 floor — every recognisable user-authored composition (canonical / nested-category / `.constrained` / `.optional` / `.default` chains / transitive references) resolves to its underlying class.
3. **rigor-dry-rb continuation** ([ADR-12](adr/12-dry-rb-packaging.md) roadmap continuation; slicing decision in [`docs/design/20260517-dry-validation-slicing.md`](design/20260517-dry-validation-slicing.md)). **LANDED in v0.1.6**: dry-types slices 1+2+3+4 (full alias coverage), dry-schema slices 1+2 (Schema declaration recognition + `each(<T>)` list slot), dry-validation slice 1 (Contract recognition + RBS overlay typing `Contract#call → Result`). **Remaining (demand-driven)**: dry-schema slice 2+ surface beyond `each` (typed `result.to_h` synthesis via ADR-16 Tier C heredoc-template substrate — per-Contract precision uplift); dry-validation slice 2 (params-block typing via `:dry_schema_table` consumption) + slice 3 (`json { ... }` parity); dry-monads (still blocked on `Result[T, E]` / `Maybe[T]` carrier question — two routes documented (new `Rigor::Type::*` kinds vs `Union`-based workaround), decision deferred until concrete demand).
4. **ADR-18 follow-up slices.** Slice 4 (TraitRegistry parity for `returns_from_arg:`) and the chained-call argument extension (`Types::String.constrained(...)` → resolve through chain head) both stay demand-driven. The current floor handles `ConstantReadNode` / `ConstantPathNode` shapes which cover the canonical authoring case.
5. **ADR-17 follow-up slices.** Slice 3b (`Cache::Descriptor::PreEvalEntry`) deferred until measured pain. Slice 5 (full-project 2-pass discovery), slice 6 (plugin-API hook) stay demand-driven.
6. **Per-call return-type precision from gem source** (ADR-10 walker enhancement, option C lazy / on-demand). Phase B floor (heuristic literal-tail extraction) landed in `e40947c`; option C wires gem inference lazily at call-site request — substantial architectural change to the walker / dispatcher boundary, gated on demand.
7. **`rigor-graphql` slices 1+2a+2b+2c+2d LANDED.** Last pending Tier 3 ecosystem plugin closed. `Schema::Object` + list wrappers (`field :tags, [String]`) + `Schema::Enum` + `Schema::InputObject` + `Schema::Mutation` recognition; publishes FOUR cross-plugin facts. Metadata-recorder plugin shape (not an ADR-16 substrate consumer — see survey § "GraphQL-Ruby" for why). Future slices (resolver-method type-check, `<Type>.array` / `<Type>!` chain forms beyond the bracket form, string-form `field :foo, "User"` diagnostic, `Schema.execute(...)` result typing) stay demand-driven. CHANGELOG `[Unreleased]` § Added.
8. **O4 Layer 3 per-gem-version cache (slice 3 architecture)** — DEFERRED until measured pain. See "Open Engineering Items" below for the Ruby::Box future-direction context.
9. **ADR-16 demand-driven follow-ups** — (a) **slice 5b** Tier D engine integration (matched external files run with `self_type` narrowed + `bound_ivars` pre-bound); (b) full ADR-13 `Plugin::TypeNodeResolver` chain wiring for `returns:` strings (unlocks utility-type-shaped substrate returns like `Array[String]` / `Pick<T, K>`). Both pinned in ADR-16 § Implementation slicing footnotes; awaiting concrete plugin-author cases.
10. **Editor mode follow-ups** — see [`docs/design/20260516-editor-mode.md`](design/20260516-editor-mode.md) § "Out of scope for v1". The fast next step is the **project-context snapshot cache** so pre-passes (synthetic-method scanner, project-patched scanner, dependency-source walker) skip their walks when only the buffered file changed; the larger lever is the **per-file diagnostic cache** that upgrades editor mode from option A (single-file scope) to option B (PHPStan-shape: project scope with one substituted file + incremental diagnostic cache). Both demand-driven; first concrete editor-extension consumer of the v1 CLI surface naturally surfaces the priority.
11. **LSP follow-ups** — see [`docs/design/20260517-language-server.md`](design/20260517-language-server.md) (v1) + [`docs/design/20260517-lsp-hover-completion.md`](design/20260517-lsp-hover-completion.md) (v2). Highest-leverage queued items: (a) **Ractor pool dispatch** for parallel multi-buffer publishes (v1 slice-8 second half; needs `Analysis::Runner` refactor to accept a pre-built persistent Environment); (b) **`textDocument/definition`** (v1 slice 9; needs Reflection-side symbol index keyed on FILE:LINE); (c) **incremental `didChange` sync** (v1 slice 10; UTF-16 offset bookkeeping); (d) **`textDocument/signatureHelp`** (v2 natural complement; parse-recovery for `foo(|` is its own slice); (e) **hash-key completion** for `HashShape` carriers (the most type-driven completion Rigor could ship, gated on `hash[:|]` parse-recovery). Capabilities still out-of-scope (codeAction / rename / semanticTokens / inlayHint / snippets / bare-name / symbol / multi-overload / `completionItem/resolve`) each need their own design pass; priorities surface from concrete editor-integration demand.

## Open Engineering Items

Persistent items the next implementer benefits from seeing without re-reading the full thread. Items already absorbed into a released milestone are referenced through `CHANGELOG.md` rather than restated.

### Survey-driven (v0.1.5 cycle, all items closed)

The fourteen-project real-world Rails survey ran through three rounds during the v0.1.5 cycle. All items below are closed (full recap in `CHANGELOG.md` § `[0.1.5]`); kept here as a reference index of "what each ID is" for cross-doc lookups.

| ID | Status | Item |
| --- | --- | --- |
| O1 | landed (MVP, v2) | `examples/rigor-activesupport-core-ext/` opt-in RBS bundle for the top ~50 ActiveSupport `core_ext` selectors. v2 added `compact_blank` / `exclude?` / `index_with` / `Hash.from_xml` / `DateTime` calculations after the round-2 sweep. |
| O2 | substrate floor + precision promotion LANDED (Tier A/B/C engine + Tier D contract + Concern re-targeting + 3 worked consumer plugins + docs + slice 6 precision); slice 5b + ADR-13 resolver-chain wiring queued | Macro-template / heredoc-Ruby expansion. Shipped through [ADR-16](adr/16-macro-expansion.md) slices 1–5a + 7 (commits 584ae85…56706a5) + slice 6a-TierB / 6b-TierC (commits d174fff / d7b1943). Substrate is functionally complete at the WD13 floor PLUS precision promotion for the common case (Tier B → module RBS, Tier C → plain class name lookup). tDiary's `instance_eval` plugin pattern + Rails-generator `.rb`-as-ERB templates stay relevant for the deferred slice 5b (Tier D engine). Parameterised / utility-type substrate returns stay deferred to demand-driven ADR-13 resolver-chain wiring. |
| O3 | not-an-issue | Early-exit narrowing (`next if x.nil?` / `return if x.nil?`) already worked; survey residuals were `Object#blank?` / `#present?` / `#try`, which O1 covers. |
| O4 | layers 1+2+3 (slices 1+2+3) landed | Bundler awareness. Layer 1 (`bundler.bundle_path`) + Layer 2 (`.bundle/config` / `vendor/bundle/` auto-detect) + Layer 3 slice 1 (`Gemfile.lock` parse + bundle-sig filter, commit `3c99eed`) + Layer 3 slice 2 (`rbs_collection.lock.yaml` awareness, commit `46c9ec7`) + Layer 3 slice 3 (graceful-degradation `:info` diagnostic when locked gems have no RBS coverage). The per-gem-version cache descriptor (so `bundle update` invalidates only the affected gem's slice instead of the whole project) stays queued as a larger architectural change. |
| O5 | landed (`ac14c45`) | `Hash[K, V] <:= Enumerable[[K, V]]` parametrized-ancestor projection in `Inference::Acceptance#accepts_nominal_from_nominal`. Hand-rolled mapping for Hash → Enumerable today; general RBS-driven `definition.ancestors[i].args` projection deferred. |
| O6 | landed (`4698437`) | `MethodDispatcher::CONSTANT_CONSTRUCTORS` deep-share (Proc values were not shareable under shallow `.freeze`). Pool ≡ sequential on GitLab FOSS after fix. |
| O7 | landed (`3c4a7ff`) | `RbsLoader#env` memoises failure. Pre-fix, a single conflicting `signature_paths:` entry rebuilt env per AST node (390× / file, ~35 s for one controller). Post-fix: 0.15 s for 5 controllers (~550× speedup) with a single user-facing warning naming the offending file. Unblocks O4 Layer 3 — gem-shipped sigs that conflict with stdlib RBS now degrade gracefully. |

### Stdlib coverage expansion — open design questions (2026-05-16)

Driven by the v0.1.5 work to expand `Environment::DEFAULT_LIBRARIES` (commits `0a4ffea` + follow-on). Twenty-three additional stdlib libraries now auto-load (1,273 → 1,412 RBS classes). Two design judgment points surfaced and are deferred:

1. **Stdlib name conflicts with rigor's internal carriers (RESOLVED).** `singleton` (stdlib `Singleton` mixin) initially clashed with `Rigor::Type::Singleton` in lexical scope, surfacing an `undefined-method` false positive on `lib/rigor/type/singleton.rb#==`'s `is_a?(Singleton)` narrowing. Resolved via the lexical-nesting fix in `Inference::Narrowing#analyse_class_predicate`: the bare-name argument now resolves through the chain derived from `scope.self_type` (mirroring `Module.nesting`-driven constant lookup) so the nested `Rigor::Type::Singleton` wins over the top-level stdlib `Singleton`. `singleton` is now in `DEFAULT_LIBRARIES`. Broader lexical-resolution coverage (method-dispatch receivers, attribute access, etc.) is gated on further demand — the fix is surgical to the class-predicate narrowing surface.

2. **Stdlib RBS coverage gaps in upstream `rbs` gem.** `strscan` initially blocked Batch 2 because the bundled RBS only declares `StringScanner#[](Integer)`, while real Ruby supports `StringScanner#[](Symbol)` for named captures — which rigor itself uses in `lib/rigor/builtins/imported_refinements.rb:422,424`. The expedient resolution (response path (a) below) applied two `# rigor:disable argument-type-mismatch` directives at the affected call sites and moved `strscan` into `DEFAULT_LIBRARIES`, so users analysing parser code get StringScanner dispatch precision. The remaining response paths stay open as longer-term fixes:
   - **(a) Skip the library** — initially used in Batch 2; superseded by Batch 3.5 in favour of (a')
   - **(a')** **In-source `# rigor:disable` directives** at the gap-affected call sites + load the library. What landed in Batch 3.5. Trade-off: rigor's own code carries a maintenance burden when upstream RBS gets fixed (must remove the directives); the burden is bounded (two call sites in one file). This is the same pattern already used for `Bundler::LazySpecification#platform` in `lockfile_resolver.rb`.
   - **(b) Hand-author an RBS overlay** under rigor's own `sig/`. Per AGENTS.md § "RBS Authorship" the project policy is to prefer `rigor sig-gen` over hand-authored RBS, but "filling an upstream RBS shim gap" is arguably a different category from "generating from inference". A small focused overlay (`StringScanner#[](Symbol)` row) would let rigor's own code drop the disable directives.
   - **(c) Upstream the fix to `ruby/rbs`** and bump the gem. Highest-quality long-term path; needs cross-project coordination.

   The same pattern will recur for other stdlib RBS gaps as the library set expands. The decision tree is: when an upstream RBS gap is surfaced by a single internal call site, prefer (a') (disable directive); when it surfaces across multiple call sites or in user-facing code, escalate to (b) or (c).

### Session-end carryover (2026-05-17)

Notes for the next implementer beyond the slice-by-slice landed list:

1. **`make verify` is now parallel by default.** AGENTS.md verification protocol updated. If you see a parallel-only flake, fall back to `make verify-sequential` to confirm whether it's worker isolation or a real bug, then file. `runner_pool_spec.rb` is excluded from BOTH paths (sequential via `RSpec.config.exclude_pattern`, parallel via `parallel_rspec --exclude-pattern`); `RIGOR_INCLUDE_RACTOR_POOL=1` opts it back in (mirrored on both sides). Run `make test-ractor-pool` for repeatable pool-spec coverage in isolation.
2. **Spec-suite hot spots — partially optimised (2026-05-17).** Per-file isolated wall times before the helper-side fix: `runner_spec.rb` 42.6s, `cli_spec.rb` 19.7s, `sorbet_plugin_spec.rb` 14.8s, LSP specs combined ~11s, everything else <5s individually. The slowest worker (which carries `runner_spec.rb`) was bottlenecking the parallel run at ~45s wall time. The **content-keyed sig dir + shared workspace** lever in `spec/support/runner_helpers.rb` (CHANGELOG `[Unreleased]` § Performance) cut `runner_spec.rb` to **25.4s isolated** and `make verify` parallel wall time **65.6s → 52.6s (-20%)**. The remaining ROADMAP § "Performance / scalability → Spec-suite runtime breakdown" levers stay queued: (a) `before(:context)`-style Environment sharing in `runner_spec.rb` (still possible for groups whose configs don't change Environment-relevant state — but the helper-side fix already cleared the biggest sig-related component, so the remaining win is smaller); (b) `Analysis::Runner.run_source(source:, path:, ...)` in-memory entry point (clean public API for embedders such as LSP / editor mode; ~5% test-suite win on top of the helper fix). LSP-spec splitting (`make verify-lsp` or `make verify-no-lsp`) was investigated and REJECTED — LSP is only ~5% of sequential / ~1% of parallel time, splitting buys nothing meaningful.
3. **LSP v1 + v2 + follow-ups now ship 9 advertised capabilities.** The lsp subsystem is feature-complete for the "keystroke-fast linting + hover + completion + signatureHelp + folding + selection" loop. Remaining LSP work (codeAction / rename / semanticTokens / inlayHint / definition / incremental sync / Ractor pool dispatch) is in ROADMAP § "Editor / IDE integration" — each needs its own design pass; priorities surface from concrete editor-integration demand.
4. **v0.1.6 is ship-ready as-is.** The accumulated work (ADR-12 / ADR-17 / ADR-18 floors + worked-consumer state + editor mode v1 + LSP v1/v2 + LSP polish + spec-suite parallel default) is purely additive — no behaviour change for existing CLI consumers. `bundle exec rake release` per the `rigor-release-prep` SKILL is the natural next decision; awaits explicit user authorisation.
5. **Plugin packaging ADRs to keep in mind when adding `rigor-*` work.** [ADR-12 (dry-rb)](adr/12-dry-rb-packaging.md) sets the per-gem + meta umbrella pattern; [ADR-19 (LSP)](adr/19-language-server-packaging.md) decides the LSP stays bundled in `rigortype` (not a separate `rigor-lsp` gem) with explicit trigger conditions for re-evaluation. Both ADRs document the "no premature gem split" stance the project takes.
6. **Plugin `cache_for(...)` descriptor completeness — LANDED (2026-05-17).** Fixed in CHANGELOG `[Unreleased]` § Added "Plugin cache descriptor correctness fix". New `Plugin::Base#glob_descriptor(roots, *patterns)` helper enumerates every glob-matched file with `:digest` `FileEntry` rows; the three affected plugins (actioncable / actionmailer / rails-i18n) now thread that descriptor through `cache_for(..., descriptor: …)` so the cache key reflects project state BEFORE the producer runs rather than depending on the in-process `IoBoundary` read history. The three integration specs now opt in to the shared cache via `let(:default_run_plugin_cache_store) { :shared }` and pass — earlier the same opt-in surfaced stale-cache regressions, which is exactly the bug this fix addresses.

### Session-end carryover (2026-05-18)

Notes for the next implementer beyond the slice-by-slice landed list, accumulated on top of the 2026-05-17 carryover:

1. **dry-rb foundation pair is now complete in v0.1.6.** `rigor-dry-types` (slices 1-4) + `rigor-dry-schema` (slices 1+2) + `rigor-dry-validation` (slice 1) all landed; `rigor-dry-struct` (Tier C substrate consumer) already shipped in v0.1.5 + v0.2.0 ADR-18 uplift. Cross-plugin shape symmetry is intentional: schema rows + graphql field/arg rows share a `{type:, list:, …}` slot pattern so a future `rigor-dry-validation` slice 2 (typed `result.to_h` synthesis) can consume `:dry_schema_table` uniformly. Remaining dry-rb work in priority order: dry-schema slice 2+ → dry-validation slice 2 → dry-validation slice 3 → dry-monads (after ADR-3 amendment). All demand-driven.
2. **Tier 3 ecosystem plugins are now complete in v0.1.6.** `rigor-graphql` (Tier 3D) closes the last pending Tier 3 slot. The plugin family inventory: Tier 1 (4 plugins) + Tier 2 (3 plugins) + Tier 3 (6 plugins including graphql) + dry-rb (4 plugins) + opt-in bundles (2 entries) + meta-gem (1 entry) = **twenty-nine `examples/` entries** (twenty-seven worked plugins + one RBS-only bundle + one meta-gem).
3. **`rigor-rails` meta-gem scaffold is publication-ready.** The gemspec + entry point + README + 5-case integration spec all in place. Activation in the wild waits on the Tier 1+2 sub-plugins' subtree-split + RubyGems publish per the [`rigor-plugin-author`](../.codex/skills/rigor-plugin-author/SKILL.md) SKILL. Per ADR-12 WD1 the umbrella is Gemfile-convenience only — `.rigor.yml` activation stays per-plugin.
4. **v0.1.6 is ship-ready as-is.** The accumulated work (ADR-12 / ADR-17 / ADR-18 floors + worked-consumer state + editor mode v1 + LSP v1/v2 + LSP polish + LSP performance trifecta (`Analysis::ProjectScan` pre-pass cache + Environment sharing + plugin cache descriptor correctness fix) + spec-suite performance trifecta (parallel-by-default + content-keyed sig dir + plugin_helpers opt-in shared cache) + four new ecosystem plugins (dry-schema, graphql, dry-validation) + meta-gem scaffold + two design notes) is purely additive — no behaviour change for existing CLI consumers. `bundle exec rake release` per the `rigor-release-prep` SKILL is the natural next decision; awaits explicit user authorisation.
5. **CLI editor-mode disk-backed snapshot cache is fully designed but unimplemented.** [`docs/design/20260518-cli-disk-snapshot-cache.md`](design/20260518-cli-disk-snapshot-cache.md) records the 5-phase implementation pathway (Marshalable scan / key derivation / Cache::Store producer / Runner integration / FactStore snapshot API). Implementation deferred until a concrete editor extension shells out to `rigor check --tmp-file` and reports the ~1s wall as UX pain. The LSP path already covers 90%+ editor cases at ≤5 ms / publish, so this slice is a niche win awaiting demand.
6. **[ADR-20 Lightweight HKT](adr/20-lightweight-hkt.md) — slices 1 + 2a + 2b + 2c + 2d + 2e + 3 + 6 + § D3 (programmatic + parser + membership) + symbolize_names + YAML.safe_load + permitted_classes post-reduce + CSV.parse / CSV.read + kv-form refactor + bundled body via parser + parser arg-list union fix + Ractor isolation fixes + lazy hkt_registry holder + handbook chapter 12 ALL LANDED in v0.1.6 — substantial ADR-20 implementation complete.** **32 commits this session** (`0dbb388` through `a8932a1`); 185 HKT-dedicated specs (181 + `JSON.load_file` follow-up); `make verify` clean at 3748 examples / 0 failures / 598 lint files / rigor check pass (6 → 3 self-check warnings after the bool-polymorphism + HashShape-narrowing fixes; Steep lenient profile also clean, with two scratch-ivar nil-guard cleanups landed for code-quality parity). **External-codebase analysis driven engine fix** (`88e6534`): parameter default value expressions now execute under the method's body scope (instance `self_type`), closing 97.2% of false positives on `prism-1.9.0` (938 → 26 errors) and 22.7% on `rbs-4.0.2` (255 → 197). The bug never surfaced on rigor's self-check because the `def copy(x: self.x)` idiom is 0 in rigor lib/examples/spec. **CLI-verified end-to-end shapes**: `JSON.parse(s)` → `Array[json::value[String]] \| Float \| Hash[String, json::value[String]] \| ...`; `JSON.parse(s, symbolize_names: true)` → `Hash[Symbol, json::value[Symbol]]`; `YAML.safe_load(s, permitted_classes: [Date])` → `Date \| ...` added; `Psych.safe_load` parity; `CSV.parse("a,b\n1,2\n")` → `Array[Array[String \| nil]]`. **User-authoring loop closed at three independent levels**: (a) `%a{rigor:v1:hkt_register / hkt_define}` directives in shipped `.rbs` files (via `Environment.for_project` scan), (b) plugin manifest `hkt_registrations: [...]` / `hkt_definitions: [...]` (via `Plugin::Registry#hkt_overlay_registry`), (c) hardcoded Rigor-bundled `Builtins::HktBuiltins` for stdlib methods. Body grammar covers full ADR-20 § D3: union + atoms + nominal_app + app_ref + param + conditional (`<:`, `==`, `in [...]` tests) with trinary reducer verdict (yes / no / maybe-widens-to-union per WD7). Merge order in `env.hkt_registry` (last-write-wins): builtins → plugin overlay → RBS env scan. **Remaining ADR-20 work** (all demand-driven, none scheduled): **Slice 4** (dry-monads `Result[T, E]` / `Maybe[T]` carriers — first multi-arg HKT validation, blocked on ADR-3 amendment per `project_dry_plugins_roadmap` memory); **Slice 5** (sugar via recursive `type` aliases — gated on user feedback that the explicit `%a{...}` form is too verbose); **pattern-matching with binder extraction** (lisp-eval needs `(E <: [:+, A, B] ? lisp_type[A] : ...)` shape with `A` / `B` as new bindings; biggest remaining gap, needs its own design pass); **rigor-lisp-eval demo migration** (gated on binder extraction); growing `METHOD_RETURN_OVERRIDES` to more stdlib methods (`JSON.load_file` / `JSON.load_file!` landed in `9013fe7`; `Marshal.load` truly returns Top so no HKT envelope applies; `JSON.[]` is parse-or-generate polymorphic which needs first-arg discrimination not yet supported; demand-driven for further additions). The next implementer should treat ADR-20 as substantially complete and pick from the remaining list only when a concrete use case surfaces.

### Pre-survey persistent items

-2. **External-codebase analysis: business-use SDKs (2026-05-18).** Ran `rigor check lib` against three production Ruby SDKs cloned shallowly into `/tmp/sdks/`: `facebook-ruby-business-sdk` (1,220 files), `google-api-ruby-client/google-apis-core` (25 files), `google-cloud-ruby/google-cloud-storage` (27 files, sparse). Triaged 79 errors + 8 warnings across the three. **Real bugs surfaced**: 6 files in `facebook/lib/facebook_ads/ad_objects/` (auto-generated) contain literal Ruby syntax errors — `has_edge : do |edge|` — that even `ruby -c` rejects. **Engine improvements driven by the analysis**: (β) cross-file class discovery pre-pass LANDED in `a8932a1` — `ScopeIndexer.discovered_classes_for_paths` walks every project file's `class Foo` declarations and seeds the project-wide table into each per-file `default_scope`, closing 5 false-positive `singleton(File)` errors on google-cloud-storage where `Google::Cloud::Storage::File` is a user-defined class shadowing stdlib `::File`. Modules are intentionally excluded from the seed (rigor self-check exposed that registering modules surfaces a separate `module_function` fall-through limitation; documented as queued follow-up). **Queued engine items** (not yet implemented): (α) `x ||= expr` non-nil narrowing inside closures — 4 google-apis-core false positives observed (`download_offset` read as `nil` after `||= 0` inside a `proc do |chunk|` block); simple-case reproducers narrow correctly, so the failure mode involves nested closure interaction that needs deeper investigation. Cross-file `discovered_methods` pre-pass (mirror of (β) for methods) would unlock module_function support and further close user-defined-method false positives on cross-file calls.

-1. **External-codebase analysis: Steep 2.0.0 (2026-05-18).** Ran `rigor check lib` against `tool/steep/vendor/bundle/ruby/4.0.0/gems/steep-2.0.0/lib` (141 files) — surfaced 107 diagnostics (92 errors + 15 warnings) across 28 files. Triaged by category: (A) 36 × Struct.new dynamic class generation unmodelled; (B) ~6 × `is_a?(RBS::AST::Declarations::Base)` narrowing to abstract base hides mixin methods (`annotations`, `location`); (C) ~7 × assignment-in-`or` / `||` narrowing not propagated through `x = foo or raise` idiom; (D) ~5 × closure-block mutation not propagated (same root as item 0 below — covers `each { flag = true }`, `OptionParser#parse!` capture mutation); (E) ~10 × **real Steep dead-assignment bugs** (refactor leftovers in `type_construction.rb` at L656, L2875, L2878, L5077; also `goto_service`, `content_change`, `source`, `locator`, `ast/types/helper`, `ast/types/name`); (F) ~40+ × RBS coverage gaps (Steep ships no `sig/` in its gem). **Queued engine improvements observed** (none scheduled — all demand-driven): (C) **assignment-in-`or` narrowing** for `x = foo or raise` / `x = foo || raise` idioms — small-to-medium implementation (`Inference::Narrowing#analyse_or` extension, detect `assignment_to_local? && non_returning?` LHS+RHS pair, narrow truthy scope's write target by removing falsey portion of RHS). Demand measurement: 0 occurrences in `rigor lib/`, 0 in `examples/`, 0 in `spec/`; 7 in Steep. Internal demand is below the project's "demand-driven" bar — record and queue rather than implement. **Real Steep findings** (category E) are upstream reports rather than rigor changes; Steep's own lenient profile misses them because `Ruby::UnusedLocal` is below `:hint` by default — a data point that rigor's `flow.dead-assignment` rule at `:warning` severity has stronger bug-surface power than Steep's default profile on the same idiom.

0. **Self-analysis: loop-mutation tracking for `flow.always-truthy-condition` (queued engine improvement).** `rigor check lib` after the bool-polymorphism + HashShape-narrowing fixes (commits `7c4efce` + `dd917b5`) still surfaces 3 warnings, all of the same shape: `arr = [seed]; while ...; arr << x; end; if arr.size == N` / `arr.empty?` is reported as always-truthy because `Inference::Narrowing` doesn't reflect the loop body's `<<` / `push` mutation into the size/empty narrowing. Affected sites: [hkt_body_parser.rb:140](lib/rigor/inference/hkt_body_parser.rb:140), [hkt_body_parser.rb:307](lib/rigor/inference/hkt_body_parser.rb:307), [hkt_registry.rb:212](lib/rigor/inference/hkt_registry.rb:212). Fix lives in the mutation-effect model under `docs/type-specification/control-flow-analysis.md` § "mutation effects" — likely a medium-sized engine change. Stays queued until concrete demand surfaces (the warnings here are the demand signal; their existing site count is small enough that the engine cost has to be paid).

1. **Sig-gen `update_existing` does not yet collapse sibling parent / child class blocks.** Gap (c)'s tree-builder fix lives in `Writer#render_new_file` (the create-new path). When updating an existing target file, `merge_class` still resolves each candidate's `class_name` independently — if both `Foo::Bar` and `Foo::Bar::Child` decls already exist as flat siblings, sig-gen leaves them flat. Re-flowing an existing file into the nested layout would require parsing the existing decl tree and rewriting it, which is out of scope for a follow-up fix. Users who want the canonical nested layout regenerate from scratch (delete the target sig file and rerun).
2. **In-memory `Analysis::Runner.run_source` entry point (test-only perf follow-up).** The `RunnerHelpers#analyze` test helper materialises a tmpdir per call (write source file, chdir, run, clean up). At ~25-50ms per call × hundreds of runner-spec calls, that's a real share of suite wall-clock that an in-memory entry point could eliminate. Sketch: add `Runner.run_source(source:, path: "code.rb", environment:, config:)` that bypasses path expansion and accepts a `{path => bytes}` virtual file table. The helper would call it for the `analyze(source: "...")` shape (no files / sig). Expected delta: ~5% sequential, ~3% parallel — not worth doing standalone, but a natural complement if test-suite expansion continues.
3. **Fork-based file-level parallelism for `rigor check`.** Stackprof of warm `rigor check lib` shows ~50% inference, ~22% `Marshal.load`, ~17% GC. The Phase 4b Ractor path handles the parallelism story for v0.1.5; a fork-based path remains a parallel (non-exclusive) option for hosts where Ractors are unavailable or where COW sharing of pre-warmed `Environment` blobs would beat per-Ractor env build. Implementation sketch: `Runner#run` forks workers per file-chunk, each writes diagnostics to a pipe, parent re-assembles in original path order.

## Reading Order for a Returning Implementer

The default goal for the next session is **either cut the v0.1.6 release** (with ADR-12 / ADR-17 / ADR-18 all at floor + worked-consumer state) **or continue the cycle** with one of the natural entries above. Read in this order:

1. `CHANGELOG.md` `[Unreleased]` section — accumulates v0.1.6 work as it lands.
2. [`docs/ROADMAP.md`](ROADMAP.md) § "v0.1.6 — accumulating on master" — the cycle's ADR-12 / ADR-17 / ADR-18 envelope.
3. [`docs/adr/12-dry-rb-packaging.md`](adr/12-dry-rb-packaging.md), [`docs/adr/17-monkey-patch-pre-evaluation.md`](adr/17-monkey-patch-pre-evaluation.md), [`docs/adr/18-substrate-per-call-site-return-type.md`](adr/18-substrate-per-call-site-return-type.md) — the three v0.1.6 ADRs (12 accepted, 17 accepted, 18 proposed-and-implemented).
4. [`docs/adr/16-macro-expansion.md`](adr/16-macro-expansion.md) — the substrate ADR-18 amends; orient against the existing slice plan + WD13.
5. [`docs/adr/9-cross-plugin-api.md`](adr/9-cross-plugin-api.md) — `Plugin::FactStore` is the channel ADR-18 reads from / `rigor-dry-types` writes to.
6. [`docs/adr/10-dependency-source-inference.md`](adr/10-dependency-source-inference.md) — ADR-10 walker (gem-source inference); the heuristic return-type extractor (`Phase B`, `e40947c`) is reused by `ProjectPatchedScanner`.
7. [`examples/README.md`](../examples/README.md) — comparison table over twenty-nine `examples/` entries (twenty-seven worked plugins + one RBS-only bundle + one Gemfile-convenience meta-gem). v0.1.6 additions: `rigor-dry-types` slice 4 expansion + new plugins `rigor-dry-schema`, `rigor-graphql`, `rigor-dry-validation`, plus the `rigor-rails` meta-gem scaffold.
8. [`docs/notes/20260515-real-world-rails-survey.md`](notes/20260515-real-world-rails-survey.md) — the survey that drove v0.1.5's production-rigor improvements; still authoritative when the next batch of real-world projects gets analysed.
9. [`docs/adr/15-ractor-concurrency.md`](adr/15-ractor-concurrency.md) + [`docs/design/20260514-ractor-migration.md`](design/20260514-ractor-migration.md) — Ractor migration contract; relevant when adding plugin surfaces that need shareability review (ADR-18 was a pass).
10. [`.codex/skills/rigor-plugin-author/SKILL.md`](../.codex/skills/rigor-plugin-author/SKILL.md) — playbook for authoring a new plugin (used as template for `rigor-dry-types` slice 1).
11. [`docs/internal-spec/public-api.md`](internal-spec/public-api.md) — public-vs-internal stability boundary. Cross-reference `spec/rigor/public_api_drift_spec.rb` before extending any pinned namespace.
12. [`docs/adr/2-extension-api.md`](adr/2-extension-api.md) and [`docs/adr/7-v0.1.0-slice-decisions.md`](adr/7-v0.1.0-slice-decisions.md) — the binding design and per-slice working decisions for the v0.1.0 plugin contract that v0.1.x extends.
13. [`docs/adr/3-type-representation.md`](adr/3-type-representation.md) Working Decisions — OQ1 / OQ2 / OQ3 outcomes still bind the type-object public surface plugins consume.

Key surfaces to start from for common follow-up work:

- **ADR-18 substrate amendment**: `lib/rigor/plugin/macro/heredoc_template.rb` (Emit + ReturnsFromArg), `lib/rigor/inference/synthetic_method_scanner.rb` (`resolve_emit_return_type` + `argument_source_representation` + `resolve_returns_from_arg`), `lib/rigor/analysis/runner.rb#run` (plugin prepare reordering + `shared_fact_store`).
- **ADR-17 pre-eval pipeline**: `lib/rigor/inference/project_patched_methods.rb` (registry), `lib/rigor/inference/project_patched_scanner.rb` (pre-pass + duplicate-declaration), `lib/rigor/inference/method_dispatcher.rb#try_project_patched_method` (dispatcher tier), `lib/rigor/configuration.rb#expand_pre_eval_entries` (slice-4 glob).
- **ADR-12 dry-rb foundation**: `examples/rigor-dry-types/lib/rigor/plugin/dry_types.rb` + `dry_types/alias_scanner.rb` (canonical + nested + compositions); `examples/rigor-dry-struct/lib/rigor/plugin/dry_struct.rb` (consumer manifest for `:dry_type_aliases`).
- **O4 Bundler-awareness surface (unchanged since v0.1.5)**: `lib/rigor/environment/bundle_sig_discovery.rb`, `lib/rigor/environment/lockfile_resolver.rb`, `lib/rigor/environment/rbs_collection_discovery.rb`, `lib/rigor/environment/rbs_coverage_report.rb`.
