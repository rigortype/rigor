- **[inference]** A block passed to `each_with_index`, `detect`, `map` or another eager Enumerable method on an `IO`, `File` or `StringIO` now gets the same loop-body re-narrowing as `each_line`. A flag set in one `case` arm and tested in another no longer makes the method's caller report `flow.always-truthy-condition` on correct code. A local written in such a block also keeps its type after the call instead of degrading to `Dynamic[top]`, so a real nil-receiver diagnostic can now follow it. ([#1308](https://github.com/rigortype/rigor/pull/1308))
