- **[inference]** A hash literal given a default with `default=` or `default_proc=` no longer reads a missing key as `nil`, so `counts = { a: 1 }; counts.default = 0; counts[:b] + 1` no longer reports an undefined method on `nil`, while `counts[:a]` still reads `1`; a String-keyed literal after `compare_by_identity` no longer claims its literal key still finds the pair. [#1280](https://github.com/rigortype/rigor/pull/1280)
