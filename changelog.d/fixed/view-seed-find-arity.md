- **[plugins]** rigor-actionpack no longer seeds a view's `@ivar` as a single model when the controller assigned it from a call that returns, or may return, several records — `Model.find(a, b)`, which rigor-activerecord types as `Array[Model]`, as well as `Model.find([1, 2])`, `Model.find(*ids)`, a `find` with a block, and `Model.create([…])`; the ivar reads `Dynamic` instead. [#1323](https://github.com/rigortype/rigor/pull/1323)
