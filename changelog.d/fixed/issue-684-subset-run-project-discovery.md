- **[check]** `rigor check path/to/one_file.rb` now reports what a whole-project run reports for that file: the cross-file discovery pass spans the configured project even when the analysis targets an explicit file list, so a method defined outside the list no longer draws a false `call.undefined-method`. ([#684](https://github.com/rigortype/rigor/issues/684), [#734](https://github.com/rigortype/rigor/pull/734))
