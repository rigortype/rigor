- **[engine]** The return-memo store gate keeps one criterion instead of two: the fixpoint-consult half never fired (its counter had no writer since the day it was added) and the transient-event log already covers the case it was meant to catch, so it is gone along with the `consult-tainted` column of the `RIGOR_BUDGET_TRACE` return-memo profile. Diagnostic output is unchanged ([#881](https://github.com/rigortype/rigor/pull/881)).
