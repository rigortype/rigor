- **[rigor check]** A plugin whose manifest raises while contributing Lightweight HKT registrations now surfaces once per run as an `rbs.coverage.hkt-scan-failed` error naming that plugin, with the run continuing over the bundled registrations and your own `.rbs` overlay, instead of aborting with an uncaught exception. ([#791](https://github.com/rigortype/rigor/issues/791), [#802](https://github.com/rigortype/rigor/pull/802))
