- **[plugins]** `rigor-activerecord`'s `ActiveRecord::Relation[Elem]` now stands down against an `rbs collection install`'s non-generic `Relation` on cached runs too — 0.3.8 applied the stand-down only under `--no-cache`, so every default run still failed the class's definition build and read every call into a relation as `Dynamic[top]` — the stood-down file is no longer reported as a quarantined signature, and the stand-down is reported as `rbs.coverage.plugin-signature-stood-down` (info) naming both files. ([#610](https://github.com/rigortype/rigor/issues/610), [#848](https://github.com/rigortype/rigor/pull/848), thank you @n-rodriguez!)
