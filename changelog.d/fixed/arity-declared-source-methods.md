- **[check]** `call.wrong-arity` now fires on a project-defined method that also carries a trustworthy `sig/` declaration or has any inline `# @rbs` / `#:` type authored on it (parameter or return alike), instead of silently exempting every source-defined method regardless of its declared arity; a method with no authored type at all — including an unannotated sibling in a file `rigor-rbs-inline` otherwise annotates — stays silent. ([#999](https://github.com/rigortype/rigor/pull/999))
