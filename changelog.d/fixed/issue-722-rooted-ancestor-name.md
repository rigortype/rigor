- **[types]** `class Sub < ::Base` now inherits the top-level `Base` even inside a namespace that defines its own `Base`: a rooted ancestor name was recorded without its root and then resolved through the enclosing nesting, so the subclass inherited the shadow. ([#722](https://github.com/rigortype/rigor/issues/722), [#637](https://github.com/rigortype/rigor/issues/637), [#754](https://github.com/rigortype/rigor/pull/754))
