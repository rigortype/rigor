- **[plugins]** rigor-actionpack now follows Action View's `.js` → `.html` partial lookup, so a `.js.erb` template that renders an HTML-only partial reaches that partial's effects instead of keeping a `template-not-analysed` taint — and it reads (without compiling) the non-ERB views beside it, so the lookup stops where a Haml or Builder template is what Rails runs. ([#1070](https://github.com/rigortype/rigor/pull/1070))
