- **[rigor coverage]** `--protection --mutation` no longer reports mutants as survivors when Rigor's own analysis crashed while measuring them, and no longer passes `--threshold` on a file it could not measure at all: those mutants go to the `harness_errors` bucket, the effectiveness percentage is withheld rather than printed as 100%, and the command exits non-zero naming the unmeasured files ([#686](https://github.com/rigortype/rigor/issues/686)).
