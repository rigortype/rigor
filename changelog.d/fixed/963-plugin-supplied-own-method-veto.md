- **[inference]** A same-named top-level `def` no longer binds ahead of a plugin-supplied member on an implicit-self call: the own-method veto now asks each loaded plugin (`Plugin::Base#supplies_method?`, precise per-model in `rigor-activerecord` — column readers, `?` predicates, associations, aliases, finders, scopes) and the synthetic-method index, so `title.upcase` inside `class Post < ApplicationRecord` reaches the column, while a name no class, plugin, or ancestor defines still binds the top-level `def` and still fires. [#963](https://github.com/rigortype/rigor/issues/963)
