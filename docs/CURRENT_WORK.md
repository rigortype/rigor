# Current Work — Inference Engine Checkpoint

This is a transient bookmark used to break a long implementation thread into reviewable chunks. The **normative** contracts and slice roadmap remain in [`docs/internal-spec/inference-engine.md`](internal-spec/inference-engine.md) and [`docs/adr/4-type-inference-engine.md`](adr/4-type-inference-engine.md). The forward-looking commitment envelope (active cycle + queued work) lives in [`docs/ROADMAP.md`](ROADMAP.md); the released-version record is `CHANGELOG.md`. If this file disagrees with any of those, the spec / ADR / roadmap binds and this file is out of date.

## Status

**v0.1.7 released (2026-05-20).** Slice-by-slice recap in `CHANGELOG.md` § `[0.1.7]` (headline: ADR-22 baseline mechanism slices 1 + 2). The active cycle is now accumulating in `CHANGELOG.md` § `[Unreleased]` — themed around the **Mastodon-survey diagnostic-quality push**: see § "Where the Work Resumes" below.

### Where the Work Resumes — `[Unreleased]` cycle (2026-05-20)

The current cycle is a real-world false-positive reduction pass driven by the [`docs/notes/20260519-oss-library-survey.md`](notes/20260519-oss-library-survey.md) Mastodon survey. **8 commits ahead of `origin/master`, NOT pushed** (push + release user-gated per AGENTS.md). Landed so far:

1. **ADR-15 Amendment — fork-based worker pool replaces the Ractor pool.** A CI segfault root-caused to two independent Ractor-pool defects on Ruby 4.0.x (CRuby heap-UAF in `rb_vm_ci_lookup`, filed as **Ruby Bug #22075**; plus a deterministic `Ractor::IsolationError`). New `Runner#analyze_files_in_fork_pool` is the active `workers > 0` backend; the Ractor pool survives behind `RIGOR_POOL_BACKEND=ractor`. Investigation note: [`docs/notes/20260520-ractor-pool-cruby-uaf.md`](notes/20260520-ractor-pool-cruby-uaf.md). Commit `86ed912`.
2. **[ADR-23](adr/23-diagnostic-triage-command.md) `rigor triage` subcommand (slices 1+2).** Rule-ID distribution + per-file hotspots + six-recogniser heuristic-hint catalogue. Read-only / advisory. Commits `9b58655`, `9ed624b`.
3. **Mastodon FP cluster fixes (worked in order):**
   - **Cluster 3 — stdlib RBS coverage gaps.** `references/rbs` widened (`StringScanner#[]`, `Resolv#initialize`) on PR branch `widen-strscan-resolv-stdlib-sigs` — branch push + upstream PR creation are the user's task. Plus `Hash#deep_transform_keys!` family added to `rigor-activesupport-core-ext` (commit `e7ef6ef`).
   - **Cluster 1 — `belongs_to` non-nullable by default.** `rigor-activerecord` v0.1.0 → v0.2.0; `belongs_to` accessors now type `Nominal[Target]` not `Target?` unless `optional: true`. Commit `bdbd46c`.
   - **Cluster 2 — explicit-receiver calls resolving to private fallback methods.** `Favourite.select(:col)` mis-resolved through the user-class fallback to the private `Kernel#select` (`-> Array[String]`); the wrong `Array[String]` cascaded into `undefined-method` / `argument-type-mismatch` FPs. `try_user_class_fallback` now passes `public_only: true` for explicit non-`self` receivers; `RbsDispatch` skips `:private` methods under that flag. Commit `fb807fb`. **DONE.**
   - **Cluster 1b — `fail_with_message(...) if x.nil?` guard helper does not narrow** (≈42 `possible-nil-receiver` in `lib/mastodon/cli/accounts.rb`). Root cause: rigor does not resolve implicit-self method calls inside a method body — they type `Dynamic[top]`, so the guard helper's `bot` return is invisible. Designed as **[ADR-24](adr/24-self-method-call-resolution.md)** (proposed, 7 working decisions, 4 implementation slices). Commit `3fffb3e`. **Not yet implemented — ADR-24 slice 1 is the natural next entry.**

### Next entry for the returning implementer

- **Cluster 4 — flow-folding warnings** (`flow.always-truthy-condition` / `always-falsey`): the last untriaged Mastodon cluster. Inspect the residual warnings on `/tmp/mastodon-bench` and decide real-bug vs FP. *(Was the next pending task when this checkpoint was written.)*
- **ADR-24 implementation** — slice 1 (same-class implicit-self call resolution → adopt the callee's inferred return type) is the highest-value follow-up; it closes the ≈42-diagnostic cluster-1b FP cluster once slices 1+5+6 land. See ADR-24 § "Implementation slicing".
- **`rigor triage` slices 3+4** (SKILL integration / structured `Diagnostic` fields) remain demand-driven.

v0.1.5 themes (frozen on tag, full detail in CHANGELOG):

1. **ADR-15 Ractor migration end-to-end** (Phases 1, 2a, 2b, 3a, 4a, 4b, 4b.x, 4c) + spec-suite performance wins (162s → 27s on 12-core via `Cache::Store` thread-safe + in-process memo + `parallel_tests`).
2. **Real-world Rails / Ruby survey + production-rigor improvements.** Fourteen projects (31,840 files) drove `plugins/rigor-activesupport-core-ext/` (opt-in RBS bundle, −75 % total diagnostics), `data/vendored_gem_sigs/` (six native-extension gems by default), assignment-in-condition narrowing, four deep-shareability Ractor follow-ups, `Hash[K, V] <:= Enumerable[[K, V]]` projection, `CONSTANT_CONSTRUCTORS` Proc-share fix, `RbsLoader#env` failure-memo (~550× speedup).
3. **[ADR-16](adr/16-macro-expansion.md) macro / DSL expansion substrate** — floor + precision promotion landed; four-tier substrate (Tier A block-as-method / Tier B trait-inlining registry / Tier C heredoc-template / Tier D external-file contract-only) with three worked consumer plugins. Closes ROADMAP O2 at the WD13 floor.
4. **O4 Layer 3 (slices 1+2+3) target-project RBS source discovery + DEFAULT_LIBRARIES expansion.** `Rigor::Environment::LockfileResolver`, `RbsCollectionDiscovery`, `RbsCoverageReport`, missing-gem `:info` diagnostic, +31 stdlib libraries auto-loaded (1,273 → 1,427 RBS classes), `is_a?(C)` lexical-nesting constant resolution.

v0.1.7 themes (frozen on tag, full detail in CHANGELOG `[0.1.7]`):

1. **[ADR-22](adr/22-baseline-and-project-onboarding.md) baseline mechanism + project-onboarding** (proposed; nine working decisions, slice 1 + 2 IMPLEMENTED). PHPStan-shaped `.rigor-baseline.yml` recording `(file, qualified_rule, count)` snapshots with rigor-side adjustments — rule-ID default + opt-in message-pattern mode (WD1), ALL-or-NOTHING per-bucket threshold semantics (WD4), explicit-loading only via `baseline:` config key (WD2 (b)), filter as the LAST suppression layer (WD6), stderr summary line (WD7), dedicated schema (`version: 1` + `ignored:`) chosen over PHPStan's config-include reuse on the human-vs-tool-edit asymmetry (WD9). New `Rigor::Analysis::Baseline` value object + `Rigor::CLI::BaselineCommand` subcommand router + `rigor check --baseline=PATH` / `--no-baseline` CLI flags + `.rigor.yml` `baseline:` config key + JSON Schema property. **Slice 1** ships `generate` + the load/filter primitive. **Slice 2** ships `dump` (text + json, `--rule` / `--file` filters) + `drift` (per-bucket status `:within` / `:over` / `:cleared` / `:reducible`) + `prune` (drop cleared buckets + `--dry-run`). Spec coverage: 45 examples / 0 failures.
2. **Real-world survey-driven plugin / engine fixes (D-track).** Five-project survey ([Mastodon / Redmine / Solidus / tdiary-core / dependabot-core](notes/20260519-oss-library-survey.md)) drove fixes against ~6,697 → 3,457 total diagnostics (-48%). Key fixes: rigor-rails-routes `only:`/`except:` symbol coercion + uncountable noun handling + `root :as => 'home'` parsing; rigor-actionpack concern transitive include + render-template extensions widened (HAML / Slim / JBuilder) + cross-plugin dedup with rigor-rails-routes; rigor-activerecord `belongs_to` as `find_by` alias; rigor-actionmailer private + before_action exclusion; rigor-rails-i18n CLDR pluralization namespace; rigor-activesupport-core-ext `Object#as_json` overlay; engine universal-equality methods (`==` / `eql?` / `equal?` / `<=>`) skip arg-type-mismatch; load-error per-file → per-run; CGI extras RBS overlay (`include CGI::QueryExtension`).
3. **ADR-15 Amendment — Ractor pool root-caused as unusable; fork pool replaces it.** A CI segfault traced to the Ractor worker pool (ADR-15 Phase 4) being unusable on Ruby 4.0.x: a CRuby concurrent-Ractor heap-use-after-free in `rb_vm_ci_lookup` (filed as Ruby Bug #22075, ~70 % crash rate) plus a deterministic `Ractor::IsolationError` that makes every worker emit 100 % `internal analyzer error`. Investigation in [`docs/notes/20260520-ractor-pool-cruby-uaf.md`](notes/20260520-ractor-pool-cruby-uaf.md). New fork-based backend (`Runner#analyze_files_in_fork_pool`) is now the active `workers > 0` path — separate processes sidestep both defects; the Ractor pool is preserved behind `RIGOR_POOL_BACKEND=ractor`. ~2× faster than sequential on Mastodon (1303 files), memory-safe, `runner_fork_pool_spec.rb` in the default suite. Known follow-up: a small deterministic pool/sequential diagnostic divergence (cross-file-analysis-order effect inherent to the worker-pool model).

4. **[ADR-23](adr/23-diagnostic-triage-command.md) `rigor triage` — diagnostic-triage subcommand (slices 1+2 IMPLEMENTED).** A `check`-derived subcommand that summarises the diagnostic stream — rule-ID distribution, per-file hotspots, and a six-recogniser heuristic-hint catalogue (likely ActiveSupport `core_ext` / project monkey-patch / gem-without-RBS / AR relation misinference / systemic cluster / low-count = likely genuine bug). Read-only and advisory; `--format json` feeds the ADR-22 onboarding SKILLs. New `Rigor::Triage` + `Triage::Catalogue` + `CLI::TriageCommand` + `TriageRenderer`; 19 spec examples. On Mastodon it condenses 489 diagnostics into five actionable hints. Companion to ADR-22 (ADR-22 records *what is there*; ADR-23 explains *what it means*). Slices 3 (SKILL integration) + 4 (structured `Diagnostic` fields + plugin recognisers) remain.

5. **microsoft/waza CLI + SKILL quality improvement.** `microsoft/waza` (Agent Skills evaluation framework) added to the Flake dev shell ([`5b4c179`](#)). All four SKILLs improved through `waza check` + `waza quality` LLM-judge feedback. `skills/rigor-plugin-author/` re-organised into progressive-disclosure (`SKILL.md` + 4 `references/`) → 1430 tokens (from 9658), Quality 4.67/5.0 (3-judge mean, up from 4.13). Then re-homed back to `.claude/skills/` after recognising it's contributor-only (the external-author variant is v0.1.9 commitment per ADR-22 WD8).

The v0.1.6 themes (frozen on tag, full detail in CHANGELOG `[0.1.6]`):

1. **ADR-12 / ADR-17 / ADR-18 floors + worked consumers.** `rigor-dry-types` slices 1–4 (full alias coverage incl. transitive); `rigor-dry-schema` slices 1+2 (recognition + `each` list slot); `rigor-dry-validation` slice 1 (Contract recognition + RBS overlay); ADR-17 slices 1+2+3a+4 (pre-eval plumbing, monkey-patch registry, dispatcher tier, glob); ADR-18 slices 1+2+3+5 (`returns_from_arg:` DSL + end-to-end dry-struct uplift). ADR-10 Phase B heuristic return-type extraction.
2. **Editor mode v1 + Language Server v1/v2 + LSP polish/performance.** `--tmp-file`/`--instead-of` paired flags + `BufferBinding`; `rigor lsp` subcommand (8-slice v1); v2 type-aware hover + `textDocument/completion`; LSP follow-ups (signatureHelp + hash-key completion + editor guide + e2e spec); LSP polish (6 slices; 9 capabilities total); LSP performance trifecta (`ProjectScan` pre-pass cache + Environment sharing + plugin cache descriptor fix). Design docs: [`docs/design/20260516-editor-mode.md`](design/20260516-editor-mode.md), [`docs/design/20260517-language-server.md`](design/20260517-language-server.md), [`docs/design/20260517-lsp-hover-completion.md`](design/20260517-lsp-hover-completion.md).
3. **Ecosystem plugins.** `rigor-graphql` slices 1+2a–2d (Schema::Object / Enum / InputObject / Mutation; four cross-plugin facts); `rigor-dry-schema` slices 1+2; `rigor-dry-validation` slice 1; `rigor-rails` meta-gem scaffold; `rigor-minitest`; `rigor-rspec-rails`; `rigor-shoulda-matchers`. 29 `plugins/` + `examples/` entries total; Tier 3 roster closed.
4. **ADR-20 Lightweight HKT.** 32 commits; carrier + parser + full § D3 conditional grammar; `JSON.parse` / `YAML.safe_load` / `CSV.parse` / `CSV.read` `METHOD_RETURN_OVERRIDES`; three user-authoring paths (`.rbs` directives / plugin manifest / builtins). Handbook chapter 12.
5. **Engine improvements.** Prism leaf handlers; `def Foo.method` singleton promotion; `class << Foo` singleton scope; `eval_call` DefNode walk; `OverloadSelector` pass 1.5 + receiver-affinity pre-sort + `Acceptance` ancestor-chain fallback; `StaticReturnRefinements` tier (`Kernel#__dir__`, `File.expand_path`, `File.dirname`); `Module.new`/`Class.new` block walk; regex `$1..$9` narrowing; `rescue…return` post-block flow; module-mixin `self_type`; parameter default scope fix (−97% false positives on prism-1.9.0); `FlowContribution::Fact :local`; `eval_and_or` early-return narrowing; cross-file class discovery pre-pass; RBS overlays (prism / bundler / rubygems / did_you_mean). References survey (`references/ruby/lib`) 1756 → 354 errors (−80%).
6. **Spec-suite + repo layout.** Parallel-by-default (`217s → 60s` on 12-core); content-keyed sig dir (runner_spec 39.6s → 25.4s); opt-in shared plugin cache (sorbet_plugin_spec 13.1s → 4.7s); Pillar 2 slices 1+2+3 (rigor-rspec matcher narrowing + `let`/`subject` cross-binding + factory model-class). Repository layout split: `plugins/<id>/` (27 production plugins) vs `examples/<id>/` (5 walkthroughs).

Natural entries for the remaining work (post `[Unreleased]` in-flight slices):

1. **Release-prep candidate.** The `[Unreleased]` cycle (Mastodon-survey FP-reduction pass — fork pool, `rigor triage`, clusters 1/2/3) is implementation-complete with verification. `bundle exec rake release` per [`.claude/skills/rigor-release-prep/SKILL.md`](../.claude/skills/rigor-release-prep/SKILL.md) is a natural ship-state decision once cluster 4 + any ADR-24 work is settled; awaits explicit user authorisation.
2. **ADR-22 follow-up slices.** Slice 5 (`regenerate` + `--baseline-strict` CI gate) demand-driven. Slices 3 + 4 (the external-user SKILL trio — `rigor-project-init` / `rigor-baseline-reduce` / external `rigor-plugin-author`) committed to v0.1.9 per WD8; v0.1.7 / v0.1.8 reserved for collecting empirical baseline data from the field to inform the SKILL trio's defaults.
3. **rigor-dry-rb continuation** ([ADR-12](adr/12-dry-rb-packaging.md) roadmap continuation; slicing decision in [`docs/design/20260517-dry-validation-slicing.md`](design/20260517-dry-validation-slicing.md)). Dry-types slices 1–4 + dry-schema slices 1+2 + dry-validation slice 1 all landed. **Remaining (demand-driven)**: dry-schema slice 2+ (typed `result.to_h` synthesis via ADR-16 Tier C); dry-validation slice 2 (params-block typing via `:dry_schema_table`) + slice 3 (`json { ... }` parity); dry-monads (`Result[T, E]` / `Maybe[T]` carrier — blocked on ADR-3 amendment).
4. **ADR-18 follow-up slices.** Slice 4 (TraitRegistry parity for `returns_from_arg:`) and the chained-call argument extension (`Types::String.constrained(...)` → resolve through chain head) both stay demand-driven. The current floor handles `ConstantReadNode` / `ConstantPathNode` shapes which cover the canonical authoring case.
5. **ADR-17 follow-up slices.** Slice 3b (`Cache::Descriptor::PreEvalEntry`) deferred until measured pain. Slice 5 (full-project 2-pass discovery), slice 6 (plugin-API hook) stay demand-driven.
6. **Per-call return-type precision from gem source** (ADR-10 walker enhancement, option C lazy / on-demand). Phase B floor landed; option C wires gem inference lazily at call-site request — substantial architectural change to the walker / dispatcher boundary, gated on demand.
7. **rigor-graphql future slices** — resolver-method type-check, `<Type>.array` / `<Type>!` chain forms, string-form `field :foo, "User"` diagnostic, `Schema.execute(...)` result typing. All demand-driven.
8. **O4 Layer 3 per-gem-version cache (slice 3 architecture)** — DEFERRED until measured pain.
9. **ADR-16 demand-driven follow-ups** — (a) **slice 5b** Tier D engine integration (matched external files run with `self_type` narrowed + `bound_ivars` pre-bound); (b) full ADR-13 `Plugin::TypeNodeResolver` chain wiring for `returns:` strings (utility-type-shaped substrate returns like `Array[String]` / `Pick<T, K>`). Both pinned in ADR-16 § Implementation slicing footnotes; awaiting concrete plugin-author cases.
10. **Editor mode follow-ups** — see [`docs/design/20260516-editor-mode.md`](design/20260516-editor-mode.md) § "Out of scope for v1". Key levers: **per-file diagnostic cache** (option A → option B upgrade) and **disk-backed `ProjectScan` snapshot cache** for CLI shell-out path (design at [`docs/design/20260518-cli-disk-snapshot-cache.md`](design/20260518-cli-disk-snapshot-cache.md)). Both demand-driven.
11. **LSP follow-ups** — see [`docs/design/20260517-language-server.md`](design/20260517-language-server.md) (v1) + [`docs/design/20260517-lsp-hover-completion.md`](design/20260517-lsp-hover-completion.md) (v2). Highest-leverage queued items: (a) **Ractor pool dispatch** for parallel multi-buffer publishes; (b) **`textDocument/definition`** (needs Reflection-side symbol index keyed on FILE:LINE); (c) **incremental `didChange` sync** (UTF-16 offset bookkeeping). Capabilities still out-of-scope (codeAction / rename / semanticTokens / inlayHint / snippets / bare-name / symbol / multi-overload / `completionItem/resolve`) each need their own design pass.

## Open Engineering Items

Persistent items the next implementer benefits from seeing without re-reading the full thread. Items already absorbed into a released milestone are referenced through `CHANGELOG.md` rather than restated.

### Survey-driven (v0.1.5 cycle, all items closed)

The fourteen-project real-world Rails survey ran through three rounds during the v0.1.5 cycle. All items below are closed (full recap in `CHANGELOG.md` § `[0.1.5]`); kept here as a reference index of "what each ID is" for cross-doc lookups.

| ID | Status | Item |
| --- | --- | --- |
| O1 | landed (MVP, v2) | `plugins/rigor-activesupport-core-ext/` opt-in RBS bundle for the top ~50 ActiveSupport `core_ext` selectors. v2 added `compact_blank` / `exclude?` / `index_with` / `Hash.from_xml` / `DateTime` calculations after the round-2 sweep. |
| O2 | substrate floor + precision promotion LANDED (Tier A/B/C engine + Tier D contract + Concern re-targeting + 3 worked consumer plugins + docs + slice 6 precision); slice 5b + ADR-13 resolver-chain wiring queued | Macro-template / heredoc-Ruby expansion. Shipped through [ADR-16](adr/16-macro-expansion.md) slices 1–5a + 7 (commits 584ae85…56706a5) + slice 6a-TierB / 6b-TierC (commits d174fff / d7b1943). Substrate is functionally complete at the WD13 floor PLUS precision promotion for the common case (Tier B → module RBS, Tier C → plain class name lookup). tDiary's `instance_eval` plugin pattern + Rails-generator `.rb`-as-ERB templates stay relevant for the deferred slice 5b (Tier D engine). Parameterised / utility-type substrate returns stay deferred to demand-driven ADR-13 resolver-chain wiring. |
| O3 | not-an-issue | Early-exit narrowing (`next if x.nil?` / `return if x.nil?`) already worked; survey residuals were `Object#blank?` / `#present?` / `#try`, which O1 covers. |
| O4 | layers 1+2+3 (slices 1+2+3) landed | Bundler awareness. Layer 1 (`bundler.bundle_path`) + Layer 2 (`.bundle/config` / `vendor/bundle/` auto-detect) + Layer 3 slice 1 (`Gemfile.lock` parse + bundle-sig filter, commit `3c99eed`) + Layer 3 slice 2 (`rbs_collection.lock.yaml` awareness, commit `46c9ec7`) + Layer 3 slice 3 (graceful-degradation `:info` diagnostic when locked gems have no RBS coverage). The per-gem-version cache descriptor (so `bundle update` invalidates only the affected gem's slice instead of the whole project) stays queued as a larger architectural change. |
| O5 | landed (`ac14c45`) | `Hash[K, V] <:= Enumerable[[K, V]]` parametrized-ancestor projection in `Inference::Acceptance#accepts_nominal_from_nominal`. Hand-rolled mapping for Hash → Enumerable today; general RBS-driven `definition.ancestors[i].args` projection deferred. |
| O6 | landed (`4698437`) | `MethodDispatcher::CONSTANT_CONSTRUCTORS` deep-share (Proc values were not shareable under shallow `.freeze`). Pool ≡ sequential on GitLab FOSS after fix. |
| O7 | landed (`3c4a7ff`) | `RbsLoader#env` memoises failure. Pre-fix, a single conflicting `signature_paths:` entry rebuilt env per AST node (390× / file, ~35 s for one controller). Post-fix: 0.15 s for 5 controllers (~550× speedup) with a single user-facing warning naming the offending file. Unblocks O4 Layer 3 — gem-shipped sigs that conflict with stdlib RBS now degrade gracefully. |

### Stdlib coverage expansion — open design questions (2026-05-16)

Driven by the v0.1.5 work to expand `Environment::DEFAULT_LIBRARIES` (commits `0a4ffea` + follow-on). Twenty-three additional stdlib libraries now auto-load (1,273 → 1,412 RBS classes). Two design judgment points surfaced and are deferred:

1. **Stdlib name conflicts with rigor's internal carriers (RESOLVED).** `singleton` (stdlib `Singleton` mixin) initially clashed with `Rigor::Type::Singleton` in lexical scope, surfacing an `undefined-method` false positive on `lib/rigor/type/singleton.rb#==`'s `is_a?(Singleton)` narrowing. Resolved via the lexical-nesting fix in `Inference::Narrowing#analyse_class_predicate`: the bare-name argument now resolves through the chain derived from `scope.self_type` (mirroring `Module.nesting`-driven constant lookup) so the nested `Rigor::Type::Singleton` wins over the top-level stdlib `Singleton`. `singleton` is now in `DEFAULT_LIBRARIES`. Broader lexical-resolution coverage (method-dispatch receivers, attribute access, etc.) is gated on further demand — the fix is surgical to the class-predicate narrowing surface.

2. **Stdlib RBS coverage gaps in upstream `rbs` gem.** `strscan` initially blocked Batch 2 because the bundled RBS only declares `StringScanner#[](Integer)`, while real Ruby supports `StringScanner#[](Symbol)` for named captures — which rigor itself uses in `lib/rigor/builtins/imported_refinements.rb:422,424`. The expedient resolution (response path (a) below) applied two `# rigor:disable argument-type-mismatch` directives at the affected call sites and moved `strscan` into `DEFAULT_LIBRARIES`, so users analysing parser code get StringScanner dispatch precision. The remaining response paths stay open as longer-term fixes:
   - **(a) Skip the library** — initially used in Batch 2; superseded by Batch 3.5 in favour of (a')
   - **(a')** **In-source `# rigor:disable` directives** at the gap-affected call sites + load the library. What landed in Batch 3.5. Trade-off: rigor's own code carries a maintenance burden when upstream RBS gets fixed (must remove the directives); the burden is bounded (two call sites in one file). This is the same pattern already used for `Bundler::LazySpecification#platform` in `lockfile_resolver.rb`.
   - **(b) Hand-author an RBS overlay** under rigor's own `sig/`. Per AGENTS.md § "RBS Authorship" the project policy is to prefer `rigor sig-gen` over hand-authored RBS, but "filling an upstream RBS shim gap" is arguably a different category from "generating from inference". A small focused overlay (`StringScanner#[](Symbol)` row) would let rigor's own code drop the disable directives.
   - **(c) Upstream the fix to `ruby/rbs`** and bump the gem. Highest-quality long-term path; needs cross-project coordination.

   The same pattern will recur for other stdlib RBS gaps as the library set expands. The decision tree is: when an upstream RBS gap is surfaced by a single internal call site, prefer (a') (disable directive); when it surfaces across multiple call sites or in user-facing code, escalate to (b) or (c).

### Session state (2026-05-20)

1. **8 commits ahead of `origin/master`, NOT pushed.** Push + release decision user-gated per AGENTS.md release cadence policy. The `[Unreleased]` cycle is the Mastodon-survey FP-reduction pass (see § "Where the Work Resumes" at the top). `make verify` passes; `rigor check lib` self-check clean.
2. **`references/rbs` PR branch `widen-strscan-resolv-stdlib-sigs`** holds two stdlib-RBS widening commits (`StringScanner#[]`, `Resolv#initialize`) — branch push + upstream `ruby/rbs` PR creation are the user's task, not done automatically.
3. **Cluster 4 (flow-folding warnings) is the open Mastodon-survey item**; ADR-24 (implicit-self call resolution) is proposed but unimplemented — its slice 1 closes the ≈42-diagnostic cluster-1b FP cluster.
4. **`make verify` is parallel by default** (`rake spec_parallel`). `runner_pool_spec.rb` excluded from both paths; `RIGOR_INCLUDE_RACTOR_POOL=1` opts back in. Occasionally hangs on `cli_spec.rb` / `runner_pool_spec.rb` under Ruby 4.0.4 due to a Ractor host-side crash (reproducible on master without this session's changes); use targeted `bundle exec rspec` as the fallback.
3. **LSP ships 9 advertised capabilities** (textDocumentSync / hover / completion / signatureHelp / documentSymbol / foldingRange / selectionRange + workspace/didChangeWatchedFiles + workspace/didChangeConfiguration). Feature-complete for the keystroke-fast linting + hover + completion loop. Remaining LSP work (codeAction / rename / semanticTokens / definition / incremental sync / Ractor pool dispatch) in ROADMAP § "Editor / IDE integration".
4. **New plugin home**: `plugins/<id>/` for production plugins (27 total, targeting real gems/frameworks); `examples/<id>/` reserved for architectural-surface walkthroughs (5 total). The `rigor-plugin-author` SKILL reflects the dual layout.
5. **Queued narrowing items still open:** (a) `x = expr() or raise` doesn't narrow `x` to non-nil — `Inference::Narrowing#analyse_or` extension needed, minimal repro at `/tmp/rigor-refs-check/or_raise_narrow.rb`; (d) `Hash === expr` case-equality narrowing (`open3.rb:226` shape). Items (b) / (c) / (e) / (f) from the 2026-05-18 references survey CLOSED in the 2026-05-19 session.
6. **Queued StaticReturnRefinements additions** (single-table edit each — `OVERRIDES` constant + two spec cases): `Kernel#caller` / `Kernel#caller_locations` → `Array[String] | nil`; `File.basename` → `non-empty-string`; `__method__` / `__callee__` → `Symbol | nil`. (`File.expand_path` and `File.dirname` LANDED in the 2026-05-19 session.)
7. **`rigor-rails` meta-gem** is publication-ready; activation awaits the Tier 1+2 sub-plugins' subtree-split + RubyGems publish. `.rigor.yml` activation stays per-plugin per ADR-12 WD1.
8. **ADR-20 remaining work** (all demand-driven): Slice 4 dry-monads `Result[T, E]`/`Maybe[T]` (blocked on ADR-3 amendment); Slice 5 `type`-alias sugar; pattern-matching binder extraction (lisp-eval migration gated on this); growing `METHOD_RETURN_OVERRIDES` to more stdlib methods (demand-driven).

### Pre-survey persistent items

-2. **External-codebase analysis: business-use SDKs (2026-05-18).** facebook-ruby-business-sdk (1,220 files) / google-apis-core (25 files) / google-cloud-storage (27 files); 79 errors + 8 warnings triaged. Cross-file class discovery pre-pass LANDED (`a8932a1`; `ScopeIndexer.discovered_classes_for_paths`). **Queued**: (α) `x ||= expr` non-nil narrowing inside closures — 4 google-apis-core false positives; simple-case reproducers work, failure involves nested closure interaction. Cross-file `discovered_methods` pre-pass (mirrors the landed class-discovery; would unlock module_function support).

-1. **External-codebase analysis: Steep 2.0.0 (2026-05-18).** 141 files / 107 diagnostics triaged: (A) 36 × Struct.new dynamic class generation unmodelled; (C) 7 × `x = foo or raise` narrowing gap (also queued item (a) above; 0 in rigor lib/ so internal demand below "demand-driven" bar); (E) ~10 × real Steep dead-assignment bugs (upstream reports — data point that rigor's `flow.dead-assignment` at `:warning` surfaces these while Steep's own lenient profile misses them).

0. **Self-analysis: loop-mutation tracking for `flow.always-truthy-condition` (queued engine improvement).** `rigor check lib` after the bool-polymorphism + HashShape-narrowing fixes (commits `7c4efce` + `dd917b5`) still surfaces 3 warnings, all of the same shape: `arr = [seed]; while ...; arr << x; end; if arr.size == N` / `arr.empty?` is reported as always-truthy because `Inference::Narrowing` doesn't reflect the loop body's `<<` / `push` mutation into the size/empty narrowing. Affected sites: [hkt_body_parser.rb:140](lib/rigor/inference/hkt_body_parser.rb:140), [hkt_body_parser.rb:307](lib/rigor/inference/hkt_body_parser.rb:307), [hkt_registry.rb:212](lib/rigor/inference/hkt_registry.rb:212). Fix lives in the mutation-effect model under `docs/type-specification/control-flow-analysis.md` § "mutation effects" — likely a medium-sized engine change. Stays queued until concrete demand surfaces (the warnings here are the demand signal; their existing site count is small enough that the engine cost has to be paid).

1. **Sig-gen `update_existing` does not yet collapse sibling parent / child class blocks.** Gap (c)'s tree-builder fix lives in `Writer#render_new_file` (the create-new path). When updating an existing target file, `merge_class` still resolves each candidate's `class_name` independently — if both `Foo::Bar` and `Foo::Bar::Child` decls already exist as flat siblings, sig-gen leaves them flat. Re-flowing an existing file into the nested layout would require parsing the existing decl tree and rewriting it, which is out of scope for a follow-up fix. Users who want the canonical nested layout regenerate from scratch (delete the target sig file and rerun).
2. **In-memory `Analysis::Runner.run_source` entry point (test-only perf follow-up).** The `RunnerHelpers#analyze` test helper materialises a tmpdir per call (write source file, chdir, run, clean up). At ~25-50ms per call × hundreds of runner-spec calls, that's a real share of suite wall-clock that an in-memory entry point could eliminate. Sketch: add `Runner.run_source(source:, path: "code.rb", environment:, config:)` that bypasses path expansion and accepts a `{path => bytes}` virtual file table. The helper would call it for the `analyze(source: "...")` shape (no files / sig). Expected delta: ~5% sequential, ~3% parallel — not worth doing standalone, but a natural complement if test-suite expansion continues.
3. **Fork-based file-level parallelism for `rigor check`.** Stackprof of warm `rigor check lib` shows ~50% inference, ~22% `Marshal.load`, ~17% GC. The Phase 4b Ractor path handles the parallelism story for v0.1.5; a fork-based path remains a parallel (non-exclusive) option for hosts where Ractors are unavailable or where COW sharing of pre-warmed `Environment` blobs would beat per-Ractor env build. Implementation sketch: `Runner#run` forks workers per file-chunk, each writes diagnostics to a pipe, parent re-assembles in original path order.

## Reading Order for a Returning Implementer

The default goal for the next session is **either cut the v0.1.6 release** (with ADR-12 / ADR-17 / ADR-18 all at floor + worked-consumer state) **or continue the cycle** with one of the natural entries above. Read in this order:

1. `CHANGELOG.md` `[Unreleased]` section — accumulates v0.1.6 work as it lands.
2. [`docs/ROADMAP.md`](ROADMAP.md) § "v0.1.6 — accumulating on master" — the cycle's ADR-12 / ADR-17 / ADR-18 envelope.
3. [`docs/adr/12-dry-rb-packaging.md`](adr/12-dry-rb-packaging.md), [`docs/adr/17-monkey-patch-pre-evaluation.md`](adr/17-monkey-patch-pre-evaluation.md), [`docs/adr/18-substrate-per-call-site-return-type.md`](adr/18-substrate-per-call-site-return-type.md) — the three v0.1.6 ADRs (12 accepted, 17 accepted, 18 proposed-and-implemented).
4. [`docs/adr/16-macro-expansion.md`](adr/16-macro-expansion.md) — the substrate ADR-18 amends; orient against the existing slice plan + WD13.
5. [`docs/adr/9-cross-plugin-api.md`](adr/9-cross-plugin-api.md) — `Plugin::FactStore` is the channel ADR-18 reads from / `rigor-dry-types` writes to.
6. [`docs/adr/10-dependency-source-inference.md`](adr/10-dependency-source-inference.md) — ADR-10 walker (gem-source inference); the heuristic return-type extractor (`Phase B`, `e40947c`) is reused by `ProjectPatchedScanner`.
7. [`plugins/README.md`](../plugins/README.md) — production-plugin catalogue (Rails Tier 1/2/3, testing matchers, dry-rb foundation, ADR-16 substrate consumers, cross-plugin fact-channel table). [`examples/README.md`](../examples/README.md) — five plugin-contract walkthroughs (`rigor-deprecations` / `rigor-lisp-eval` / `rigor-pattern` / `rigor-routes` / `rigor-units`) + architectural-surface map. Repository layout split landed in commit `ae1b1c9` (2026-05-19); see the 2026-05-19 session-end carryover above for the rationale.
8. [`docs/notes/20260515-real-world-rails-survey.md`](notes/20260515-real-world-rails-survey.md) — the survey that drove v0.1.5's production-rigor improvements; still authoritative when the next batch of real-world projects gets analysed.
8a. [`docs/notes/20260519-oss-library-survey.md`](notes/20260519-oss-library-survey.md) — 22-OSS-library survey that drove v0.1.6's `OverloadSelector` receiver-affinity pre-sort + `Acceptance` ancestor-chain fallback (Family 3 / BigDecimal-coerce regression). Five other diagnostic clusters (§3b mixin lookup / §3c nil-narrowing through guards / §3d always-falsey-truthy noise / §3g ivar type-divergence / §8a generator `.rb` ERB) remain queued; this note records the per-library counts so the next slice can be picked with the data in hand.
9. [`docs/adr/15-ractor-concurrency.md`](adr/15-ractor-concurrency.md) + [`docs/design/20260514-ractor-migration.md`](design/20260514-ractor-migration.md) — Ractor migration contract; relevant when adding plugin surfaces that need shareability review (ADR-18 was a pass).
10. [`skills/rigor-plugin-author/SKILL.md`](../skills/rigor-plugin-author/SKILL.md) — playbook for authoring a new plugin (used as template for `rigor-dry-types` slice 1).
11. [`docs/internal-spec/public-api.md`](internal-spec/public-api.md) — public-vs-internal stability boundary. Cross-reference `spec/rigor/public_api_drift_spec.rb` before extending any pinned namespace.
12. [`docs/adr/2-extension-api.md`](adr/2-extension-api.md) and [`docs/adr/7-v0.1.0-slice-decisions.md`](adr/7-v0.1.0-slice-decisions.md) — the binding design and per-slice working decisions for the v0.1.0 plugin contract that v0.1.x extends.
13. [`docs/adr/3-type-representation.md`](adr/3-type-representation.md) Working Decisions — OQ1 / OQ2 / OQ3 outcomes still bind the type-object public surface plugins consume.

Key surfaces to start from for common follow-up work:

- **ADR-18 substrate amendment**: `lib/rigor/plugin/macro/heredoc_template.rb` (Emit + ReturnsFromArg), `lib/rigor/inference/synthetic_method_scanner.rb` (`resolve_emit_return_type` + `argument_source_representation` + `resolve_returns_from_arg`), `lib/rigor/analysis/runner.rb#run` (plugin prepare reordering + `shared_fact_store`).
- **ADR-17 pre-eval pipeline**: `lib/rigor/inference/project_patched_methods.rb` (registry), `lib/rigor/inference/project_patched_scanner.rb` (pre-pass + duplicate-declaration), `lib/rigor/inference/method_dispatcher.rb#try_project_patched_method` (dispatcher tier), `lib/rigor/configuration.rb#expand_pre_eval_entries` (slice-4 glob).
- **ADR-12 dry-rb foundation**: `plugins/rigor-dry-types/lib/rigor/plugin/dry_types.rb` + `dry_types/alias_scanner.rb` (canonical + nested + compositions); `plugins/rigor-dry-struct/lib/rigor/plugin/dry_struct.rb` (consumer manifest for `:dry_type_aliases`).
- **O4 Bundler-awareness surface (unchanged since v0.1.5)**: `lib/rigor/environment/bundle_sig_discovery.rb`, `lib/rigor/environment/lockfile_resolver.rb`, `lib/rigor/environment/rbs_collection_discovery.rb`, `lib/rigor/environment/rbs_coverage_report.rb`.
