- **[check]** A hash literal with a `**splat` entry, a `Hash.new` filled key by key, or a `Hash[Symbol, untyped]` is no longer reported by `def.return-type-mismatch` or `call.argument-type-mismatch` against an RBS record type, so `def rec = { **BASE, b: 2 }` is accepted for `-> { a: Integer, b: Integer }`; a `Hash[K, V]` without an `untyped` arm, or one whose key or value type rules the record out (`Hash[String, untyped]`), is still reported. ([#1275](https://github.com/rigortype/rigor/pull/1275))
