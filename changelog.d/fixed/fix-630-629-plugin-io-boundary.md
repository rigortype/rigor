- **[plugins]** `rigor check` no longer keeps reporting `plugin.actionpack.missing-template` from the run cache after the template is written, and the dry-types / dry-schema / dry-validation / graphql plugins now record the project files they scan as cache dependencies, so a warm run reflects an edit to a type or schema definition. ([#PLACEHOLDER](https://github.com/rigortype/rigor/pull/PLACEHOLDER))
