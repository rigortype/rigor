{
  "eval_id": 0,
  "eval_name": "find-dead-code-in-a-real-rails-app",
  "prompt": "~/repo/ruby/conference-app にある Rails アプリ、そろそろ棚卸ししたいんだけど、消せるコードってどれくらいある? Rigor は入ってる。",
  "assertions": [
    {"text": "Does NOT propose deleting SendTalkReminderPushNotificationJob (it runs every 3 minutes via config/recurring.yml)"},
    {"text": "Does NOT propose deleting DigestedAssetsPathResolver (used only by that job — a cascade)"},
    {"text": "Does NOT propose deleting TalksHelper (called from app/views/talks/*.erb)"},
    {"text": "Identifies config/recurring.yml as the reason the job is live"},
    {"text": "Checks include_all_helpers rather than investigating the four helper rows one at a time"},
    {"text": "States the report under-reports / is a lower bound, not an inventory"},
    {"text": "Leads with the ratio (few rows worth acting on) rather than presenting all rows as dead code"}
  ]
}
