- **[check]** A call on a value typed as a mixin module no longer draws `call.undefined-method`: the surface belongs to whatever includes the module, so it cannot be enumerated — this covers a mixin's `self.method` and `self.class.method` alike, while a typo on a namespace module's own `module_function` surface keeps reporting. ([#739](https://github.com/rigortype/rigor/issues/739), [#741](https://github.com/rigortype/rigor/pull/741))
