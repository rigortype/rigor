- **[sig]** The bundled ActiveSupport overlay and the `rigor-activesupport-core-ext` plugin now declare `Enumerable#many?` (with and without a block), `Enumerable#in_order_of`, `Array#second_to_last` / `#third_to_last` / `#extract_options!` and `Hash#extractable_options?`, so `records.to_a.many?` in a Rails app no longer reports a false `call.undefined-method`. [#1326](https://github.com/rigortype/rigor/pull/1326)
