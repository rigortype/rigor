- **[sig-gen]** A class opened with a rooted header (`class ::Foo` inside `module MyApp`) is now generated under the name Ruby gives it rather than under the enclosing module, so `rigor sig-gen` no longer writes a `MyApp::Foo` declaration nothing calls, and `rigor effects check` reports the `def` line for such a class instead of only its file ([#893](https://github.com/rigortype/rigor/pull/893))
