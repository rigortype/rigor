- **[sig-gen]** A base class's method signature is no longer emitted when a project subclass overrides it and the override cannot be typed: RBS resolves the undeclared override through its ancestors, so the base's precise return became the subclass's answer and reported false `call.undefined-method` on the subclass's own working code. ([#744](https://github.com/rigortype/rigor/issues/744), [#745](https://github.com/rigortype/rigor/pull/745))
