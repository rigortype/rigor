- **[inference]** Bounded Float folds: `rand(1..6)` / `rand(0.0...1.0)` / `Random.rand(1.0..2.0)` type as the range they name; `Math.sqrt`, `exp`, `log` and the other monotone `Math` functions map a bounded argument to the range between the images of its bounds (`Math.sqrt(ARGV.size)` is `Float[0.0..]`); a bounded Float folds `abs`, `nan?` (always `false`), `finite?`, `floor` / `round` / `to_i` (onto an Integer range), `between?` and `clamp`; and `clamp(range)` narrows a bounded Integer or Float to the bracket. A call that can raise for a value in the range keeps its RBS type ([ADR-109](https://github.com/rigortype/rigor/blob/master/docs/adr/109-ruby-native-range-notation.md), [#857](https://github.com/rigortype/rigor/pull/857)).
