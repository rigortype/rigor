- **[plugins]** `rigor-activerecord` now indexes a model declared with a rooted constant path (`class ::User < ApplicationRecord`) under its plain name, so `rigor-actionpack`, `rigor-factorybot` and `rigor-shoulda-matchers` find it in the shared `:model_index` fact and the plugin's own column checks and finder typing cover it — along with its STI subclasses, a `< ::ApplicationRecord` base, and call sites that spell the receiver `::User`. ([#583](https://github.com/rigortype/rigor/issues/583))
