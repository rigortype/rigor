- **[inference]** A `Struct.new` / `Data.define` / `Class.new` / `Module.new` factory assigned through a constant path (`Holder::Thing = Struct.new(:a) do … end`) is now recognised as the class it names, so its members and block-defined methods resolve instead of reporting `undefined method … for Struct`; a rooted `::Table::ROWS[k] = 1` also stops widening the same-named constant in the enclosing namespace ([#903](https://github.com/rigortype/rigor/pull/903))
