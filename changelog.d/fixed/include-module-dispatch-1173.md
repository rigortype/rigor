- **[inference]** A discovered class's `include M` where `M` is RBS-known now resolves M's instance declarations — `include Enumerable` / `include Comparable` on a project class answers `sort` / `clamp` instead of `Dynamic[top]`, and a method-level `[A] (A) -> A` (e.g. `Rigor::Testing#dump_type` after `include Rigor::Testing`) binds its variable from the call's arguments. The arm declines when a project `def`, an outside-the-body `include` mark, or a `pre_eval:` patch means the declaration found is not the method that runs. [#1173](https://github.com/rigortype/rigor/issues/1173)
