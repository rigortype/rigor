- **[inference]** `case/in` pattern names now bind against the case subject's type instead of `Dynamic[top]` — a tuple or `Array[T]` subject hands each slot its element, hash patterns read the subject's `deconstruct_keys`, a pattern's own class constraint binds on an opaque subject, and a `case/in` with no `else` no longer nil-injects the names it bound. ([#1164](https://github.com/rigortype/rigor/pull/1164))
