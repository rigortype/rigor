- **[skills]** `rigor-type-oracle` now treats an inline annotation a human wrote as documentation (`#: void`, a literal union, a parameter contract) as intent the check verifies rather than a guess to rewrite, and the type-specification's style guidance says which inline annotations are worth writing — a return the name does not suggest, a type that says more than the nominal class — and which only restate what the reader already has. ([#843](https://github.com/rigortype/rigor/pull/843))
