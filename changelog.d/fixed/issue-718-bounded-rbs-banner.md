- **[rbs]** A duplicate RBS method declaration no longer prints one stderr failure banner for every class reached by a pooled internal walk: the fallback is collapsed to one bounded summary while `rbs.coverage.definition-build-failed` still names the failed class and member, and runs that stop before aggregation still retain the stderr warning. ([#718](https://github.com/rigortype/rigor/issues/718))
