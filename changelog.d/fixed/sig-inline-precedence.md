- **[rigor-rbs-inline]** A method declared both in `sig/` and by an inline rbs-inline annotation no longer collides: the `.rbs` declaration wins for that one member, the rest of the file's annotations still bind, and the class keeps its method surface instead of degrading to `Dynamic[top]` behind an `rbs.coverage.definition-build-failed` row. Each dropped member is reported as `plugin.rbs-inline.source-rbs-annotation-not-honoured` (info) naming both files. ([#PRNUM](https://github.com/rigortype/rigor/pull/PRNUM))
