- **[engine]** A `git:`-sourced gem now owns its constants in the missing-RBS provenance index, so references into it are reported as "this gem ships no RBS" rather than as a generic engine gap — the index walked only the RubyGems install layout, which is the same blind spot [#611](https://github.com/rigortype/rigor/issues/611) fixed for signature discovery. ([#763](https://github.com/rigortype/rigor/issues/763))
