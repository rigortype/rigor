- **[plugins]** A controller action's effect summary now includes what its template does — `render :show`, `render partial: "card"`, `render template:` / `action:` and the implicit render of `<controller>/<action>` all reach the view's own unit, a template reaches the partials it renders, and the `template-not-analysed` taint clears on a render that reached one while surviving on a computed target, a layout or a template the plugin does not claim. [#1057](https://github.com/rigortype/rigor/pull/1057)
