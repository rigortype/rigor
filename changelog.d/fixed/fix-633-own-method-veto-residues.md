- **[inference]** An implicit-self call now resolves to an inherited `attr_*` / `define_method`, to a method declared on an ancestor that precedes `::Object` (`Exception#message`, `Array#first`, `Comparable#clamp`), and to a class method inherited from a superclass, instead of binding a same-named top-level `def` and reporting a false `undefined method` on nil. ([#955](https://github.com/rigortype/rigor/pull/955))
