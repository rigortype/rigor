- **[dev]** A precision-snapshot fixture with no golden now fails the suite, naming the one-fixture `UPDATE_SNAPSHOTS=<name>` command that writes it, instead of reporting `pending` — seven fixtures had accumulated in that state, every one of them added by the inference change whose regressions the gate exists to catch, because the only way to generate a golden was to rewrite all 110 at once. ([#698](https://github.com/rigortype/rigor/issues/698))
